// @generated by OntOS Codesmith Core Read v1; adapted for Core identity.
import { Schema } from '@modern-js/bff-effect/effect-client';

const LegalEntityIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('LegalEntityId'));
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('TenantId'));

export const LegalEntityDetailRefSchema = Schema.Struct({
  moduleId: Schema.Literal('core.identity'),
  resourceId: Schema.toEncoded(LegalEntityIdSchema),
  resourceType: Schema.Literal('core.identity.legal-entity'),
  tenantId: Schema.toEncoded(TenantIdSchema),
});
export const LegalEntityDetailRequestSchema = Schema.Struct({ legalEntityRef: LegalEntityDetailRefSchema });
export type LegalEntityDetailRequest = typeof LegalEntityDetailRequestSchema.Type;
export const LegalEntityDetailResponseSchema = Schema.Struct({
  legalEntityRef: LegalEntityDetailRefSchema,
  legalName: Schema.String,
  status: Schema.Literals(['active', 'suspended', 'archived']),
});
export type LegalEntityDetailResponse = typeof LegalEntityDetailResponseSchema.Type;
