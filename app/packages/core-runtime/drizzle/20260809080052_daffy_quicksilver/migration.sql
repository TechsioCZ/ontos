CREATE UNIQUE INDEX "core_auth_bindings_api_key_subject_global_uk" ON "core"."principal_auth_bindings" USING btree ("provider","subject_type","provider_subject_id") WHERE "core"."principal_auth_bindings"."subject_type" = 'api_key';--> statement-breakpoint
ALTER TABLE "core"."principal_auth_bindings" ADD CONSTRAINT "core_auth_bindings_lifecycle_ck" CHECK (("core"."principal_auth_bindings"."status" = 'revoked' and "core"."principal_auth_bindings"."revoked_at" is not null) or ("core"."principal_auth_bindings"."status" in ('active', 'disabled') and "core"."principal_auth_bindings"."revoked_at" is null));