// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner core.identity
// @ontos-resource-slug legal-entity
import type { OntosResourceType } from '../modules/manifest.ts';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('LegalEntityId'));
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('TenantId'));

export const LegalEntityRefSchema = Schema.Struct({
  moduleId: Schema.Literal('core.identity'),
  resourceId: Schema.toEncoded(ResourceIdSchema),
  resourceType: Schema.Literal('core.identity.legal-entity'),
  tenantId: Schema.toEncoded(TenantIdSchema),
});
export type LegalEntityRef = typeof LegalEntityRefSchema.Type;

export const legalEntityResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Legal Entity resource.',
  key: 'core.identity.legal-entity',
  label: 'Legal Entity',
  owningModuleId: 'core.identity',
} as const satisfies OntosResourceType;
