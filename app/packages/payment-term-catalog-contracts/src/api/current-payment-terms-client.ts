// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CurrentPaymentTermsApi } from '../apis/current-payment-terms.ts';
import type { CurrentPaymentTermsRequest } from '../apis/current-payment-terms.ts';
import { operationGateway } from './action-gateway.ts';

export interface CurrentPaymentTermsClientOptions {
  readonly baseUrl?: string | URL;
}

type CurrentPaymentTermsAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CurrentPaymentTermsClientOptions,
];

type CurrentPaymentTermsOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CurrentPaymentTermsClientOptions,
];

const currentPaymentTermsClient = (
  // oxlint-disable-next-line typescript/no-unnecessary-type-arguments -- Codesmith keeps the credential payload explicit as part of the governed-client structural contract.
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: CurrentPaymentTermsClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CurrentPaymentTermsApi,
      credential,
      defaultApiPrefix: '/payment-term-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCurrentPaymentTermsWithAuthorization = (
  payload: CurrentPaymentTermsRequest,
  ...[credential, requestCorrelation, options = {}]: CurrentPaymentTermsAuthorizedInvocation
) =>
  currentPaymentTermsClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.currentPaymentTerms.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCurrentPaymentTerms = (
  payload: CurrentPaymentTermsRequest,
  ...[requestCorrelation, options = {}]: CurrentPaymentTermsOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCurrentPaymentTermsWithAuthorization(payload, credential, requestCorrelation, options),
  );
