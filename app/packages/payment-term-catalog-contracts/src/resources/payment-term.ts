// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner payment.term-catalog
// @ontos-resource-slug payment-term
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const checkedIdentifier = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedIdentifier.pipe(
  Schema.brand('PaymentTermResourceId'),
  Schema.decodeTo(checkedIdentifier),
);
const TenantIdSchema = checkedIdentifier.pipe(
  Schema.brand('PaymentTermTenantId'),
  Schema.decodeTo(checkedIdentifier),
);

export const PaymentTermRefSchema = Schema.Struct({
  moduleId: Schema.Literal('payment.term-catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('payment.term-catalog.payment-term'),
  tenantId: TenantIdSchema,
});
export type PaymentTermRef = typeof PaymentTermRefSchema.Type;

export const paymentTermResourceDescriptor = {
  capabilities: {
    graphVisible: true,
    linkable: true,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Stable Payment-owned contractual due-date semantics.',
  key: 'payment.term-catalog.payment-term',
  label: 'Payment Term',
  owningModuleId: 'payment.term-catalog',
} as const satisfies OntosResourceType;
