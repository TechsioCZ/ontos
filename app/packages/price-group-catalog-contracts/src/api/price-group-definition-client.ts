// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';

import { PriceGroupDefinitionApi } from '../apis/price-group-definition.ts';
import type { PriceGroupDefinitionRequest } from '../apis/price-group-definition.ts';
import { operationGateway } from './action-gateway.ts';

export interface PriceGroupDefinitionClientOptions {
  readonly baseUrl?: string | URL;
}

type PriceGroupDefinitionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PriceGroupDefinitionClientOptions,
];
type PriceGroupDefinitionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PriceGroupDefinitionClientOptions,
];

const priceGroupDefinitionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PriceGroupDefinitionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PriceGroupDefinitionApi,
      credential,
      defaultApiPrefix: '/price-group-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executePriceGroupDefinitionWithAuthorization = (
  payload: PriceGroupDefinitionRequest,
  ...[credential, requestCorrelation, options = {}]: PriceGroupDefinitionAuthorizedInvocation
) =>
  priceGroupDefinitionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.priceGroupDefinition.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executePriceGroupDefinition = (
  payload: PriceGroupDefinitionRequest,
  ...[requestCorrelation, options = {}]: PriceGroupDefinitionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePriceGroupDefinitionWithAuthorization(payload, credential, requestCorrelation, options),
  );
