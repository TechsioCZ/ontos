// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';

import { ValidatePriceGroupCompatibilityApi } from '../apis/validate-price-group-compatibility.ts';
import type { ValidatePriceGroupCompatibilityRequest } from '../apis/validate-price-group-compatibility.ts';
import { operationGateway } from './action-gateway.ts';

export interface ValidatePriceGroupCompatibilityClientOptions {
  readonly baseUrl?: string | URL;
}

type ValidatePriceGroupCompatibilityAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ValidatePriceGroupCompatibilityClientOptions,
];
type ValidatePriceGroupCompatibilityOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ValidatePriceGroupCompatibilityClientOptions,
];

const validatePriceGroupCompatibilityClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ValidatePriceGroupCompatibilityClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ValidatePriceGroupCompatibilityApi,
      credential,
      defaultApiPrefix: '/price-group-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeValidatePriceGroupCompatibilityWithAuthorization = (
  payload: ValidatePriceGroupCompatibilityRequest,
  ...[credential, requestCorrelation, options = {}]: ValidatePriceGroupCompatibilityAuthorizedInvocation
) =>
  validatePriceGroupCompatibilityClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.validatePriceGroupCompatibility.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeValidatePriceGroupCompatibility = (
  payload: ValidatePriceGroupCompatibilityRequest,
  ...[requestCorrelation, options = {}]: ValidatePriceGroupCompatibilityOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeValidatePriceGroupCompatibilityWithAuthorization(payload, credential, requestCorrelation, options),
  );
