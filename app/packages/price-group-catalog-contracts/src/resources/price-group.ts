// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner pricing.price-group-catalog
// @ontos-resource-slug price-group
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const checkedIdentifier = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
export const PriceGroupResourceIdSchema = checkedIdentifier.pipe(
  Schema.brand('PriceGroupResourceId'),
  Schema.decodeTo(checkedIdentifier),
);
export const PriceGroupTenantIdSchema = checkedIdentifier.pipe(
  Schema.brand('PriceGroupTenantId'),
  Schema.decodeTo(checkedIdentifier),
);

export const PriceGroupRefSchema = Schema.Struct({
  moduleId: Schema.Literal('pricing.price-group-catalog'),
  resourceId: PriceGroupResourceIdSchema,
  resourceType: Schema.Literal('pricing.price-group-catalog.price-group'),
  tenantId: PriceGroupTenantIdSchema,
});
export type PriceGroupRef = typeof PriceGroupRefSchema.Type;

export const priceGroupResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Stable tenant-scoped Price Group classification identity.',
  key: 'pricing.price-group-catalog.price-group',
  label: 'Price Group',
  owningModuleId: 'pricing.price-group-catalog',
} as const satisfies OntosResourceType;
