diff --git a/dist/cjs/release-envelope/plugin.js b/dist/cjs/release-envelope/plugin.js
--- a/dist/cjs/release-envelope/plugin.js
+++ b/dist/cjs/release-envelope/plugin.js
@@ -76,14 +76,16 @@
             api.onBeforeDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
+                const { apiOnly, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 await emitBuildEnvelope(configuredTarget);
-                const { distDirectory } = api.getAppContext();
                 await (0, external_framework_output_js_namespaceObject.verifyBuildOutputReleaseEnvelope)(distDirectory, configuredTarget);
             });
             api.onAfterDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
-                const { appDirectory, distDirectory } = api.getAppContext();
+                const { apiOnly, appDirectory, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 const outputDirectory = external_node_path_default().join(appDirectory, '.output');
                 const releaseEnvelope = await (0, external_framework_output_js_namespaceObject.emitNodeStagedReleaseEnvelope)({
                     distDirectory,
diff --git a/dist/cjs/cloudflare/index.js b/dist/cjs/cloudflare/index.js
--- a/dist/cjs/cloudflare/index.js
+++ b/dist/cjs/cloudflare/index.js
@@ -83,7 +83,7 @@
                 distDirectory,
                 target: 'cloudflare'
             });
-            if (releaseEnvelopeEnabled) await (0, framework_output_js_namespaceObject.verifyBuildOutputReleaseEnvelope)(distDirectory, 'cloudflare');
+            if (releaseEnvelopeEnabled && !apiOnly) await (0, framework_output_js_namespaceObject.verifyBuildOutputReleaseEnvelope)(distDirectory, 'cloudflare');
             await utils_namespaceObject.fs.copy(distDirectory, publicDirectory, {
                 filter: (src)=>{
                     const relativePath = external_node_path_default().relative(distDirectory, src).replace(/\\/gu, '/');
@@ -120,7 +120,7 @@
                 spaces: 2
             });
             await utils_namespaceObject.fs.writeJSON(outputPlan.paths.outputPackage, outputPlan.packages.output);
-            hasReleaseEnvelope = releaseEnvelopeEnabled && Boolean(await (0, framework_output_js_namespaceObject.stageCloudflareReleaseEnvelope)({
+            hasReleaseEnvelope = releaseEnvelopeEnabled && !apiOnly && Boolean(await (0, framework_output_js_namespaceObject.stageCloudflareReleaseEnvelope)({
                 distDirectory,
                 outputDirectory
             }));
diff --git a/dist/esm-node/cloudflare/index.mjs b/dist/esm-node/cloudflare/index.mjs
--- a/dist/esm-node/cloudflare/index.mjs
+++ b/dist/esm-node/cloudflare/index.mjs
@@ -42,7 +42,7 @@
                 distDirectory,
                 target: 'cloudflare'
             });
-            if (releaseEnvelopeEnabled) await verifyBuildOutputReleaseEnvelope(distDirectory, 'cloudflare');
+            if (releaseEnvelopeEnabled && !apiOnly) await verifyBuildOutputReleaseEnvelope(distDirectory, 'cloudflare');
             await fs.copy(distDirectory, publicDirectory, {
                 filter: (src)=>{
                     const relativePath = node_path.relative(distDirectory, src).replace(/\\/gu, '/');
@@ -79,7 +79,7 @@
                 spaces: 2
             });
             await fs.writeJSON(outputPlan.paths.outputPackage, outputPlan.packages.output);
-            hasReleaseEnvelope = releaseEnvelopeEnabled && Boolean(await stageCloudflareReleaseEnvelope({
+            hasReleaseEnvelope = releaseEnvelopeEnabled && !apiOnly && Boolean(await stageCloudflareReleaseEnvelope({
                 distDirectory,
                 outputDirectory
             }));
diff --git a/dist/cjs/cloudflare-output-verifier/identity.js b/dist/cjs/cloudflare-output-verifier/identity.js
--- a/dist/cjs/cloudflare-output-verifier/identity.js
+++ b/dist/cjs/cloudflare-output-verifier/identity.js
@@ -56,6 +56,7 @@
     ]){
         const marker = stamped.surfaces[surface];
         const expected = declared?.surfaces?.[surface];
+        if (!expected && !marker) continue;
         if (declared?.surfaces && !expected) {
             if (marker) (0, external_issues_js_namespaceObject.addIssue)(issues, {
                 code: 'delivery-unit-drift',
diff --git a/dist/esm-node/cloudflare-output-verifier/identity.mjs b/dist/esm-node/cloudflare-output-verifier/identity.mjs
--- a/dist/esm-node/cloudflare-output-verifier/identity.mjs
+++ b/dist/esm-node/cloudflare-output-verifier/identity.mjs
@@ -25,6 +25,7 @@
     ]){
         const marker = stamped.surfaces[surface];
         const expected = declared?.surfaces?.[surface];
+        if (!expected && !marker) continue;
         if (declared?.surfaces && !expected) {
             if (marker) addIssue(issues, {
                 code: 'delivery-unit-drift',
diff --git a/dist/cjs/cloudflare-output-contract.js b/dist/cjs/cloudflare-output-contract.js
--- a/dist/cjs/cloudflare-output-contract.js
+++ b/dist/cjs/cloudflare-output-contract.js
@@ -54,6 +54,7 @@
     'fs/promises',
     'http',
     'http2',
+    'https',
     'inspector',
     'module',
     'net',
diff --git a/dist/esm-node/cloudflare-output-contract.mjs b/dist/esm-node/cloudflare-output-contract.mjs
--- a/dist/esm-node/cloudflare-output-contract.mjs
+++ b/dist/esm-node/cloudflare-output-contract.mjs
@@ -26,6 +26,7 @@
     'fs/promises',
     'http',
     'http2',
+    'https',
     'inspector',
     'module',
     'net',
diff --git a/dist/esm/cloudflare-output-contract.mjs b/dist/esm/cloudflare-output-contract.mjs
--- a/dist/esm/cloudflare-output-contract.mjs
+++ b/dist/esm/cloudflare-output-contract.mjs
@@ -25,6 +25,7 @@
     'fs/promises',
     'http',
     'http2',
+    'https',
     'inspector',
     'module',
     'net',
diff --git a/dist/esm/cloudflare-output-verifier/identity.mjs b/dist/esm/cloudflare-output-verifier/identity.mjs
--- a/dist/esm/cloudflare-output-verifier/identity.mjs
+++ b/dist/esm/cloudflare-output-verifier/identity.mjs
@@ -24,6 +24,7 @@
     ]){
         const marker = stamped.surfaces[surface];
         const expected = declared?.surfaces?.[surface];
+        if (!expected && !marker) continue;
         if (declared?.surfaces && !expected) {
             if (marker) addIssue(issues, {
                 code: 'delivery-unit-drift',
diff --git a/dist/esm/cloudflare/index.mjs b/dist/esm/cloudflare/index.mjs
--- a/dist/esm/cloudflare/index.mjs
+++ b/dist/esm/cloudflare/index.mjs
@@ -41,7 +41,7 @@
                 distDirectory,
                 target: 'cloudflare'
             });
-            if (releaseEnvelopeEnabled) await verifyBuildOutputReleaseEnvelope(distDirectory, 'cloudflare');
+            if (releaseEnvelopeEnabled && !apiOnly) await verifyBuildOutputReleaseEnvelope(distDirectory, 'cloudflare');
             await fs.copy(distDirectory, publicDirectory, {
                 filter: (src)=>{
                     const relativePath = node_path.relative(distDirectory, src).replace(/\\/gu, '/');
@@ -78,7 +78,7 @@
                 spaces: 2
             });
             await fs.writeJSON(outputPlan.paths.outputPackage, outputPlan.packages.output);
-            hasReleaseEnvelope = releaseEnvelopeEnabled && Boolean(await stageCloudflareReleaseEnvelope({
+            hasReleaseEnvelope = releaseEnvelopeEnabled && !apiOnly && Boolean(await stageCloudflareReleaseEnvelope({
                 distDirectory,
                 outputDirectory
             }));
diff --git a/dist/esm-node/release-envelope/plugin.mjs b/dist/esm-node/release-envelope/plugin.mjs
--- a/dist/esm-node/release-envelope/plugin.mjs
+++ b/dist/esm-node/release-envelope/plugin.mjs
@@ -35,14 +35,16 @@
             api.onBeforeDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
+                const { apiOnly, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 await emitBuildEnvelope(configuredTarget);
-                const { distDirectory } = api.getAppContext();
                 await verifyBuildOutputReleaseEnvelope(distDirectory, configuredTarget);
             });
             api.onAfterDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
-                const { appDirectory, distDirectory } = api.getAppContext();
+                const { apiOnly, appDirectory, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 const outputDirectory = node_path.join(appDirectory, '.output');
                 const releaseEnvelope = await emitNodeStagedReleaseEnvelope({
                     distDirectory,
diff --git a/dist/esm/release-envelope/plugin.mjs b/dist/esm/release-envelope/plugin.mjs
--- a/dist/esm/release-envelope/plugin.mjs
+++ b/dist/esm/release-envelope/plugin.mjs
@@ -34,14 +34,16 @@
             api.onBeforeDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
+                const { apiOnly, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 await emitBuildEnvelope(configuredTarget);
-                const { distDirectory } = api.getAppContext();
                 await verifyBuildOutputReleaseEnvelope(distDirectory, configuredTarget);
             });
             api.onAfterDeploy(async ()=>{
                 const configuredTarget = resolveActiveDeployTarget(api, resolveDeployTarget);
                 if ('node' !== configuredTarget) return;
-                const { appDirectory, distDirectory } = api.getAppContext();
+                const { apiOnly, appDirectory, distDirectory } = api.getAppContext();
+                if (apiOnly) return;
                 const outputDirectory = node_path.join(appDirectory, '.output');
                 const releaseEnvelope = await emitNodeStagedReleaseEnvelope({
                     distDirectory,
