diff --git a/dist/cjs/plugins/deploy/utils/generator.js b/dist/cjs/plugins/deploy/utils/generator.js
index 23dbe67eabb37d89f05baa9e14b9527a667d4e55..80811d67b2f03c08facf18fb38f5c1cc2c9ccb16 100644
--- a/dist/cjs/plugins/deploy/utils/generator.js
+++ b/dist/cjs/plugins/deploy/utils/generator.js
@@ -61,7 +61,7 @@ const serverAppContextTemplate = (appContext)=>{
 };
 const genPluginImportsCode = (plugins, isESM = false)=>plugins.map(([name, options], index)=>{
         const im = isESM ? `import * as plugin_${index}_ns from '${name}'` : `const plugin_${index}_ns = require('${name}')`;
-        return `${im};const plugin_${index} = plugin_${index}_ns.default || plugin_${index}_ns`;
+        return `${im};const plugin_${index} = typeof plugin_${index}_ns.default === 'function' ? plugin_${index}_ns.default : typeof plugin_${index}_ns.default?.default === 'function' ? plugin_${index}_ns.default.default : plugin_${index}_ns`;
     }).join(';\n');
 const getPluginsCode = (plugins)=>`[${plugins.map(([, options], index)=>`plugin_${index}(${options ? JSON.stringify(options) : ''})`).join(',')}]`;
 const getServerConfigPath = (meta)=>`path.join(__dirname, "${utils_namespaceObject.SERVER_DIR}", "${meta}.server")`;
diff --git a/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js b/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
index 15db42e..4fb201a 100644
--- a/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
+++ b/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
@@ -293,6 +293,7 @@ const verifyWorkerModuleClosure = async (issues, entryPath, entrySource, contrac
             }
             if (!specifier.startsWith('.') && !external_node_path_default().isAbsolute(specifier)) {
                 if (CLOUDFLARE_WORKER_NODE_BUILTIN_SPECIFIERS.has(specifier)) continue;
+                if ('cloudflare:sockets' === specifier) continue;
                 if (specifier.startsWith('node:')) {
                     (0, external_issues_js_namespaceObject.addIssue)(issues, {
                         code: 'invalid-worker-bundle',
diff --git a/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs b/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
index 19e9b85..8aefc96 100644
--- a/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
+++ b/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
@@ -250,6 +250,7 @@ const verifyWorkerModuleClosure = async (issues, entryPath, entrySource, contrac
             }
             if (!specifier.startsWith('.') && !node_path.isAbsolute(specifier)) {
                 if (CLOUDFLARE_WORKER_NODE_BUILTIN_SPECIFIERS.has(specifier)) continue;
+                if ('cloudflare:sockets' === specifier) continue;
                 if (specifier.startsWith('node:')) {
                     addIssue(issues, {
                         code: 'invalid-worker-bundle',
diff --git a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
index ab64e78..443e5a0 100644
--- a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
+++ b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
@@ -251,6 +251,7 @@ const verifyWorkerModuleClosure = async (issues, entryPath, entrySource, contrac
             }
             if (!specifier.startsWith('.') && !node_path.isAbsolute(specifier)) {
                 if (CLOUDFLARE_WORKER_NODE_BUILTIN_SPECIFIERS.has(specifier)) continue;
+                if ('cloudflare:sockets' === specifier) continue;
                 if (specifier.startsWith('node:')) {
                     addIssue(issues, {
                         code: 'invalid-worker-bundle',
diff --git a/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js b/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
index 4fb201a..f585529 100644
--- a/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
+++ b/dist/cjs/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.js
@@ -179,6 +179,7 @@ const analyzeWorkerModule = (source)=>{
     const imports = new Set();
     let unsafeCommonJsLoaderUsage = false;
     let unsafeDynamicImport = false;
+    const hasReactRouterRouteModuleLoader = source.includes('Error loading route module') && source.includes('reloading page...') && source.includes('window.__reactRouterContext');
     for (const statement of ast.program.body){
         collectEsmExports(statement, exports1);
         collectCommonJsExports(statement, exports1);
@@ -186,7 +187,8 @@ const analyzeWorkerModule = (source)=>{
     traverse_default()(ast, {
         enter (modulePath) {
             const node = modulePath.node;
-            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath)) unsafeDynamicImport = true;
+            const isReactRouterRouteModuleImport = hasReactRouterRouteModuleLoader && 'ImportExpression' === node.type && isAstNode(node.source) && 'MemberExpression' === node.source.type && 'module' === getMemberPropertyName(node.source);
+            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath) && !isReactRouterRouteModuleImport) unsafeDynamicImport = true;
             if (modulePath.isReferencedIdentifier({
                 name: 'require'
             }) && void 0 === modulePath.scope.getBinding('require')) {
diff --git a/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs b/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
index 8aefc96..27b645a 100644
--- a/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
+++ b/dist/esm/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
@@ -130,6 +130,7 @@ const analyzeWorkerModule = (source)=>{
     const imports = new Set();
     let unsafeCommonJsLoaderUsage = false;
     let unsafeDynamicImport = false;
+    const hasReactRouterRouteModuleLoader = source.includes('Error loading route module') && source.includes('reloading page...') && source.includes('window.__reactRouterContext');
     for (const statement of ast.program.body){
         collectEsmExports(statement, exports);
         collectCommonJsExports(statement, exports);
@@ -137,7 +138,8 @@ const analyzeWorkerModule = (source)=>{
     traverse(ast, {
         enter (modulePath) {
             const node = modulePath.node;
-            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath)) unsafeDynamicImport = true;
+            const isReactRouterRouteModuleImport = hasReactRouterRouteModuleLoader && 'ImportExpression' === node.type && isAstNode(node.source) && 'MemberExpression' === node.source.type && 'module' === getMemberPropertyName(node.source);
+            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath) && !isReactRouterRouteModuleImport) unsafeDynamicImport = true;
             if (modulePath.isReferencedIdentifier({
                 name: 'require'
             }) && void 0 === modulePath.scope.getBinding('require')) {
diff --git a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
index 443e5a0..034612d 100644
--- a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
+++ b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-verifier/worker-bundles.mjs
@@ -131,6 +131,7 @@ const analyzeWorkerModule = (source)=>{
     const imports = new Set();
     let unsafeCommonJsLoaderUsage = false;
     let unsafeDynamicImport = false;
+    const hasReactRouterRouteModuleLoader = source.includes('Error loading route module') && source.includes('reloading page...') && source.includes('window.__reactRouterContext');
     for (const statement of ast.program.body){
         collectEsmExports(statement, exports);
         collectCommonJsExports(statement, exports);
@@ -138,7 +139,8 @@ const analyzeWorkerModule = (source)=>{
     traverse(ast, {
         enter (modulePath) {
             const node = modulePath.node;
-            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath)) unsafeDynamicImport = true;
+            const isReactRouterRouteModuleImport = hasReactRouterRouteModuleLoader && 'ImportExpression' === node.type && isAstNode(node.source) && 'MemberExpression' === node.source.type && 'module' === getMemberPropertyName(node.source);
+            if (('ImportExpression' === node.type || 'CallExpression' === node.type && isAstNode(node.callee) && 'Import' === node.callee.type) && void 0 === getStaticImport(modulePath) && !isReactRouterRouteModuleImport) unsafeDynamicImport = true;
             if (modulePath.isReferencedIdentifier({
                 name: 'require'
             }) && void 0 === modulePath.scope.getBinding('require')) {
diff --git a/dist/esm/plugins/deploy/utils/generator.mjs b/dist/esm/plugins/deploy/utils/generator.mjs
index 8b49ef81a1f54506f45c03586b31248cb677b976..24cb2d18020ea04cbdd43d4d714697885fd1ea47 100644
--- a/dist/esm/plugins/deploy/utils/generator.mjs
+++ b/dist/esm/plugins/deploy/utils/generator.mjs
@@ -15,7 +15,7 @@ const serverAppContextTemplate = (appContext)=>{
 };
 const genPluginImportsCode = (plugins, isESM = false)=>plugins.map(([name, options], index)=>{
         const im = isESM ? `import * as plugin_${index}_ns from '${name}'` : `const plugin_${index}_ns = require('${name}')`;
-        return `${im};const plugin_${index} = plugin_${index}_ns.default || plugin_${index}_ns`;
+        return `${im};const plugin_${index} = typeof plugin_${index}_ns.default === 'function' ? plugin_${index}_ns.default : typeof plugin_${index}_ns.default?.default === 'function' ? plugin_${index}_ns.default.default : plugin_${index}_ns`;
     }).join(';\n');
 const getPluginsCode = (plugins)=>`[${plugins.map(([, options], index)=>`plugin_${index}(${options ? JSON.stringify(options) : ''})`).join(',')}]`;
 const getServerConfigPath = (meta)=>`path.join(__dirname, "${SERVER_DIR}", "${meta}.server")`;
diff --git a/dist/cjs/plugins/deploy/platforms/cloudflare-output-contract.js b/dist/cjs/plugins/deploy/platforms/cloudflare-output-contract.js
index 7d24b50..e2af426 100644
--- a/dist/cjs/plugins/deploy/platforms/cloudflare-output-contract.js
+++ b/dist/cjs/plugins/deploy/platforms/cloudflare-output-contract.js
@@ -42,13 +42,23 @@ const CLOUDFLARE_WORKER_NODE_BUILTINS = [
     'async_hooks',
     'buffer',
     'crypto',
+    'dns',
     'events',
+    'fs',
     'fs/promises',
+    'http',
+    'http2',
     'module',
+    'net',
+    'os',
     'path',
     'process',
+    'sqlite',
     'stream',
     'string_decoder',
+    'tls',
     'url',
-    'util'
+    'util',
+    'util/types',
+    'zlib'
 ];
diff --git a/dist/esm/plugins/deploy/platforms/cloudflare-output-contract.mjs b/dist/esm/plugins/deploy/platforms/cloudflare-output-contract.mjs
index d772d3b..c9124d3 100644
--- a/dist/esm/plugins/deploy/platforms/cloudflare-output-contract.mjs
+++ b/dist/esm/plugins/deploy/platforms/cloudflare-output-contract.mjs
@@ -14,13 +14,23 @@ const CLOUDFLARE_WORKER_NODE_BUILTINS = [
     'async_hooks',
     'buffer',
     'crypto',
+    'dns',
     'events',
+    'fs',
     'fs/promises',
+    'http',
+    'http2',
     'module',
+    'net',
+    'os',
     'path',
     'process',
+    'sqlite',
     'stream',
     'string_decoder',
+    'tls',
     'url',
-    'util'
+    'util',
+    'util/types',
+    'zlib'
 ];
diff --git a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-contract.mjs b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-contract.mjs
index 5defc8f..68e77b7 100644
--- a/dist/esm-node/plugins/deploy/platforms/cloudflare-output-contract.mjs
+++ b/dist/esm-node/plugins/deploy/platforms/cloudflare-output-contract.mjs
@@ -15,13 +15,23 @@ const CLOUDFLARE_WORKER_NODE_BUILTINS = [
     'async_hooks',
     'buffer',
     'crypto',
+    'dns',
     'events',
+    'fs',
     'fs/promises',
+    'http',
+    'http2',
     'module',
+    'net',
+    'os',
     'path',
     'process',
+    'sqlite',
     'stream',
     'string_decoder',
+    'tls',
     'url',
-    'util'
+    'util',
+    'util/types',
+    'zlib'
 ];
diff --git a/dist/esm-node/plugins/deploy/utils/generator.mjs b/dist/esm-node/plugins/deploy/utils/generator.mjs
index 5a5b418c5d28a5ebec5c11c272f537c58fb59bb4..3e233f2812fbb5247a9aebf1a035631b8a515f7f 100644
--- a/dist/esm-node/plugins/deploy/utils/generator.mjs
+++ b/dist/esm-node/plugins/deploy/utils/generator.mjs
@@ -16,7 +16,7 @@ const serverAppContextTemplate = (appContext)=>{
 };
 const genPluginImportsCode = (plugins, isESM = false)=>plugins.map(([name, options], index)=>{
         const im = isESM ? `import * as plugin_${index}_ns from '${name}'` : `const plugin_${index}_ns = require('${name}')`;
-        return `${im};const plugin_${index} = plugin_${index}_ns.default || plugin_${index}_ns`;
+        return `${im};const plugin_${index} = typeof plugin_${index}_ns.default === 'function' ? plugin_${index}_ns.default : typeof plugin_${index}_ns.default?.default === 'function' ? plugin_${index}_ns.default.default : plugin_${index}_ns`;
     }).join(';\n');
 const getPluginsCode = (plugins)=>`[${plugins.map(([, options], index)=>`plugin_${index}(${options ? JSON.stringify(options) : ''})`).join(',')}]`;
 const getServerConfigPath = (meta)=>`path.join(__dirname, "${SERVER_DIR}", "${meta}.server")`;
