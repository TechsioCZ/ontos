/** Arithmetic over the same literals whose names are not durations. */
const total = 7;

export const requestsPerMinute = total / 60;
export const pricePerHour = total * 60;
export const gridColumnsCount = 5 * 60;
