/** Arithmetic that is not a duration: either the name or the factor rules it out. */
const price = 3;
const base = 11;

export const columns = 5 * 60;
export const totalCents = price * 1000;
export const cacheMs = base * 7;
export const halfMs = base / 2;
