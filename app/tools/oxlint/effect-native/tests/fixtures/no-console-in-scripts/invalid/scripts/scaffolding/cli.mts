// expect-count: 1
// Policy boundary: oxlint.config.ts allows successful operational output; audit B3/A6 targets diagnostic logging.
export const generatedHandler = `
export function handler(): void {
	console.log("generated output");
	process.stdout.write("generated bytes\\n");
}
`;

export function runScaffold(help: string, message: string): void {
	console.log(help);
	console.error(`Scaffold failed: ${message}`);
}
