export const render = `
// await fetch('/x').then(f); async; Effect.runPromise(task);
const chunks = 'static/js/async/';
const queue = "async-queue";
const managed = runtime.runPromise(program);
const local = runSync(localProgram);
const effect = ManagedRuntime.make(AppLayer);
export const adapt = () => effect.runPromise(program);
const effectRouteParams = Schema.Struct({ id: CustomerId });
type Headers = Readonly<Partial<Record<string, string>>>;
`;
console.log(`await review before merging`);
