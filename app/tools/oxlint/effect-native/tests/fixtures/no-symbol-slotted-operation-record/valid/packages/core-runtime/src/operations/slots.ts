/** Declaring a symbol is not the defect; slotting a capability behind it is. */
export const operationHandler: unique symbol = Symbol('@app/core-runtime/operations/handler');
export const OPERATION_KEY = 'operation';
