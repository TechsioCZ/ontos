// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug add-product-category-assignment
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { addProductCategoryAssignmentAction } from '../src/actions/add-product-category-assignment.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  addProductCategoryAssignmentActionProblem,
  addProductCategoryAssignmentActionSchemaErrorLive,
  mapAddProductCategoryAssignmentActionProblem,
} from './add-product-category-assignment-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: addProductCategoryAssignmentActionProblem.authentication,
  unavailable: () => addProductCategoryAssignmentActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('AddProductCategoryAssignmentActionServer.execute')(
  function* executeAddProductCategoryAssignment(
    payload: typeof addProductCategoryAssignmentAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(addProductCategoryAssignmentActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(addProductCategoryAssignmentAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(addProductCategoryAssignmentActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: addProductCategoryAssignmentActionProblem.internal,
      invalidCorrelationProblem: addProductCategoryAssignmentActionProblem.invalid,
      mapError: mapAddProductCategoryAssignmentActionProblem,
      payload: decoded,
      registration: addProductCategoryAssignmentAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const addProductCategoryAssignmentActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'addProductCategoryAssignmentAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(addProductCategoryAssignmentActionSchemaErrorLive));
