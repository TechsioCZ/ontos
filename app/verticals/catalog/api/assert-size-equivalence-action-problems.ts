// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug assert-size-equivalence
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  AssertSizeEquivalenceActionAlreadyCommittedProblemSchema,
  AssertSizeEquivalenceActionAuthenticationProblemSchema,
  AssertSizeEquivalenceActionCommitIndeterminateProblemSchema,
  AssertSizeEquivalenceActionConflictProblemSchema,
  AssertSizeEquivalenceActionForbiddenProblemSchema,
  AssertSizeEquivalenceActionIneligibleProblemSchema,
  AssertSizeEquivalenceActionInternalProblemSchema,
  AssertSizeEquivalenceActionInvalidProblemSchema,
  AssertSizeEquivalenceActionNotFoundProblemSchema,
  AssertSizeEquivalenceActionPreconditionProblemSchema,
  AssertSizeEquivalenceActionSchemaErrorMiddleware,
  AssertSizeEquivalenceActionUnavailableProblemSchema,
} from '../shared/apis/assert-size-equivalence-action.ts';
import type { AssertSizeEquivalenceActionProblem } from '../shared/apis/assert-size-equivalence-action.ts';
import { assertSizeEquivalenceAction } from '../src/actions/assert-size-equivalence.action.ts';

type DomainError = typeof assertSizeEquivalenceAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends AssertSizeEquivalenceActionProblem['_tag']> = Extract<
  AssertSizeEquivalenceActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const assertSizeEquivalenceActionProblem = {
  authentication: (): ProblemOf<'AssertSizeEquivalenceActionAuthenticationProblem'> =>
    AssertSizeEquivalenceActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'AssertSizeEquivalenceActionConflictProblem'>['code'],
  ): ProblemOf<'AssertSizeEquivalenceActionConflictProblem'> =>
    AssertSizeEquivalenceActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'AssertSizeEquivalenceActionForbiddenProblem'>['code'],
  ): ProblemOf<'AssertSizeEquivalenceActionForbiddenProblem'> =>
    AssertSizeEquivalenceActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'AssertSizeEquivalenceActionIneligibleProblem'>['code'],
  ): ProblemOf<'AssertSizeEquivalenceActionIneligibleProblem'> =>
    AssertSizeEquivalenceActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'AssertSizeEquivalenceActionInternalProblem'> =>
    AssertSizeEquivalenceActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'AssertSizeEquivalenceActionInvalidProblem'> =>
    AssertSizeEquivalenceActionInvalidProblemSchema.make({
      detail: 'The assert-size-equivalence Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'AssertSizeEquivalenceActionNotFoundProblem'>['code'],
  ): ProblemOf<'AssertSizeEquivalenceActionNotFoundProblem'> =>
    AssertSizeEquivalenceActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'AssertSizeEquivalenceActionPreconditionProblem'> =>
    AssertSizeEquivalenceActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'AssertSizeEquivalenceActionUnavailableProblem'>['code'],
  ): ProblemOf<'AssertSizeEquivalenceActionUnavailableProblem'> =>
    AssertSizeEquivalenceActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): AssertSizeEquivalenceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceUnavailable: () =>
        assertSizeEquivalenceActionProblem.unavailable('catalog_persistence_unavailable'),
      SizePersistenceConflict: (failure) =>
        Match.value(failure.conflict).pipe(
          Match.when('INVALID_INPUT', assertSizeEquivalenceActionProblem.invalid),
          Match.when('NOT_FOUND', () => assertSizeEquivalenceActionProblem.notFound('size_equivalence_not_found')),
          Match.when('RETIRED', () => assertSizeEquivalenceActionProblem.ineligible('size_equivalence_ineligible')),
          Match.whenOr('REVISION', 'ACTION_INVOCATION_ID', () =>
            assertSizeEquivalenceActionProblem.conflict('size_equivalence_conflict'),
          ),
          Match.exhaustive,
        ),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): AssertSizeEquivalenceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        AssertSizeEquivalenceActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSERT_SIZE_EQUIVALENCE',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: assertSizeEquivalenceActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        AssertSizeEquivalenceActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSERT_SIZE_EQUIVALENCE',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: assertSizeEquivalenceActionProblem.internal,
      ActionIdempotencyKeyRequired: assertSizeEquivalenceActionProblem.precondition,
      ActionInvocationNotFound: (failure) => assertSizeEquivalenceActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => assertSizeEquivalenceActionProblem.conflict(failure.code),
      ActionPayloadValidationError: assertSizeEquivalenceActionProblem.invalid,
      ActionPermissionCheckError: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => assertSizeEquivalenceActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => assertSizeEquivalenceActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => assertSizeEquivalenceActionProblem.conflict(failure.code),
      ActionResultValidationError: assertSizeEquivalenceActionProblem.internal,
      ActionTransactionError: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: assertSizeEquivalenceActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => assertSizeEquivalenceActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: assertSizeEquivalenceActionProblem.authentication,
      OperationContextDenied: (failure) => assertSizeEquivalenceActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => assertSizeEquivalenceActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => assertSizeEquivalenceActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(assertSizeEquivalenceAction.descriptor.domainErrorSchema);
export const mapAssertSizeEquivalenceActionProblem = (
  error: ActionCoreError | DomainError,
): AssertSizeEquivalenceActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const assertSizeEquivalenceActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  AssertSizeEquivalenceActionSchemaErrorMiddleware,
  () => Effect.fail(assertSizeEquivalenceActionProblem.invalid()),
);
