// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug assign-catalog-media
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  AssignCatalogMediaActionAlreadyCommittedProblemSchema,
  AssignCatalogMediaActionAuthenticationProblemSchema,
  AssignCatalogMediaActionCommitIndeterminateProblemSchema,
  AssignCatalogMediaActionConflictProblemSchema,
  AssignCatalogMediaActionForbiddenProblemSchema,
  AssignCatalogMediaActionIneligibleProblemSchema,
  AssignCatalogMediaActionInternalProblemSchema,
  AssignCatalogMediaActionInvalidProblemSchema,
  AssignCatalogMediaActionNotFoundProblemSchema,
  AssignCatalogMediaActionPreconditionProblemSchema,
  AssignCatalogMediaActionSchemaErrorMiddleware,
  AssignCatalogMediaActionUnavailableProblemSchema,
} from '../shared/apis/assign-catalog-media-action.ts';
import type { AssignCatalogMediaActionProblem } from '../shared/apis/assign-catalog-media-action.ts';
import { assignCatalogMediaAction } from '../src/actions/assign-catalog-media.action.ts';

type DomainError = typeof assignCatalogMediaAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends AssignCatalogMediaActionProblem['_tag']> = Extract<
  AssignCatalogMediaActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const assignCatalogMediaActionProblem = {
  authentication: (): ProblemOf<'AssignCatalogMediaActionAuthenticationProblem'> =>
    AssignCatalogMediaActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'AssignCatalogMediaActionConflictProblem'>['code'],
  ): ProblemOf<'AssignCatalogMediaActionConflictProblem'> =>
    AssignCatalogMediaActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'AssignCatalogMediaActionForbiddenProblem'>['code'],
  ): ProblemOf<'AssignCatalogMediaActionForbiddenProblem'> =>
    AssignCatalogMediaActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'AssignCatalogMediaActionIneligibleProblem'>['code'],
  ): ProblemOf<'AssignCatalogMediaActionIneligibleProblem'> =>
    AssignCatalogMediaActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'AssignCatalogMediaActionInternalProblem'> =>
    AssignCatalogMediaActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'AssignCatalogMediaActionInvalidProblem'> =>
    AssignCatalogMediaActionInvalidProblemSchema.make({
      detail: 'The assign-catalog-media Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'AssignCatalogMediaActionNotFoundProblem'>['code'],
  ): ProblemOf<'AssignCatalogMediaActionNotFoundProblem'> =>
    AssignCatalogMediaActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'AssignCatalogMediaActionPreconditionProblem'> =>
    AssignCatalogMediaActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'AssignCatalogMediaActionUnavailableProblem'>['code'],
  ): ProblemOf<'AssignCatalogMediaActionUnavailableProblem'> =>
    AssignCatalogMediaActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): AssignCatalogMediaActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogMediaConflict: () => assignCatalogMediaActionProblem.conflict('catalog_media_conflict'),
      CatalogMediaNotFound: () => assignCatalogMediaActionProblem.notFound('catalog_media_not_found'),
      CatalogPersistenceUnavailable: () =>
        assignCatalogMediaActionProblem.unavailable('catalog_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): AssignCatalogMediaActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        AssignCatalogMediaActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSIGN_CATALOG_MEDIA',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: assignCatalogMediaActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        AssignCatalogMediaActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSIGN_CATALOG_MEDIA',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: assignCatalogMediaActionProblem.internal,
      ActionIdempotencyKeyRequired: assignCatalogMediaActionProblem.precondition,
      ActionInvocationNotFound: (failure) => assignCatalogMediaActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => assignCatalogMediaActionProblem.conflict(failure.code),
      ActionPayloadValidationError: assignCatalogMediaActionProblem.invalid,
      ActionPermissionCheckError: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => assignCatalogMediaActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => assignCatalogMediaActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => assignCatalogMediaActionProblem.conflict(failure.code),
      ActionResultValidationError: assignCatalogMediaActionProblem.internal,
      ActionTransactionError: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: assignCatalogMediaActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => assignCatalogMediaActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: assignCatalogMediaActionProblem.authentication,
      OperationContextDenied: (failure) => assignCatalogMediaActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => assignCatalogMediaActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => assignCatalogMediaActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(assignCatalogMediaAction.descriptor.domainErrorSchema);
export const mapAssignCatalogMediaActionProblem = (
  error: ActionCoreError | DomainError,
): AssignCatalogMediaActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const assignCatalogMediaActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  AssignCatalogMediaActionSchemaErrorMiddleware,
  () => Effect.fail(assignCatalogMediaActionProblem.invalid()),
);
