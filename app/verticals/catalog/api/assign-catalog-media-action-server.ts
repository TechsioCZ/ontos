// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug assign-catalog-media
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { assignCatalogMediaAction } from '../src/actions/assign-catalog-media.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  assignCatalogMediaActionProblem,
  assignCatalogMediaActionSchemaErrorLive,
  mapAssignCatalogMediaActionProblem,
} from './assign-catalog-media-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: assignCatalogMediaActionProblem.authentication,
  unavailable: () => assignCatalogMediaActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('AssignCatalogMediaActionServer.execute')(function* executeAssignCatalogMedia(
  payload: typeof assignCatalogMediaAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(assignCatalogMediaActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(assignCatalogMediaAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(assignCatalogMediaActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: assignCatalogMediaActionProblem.internal,
    invalidCorrelationProblem: assignCatalogMediaActionProblem.invalid,
    mapError: mapAssignCatalogMediaActionProblem,
    payload: decoded,
    registration: assignCatalogMediaAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const assignCatalogMediaActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'assignCatalogMediaAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(assignCatalogMediaActionSchemaErrorLive));
