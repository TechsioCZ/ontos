// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug assign-sku
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  AssignSkuActionAlreadyCommittedProblemSchema,
  AssignSkuActionAuthenticationProblemSchema,
  AssignSkuActionCommitIndeterminateProblemSchema,
  AssignSkuActionConflictProblemSchema,
  AssignSkuActionForbiddenProblemSchema,
  AssignSkuActionIneligibleProblemSchema,
  AssignSkuActionInternalProblemSchema,
  AssignSkuActionInvalidProblemSchema,
  AssignSkuActionNotFoundProblemSchema,
  AssignSkuActionPreconditionProblemSchema,
  AssignSkuActionSchemaErrorMiddleware,
  AssignSkuActionUnavailableProblemSchema,
} from '../shared/apis/assign-sku-action.ts';
import type { AssignSkuActionProblem } from '../shared/apis/assign-sku-action.ts';
import { assignSkuAction } from '../src/actions/assign-sku.action.ts';

type DomainError = typeof assignSkuAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends AssignSkuActionProblem['_tag']> = Extract<AssignSkuActionProblem, { readonly _tag: Tag }>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const assignSkuActionProblem = {
  authentication: (): ProblemOf<'AssignSkuActionAuthenticationProblem'> =>
    AssignSkuActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (code: ProblemOf<'AssignSkuActionConflictProblem'>['code']): ProblemOf<'AssignSkuActionConflictProblem'> =>
    AssignSkuActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'AssignSkuActionForbiddenProblem'>['code'],
  ): ProblemOf<'AssignSkuActionForbiddenProblem'> =>
    AssignSkuActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'AssignSkuActionIneligibleProblem'>['code'],
  ): ProblemOf<'AssignSkuActionIneligibleProblem'> =>
    AssignSkuActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'AssignSkuActionInternalProblem'> =>
    AssignSkuActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'AssignSkuActionInvalidProblem'> =>
    AssignSkuActionInvalidProblemSchema.make({
      detail: 'The assign-sku Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (code: ProblemOf<'AssignSkuActionNotFoundProblem'>['code']): ProblemOf<'AssignSkuActionNotFoundProblem'> =>
    AssignSkuActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'AssignSkuActionPreconditionProblem'> =>
    AssignSkuActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'AssignSkuActionUnavailableProblem'>['code'],
  ): ProblemOf<'AssignSkuActionUnavailableProblem'> =>
    AssignSkuActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): AssignSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      SkuActionConflict: () => assignSkuActionProblem.conflict('sku_action_conflict'),
      SkuActionInvalid: () => assignSkuActionProblem.ineligible('sku_action_invalid'),
      SkuActionNotFound: () => assignSkuActionProblem.notFound('sku_action_not_found'),
      SkuActionStale: () => assignSkuActionProblem.conflict('sku_action_stale'),
      SkuPersistenceUnavailable: () => assignSkuActionProblem.unavailable('sku_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): AssignSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        AssignSkuActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSIGN_SKU',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: assignSkuActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        AssignSkuActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_ASSIGN_SKU',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: assignSkuActionProblem.internal,
      ActionIdempotencyKeyRequired: assignSkuActionProblem.precondition,
      ActionInvocationNotFound: (failure) => assignSkuActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => assignSkuActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => assignSkuActionProblem.conflict(failure.code),
      ActionPayloadValidationError: assignSkuActionProblem.invalid,
      ActionPermissionCheckError: (failure) => assignSkuActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => assignSkuActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => assignSkuActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => assignSkuActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => assignSkuActionProblem.conflict(failure.code),
      ActionResultValidationError: assignSkuActionProblem.internal,
      ActionTransactionError: (failure) => assignSkuActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: assignSkuActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => assignSkuActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => assignSkuActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: assignSkuActionProblem.authentication,
      OperationContextDenied: (failure) => assignSkuActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => assignSkuActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => assignSkuActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(assignSkuAction.descriptor.domainErrorSchema);
export const mapAssignSkuActionProblem = (error: ActionCoreError | DomainError): AssignSkuActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const assignSkuActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  AssignSkuActionSchemaErrorMiddleware,
  () => Effect.fail(assignSkuActionProblem.invalid()),
);
