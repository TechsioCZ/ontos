// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug change-local-override
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { changeLocalOverrideAction } from '../src/actions/change-local-override.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  changeLocalOverrideActionProblem,
  changeLocalOverrideActionSchemaErrorLive,
  mapChangeLocalOverrideActionProblem,
} from './change-local-override-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: changeLocalOverrideActionProblem.authentication,
  unavailable: () => changeLocalOverrideActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ChangeLocalOverrideActionServer.execute')(function* executeChangeLocalOverride(
  payload: typeof changeLocalOverrideAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(changeLocalOverrideActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(changeLocalOverrideAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(changeLocalOverrideActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: changeLocalOverrideActionProblem.internal,
    invalidCorrelationProblem: changeLocalOverrideActionProblem.invalid,
    mapError: mapChangeLocalOverrideActionProblem,
    payload: decoded,
    registration: changeLocalOverrideAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const changeLocalOverrideActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'changeLocalOverrideAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(changeLocalOverrideActionSchemaErrorLive));
