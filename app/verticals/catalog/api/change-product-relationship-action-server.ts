// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug change-product-relationship
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { changeProductRelationshipAction } from '../src/actions/change-product-relationship.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  changeProductRelationshipActionProblem,
  changeProductRelationshipActionSchemaErrorLive,
  mapChangeProductRelationshipActionProblem,
} from './change-product-relationship-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: changeProductRelationshipActionProblem.authentication,
  unavailable: () => changeProductRelationshipActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ChangeProductRelationshipActionServer.execute')(function* executeChangeProductRelationship(
  payload: typeof changeProductRelationshipAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(changeProductRelationshipActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(changeProductRelationshipAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(changeProductRelationshipActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: changeProductRelationshipActionProblem.internal,
    invalidCorrelationProblem: changeProductRelationshipActionProblem.invalid,
    mapError: mapChangeProductRelationshipActionProblem,
    payload: decoded,
    registration: changeProductRelationshipAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const changeProductRelationshipActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'changeProductRelationshipAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(changeProductRelationshipActionSchemaErrorLive));
