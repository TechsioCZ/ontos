// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug correct-product
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CorrectProductActionAlreadyCommittedProblemSchema,
  CorrectProductActionAuthenticationProblemSchema,
  CorrectProductActionCommitIndeterminateProblemSchema,
  CorrectProductActionConflictProblemSchema,
  CorrectProductActionForbiddenProblemSchema,
  CorrectProductActionIneligibleProblemSchema,
  CorrectProductActionInternalProblemSchema,
  CorrectProductActionInvalidProblemSchema,
  CorrectProductActionNotFoundProblemSchema,
  CorrectProductActionPreconditionProblemSchema,
  CorrectProductActionSchemaErrorMiddleware,
  CorrectProductActionUnavailableProblemSchema,
} from '../shared/apis/correct-product-action.ts';
import type { CorrectProductActionProblem } from '../shared/apis/correct-product-action.ts';
import { correctProductAction } from '../src/actions/correct-product.action.ts';

type DomainError = typeof correctProductAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CorrectProductActionProblem['_tag']> = Extract<
  CorrectProductActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const correctProductActionProblem = {
  authentication: (): ProblemOf<'CorrectProductActionAuthenticationProblem'> =>
    CorrectProductActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CorrectProductActionConflictProblem'>['code'],
  ): ProblemOf<'CorrectProductActionConflictProblem'> =>
    CorrectProductActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CorrectProductActionForbiddenProblem'>['code'],
  ): ProblemOf<'CorrectProductActionForbiddenProblem'> =>
    CorrectProductActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CorrectProductActionIneligibleProblem'>['code'],
  ): ProblemOf<'CorrectProductActionIneligibleProblem'> =>
    CorrectProductActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CorrectProductActionInternalProblem'> =>
    CorrectProductActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CorrectProductActionInvalidProblem'> =>
    CorrectProductActionInvalidProblemSchema.make({
      detail: 'The correct-product Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CorrectProductActionNotFoundProblem'>['code'],
  ): ProblemOf<'CorrectProductActionNotFoundProblem'> =>
    CorrectProductActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CorrectProductActionPreconditionProblem'> =>
    CorrectProductActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CorrectProductActionUnavailableProblem'>['code'],
  ): ProblemOf<'CorrectProductActionUnavailableProblem'> =>
    CorrectProductActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CorrectProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceConflict: () => correctProductActionProblem.conflict('catalog_persistence_conflict'),
      CatalogPersistenceUnavailable: () => correctProductActionProblem.unavailable('catalog_persistence_unavailable'),
      ProductCorrectionRequired: () => correctProductActionProblem.ineligible('product_correction_required'),
      ProductLifecycleConflict: () => correctProductActionProblem.conflict('product_lifecycle_conflict'),
      ProductNotCatalogReady: () => correctProductActionProblem.ineligible('product_not_catalog_ready'),
      ProductNotFound: () => correctProductActionProblem.notFound('product_not_found'),
      ProductPersistenceConflict: () => correctProductActionProblem.conflict('product_persistence_conflict'),
      ProductRevisionConflict: () => correctProductActionProblem.conflict('product_revision_conflict'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CorrectProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CorrectProductActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CORRECT_PRODUCT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: correctProductActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CorrectProductActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CORRECT_PRODUCT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: correctProductActionProblem.internal,
      ActionIdempotencyKeyRequired: correctProductActionProblem.precondition,
      ActionInvocationNotFound: (failure) => correctProductActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => correctProductActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => correctProductActionProblem.conflict(failure.code),
      ActionPayloadValidationError: correctProductActionProblem.invalid,
      ActionPermissionCheckError: (failure) => correctProductActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => correctProductActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => correctProductActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => correctProductActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => correctProductActionProblem.conflict(failure.code),
      ActionResultValidationError: correctProductActionProblem.internal,
      ActionTransactionError: (failure) => correctProductActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: correctProductActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => correctProductActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => correctProductActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: correctProductActionProblem.authentication,
      OperationContextDenied: (failure) => correctProductActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => correctProductActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => correctProductActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(correctProductAction.descriptor.domainErrorSchema);
export const mapCorrectProductActionProblem = (error: ActionCoreError | DomainError): CorrectProductActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const correctProductActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CorrectProductActionSchemaErrorMiddleware,
  () => Effect.fail(correctProductActionProblem.invalid()),
);
