// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug correct-sku
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CorrectSkuActionAlreadyCommittedProblemSchema,
  CorrectSkuActionAuthenticationProblemSchema,
  CorrectSkuActionCommitIndeterminateProblemSchema,
  CorrectSkuActionConflictProblemSchema,
  CorrectSkuActionForbiddenProblemSchema,
  CorrectSkuActionIneligibleProblemSchema,
  CorrectSkuActionInternalProblemSchema,
  CorrectSkuActionInvalidProblemSchema,
  CorrectSkuActionNotFoundProblemSchema,
  CorrectSkuActionPreconditionProblemSchema,
  CorrectSkuActionSchemaErrorMiddleware,
  CorrectSkuActionUnavailableProblemSchema,
} from '../shared/apis/correct-sku-action.ts';
import type { CorrectSkuActionProblem } from '../shared/apis/correct-sku-action.ts';
import { correctSkuAction } from '../src/actions/correct-sku.action.ts';

type DomainError = typeof correctSkuAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CorrectSkuActionProblem['_tag']> = Extract<CorrectSkuActionProblem, { readonly _tag: Tag }>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const correctSkuActionProblem = {
  authentication: (): ProblemOf<'CorrectSkuActionAuthenticationProblem'> =>
    CorrectSkuActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CorrectSkuActionConflictProblem'>['code'],
  ): ProblemOf<'CorrectSkuActionConflictProblem'> =>
    CorrectSkuActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CorrectSkuActionForbiddenProblem'>['code'],
  ): ProblemOf<'CorrectSkuActionForbiddenProblem'> =>
    CorrectSkuActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CorrectSkuActionIneligibleProblem'>['code'],
  ): ProblemOf<'CorrectSkuActionIneligibleProblem'> =>
    CorrectSkuActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CorrectSkuActionInternalProblem'> =>
    CorrectSkuActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CorrectSkuActionInvalidProblem'> =>
    CorrectSkuActionInvalidProblemSchema.make({
      detail: 'The correct-sku Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CorrectSkuActionNotFoundProblem'>['code'],
  ): ProblemOf<'CorrectSkuActionNotFoundProblem'> =>
    CorrectSkuActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CorrectSkuActionPreconditionProblem'> =>
    CorrectSkuActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CorrectSkuActionUnavailableProblem'>['code'],
  ): ProblemOf<'CorrectSkuActionUnavailableProblem'> =>
    CorrectSkuActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CorrectSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      SkuActionConflict: () => correctSkuActionProblem.conflict('sku_action_conflict'),
      SkuActionInvalid: () => correctSkuActionProblem.ineligible('sku_action_invalid'),
      SkuActionNotFound: () => correctSkuActionProblem.notFound('sku_action_not_found'),
      SkuActionStale: () => correctSkuActionProblem.conflict('sku_action_stale'),
      SkuPersistenceUnavailable: () => correctSkuActionProblem.unavailable('sku_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CorrectSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CorrectSkuActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CORRECT_SKU',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: correctSkuActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CorrectSkuActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CORRECT_SKU',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: correctSkuActionProblem.internal,
      ActionIdempotencyKeyRequired: correctSkuActionProblem.precondition,
      ActionInvocationNotFound: (failure) => correctSkuActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => correctSkuActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => correctSkuActionProblem.conflict(failure.code),
      ActionPayloadValidationError: correctSkuActionProblem.invalid,
      ActionPermissionCheckError: (failure) => correctSkuActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => correctSkuActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => correctSkuActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => correctSkuActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => correctSkuActionProblem.conflict(failure.code),
      ActionResultValidationError: correctSkuActionProblem.internal,
      ActionTransactionError: (failure) => correctSkuActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: correctSkuActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => correctSkuActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => correctSkuActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: correctSkuActionProblem.authentication,
      OperationContextDenied: (failure) => correctSkuActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => correctSkuActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => correctSkuActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(correctSkuAction.descriptor.domainErrorSchema);
export const mapCorrectSkuActionProblem = (error: ActionCoreError | DomainError): CorrectSkuActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const correctSkuActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CorrectSkuActionSchemaErrorMiddleware,
  () => Effect.fail(correctSkuActionProblem.invalid()),
);
