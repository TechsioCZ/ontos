// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-brand
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CreateBrandActionAlreadyCommittedProblemSchema,
  CreateBrandActionAuthenticationProblemSchema,
  CreateBrandActionCommitIndeterminateProblemSchema,
  CreateBrandActionConflictProblemSchema,
  CreateBrandActionForbiddenProblemSchema,
  CreateBrandActionIneligibleProblemSchema,
  CreateBrandActionInternalProblemSchema,
  CreateBrandActionInvalidProblemSchema,
  CreateBrandActionNotFoundProblemSchema,
  CreateBrandActionPreconditionProblemSchema,
  CreateBrandActionSchemaErrorMiddleware,
  CreateBrandActionUnavailableProblemSchema,
} from '../shared/apis/create-brand-action.ts';
import type { CreateBrandActionProblem } from '../shared/apis/create-brand-action.ts';
import { createBrandAction } from '../src/actions/create-brand.action.ts';

type DomainError = typeof createBrandAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CreateBrandActionProblem['_tag']> = Extract<
  CreateBrandActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'brand_stale'; readonly kind: 'conflict' }
  | { readonly code: 'brand_invalid'; readonly kind: 'ineligible' }
  | { readonly code: 'brand_not_found'; readonly kind: 'notFound' }
  | { readonly code: 'brand_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const brandActionErrorProblemByCode = {
  brand_invalid: { code: 'brand_invalid', kind: 'ineligible' },
  brand_not_found: { code: 'brand_not_found', kind: 'notFound' },
  brand_stale: { code: 'brand_stale', kind: 'conflict' },
  brand_unavailable: { code: 'brand_unavailable', kind: 'unavailable' },
} as const satisfies Record<Extract<DomainError, { readonly _tag: 'BrandActionError' }>['code'], DomainProblemIdentity>;

export const createBrandActionProblem = {
  authentication: (): ProblemOf<'CreateBrandActionAuthenticationProblem'> =>
    CreateBrandActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CreateBrandActionConflictProblem'>['code'],
  ): ProblemOf<'CreateBrandActionConflictProblem'> =>
    CreateBrandActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CreateBrandActionForbiddenProblem'>['code'],
  ): ProblemOf<'CreateBrandActionForbiddenProblem'> =>
    CreateBrandActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CreateBrandActionIneligibleProblem'>['code'],
  ): ProblemOf<'CreateBrandActionIneligibleProblem'> =>
    CreateBrandActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CreateBrandActionInternalProblem'> =>
    CreateBrandActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CreateBrandActionInvalidProblem'> =>
    CreateBrandActionInvalidProblemSchema.make({
      detail: 'The create-brand Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CreateBrandActionNotFoundProblem'>['code'],
  ): ProblemOf<'CreateBrandActionNotFoundProblem'> =>
    CreateBrandActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CreateBrandActionPreconditionProblem'> =>
    CreateBrandActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CreateBrandActionUnavailableProblem'>['code'],
  ): ProblemOf<'CreateBrandActionUnavailableProblem'> =>
    CreateBrandActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): CreateBrandActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => createBrandActionProblem.conflict(matched.code)),
    Match.when({ kind: 'ineligible' as const }, (matched) => createBrandActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'notFound' as const }, (matched) => createBrandActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => createBrandActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): CreateBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      BrandActionError: (failure) => mapDomainIdentity(brandActionErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CreateBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CreateBrandActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover the original create result.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_BRAND',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: createBrandActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CreateBrandActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_BRAND',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: createBrandActionProblem.internal,
      ActionIdempotencyKeyRequired: createBrandActionProblem.precondition,
      ActionInvocationNotFound: (failure) => createBrandActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => createBrandActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => createBrandActionProblem.conflict(failure.code),
      ActionPayloadValidationError: createBrandActionProblem.invalid,
      ActionPermissionCheckError: (failure) => createBrandActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => createBrandActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => createBrandActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => createBrandActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => createBrandActionProblem.conflict(failure.code),
      ActionResultValidationError: createBrandActionProblem.internal,
      ActionTransactionError: (failure) => createBrandActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: createBrandActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => createBrandActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => createBrandActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: createBrandActionProblem.authentication,
      OperationContextDenied: (failure) => createBrandActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => createBrandActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => createBrandActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(createBrandAction.descriptor.domainErrorSchema);
export const mapCreateBrandActionProblem = (error: ActionCoreError | DomainError): CreateBrandActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const createBrandActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CreateBrandActionSchemaErrorMiddleware,
  () => Effect.fail(createBrandActionProblem.invalid()),
);
