// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-package-definition
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createPackageDefinitionAction } from '../src/actions/create-package-definition.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createPackageDefinitionActionProblem,
  createPackageDefinitionActionSchemaErrorLive,
  mapCreatePackageDefinitionActionProblem,
} from './create-package-definition-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createPackageDefinitionActionProblem.authentication,
  unavailable: () => createPackageDefinitionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreatePackageDefinitionActionServer.execute')(function* executeCreatePackageDefinition(
  payload: typeof createPackageDefinitionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createPackageDefinitionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createPackageDefinitionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createPackageDefinitionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createPackageDefinitionActionProblem.internal,
    invalidCorrelationProblem: createPackageDefinitionActionProblem.invalid,
    mapError: mapCreatePackageDefinitionActionProblem,
    payload: decoded,
    registration: createPackageDefinitionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createPackageDefinitionActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'createPackageDefinitionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createPackageDefinitionActionSchemaErrorLive));
