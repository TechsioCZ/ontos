// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-product
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CreateProductActionAlreadyCommittedProblemSchema,
  CreateProductActionAuthenticationProblemSchema,
  CreateProductActionCommitIndeterminateProblemSchema,
  CreateProductActionConflictProblemSchema,
  CreateProductActionForbiddenProblemSchema,
  CreateProductActionIneligibleProblemSchema,
  CreateProductActionInternalProblemSchema,
  CreateProductActionInvalidProblemSchema,
  CreateProductActionNotFoundProblemSchema,
  CreateProductActionPreconditionProblemSchema,
  CreateProductActionSchemaErrorMiddleware,
  CreateProductActionUnavailableProblemSchema,
} from '../shared/apis/create-product-action.ts';
import type { CreateProductActionProblem } from '../shared/apis/create-product-action.ts';
import { createProductAction } from '../src/actions/create-product.action.ts';

type DomainError = typeof createProductAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CreateProductActionProblem['_tag']> = Extract<
  CreateProductActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const createProductActionProblem = {
  authentication: (): ProblemOf<'CreateProductActionAuthenticationProblem'> =>
    CreateProductActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CreateProductActionConflictProblem'>['code'],
  ): ProblemOf<'CreateProductActionConflictProblem'> =>
    CreateProductActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CreateProductActionForbiddenProblem'>['code'],
  ): ProblemOf<'CreateProductActionForbiddenProblem'> =>
    CreateProductActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CreateProductActionIneligibleProblem'>['code'],
  ): ProblemOf<'CreateProductActionIneligibleProblem'> =>
    CreateProductActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CreateProductActionInternalProblem'> =>
    CreateProductActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CreateProductActionInvalidProblem'> =>
    CreateProductActionInvalidProblemSchema.make({
      detail: 'The create-product Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CreateProductActionNotFoundProblem'>['code'],
  ): ProblemOf<'CreateProductActionNotFoundProblem'> =>
    CreateProductActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CreateProductActionPreconditionProblem'> =>
    CreateProductActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CreateProductActionUnavailableProblem'>['code'],
  ): ProblemOf<'CreateProductActionUnavailableProblem'> =>
    CreateProductActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CreateProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceConflict: () => createProductActionProblem.conflict('catalog_persistence_conflict'),
      CatalogPersistenceUnavailable: () => createProductActionProblem.unavailable('catalog_persistence_unavailable'),
      ProductCorrectionRequired: () => createProductActionProblem.ineligible('product_correction_required'),
      ProductLifecycleConflict: () => createProductActionProblem.conflict('product_lifecycle_conflict'),
      ProductNotCatalogReady: () => createProductActionProblem.ineligible('product_not_catalog_ready'),
      ProductNotFound: () => createProductActionProblem.notFound('product_not_found'),
      ProductPersistenceConflict: () => createProductActionProblem.conflict('product_persistence_conflict'),
      ProductRevisionConflict: () => createProductActionProblem.conflict('product_revision_conflict'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CreateProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CreateProductActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_PRODUCT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: createProductActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CreateProductActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_PRODUCT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: createProductActionProblem.internal,
      ActionIdempotencyKeyRequired: createProductActionProblem.precondition,
      ActionInvocationNotFound: (failure) => createProductActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => createProductActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => createProductActionProblem.conflict(failure.code),
      ActionPayloadValidationError: createProductActionProblem.invalid,
      ActionPermissionCheckError: (failure) => createProductActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => createProductActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => createProductActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => createProductActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => createProductActionProblem.conflict(failure.code),
      ActionResultValidationError: createProductActionProblem.internal,
      ActionTransactionError: (failure) => createProductActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: createProductActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => createProductActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => createProductActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: createProductActionProblem.authentication,
      OperationContextDenied: (failure) => createProductActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => createProductActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => createProductActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(createProductAction.descriptor.domainErrorSchema);
export const mapCreateProductActionProblem = (error: ActionCoreError | DomainError): CreateProductActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const createProductActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CreateProductActionSchemaErrorMiddleware,
  () => Effect.fail(createProductActionProblem.invalid()),
);
