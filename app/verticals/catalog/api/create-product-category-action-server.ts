// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-product-category
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createProductCategoryAction } from '../src/actions/create-product-category.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createProductCategoryActionProblem,
  createProductCategoryActionSchemaErrorLive,
  mapCreateProductCategoryActionProblem,
} from './create-product-category-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createProductCategoryActionProblem.authentication,
  unavailable: () => createProductCategoryActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateProductCategoryActionServer.execute')(function* executeCreateProductCategory(
  payload: typeof createProductCategoryAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createProductCategoryActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createProductCategoryAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createProductCategoryActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createProductCategoryActionProblem.internal,
    invalidCorrelationProblem: createProductCategoryActionProblem.invalid,
    mapError: mapCreateProductCategoryActionProblem,
    payload: decoded,
    registration: createProductCategoryAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createProductCategoryActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'createProductCategoryAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createProductCategoryActionSchemaErrorLive));
