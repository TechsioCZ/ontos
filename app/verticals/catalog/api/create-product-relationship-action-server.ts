// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-product-relationship
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createProductRelationshipAction } from '../src/actions/create-product-relationship.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createProductRelationshipActionProblem,
  createProductRelationshipActionSchemaErrorLive,
  mapCreateProductRelationshipActionProblem,
} from './create-product-relationship-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createProductRelationshipActionProblem.authentication,
  unavailable: () => createProductRelationshipActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateProductRelationshipActionServer.execute')(function* executeCreateProductRelationship(
  payload: typeof createProductRelationshipAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createProductRelationshipActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createProductRelationshipAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createProductRelationshipActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createProductRelationshipActionProblem.internal,
    invalidCorrelationProblem: createProductRelationshipActionProblem.invalid,
    mapError: mapCreateProductRelationshipActionProblem,
    payload: decoded,
    registration: createProductRelationshipAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createProductRelationshipActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'createProductRelationshipAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createProductRelationshipActionSchemaErrorLive));
