// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-product-type
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createProductTypeAction } from '../src/actions/create-product-type.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createProductTypeActionProblem,
  createProductTypeActionSchemaErrorLive,
  mapCreateProductTypeActionProblem,
} from './create-product-type-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createProductTypeActionProblem.authentication,
  unavailable: () => createProductTypeActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateProductTypeActionServer.execute')(function* executeCreateProductType(
  payload: typeof createProductTypeAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createProductTypeActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createProductTypeAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createProductTypeActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createProductTypeActionProblem.internal,
    invalidCorrelationProblem: createProductTypeActionProblem.invalid,
    mapError: mapCreateProductTypeActionProblem,
    payload: decoded,
    registration: createProductTypeAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createProductTypeActionApiLive = HttpApiBuilder.group(catalogApi, 'createProductTypeAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(createProductTypeActionSchemaErrorLive));
