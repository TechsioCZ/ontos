// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-set-composition
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createSetCompositionAction } from '../src/actions/create-set-composition.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createSetCompositionActionProblem,
  createSetCompositionActionSchemaErrorLive,
  mapCreateSetCompositionActionProblem,
} from './create-set-composition-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createSetCompositionActionProblem.authentication,
  unavailable: () => createSetCompositionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateSetCompositionActionServer.execute')(function* executeCreateSetComposition(
  payload: typeof createSetCompositionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createSetCompositionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createSetCompositionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createSetCompositionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createSetCompositionActionProblem.internal,
    invalidCorrelationProblem: createSetCompositionActionProblem.invalid,
    mapError: mapCreateSetCompositionActionProblem,
    payload: decoded,
    registration: createSetCompositionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createSetCompositionActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'createSetCompositionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createSetCompositionActionSchemaErrorLive));
