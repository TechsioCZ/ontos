// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-variant
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CreateVariantActionAlreadyCommittedProblemSchema,
  CreateVariantActionAuthenticationProblemSchema,
  CreateVariantActionCommitIndeterminateProblemSchema,
  CreateVariantActionConflictProblemSchema,
  CreateVariantActionForbiddenProblemSchema,
  CreateVariantActionIneligibleProblemSchema,
  CreateVariantActionInternalProblemSchema,
  CreateVariantActionInvalidProblemSchema,
  CreateVariantActionNotFoundProblemSchema,
  CreateVariantActionPreconditionProblemSchema,
  CreateVariantActionSchemaErrorMiddleware,
  CreateVariantActionUnavailableProblemSchema,
} from '../shared/apis/create-variant-action.ts';
import type { CreateVariantActionProblem } from '../shared/apis/create-variant-action.ts';
import { createVariantAction } from '../src/actions/create-variant.action.ts';

type DomainError = typeof createVariantAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CreateVariantActionProblem['_tag']> = Extract<
  CreateVariantActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const createVariantActionProblem = {
  authentication: (): ProblemOf<'CreateVariantActionAuthenticationProblem'> =>
    CreateVariantActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CreateVariantActionConflictProblem'>['code'],
  ): ProblemOf<'CreateVariantActionConflictProblem'> =>
    CreateVariantActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CreateVariantActionForbiddenProblem'>['code'],
  ): ProblemOf<'CreateVariantActionForbiddenProblem'> =>
    CreateVariantActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CreateVariantActionIneligibleProblem'>['code'],
  ): ProblemOf<'CreateVariantActionIneligibleProblem'> =>
    CreateVariantActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CreateVariantActionInternalProblem'> =>
    CreateVariantActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CreateVariantActionInvalidProblem'> =>
    CreateVariantActionInvalidProblemSchema.make({
      detail: 'The create-variant Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CreateVariantActionNotFoundProblem'>['code'],
  ): ProblemOf<'CreateVariantActionNotFoundProblem'> =>
    CreateVariantActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CreateVariantActionPreconditionProblem'> =>
    CreateVariantActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CreateVariantActionUnavailableProblem'>['code'],
  ): ProblemOf<'CreateVariantActionUnavailableProblem'> =>
    CreateVariantActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CreateVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceUnavailable: () => createVariantActionProblem.unavailable('catalog_persistence_unavailable'),
      VariantActionConflict: () => createVariantActionProblem.conflict('variant_action_conflict'),
      VariantActionNotFound: () => createVariantActionProblem.notFound('variant_action_not_found'),
      VariantCurrentBasisUnavailable: () => createVariantActionProblem.unavailable('variant_current_basis_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CreateVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CreateVariantActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_VARIANT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: createVariantActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CreateVariantActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_CREATE_VARIANT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: createVariantActionProblem.internal,
      ActionIdempotencyKeyRequired: createVariantActionProblem.precondition,
      ActionInvocationNotFound: (failure) => createVariantActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => createVariantActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => createVariantActionProblem.conflict(failure.code),
      ActionPayloadValidationError: createVariantActionProblem.invalid,
      ActionPermissionCheckError: (failure) => createVariantActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => createVariantActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => createVariantActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => createVariantActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => createVariantActionProblem.conflict(failure.code),
      ActionResultValidationError: createVariantActionProblem.internal,
      ActionTransactionError: (failure) => createVariantActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: createVariantActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => createVariantActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => createVariantActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: createVariantActionProblem.authentication,
      OperationContextDenied: (failure) => createVariantActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => createVariantActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => createVariantActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(createVariantAction.descriptor.domainErrorSchema);
export const mapCreateVariantActionProblem = (error: ActionCoreError | DomainError): CreateVariantActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const createVariantActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CreateVariantActionSchemaErrorMiddleware,
  () => Effect.fail(createVariantActionProblem.invalid()),
);
