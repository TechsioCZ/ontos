// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-variant
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { createVariantAction } from '../src/actions/create-variant.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createVariantActionProblem,
  createVariantActionSchemaErrorLive,
  mapCreateVariantActionProblem,
} from './create-variant-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createVariantActionProblem.authentication,
  unavailable: () => createVariantActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateVariantActionServer.execute')(function* executeCreateVariant(
  payload: typeof createVariantAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createVariantActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createVariantAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createVariantActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createVariantActionProblem.internal,
    invalidCorrelationProblem: createVariantActionProblem.invalid,
    mapError: mapCreateVariantActionProblem,
    payload: decoded,
    registration: createVariantAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createVariantActionApiLive = HttpApiBuilder.group(catalogApi, 'createVariantAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(createVariantActionSchemaErrorLive));
