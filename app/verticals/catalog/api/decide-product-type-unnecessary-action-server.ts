// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug decide-product-type-unnecessary
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { decideProductTypeUnnecessaryAction } from '../src/actions/decide-product-type-unnecessary.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  decideProductTypeUnnecessaryActionProblem,
  decideProductTypeUnnecessaryActionSchemaErrorLive,
  mapDecideProductTypeUnnecessaryActionProblem,
} from './decide-product-type-unnecessary-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: decideProductTypeUnnecessaryActionProblem.authentication,
  unavailable: () => decideProductTypeUnnecessaryActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('DecideProductTypeUnnecessaryActionServer.execute')(
  function* executeDecideProductTypeUnnecessary(
    payload: typeof decideProductTypeUnnecessaryAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(decideProductTypeUnnecessaryActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(decideProductTypeUnnecessaryAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(decideProductTypeUnnecessaryActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: decideProductTypeUnnecessaryActionProblem.internal,
      invalidCorrelationProblem: decideProductTypeUnnecessaryActionProblem.invalid,
      mapError: mapDecideProductTypeUnnecessaryActionProblem,
      payload: decoded,
      registration: decideProductTypeUnnecessaryAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const decideProductTypeUnnecessaryActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'decideProductTypeUnnecessaryAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(decideProductTypeUnnecessaryActionSchemaErrorLive));
