// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug govern-product-attribute-applicability
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { governProductAttributeApplicabilityAction } from '../src/actions/govern-product-attribute-applicability.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  governProductAttributeApplicabilityActionProblem,
  governProductAttributeApplicabilityActionSchemaErrorLive,
  mapGovernProductAttributeApplicabilityActionProblem,
} from './govern-product-attribute-applicability-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: governProductAttributeApplicabilityActionProblem.authentication,
  unavailable: () => governProductAttributeApplicabilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('GovernProductAttributeApplicabilityActionServer.execute')(
  function* executeGovernProductAttributeApplicability(
    payload: typeof governProductAttributeApplicabilityAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(governProductAttributeApplicabilityActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(governProductAttributeApplicabilityAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(governProductAttributeApplicabilityActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: governProductAttributeApplicabilityActionProblem.internal,
      invalidCorrelationProblem: governProductAttributeApplicabilityActionProblem.invalid,
      mapError: mapGovernProductAttributeApplicabilityActionProblem,
      payload: decoded,
      registration: governProductAttributeApplicabilityAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const governProductAttributeApplicabilityActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'governProductAttributeApplicabilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(governProductAttributeApplicabilityActionSchemaErrorLive));
