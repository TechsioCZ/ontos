// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug govern-variant-axes
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  GovernVariantAxesActionAlreadyCommittedProblemSchema,
  GovernVariantAxesActionAuthenticationProblemSchema,
  GovernVariantAxesActionCommitIndeterminateProblemSchema,
  GovernVariantAxesActionConflictProblemSchema,
  GovernVariantAxesActionForbiddenProblemSchema,
  GovernVariantAxesActionIneligibleProblemSchema,
  GovernVariantAxesActionInternalProblemSchema,
  GovernVariantAxesActionInvalidProblemSchema,
  GovernVariantAxesActionNotFoundProblemSchema,
  GovernVariantAxesActionPreconditionProblemSchema,
  GovernVariantAxesActionSchemaErrorMiddleware,
  GovernVariantAxesActionUnavailableProblemSchema,
} from '../shared/apis/govern-variant-axes-action.ts';
import type { GovernVariantAxesActionProblem } from '../shared/apis/govern-variant-axes-action.ts';
import { governVariantAxesAction } from '../src/actions/govern-variant-axes.action.ts';

type DomainError = typeof governVariantAxesAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends GovernVariantAxesActionProblem['_tag']> = Extract<
  GovernVariantAxesActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const governVariantAxesActionProblem = {
  authentication: (): ProblemOf<'GovernVariantAxesActionAuthenticationProblem'> =>
    GovernVariantAxesActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'GovernVariantAxesActionConflictProblem'>['code'],
  ): ProblemOf<'GovernVariantAxesActionConflictProblem'> =>
    GovernVariantAxesActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'GovernVariantAxesActionForbiddenProblem'>['code'],
  ): ProblemOf<'GovernVariantAxesActionForbiddenProblem'> =>
    GovernVariantAxesActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'GovernVariantAxesActionIneligibleProblem'>['code'],
  ): ProblemOf<'GovernVariantAxesActionIneligibleProblem'> =>
    GovernVariantAxesActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'GovernVariantAxesActionInternalProblem'> =>
    GovernVariantAxesActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'GovernVariantAxesActionInvalidProblem'> =>
    GovernVariantAxesActionInvalidProblemSchema.make({
      detail: 'The govern-variant-axes Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'GovernVariantAxesActionNotFoundProblem'>['code'],
  ): ProblemOf<'GovernVariantAxesActionNotFoundProblem'> =>
    GovernVariantAxesActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'GovernVariantAxesActionPreconditionProblem'> =>
    GovernVariantAxesActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'GovernVariantAxesActionUnavailableProblem'>['code'],
  ): ProblemOf<'GovernVariantAxesActionUnavailableProblem'> =>
    GovernVariantAxesActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): GovernVariantAxesActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceUnavailable: () =>
        governVariantAxesActionProblem.unavailable('catalog_persistence_unavailable'),
      VariantAxisWriteConflict: () => governVariantAxesActionProblem.conflict('variant_axis_write_conflict'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): GovernVariantAxesActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        GovernVariantAxesActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_GOVERN_VARIANT_AXES',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: governVariantAxesActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        GovernVariantAxesActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_GOVERN_VARIANT_AXES',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: governVariantAxesActionProblem.internal,
      ActionIdempotencyKeyRequired: governVariantAxesActionProblem.precondition,
      ActionInvocationNotFound: (failure) => governVariantAxesActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => governVariantAxesActionProblem.conflict(failure.code),
      ActionPayloadValidationError: governVariantAxesActionProblem.invalid,
      ActionPermissionCheckError: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => governVariantAxesActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => governVariantAxesActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => governVariantAxesActionProblem.conflict(failure.code),
      ActionResultValidationError: governVariantAxesActionProblem.internal,
      ActionTransactionError: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: governVariantAxesActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => governVariantAxesActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: governVariantAxesActionProblem.authentication,
      OperationContextDenied: (failure) => governVariantAxesActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => governVariantAxesActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => governVariantAxesActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(governVariantAxesAction.descriptor.domainErrorSchema);
export const mapGovernVariantAxesActionProblem = (
  error: ActionCoreError | DomainError,
): GovernVariantAxesActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const governVariantAxesActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  GovernVariantAxesActionSchemaErrorMiddleware,
  () => Effect.fail(governVariantAxesActionProblem.invalid()),
);
