// @generated by OntOS Codesmith module-api v1
import { makeGovernedReadHttpHandler } from '@app/core-runtime/http/governed-read';
import { makeGovernedReadProblems } from '@app/shared-contracts/server/effect-bff-runtime';
import { HttpApiBuilder } from '@modern-js/bff-effect/effect-edge';
import { catalogApi } from '../shared/api.ts';
import {
  GtinCurrentAuthenticationProblemSchema,
  GtinCurrentForbiddenProblemSchema,
  GtinCurrentInternalProblemSchema,
  GtinCurrentInvalidProblemSchema,
  GtinCurrentNotFoundProblemSchema,
  GtinCurrentPolicyConflictProblemSchema,
  GtinCurrentPolicyProblemSchema,
  GtinCurrentUnavailableProblemSchema,
} from '../shared/apis/gtin-current.ts';
import { gtinCurrentRead } from '../src/api/gtin-current.read.ts';
import { authenticateOperationPrincipal } from './auth/action-principal.ts';

const problems = makeGovernedReadProblems({
  authentication: GtinCurrentAuthenticationProblemSchema,
  forbidden: GtinCurrentForbiddenProblemSchema,
  internal: GtinCurrentInternalProblemSchema,
  invalid: GtinCurrentInvalidProblemSchema,
  notFound: GtinCurrentNotFoundProblemSchema,
  policyConflict: GtinCurrentPolicyConflictProblemSchema,
  policyIneligible: GtinCurrentPolicyProblemSchema,
  unavailable: GtinCurrentUnavailableProblemSchema,
});

export const gtinCurrentReadApiLive = HttpApiBuilder.group(catalogApi, 'gtinCurrent', (handlers) =>
  handlers.handle(
    'execute',
    makeGovernedReadHttpHandler({
      authenticatePrincipal: authenticateOperationPrincipal,
      problems,
      registration: gtinCurrentRead,
    }),
  ),
);
