// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug import-source-assertion
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { importSourceAssertionAction } from '../src/actions/import-source-assertion.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  importSourceAssertionActionProblem,
  importSourceAssertionActionSchemaErrorLive,
  mapImportSourceAssertionActionProblem,
} from './import-source-assertion-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: importSourceAssertionActionProblem.authentication,
  unavailable: () => importSourceAssertionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ImportSourceAssertionActionServer.execute')(function* executeImportSourceAssertion(
  payload: typeof importSourceAssertionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(importSourceAssertionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(importSourceAssertionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(importSourceAssertionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: importSourceAssertionActionProblem.internal,
    invalidCorrelationProblem: importSourceAssertionActionProblem.invalid,
    mapError: mapImportSourceAssertionActionProblem,
    payload: decoded,
    registration: importSourceAssertionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const importSourceAssertionActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'importSourceAssertionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(importSourceAssertionActionSchemaErrorLive));
