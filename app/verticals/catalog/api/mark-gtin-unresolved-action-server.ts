// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug mark-gtin-unresolved
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { markGtinUnresolvedAction } from '../src/actions/mark-gtin-unresolved.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  markGtinUnresolvedActionProblem,
  markGtinUnresolvedActionSchemaErrorLive,
  mapMarkGtinUnresolvedActionProblem,
} from './mark-gtin-unresolved-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: markGtinUnresolvedActionProblem.authentication,
  unavailable: () => markGtinUnresolvedActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('MarkGtinUnresolvedActionServer.execute')(function* executeMarkGtinUnresolved(
  payload: typeof markGtinUnresolvedAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(markGtinUnresolvedActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(markGtinUnresolvedAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(markGtinUnresolvedActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: markGtinUnresolvedActionProblem.internal,
    invalidCorrelationProblem: markGtinUnresolvedActionProblem.invalid,
    mapError: mapMarkGtinUnresolvedActionProblem,
    payload: decoded,
    registration: markGtinUnresolvedAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const markGtinUnresolvedActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'markGtinUnresolvedAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(markGtinUnresolvedActionSchemaErrorLive));
