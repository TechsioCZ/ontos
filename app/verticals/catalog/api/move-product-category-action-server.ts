// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug move-product-category
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { moveProductCategoryAction } from '../src/actions/move-product-category.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  moveProductCategoryActionProblem,
  moveProductCategoryActionSchemaErrorLive,
  mapMoveProductCategoryActionProblem,
} from './move-product-category-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: moveProductCategoryActionProblem.authentication,
  unavailable: () => moveProductCategoryActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('MoveProductCategoryActionServer.execute')(function* executeMoveProductCategory(
  payload: typeof moveProductCategoryAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(moveProductCategoryActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(moveProductCategoryAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(moveProductCategoryActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: moveProductCategoryActionProblem.internal,
    invalidCorrelationProblem: moveProductCategoryActionProblem.invalid,
    mapError: mapMoveProductCategoryActionProblem,
    payload: decoded,
    registration: moveProductCategoryAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const moveProductCategoryActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'moveProductCategoryAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(moveProductCategoryActionSchemaErrorLive));
