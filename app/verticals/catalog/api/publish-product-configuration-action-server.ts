// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug publish-product-configuration
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { publishProductConfigurationAction } from '../src/actions/publish-product-configuration.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  publishProductConfigurationActionProblem,
  publishProductConfigurationActionSchemaErrorLive,
  mapPublishProductConfigurationActionProblem,
} from './publish-product-configuration-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: publishProductConfigurationActionProblem.authentication,
  unavailable: () => publishProductConfigurationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('PublishProductConfigurationActionServer.execute')(
  function* executePublishProductConfiguration(
    payload: typeof publishProductConfigurationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(publishProductConfigurationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(publishProductConfigurationAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(publishProductConfigurationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: publishProductConfigurationActionProblem.internal,
      invalidCorrelationProblem: publishProductConfigurationActionProblem.invalid,
      mapError: mapPublishProductConfigurationActionProblem,
      payload: decoded,
      registration: publishProductConfigurationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const publishProductConfigurationActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'publishProductConfigurationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(publishProductConfigurationActionSchemaErrorLive));
