// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug reactivate-variant
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  ReactivateVariantActionAlreadyCommittedProblemSchema,
  ReactivateVariantActionAuthenticationProblemSchema,
  ReactivateVariantActionCommitIndeterminateProblemSchema,
  ReactivateVariantActionConflictProblemSchema,
  ReactivateVariantActionForbiddenProblemSchema,
  ReactivateVariantActionIneligibleProblemSchema,
  ReactivateVariantActionInternalProblemSchema,
  ReactivateVariantActionInvalidProblemSchema,
  ReactivateVariantActionNotFoundProblemSchema,
  ReactivateVariantActionPreconditionProblemSchema,
  ReactivateVariantActionSchemaErrorMiddleware,
  ReactivateVariantActionUnavailableProblemSchema,
} from '../shared/apis/reactivate-variant-action.ts';
import type { ReactivateVariantActionProblem } from '../shared/apis/reactivate-variant-action.ts';
import { reactivateVariantAction } from '../src/actions/reactivate-variant.action.ts';

type DomainError = typeof reactivateVariantAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends ReactivateVariantActionProblem['_tag']> = Extract<
  ReactivateVariantActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const reactivateVariantActionProblem = {
  authentication: (): ProblemOf<'ReactivateVariantActionAuthenticationProblem'> =>
    ReactivateVariantActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'ReactivateVariantActionConflictProblem'>['code'],
  ): ProblemOf<'ReactivateVariantActionConflictProblem'> =>
    ReactivateVariantActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'ReactivateVariantActionForbiddenProblem'>['code'],
  ): ProblemOf<'ReactivateVariantActionForbiddenProblem'> =>
    ReactivateVariantActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'ReactivateVariantActionIneligibleProblem'>['code'],
  ): ProblemOf<'ReactivateVariantActionIneligibleProblem'> =>
    ReactivateVariantActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'ReactivateVariantActionInternalProblem'> =>
    ReactivateVariantActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'ReactivateVariantActionInvalidProblem'> =>
    ReactivateVariantActionInvalidProblemSchema.make({
      detail: 'The reactivate-variant Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'ReactivateVariantActionNotFoundProblem'>['code'],
  ): ProblemOf<'ReactivateVariantActionNotFoundProblem'> =>
    ReactivateVariantActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'ReactivateVariantActionPreconditionProblem'> =>
    ReactivateVariantActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'ReactivateVariantActionUnavailableProblem'>['code'],
  ): ProblemOf<'ReactivateVariantActionUnavailableProblem'> =>
    ReactivateVariantActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): ReactivateVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceUnavailable: () =>
        reactivateVariantActionProblem.unavailable('catalog_persistence_unavailable'),
      VariantActionConflict: () => reactivateVariantActionProblem.conflict('variant_action_conflict'),
      VariantActionNotFound: () => reactivateVariantActionProblem.notFound('variant_action_not_found'),
      VariantCurrentBasisUnavailable: () =>
        reactivateVariantActionProblem.unavailable('variant_current_basis_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): ReactivateVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        ReactivateVariantActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_REACTIVATE_VARIANT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: reactivateVariantActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        ReactivateVariantActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_REACTIVATE_VARIANT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: reactivateVariantActionProblem.internal,
      ActionIdempotencyKeyRequired: reactivateVariantActionProblem.precondition,
      ActionInvocationNotFound: (failure) => reactivateVariantActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => reactivateVariantActionProblem.conflict(failure.code),
      ActionPayloadValidationError: reactivateVariantActionProblem.invalid,
      ActionPermissionCheckError: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => reactivateVariantActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => reactivateVariantActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => reactivateVariantActionProblem.conflict(failure.code),
      ActionResultValidationError: reactivateVariantActionProblem.internal,
      ActionTransactionError: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: reactivateVariantActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => reactivateVariantActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: reactivateVariantActionProblem.authentication,
      OperationContextDenied: (failure) => reactivateVariantActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => reactivateVariantActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => reactivateVariantActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(reactivateVariantAction.descriptor.domainErrorSchema);
export const mapReactivateVariantActionProblem = (
  error: ActionCoreError | DomainError,
): ReactivateVariantActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const reactivateVariantActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  ReactivateVariantActionSchemaErrorMiddleware,
  () => Effect.fail(reactivateVariantActionProblem.invalid()),
);
