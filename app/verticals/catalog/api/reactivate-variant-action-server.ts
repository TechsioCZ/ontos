// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug reactivate-variant
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { reactivateVariantAction } from '../src/actions/reactivate-variant.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reactivateVariantActionProblem,
  reactivateVariantActionSchemaErrorLive,
  mapReactivateVariantActionProblem,
} from './reactivate-variant-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reactivateVariantActionProblem.authentication,
  unavailable: () => reactivateVariantActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReactivateVariantActionServer.execute')(function* executeReactivateVariant(
  payload: typeof reactivateVariantAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(reactivateVariantActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(reactivateVariantAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(reactivateVariantActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: reactivateVariantActionProblem.internal,
    invalidCorrelationProblem: reactivateVariantActionProblem.invalid,
    mapError: mapReactivateVariantActionProblem,
    payload: decoded,
    registration: reactivateVariantAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const reactivateVariantActionApiLive = HttpApiBuilder.group(catalogApi, 'reactivateVariantAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(reactivateVariantActionSchemaErrorLive));
