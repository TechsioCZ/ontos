// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug release-local-override
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { releaseLocalOverrideAction } from '../src/actions/release-local-override.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  releaseLocalOverrideActionProblem,
  releaseLocalOverrideActionSchemaErrorLive,
  mapReleaseLocalOverrideActionProblem,
} from './release-local-override-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: releaseLocalOverrideActionProblem.authentication,
  unavailable: () => releaseLocalOverrideActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReleaseLocalOverrideActionServer.execute')(function* executeReleaseLocalOverride(
  payload: typeof releaseLocalOverrideAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(releaseLocalOverrideActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(releaseLocalOverrideAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(releaseLocalOverrideActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: releaseLocalOverrideActionProblem.internal,
    invalidCorrelationProblem: releaseLocalOverrideActionProblem.invalid,
    mapError: mapReleaseLocalOverrideActionProblem,
    payload: decoded,
    registration: releaseLocalOverrideAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const releaseLocalOverrideActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'releaseLocalOverrideAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(releaseLocalOverrideActionSchemaErrorLive));
