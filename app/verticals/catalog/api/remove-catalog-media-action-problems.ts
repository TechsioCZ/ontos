// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-catalog-media
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RemoveCatalogMediaActionAlreadyCommittedProblemSchema,
  RemoveCatalogMediaActionAuthenticationProblemSchema,
  RemoveCatalogMediaActionCommitIndeterminateProblemSchema,
  RemoveCatalogMediaActionConflictProblemSchema,
  RemoveCatalogMediaActionForbiddenProblemSchema,
  RemoveCatalogMediaActionIneligibleProblemSchema,
  RemoveCatalogMediaActionInternalProblemSchema,
  RemoveCatalogMediaActionInvalidProblemSchema,
  RemoveCatalogMediaActionNotFoundProblemSchema,
  RemoveCatalogMediaActionPreconditionProblemSchema,
  RemoveCatalogMediaActionSchemaErrorMiddleware,
  RemoveCatalogMediaActionUnavailableProblemSchema,
} from '../shared/apis/remove-catalog-media-action.ts';
import type { RemoveCatalogMediaActionProblem } from '../shared/apis/remove-catalog-media-action.ts';
import { removeCatalogMediaAction } from '../src/actions/remove-catalog-media.action.ts';

type DomainError = typeof removeCatalogMediaAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RemoveCatalogMediaActionProblem['_tag']> = Extract<
  RemoveCatalogMediaActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const removeCatalogMediaActionProblem = {
  authentication: (): ProblemOf<'RemoveCatalogMediaActionAuthenticationProblem'> =>
    RemoveCatalogMediaActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RemoveCatalogMediaActionConflictProblem'>['code'],
  ): ProblemOf<'RemoveCatalogMediaActionConflictProblem'> =>
    RemoveCatalogMediaActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RemoveCatalogMediaActionForbiddenProblem'>['code'],
  ): ProblemOf<'RemoveCatalogMediaActionForbiddenProblem'> =>
    RemoveCatalogMediaActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RemoveCatalogMediaActionIneligibleProblem'>['code'],
  ): ProblemOf<'RemoveCatalogMediaActionIneligibleProblem'> =>
    RemoveCatalogMediaActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RemoveCatalogMediaActionInternalProblem'> =>
    RemoveCatalogMediaActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RemoveCatalogMediaActionInvalidProblem'> =>
    RemoveCatalogMediaActionInvalidProblemSchema.make({
      detail: 'The remove-catalog-media Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RemoveCatalogMediaActionNotFoundProblem'>['code'],
  ): ProblemOf<'RemoveCatalogMediaActionNotFoundProblem'> =>
    RemoveCatalogMediaActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RemoveCatalogMediaActionPreconditionProblem'> =>
    RemoveCatalogMediaActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RemoveCatalogMediaActionUnavailableProblem'>['code'],
  ): ProblemOf<'RemoveCatalogMediaActionUnavailableProblem'> =>
    RemoveCatalogMediaActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RemoveCatalogMediaActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogMediaConflict: () => removeCatalogMediaActionProblem.conflict('catalog_media_conflict'),
      CatalogMediaNotFound: () => removeCatalogMediaActionProblem.notFound('catalog_media_not_found'),
      CatalogPersistenceUnavailable: () =>
        removeCatalogMediaActionProblem.unavailable('catalog_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RemoveCatalogMediaActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RemoveCatalogMediaActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_REMOVE_CATALOG_MEDIA',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: removeCatalogMediaActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RemoveCatalogMediaActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_REMOVE_CATALOG_MEDIA',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: removeCatalogMediaActionProblem.internal,
      ActionIdempotencyKeyRequired: removeCatalogMediaActionProblem.precondition,
      ActionInvocationNotFound: (failure) => removeCatalogMediaActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => removeCatalogMediaActionProblem.conflict(failure.code),
      ActionPayloadValidationError: removeCatalogMediaActionProblem.invalid,
      ActionPermissionCheckError: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => removeCatalogMediaActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => removeCatalogMediaActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => removeCatalogMediaActionProblem.conflict(failure.code),
      ActionResultValidationError: removeCatalogMediaActionProblem.internal,
      ActionTransactionError: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: removeCatalogMediaActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => removeCatalogMediaActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: removeCatalogMediaActionProblem.authentication,
      OperationContextDenied: (failure) => removeCatalogMediaActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => removeCatalogMediaActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => removeCatalogMediaActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(removeCatalogMediaAction.descriptor.domainErrorSchema);
export const mapRemoveCatalogMediaActionProblem = (
  error: ActionCoreError | DomainError,
): RemoveCatalogMediaActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const removeCatalogMediaActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RemoveCatalogMediaActionSchemaErrorMiddleware,
  () => Effect.fail(removeCatalogMediaActionProblem.invalid()),
);
