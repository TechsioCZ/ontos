// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-product-attribute-values
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { removeProductAttributeValuesAction } from '../src/actions/remove-product-attribute-values.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeProductAttributeValuesActionProblem,
  removeProductAttributeValuesActionSchemaErrorLive,
  mapRemoveProductAttributeValuesActionProblem,
} from './remove-product-attribute-values-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeProductAttributeValuesActionProblem.authentication,
  unavailable: () => removeProductAttributeValuesActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveProductAttributeValuesActionServer.execute')(
  function* executeRemoveProductAttributeValues(
    payload: typeof removeProductAttributeValuesAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(removeProductAttributeValuesActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(removeProductAttributeValuesAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(removeProductAttributeValuesActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: removeProductAttributeValuesActionProblem.internal,
      invalidCorrelationProblem: removeProductAttributeValuesActionProblem.invalid,
      mapError: mapRemoveProductAttributeValuesActionProblem,
      payload: decoded,
      registration: removeProductAttributeValuesAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const removeProductAttributeValuesActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'removeProductAttributeValuesAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeProductAttributeValuesActionSchemaErrorLive));
