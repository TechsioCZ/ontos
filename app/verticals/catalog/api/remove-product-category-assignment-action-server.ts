// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-product-category-assignment
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { removeProductCategoryAssignmentAction } from '../src/actions/remove-product-category-assignment.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeProductCategoryAssignmentActionProblem,
  removeProductCategoryAssignmentActionSchemaErrorLive,
  mapRemoveProductCategoryAssignmentActionProblem,
} from './remove-product-category-assignment-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeProductCategoryAssignmentActionProblem.authentication,
  unavailable: () => removeProductCategoryAssignmentActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveProductCategoryAssignmentActionServer.execute')(
  function* executeRemoveProductCategoryAssignment(
    payload: typeof removeProductCategoryAssignmentAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(removeProductCategoryAssignmentActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(removeProductCategoryAssignmentAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(removeProductCategoryAssignmentActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: removeProductCategoryAssignmentActionProblem.internal,
      invalidCorrelationProblem: removeProductCategoryAssignmentActionProblem.invalid,
      mapError: mapRemoveProductCategoryAssignmentActionProblem,
      payload: decoded,
      registration: removeProductCategoryAssignmentAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const removeProductCategoryAssignmentActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'removeProductCategoryAssignmentAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeProductCategoryAssignmentActionSchemaErrorLive));
