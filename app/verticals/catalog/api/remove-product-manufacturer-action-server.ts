// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-product-manufacturer
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { removeProductManufacturerAction } from '../src/actions/remove-product-manufacturer.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeProductManufacturerActionProblem,
  removeProductManufacturerActionSchemaErrorLive,
  mapRemoveProductManufacturerActionProblem,
} from './remove-product-manufacturer-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeProductManufacturerActionProblem.authentication,
  unavailable: () => removeProductManufacturerActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveProductManufacturerActionServer.execute')(function* executeRemoveProductManufacturer(
  payload: typeof removeProductManufacturerAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(removeProductManufacturerActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(removeProductManufacturerAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(removeProductManufacturerActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: removeProductManufacturerActionProblem.internal,
    invalidCorrelationProblem: removeProductManufacturerActionProblem.invalid,
    mapError: mapRemoveProductManufacturerActionProblem,
    payload: decoded,
    registration: removeProductManufacturerAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const removeProductManufacturerActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'removeProductManufacturerAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeProductManufacturerActionSchemaErrorLive));
