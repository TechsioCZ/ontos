// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-variant-attribute-override
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { removeVariantAttributeOverrideAction } from '../src/actions/remove-variant-attribute-override.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeVariantAttributeOverrideActionProblem,
  removeVariantAttributeOverrideActionSchemaErrorLive,
  mapRemoveVariantAttributeOverrideActionProblem,
} from './remove-variant-attribute-override-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeVariantAttributeOverrideActionProblem.authentication,
  unavailable: () => removeVariantAttributeOverrideActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveVariantAttributeOverrideActionServer.execute')(
  function* executeRemoveVariantAttributeOverride(
    payload: typeof removeVariantAttributeOverrideAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(removeVariantAttributeOverrideActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(removeVariantAttributeOverrideAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(removeVariantAttributeOverrideActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: removeVariantAttributeOverrideActionProblem.internal,
      invalidCorrelationProblem: removeVariantAttributeOverrideActionProblem.invalid,
      mapError: mapRemoveVariantAttributeOverrideActionProblem,
      payload: decoded,
      registration: removeVariantAttributeOverrideAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const removeVariantAttributeOverrideActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'removeVariantAttributeOverrideAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeVariantAttributeOverrideActionSchemaErrorLive));
