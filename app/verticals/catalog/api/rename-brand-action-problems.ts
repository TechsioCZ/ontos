// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug rename-brand
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RenameBrandActionAlreadyCommittedProblemSchema,
  RenameBrandActionAuthenticationProblemSchema,
  RenameBrandActionCommitIndeterminateProblemSchema,
  RenameBrandActionConflictProblemSchema,
  RenameBrandActionForbiddenProblemSchema,
  RenameBrandActionIneligibleProblemSchema,
  RenameBrandActionInternalProblemSchema,
  RenameBrandActionInvalidProblemSchema,
  RenameBrandActionNotFoundProblemSchema,
  RenameBrandActionPreconditionProblemSchema,
  RenameBrandActionSchemaErrorMiddleware,
  RenameBrandActionUnavailableProblemSchema,
} from '../shared/apis/rename-brand-action.ts';
import type { RenameBrandActionProblem } from '../shared/apis/rename-brand-action.ts';
import { renameBrandAction } from '../src/actions/rename-brand.action.ts';

type DomainError = typeof renameBrandAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RenameBrandActionProblem['_tag']> = Extract<
  RenameBrandActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'brand_stale'; readonly kind: 'conflict' }
  | { readonly code: 'brand_invalid'; readonly kind: 'ineligible' }
  | { readonly code: 'brand_not_found'; readonly kind: 'notFound' }
  | { readonly code: 'brand_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const brandActionErrorProblemByCode = {
  brand_invalid: { code: 'brand_invalid', kind: 'ineligible' },
  brand_not_found: { code: 'brand_not_found', kind: 'notFound' },
  brand_stale: { code: 'brand_stale', kind: 'conflict' },
  brand_unavailable: { code: 'brand_unavailable', kind: 'unavailable' },
} as const satisfies Record<Extract<DomainError, { readonly _tag: 'BrandActionError' }>['code'], DomainProblemIdentity>;

export const renameBrandActionProblem = {
  authentication: (): ProblemOf<'RenameBrandActionAuthenticationProblem'> =>
    RenameBrandActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RenameBrandActionConflictProblem'>['code'],
  ): ProblemOf<'RenameBrandActionConflictProblem'> =>
    RenameBrandActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RenameBrandActionForbiddenProblem'>['code'],
  ): ProblemOf<'RenameBrandActionForbiddenProblem'> =>
    RenameBrandActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RenameBrandActionIneligibleProblem'>['code'],
  ): ProblemOf<'RenameBrandActionIneligibleProblem'> =>
    RenameBrandActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RenameBrandActionInternalProblem'> =>
    RenameBrandActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RenameBrandActionInvalidProblem'> =>
    RenameBrandActionInvalidProblemSchema.make({
      detail: 'The rename-brand Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RenameBrandActionNotFoundProblem'>['code'],
  ): ProblemOf<'RenameBrandActionNotFoundProblem'> =>
    RenameBrandActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RenameBrandActionPreconditionProblem'> =>
    RenameBrandActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RenameBrandActionUnavailableProblem'>['code'],
  ): ProblemOf<'RenameBrandActionUnavailableProblem'> =>
    RenameBrandActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RenameBrandActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => renameBrandActionProblem.conflict(matched.code)),
    Match.when({ kind: 'ineligible' as const }, (matched) => renameBrandActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'notFound' as const }, (matched) => renameBrandActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => renameBrandActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RenameBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      BrandActionError: (failure) => mapDomainIdentity(brandActionErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RenameBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RenameBrandActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_BRAND',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: renameBrandActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RenameBrandActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_BRAND',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: renameBrandActionProblem.internal,
      ActionIdempotencyKeyRequired: renameBrandActionProblem.precondition,
      ActionInvocationNotFound: (failure) => renameBrandActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => renameBrandActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => renameBrandActionProblem.conflict(failure.code),
      ActionPayloadValidationError: renameBrandActionProblem.invalid,
      ActionPermissionCheckError: (failure) => renameBrandActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => renameBrandActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => renameBrandActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => renameBrandActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => renameBrandActionProblem.conflict(failure.code),
      ActionResultValidationError: renameBrandActionProblem.internal,
      ActionTransactionError: (failure) => renameBrandActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: renameBrandActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => renameBrandActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => renameBrandActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: renameBrandActionProblem.authentication,
      OperationContextDenied: (failure) => renameBrandActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => renameBrandActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => renameBrandActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(renameBrandAction.descriptor.domainErrorSchema);
export const mapRenameBrandActionProblem = (error: ActionCoreError | DomainError): RenameBrandActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const renameBrandActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RenameBrandActionSchemaErrorMiddleware,
  () => Effect.fail(renameBrandActionProblem.invalid()),
);
