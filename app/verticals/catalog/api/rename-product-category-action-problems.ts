// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug rename-product-category
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RenameProductCategoryActionAlreadyCommittedProblemSchema,
  RenameProductCategoryActionAuthenticationProblemSchema,
  RenameProductCategoryActionCommitIndeterminateProblemSchema,
  RenameProductCategoryActionConflictProblemSchema,
  RenameProductCategoryActionForbiddenProblemSchema,
  RenameProductCategoryActionIneligibleProblemSchema,
  RenameProductCategoryActionInternalProblemSchema,
  RenameProductCategoryActionInvalidProblemSchema,
  RenameProductCategoryActionNotFoundProblemSchema,
  RenameProductCategoryActionPreconditionProblemSchema,
  RenameProductCategoryActionSchemaErrorMiddleware,
  RenameProductCategoryActionUnavailableProblemSchema,
} from '../shared/apis/rename-product-category-action.ts';
import type { RenameProductCategoryActionProblem } from '../shared/apis/rename-product-category-action.ts';
import { renameProductCategoryAction } from '../src/actions/rename-product-category.action.ts';

type DomainError = typeof renameProductCategoryAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RenameProductCategoryActionProblem['_tag']> = Extract<
  RenameProductCategoryActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | {
      readonly code: 'category_conflict' | 'category_reference_conflict' | 'category_revision_conflict';
      readonly kind: 'conflict';
    }
  | { readonly code: 'category_not_found'; readonly kind: 'notFound' }
  | { readonly code: 'category_persistence_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const categoryActionRejectedProblemByCode = {
  category_conflict: { code: 'category_conflict', kind: 'conflict' },
  category_not_found: { code: 'category_not_found', kind: 'notFound' },
  category_reference_conflict: { code: 'category_reference_conflict', kind: 'conflict' },
  category_revision_conflict: { code: 'category_revision_conflict', kind: 'conflict' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'CategoryActionRejected' }>['code'],
  DomainProblemIdentity
>;

export const renameProductCategoryActionProblem = {
  authentication: (): ProblemOf<'RenameProductCategoryActionAuthenticationProblem'> =>
    RenameProductCategoryActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RenameProductCategoryActionConflictProblem'>['code'],
    revision?: { readonly actualRevision: number; readonly expectedRevision: number },
  ): ProblemOf<'RenameProductCategoryActionConflictProblem'> =>
    RenameProductCategoryActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      ...revision,
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RenameProductCategoryActionForbiddenProblem'>['code'],
  ): ProblemOf<'RenameProductCategoryActionForbiddenProblem'> =>
    RenameProductCategoryActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RenameProductCategoryActionIneligibleProblem'>['code'],
  ): ProblemOf<'RenameProductCategoryActionIneligibleProblem'> =>
    RenameProductCategoryActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RenameProductCategoryActionInternalProblem'> =>
    RenameProductCategoryActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RenameProductCategoryActionInvalidProblem'> =>
    RenameProductCategoryActionInvalidProblemSchema.make({
      detail: 'The rename-product-category Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RenameProductCategoryActionNotFoundProblem'>['code'],
  ): ProblemOf<'RenameProductCategoryActionNotFoundProblem'> =>
    RenameProductCategoryActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RenameProductCategoryActionPreconditionProblem'> =>
    RenameProductCategoryActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RenameProductCategoryActionUnavailableProblem'>['code'],
  ): ProblemOf<'RenameProductCategoryActionUnavailableProblem'> =>
    RenameProductCategoryActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RenameProductCategoryActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => renameProductCategoryActionProblem.conflict(matched.code)),
    Match.when({ kind: 'notFound' as const }, (matched) => renameProductCategoryActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      renameProductCategoryActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RenameProductCategoryActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CategoryActionRejected: (failure) => mapDomainIdentity(categoryActionRejectedProblemByCode[failure.code]),
      CategoryPersistenceUnavailable: () =>
        renameProductCategoryActionProblem.unavailable('category_persistence_unavailable'),
      CategoryRevisionConflict: (failure) =>
        renameProductCategoryActionProblem.conflict('category_revision_conflict', {
          actualRevision: failure.actualRevision,
          expectedRevision: failure.expectedRevision,
        }),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RenameProductCategoryActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RenameProductCategoryActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_PRODUCT_CATEGORY',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: renameProductCategoryActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RenameProductCategoryActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_PRODUCT_CATEGORY',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: renameProductCategoryActionProblem.internal,
      ActionIdempotencyKeyRequired: renameProductCategoryActionProblem.precondition,
      ActionInvocationNotFound: (failure) => renameProductCategoryActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => renameProductCategoryActionProblem.conflict(failure.code),
      ActionPayloadValidationError: renameProductCategoryActionProblem.invalid,
      ActionPermissionCheckError: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => renameProductCategoryActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => renameProductCategoryActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => renameProductCategoryActionProblem.conflict(failure.code),
      ActionResultValidationError: renameProductCategoryActionProblem.internal,
      ActionTransactionError: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: renameProductCategoryActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => renameProductCategoryActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: renameProductCategoryActionProblem.authentication,
      OperationContextDenied: (failure) => renameProductCategoryActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => renameProductCategoryActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => renameProductCategoryActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(renameProductCategoryAction.descriptor.domainErrorSchema);
export const mapRenameProductCategoryActionProblem = (
  error: ActionCoreError | DomainError,
): RenameProductCategoryActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const renameProductCategoryActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RenameProductCategoryActionSchemaErrorMiddleware,
  () => Effect.fail(renameProductCategoryActionProblem.invalid()),
);
