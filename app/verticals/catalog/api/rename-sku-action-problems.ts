// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug rename-sku
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RenameSkuActionAlreadyCommittedProblemSchema,
  RenameSkuActionAuthenticationProblemSchema,
  RenameSkuActionCommitIndeterminateProblemSchema,
  RenameSkuActionConflictProblemSchema,
  RenameSkuActionForbiddenProblemSchema,
  RenameSkuActionIneligibleProblemSchema,
  RenameSkuActionInternalProblemSchema,
  RenameSkuActionInvalidProblemSchema,
  RenameSkuActionNotFoundProblemSchema,
  RenameSkuActionPreconditionProblemSchema,
  RenameSkuActionSchemaErrorMiddleware,
  RenameSkuActionUnavailableProblemSchema,
} from '../shared/apis/rename-sku-action.ts';
import type { RenameSkuActionProblem } from '../shared/apis/rename-sku-action.ts';
import { renameSkuAction } from '../src/actions/rename-sku.action.ts';

type DomainError = typeof renameSkuAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RenameSkuActionProblem['_tag']> = Extract<RenameSkuActionProblem, { readonly _tag: Tag }>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const renameSkuActionProblem = {
  authentication: (): ProblemOf<'RenameSkuActionAuthenticationProblem'> =>
    RenameSkuActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (code: ProblemOf<'RenameSkuActionConflictProblem'>['code']): ProblemOf<'RenameSkuActionConflictProblem'> =>
    RenameSkuActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RenameSkuActionForbiddenProblem'>['code'],
  ): ProblemOf<'RenameSkuActionForbiddenProblem'> =>
    RenameSkuActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RenameSkuActionIneligibleProblem'>['code'],
  ): ProblemOf<'RenameSkuActionIneligibleProblem'> =>
    RenameSkuActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RenameSkuActionInternalProblem'> =>
    RenameSkuActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RenameSkuActionInvalidProblem'> =>
    RenameSkuActionInvalidProblemSchema.make({
      detail: 'The rename-sku Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (code: ProblemOf<'RenameSkuActionNotFoundProblem'>['code']): ProblemOf<'RenameSkuActionNotFoundProblem'> =>
    RenameSkuActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RenameSkuActionPreconditionProblem'> =>
    RenameSkuActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RenameSkuActionUnavailableProblem'>['code'],
  ): ProblemOf<'RenameSkuActionUnavailableProblem'> =>
    RenameSkuActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RenameSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      SkuActionConflict: () => renameSkuActionProblem.conflict('sku_action_conflict'),
      SkuActionInvalid: () => renameSkuActionProblem.ineligible('sku_action_invalid'),
      SkuActionNotFound: () => renameSkuActionProblem.notFound('sku_action_not_found'),
      SkuActionStale: () => renameSkuActionProblem.conflict('sku_action_stale'),
      SkuPersistenceUnavailable: () => renameSkuActionProblem.unavailable('sku_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RenameSkuActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RenameSkuActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover its original result by invocation.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_SKU',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: renameSkuActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RenameSkuActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RENAME_SKU',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: renameSkuActionProblem.internal,
      ActionIdempotencyKeyRequired: renameSkuActionProblem.precondition,
      ActionInvocationNotFound: (failure) => renameSkuActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => renameSkuActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => renameSkuActionProblem.conflict(failure.code),
      ActionPayloadValidationError: renameSkuActionProblem.invalid,
      ActionPermissionCheckError: (failure) => renameSkuActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => renameSkuActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => renameSkuActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => renameSkuActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => renameSkuActionProblem.conflict(failure.code),
      ActionResultValidationError: renameSkuActionProblem.internal,
      ActionTransactionError: (failure) => renameSkuActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: renameSkuActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => renameSkuActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => renameSkuActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: renameSkuActionProblem.authentication,
      OperationContextDenied: (failure) => renameSkuActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => renameSkuActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => renameSkuActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(renameSkuAction.descriptor.domainErrorSchema);
export const mapRenameSkuActionProblem = (error: ActionCoreError | DomainError): RenameSkuActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const renameSkuActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RenameSkuActionSchemaErrorMiddleware,
  () => Effect.fail(renameSkuActionProblem.invalid()),
);
