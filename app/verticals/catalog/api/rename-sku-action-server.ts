// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug rename-sku
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { renameSkuAction } from '../src/actions/rename-sku.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  renameSkuActionProblem,
  renameSkuActionSchemaErrorLive,
  mapRenameSkuActionProblem,
} from './rename-sku-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: renameSkuActionProblem.authentication,
  unavailable: () => renameSkuActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RenameSkuActionServer.execute')(function* executeRenameSku(
  payload: typeof renameSkuAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(renameSkuActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(renameSkuAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(renameSkuActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: renameSkuActionProblem.internal,
    invalidCorrelationProblem: renameSkuActionProblem.invalid,
    mapError: mapRenameSkuActionProblem,
    payload: decoded,
    registration: renameSkuAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const renameSkuActionApiLive = HttpApiBuilder.group(catalogApi, 'renameSkuAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(renameSkuActionSchemaErrorLive));
