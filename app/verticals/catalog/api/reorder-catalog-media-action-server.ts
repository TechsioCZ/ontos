// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug reorder-catalog-media
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { reorderCatalogMediaAction } from '../src/actions/reorder-catalog-media.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reorderCatalogMediaActionProblem,
  reorderCatalogMediaActionSchemaErrorLive,
  mapReorderCatalogMediaActionProblem,
} from './reorder-catalog-media-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reorderCatalogMediaActionProblem.authentication,
  unavailable: () => reorderCatalogMediaActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReorderCatalogMediaActionServer.execute')(function* executeReorderCatalogMedia(
  payload: typeof reorderCatalogMediaAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(reorderCatalogMediaActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(reorderCatalogMediaAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(reorderCatalogMediaActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: reorderCatalogMediaActionProblem.internal,
    invalidCorrelationProblem: reorderCatalogMediaActionProblem.invalid,
    mapError: mapReorderCatalogMediaActionProblem,
    payload: decoded,
    registration: reorderCatalogMediaAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const reorderCatalogMediaActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'reorderCatalogMediaAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reorderCatalogMediaActionSchemaErrorLive));
