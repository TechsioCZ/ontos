// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug replace-product-sizes
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { replaceProductSizesAction } from '../src/actions/replace-product-sizes.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  replaceProductSizesActionProblem,
  replaceProductSizesActionSchemaErrorLive,
  mapReplaceProductSizesActionProblem,
} from './replace-product-sizes-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: replaceProductSizesActionProblem.authentication,
  unavailable: () => replaceProductSizesActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReplaceProductSizesActionServer.execute')(function* executeReplaceProductSizes(
  payload: typeof replaceProductSizesAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(replaceProductSizesActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(replaceProductSizesAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(replaceProductSizesActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: replaceProductSizesActionProblem.internal,
    invalidCorrelationProblem: replaceProductSizesActionProblem.invalid,
    mapError: mapReplaceProductSizesActionProblem,
    payload: decoded,
    registration: replaceProductSizesAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const replaceProductSizesActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'replaceProductSizesAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(replaceProductSizesActionSchemaErrorLive));
