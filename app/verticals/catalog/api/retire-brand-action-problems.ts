// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-brand
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RetireBrandActionAlreadyCommittedProblemSchema,
  RetireBrandActionAuthenticationProblemSchema,
  RetireBrandActionCommitIndeterminateProblemSchema,
  RetireBrandActionConflictProblemSchema,
  RetireBrandActionForbiddenProblemSchema,
  RetireBrandActionIneligibleProblemSchema,
  RetireBrandActionInternalProblemSchema,
  RetireBrandActionInvalidProblemSchema,
  RetireBrandActionNotFoundProblemSchema,
  RetireBrandActionPreconditionProblemSchema,
  RetireBrandActionSchemaErrorMiddleware,
  RetireBrandActionUnavailableProblemSchema,
} from '../shared/apis/retire-brand-action.ts';
import type { RetireBrandActionProblem } from '../shared/apis/retire-brand-action.ts';
import { retireBrandAction } from '../src/actions/retire-brand.action.ts';

type DomainError = typeof retireBrandAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RetireBrandActionProblem['_tag']> = Extract<
  RetireBrandActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'brand_stale'; readonly kind: 'conflict' }
  | { readonly code: 'brand_invalid'; readonly kind: 'ineligible' }
  | { readonly code: 'brand_not_found'; readonly kind: 'notFound' }
  | { readonly code: 'brand_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const brandActionErrorProblemByCode = {
  brand_invalid: { code: 'brand_invalid', kind: 'ineligible' },
  brand_not_found: { code: 'brand_not_found', kind: 'notFound' },
  brand_stale: { code: 'brand_stale', kind: 'conflict' },
  brand_unavailable: { code: 'brand_unavailable', kind: 'unavailable' },
} as const satisfies Record<Extract<DomainError, { readonly _tag: 'BrandActionError' }>['code'], DomainProblemIdentity>;

export const retireBrandActionProblem = {
  authentication: (): ProblemOf<'RetireBrandActionAuthenticationProblem'> =>
    RetireBrandActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RetireBrandActionConflictProblem'>['code'],
  ): ProblemOf<'RetireBrandActionConflictProblem'> =>
    RetireBrandActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RetireBrandActionForbiddenProblem'>['code'],
  ): ProblemOf<'RetireBrandActionForbiddenProblem'> =>
    RetireBrandActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RetireBrandActionIneligibleProblem'>['code'],
  ): ProblemOf<'RetireBrandActionIneligibleProblem'> =>
    RetireBrandActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RetireBrandActionInternalProblem'> =>
    RetireBrandActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RetireBrandActionInvalidProblem'> =>
    RetireBrandActionInvalidProblemSchema.make({
      detail: 'The retire-brand Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RetireBrandActionNotFoundProblem'>['code'],
  ): ProblemOf<'RetireBrandActionNotFoundProblem'> =>
    RetireBrandActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RetireBrandActionPreconditionProblem'> =>
    RetireBrandActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RetireBrandActionUnavailableProblem'>['code'],
  ): ProblemOf<'RetireBrandActionUnavailableProblem'> =>
    RetireBrandActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RetireBrandActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => retireBrandActionProblem.conflict(matched.code)),
    Match.when({ kind: 'ineligible' as const }, (matched) => retireBrandActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'notFound' as const }, (matched) => retireBrandActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => retireBrandActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RetireBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      BrandActionError: (failure) => mapDomainIdentity(brandActionErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RetireBrandActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RetireBrandActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover the original retire result.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_BRAND',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: retireBrandActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RetireBrandActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_BRAND',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: retireBrandActionProblem.internal,
      ActionIdempotencyKeyRequired: retireBrandActionProblem.precondition,
      ActionInvocationNotFound: (failure) => retireBrandActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => retireBrandActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => retireBrandActionProblem.conflict(failure.code),
      ActionPayloadValidationError: retireBrandActionProblem.invalid,
      ActionPermissionCheckError: (failure) => retireBrandActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => retireBrandActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => retireBrandActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => retireBrandActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => retireBrandActionProblem.conflict(failure.code),
      ActionResultValidationError: retireBrandActionProblem.internal,
      ActionTransactionError: (failure) => retireBrandActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: retireBrandActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => retireBrandActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => retireBrandActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: retireBrandActionProblem.authentication,
      OperationContextDenied: (failure) => retireBrandActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => retireBrandActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => retireBrandActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(retireBrandAction.descriptor.domainErrorSchema);
export const mapRetireBrandActionProblem = (error: ActionCoreError | DomainError): RetireBrandActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const retireBrandActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RetireBrandActionSchemaErrorMiddleware,
  () => Effect.fail(retireBrandActionProblem.invalid()),
);
