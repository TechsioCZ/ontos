// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-brand
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retireBrandAction } from '../src/actions/retire-brand.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retireBrandActionProblem,
  retireBrandActionSchemaErrorLive,
  mapRetireBrandActionProblem,
} from './retire-brand-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retireBrandActionProblem.authentication,
  unavailable: () => retireBrandActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetireBrandActionServer.execute')(function* executeRetireBrand(
  payload: typeof retireBrandAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(retireBrandActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(retireBrandAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(retireBrandActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: retireBrandActionProblem.internal,
    invalidCorrelationProblem: retireBrandActionProblem.invalid,
    mapError: mapRetireBrandActionProblem,
    payload: decoded,
    registration: retireBrandAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const retireBrandActionApiLive = HttpApiBuilder.group(catalogApi, 'retireBrandAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(retireBrandActionSchemaErrorLive));
