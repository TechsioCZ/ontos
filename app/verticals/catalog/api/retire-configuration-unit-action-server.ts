// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-configuration-unit
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retireConfigurationUnitAction } from '../src/actions/retire-configuration-unit.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retireConfigurationUnitActionProblem,
  retireConfigurationUnitActionSchemaErrorLive,
  mapRetireConfigurationUnitActionProblem,
} from './retire-configuration-unit-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retireConfigurationUnitActionProblem.authentication,
  unavailable: () => retireConfigurationUnitActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetireConfigurationUnitActionServer.execute')(function* executeRetireConfigurationUnit(
  payload: typeof retireConfigurationUnitAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(retireConfigurationUnitActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(retireConfigurationUnitAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(retireConfigurationUnitActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: retireConfigurationUnitActionProblem.internal,
    invalidCorrelationProblem: retireConfigurationUnitActionProblem.invalid,
    mapError: mapRetireConfigurationUnitActionProblem,
    payload: decoded,
    registration: retireConfigurationUnitAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const retireConfigurationUnitActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'retireConfigurationUnitAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(retireConfigurationUnitActionSchemaErrorLive));
