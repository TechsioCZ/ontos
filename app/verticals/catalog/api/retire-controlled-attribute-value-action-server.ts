// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-controlled-attribute-value
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retireControlledAttributeValueAction } from '../src/actions/retire-controlled-attribute-value.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retireControlledAttributeValueActionProblem,
  retireControlledAttributeValueActionSchemaErrorLive,
  mapRetireControlledAttributeValueActionProblem,
} from './retire-controlled-attribute-value-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retireControlledAttributeValueActionProblem.authentication,
  unavailable: () => retireControlledAttributeValueActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetireControlledAttributeValueActionServer.execute')(
  function* executeRetireControlledAttributeValue(
    payload: typeof retireControlledAttributeValueAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(retireControlledAttributeValueActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(retireControlledAttributeValueAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(retireControlledAttributeValueActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: retireControlledAttributeValueActionProblem.internal,
      invalidCorrelationProblem: retireControlledAttributeValueActionProblem.invalid,
      mapError: mapRetireControlledAttributeValueActionProblem,
      payload: decoded,
      registration: retireControlledAttributeValueAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const retireControlledAttributeValueActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'retireControlledAttributeValueAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(retireControlledAttributeValueActionSchemaErrorLive));
