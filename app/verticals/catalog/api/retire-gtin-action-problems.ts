// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-gtin
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RetireGtinActionAlreadyCommittedProblemSchema,
  RetireGtinActionAuthenticationProblemSchema,
  RetireGtinActionCommitIndeterminateProblemSchema,
  RetireGtinActionConflictProblemSchema,
  RetireGtinActionForbiddenProblemSchema,
  RetireGtinActionIneligibleProblemSchema,
  RetireGtinActionInternalProblemSchema,
  RetireGtinActionInvalidProblemSchema,
  RetireGtinActionNotFoundProblemSchema,
  RetireGtinActionPreconditionProblemSchema,
  RetireGtinActionSchemaErrorMiddleware,
  RetireGtinActionUnavailableProblemSchema,
} from '../shared/apis/retire-gtin-action.ts';
import type { RetireGtinActionProblem } from '../shared/apis/retire-gtin-action.ts';
import { retireGtinAction } from '../src/actions/retire-gtin.action.ts';

type DomainError = typeof retireGtinAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RetireGtinActionProblem['_tag']> = Extract<RetireGtinActionProblem, { readonly _tag: Tag }>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const retireGtinActionProblem = {
  authentication: (): ProblemOf<'RetireGtinActionAuthenticationProblem'> =>
    RetireGtinActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RetireGtinActionConflictProblem'>['code'],
  ): ProblemOf<'RetireGtinActionConflictProblem'> =>
    RetireGtinActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RetireGtinActionForbiddenProblem'>['code'],
  ): ProblemOf<'RetireGtinActionForbiddenProblem'> =>
    RetireGtinActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RetireGtinActionIneligibleProblem'>['code'],
  ): ProblemOf<'RetireGtinActionIneligibleProblem'> =>
    RetireGtinActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RetireGtinActionInternalProblem'> =>
    RetireGtinActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RetireGtinActionInvalidProblem'> =>
    RetireGtinActionInvalidProblemSchema.make({
      detail: 'The retire-gtin Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RetireGtinActionNotFoundProblem'>['code'],
  ): ProblemOf<'RetireGtinActionNotFoundProblem'> =>
    RetireGtinActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RetireGtinActionPreconditionProblem'> =>
    RetireGtinActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RetireGtinActionUnavailableProblem'>['code'],
  ): ProblemOf<'RetireGtinActionUnavailableProblem'> =>
    RetireGtinActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RetireGtinActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      GtinActionConflict: () => retireGtinActionProblem.conflict('gtin_action_conflict'),
      GtinActionInvalid: () => retireGtinActionProblem.ineligible('gtin_action_invalid'),
      GtinActionNotFound: () => retireGtinActionProblem.notFound('gtin_action_not_found'),
      GtinActionStale: () => retireGtinActionProblem.conflict('gtin_action_stale'),
      GtinPersistenceUnavailable: () => retireGtinActionProblem.unavailable('gtin_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RetireGtinActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RetireGtinActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover the original retire result.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_GTIN',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: retireGtinActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RetireGtinActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_GTIN',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: retireGtinActionProblem.internal,
      ActionIdempotencyKeyRequired: retireGtinActionProblem.precondition,
      ActionInvocationNotFound: (failure) => retireGtinActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => retireGtinActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => retireGtinActionProblem.conflict(failure.code),
      ActionPayloadValidationError: retireGtinActionProblem.invalid,
      ActionPermissionCheckError: (failure) => retireGtinActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => retireGtinActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => retireGtinActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => retireGtinActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => retireGtinActionProblem.conflict(failure.code),
      ActionResultValidationError: retireGtinActionProblem.internal,
      ActionTransactionError: (failure) => retireGtinActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: retireGtinActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => retireGtinActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => retireGtinActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: retireGtinActionProblem.authentication,
      OperationContextDenied: (failure) => retireGtinActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => retireGtinActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => retireGtinActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(retireGtinAction.descriptor.domainErrorSchema);
export const mapRetireGtinActionProblem = (error: ActionCoreError | DomainError): RetireGtinActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const retireGtinActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RetireGtinActionSchemaErrorMiddleware,
  () => Effect.fail(retireGtinActionProblem.invalid()),
);
