// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-package-definition
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retirePackageDefinitionAction } from '../src/actions/retire-package-definition.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retirePackageDefinitionActionProblem,
  retirePackageDefinitionActionSchemaErrorLive,
  mapRetirePackageDefinitionActionProblem,
} from './retire-package-definition-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retirePackageDefinitionActionProblem.authentication,
  unavailable: () => retirePackageDefinitionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetirePackageDefinitionActionServer.execute')(function* executeRetirePackageDefinition(
  payload: typeof retirePackageDefinitionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(retirePackageDefinitionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(retirePackageDefinitionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(retirePackageDefinitionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: retirePackageDefinitionActionProblem.internal,
    invalidCorrelationProblem: retirePackageDefinitionActionProblem.invalid,
    mapError: mapRetirePackageDefinitionActionProblem,
    payload: decoded,
    registration: retirePackageDefinitionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const retirePackageDefinitionActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'retirePackageDefinitionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(retirePackageDefinitionActionSchemaErrorLive));
