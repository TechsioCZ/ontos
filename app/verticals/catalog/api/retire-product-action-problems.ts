// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-product
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RetireProductActionAlreadyCommittedProblemSchema,
  RetireProductActionAuthenticationProblemSchema,
  RetireProductActionCommitIndeterminateProblemSchema,
  RetireProductActionConflictProblemSchema,
  RetireProductActionForbiddenProblemSchema,
  RetireProductActionIneligibleProblemSchema,
  RetireProductActionInternalProblemSchema,
  RetireProductActionInvalidProblemSchema,
  RetireProductActionNotFoundProblemSchema,
  RetireProductActionPreconditionProblemSchema,
  RetireProductActionSchemaErrorMiddleware,
  RetireProductActionUnavailableProblemSchema,
} from '../shared/apis/retire-product-action.ts';
import type { RetireProductActionProblem } from '../shared/apis/retire-product-action.ts';
import { retireProductAction } from '../src/actions/retire-product.action.ts';

type DomainError = typeof retireProductAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RetireProductActionProblem['_tag']> = Extract<
  RetireProductActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const retireProductActionProblem = {
  authentication: (): ProblemOf<'RetireProductActionAuthenticationProblem'> =>
    RetireProductActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RetireProductActionConflictProblem'>['code'],
  ): ProblemOf<'RetireProductActionConflictProblem'> =>
    RetireProductActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RetireProductActionForbiddenProblem'>['code'],
  ): ProblemOf<'RetireProductActionForbiddenProblem'> =>
    RetireProductActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RetireProductActionIneligibleProblem'>['code'],
  ): ProblemOf<'RetireProductActionIneligibleProblem'> =>
    RetireProductActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RetireProductActionInternalProblem'> =>
    RetireProductActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RetireProductActionInvalidProblem'> =>
    RetireProductActionInvalidProblemSchema.make({
      detail: 'The retire-product Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RetireProductActionNotFoundProblem'>['code'],
  ): ProblemOf<'RetireProductActionNotFoundProblem'> =>
    RetireProductActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RetireProductActionPreconditionProblem'> =>
    RetireProductActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RetireProductActionUnavailableProblem'>['code'],
  ): ProblemOf<'RetireProductActionUnavailableProblem'> =>
    RetireProductActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RetireProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceConflict: () => retireProductActionProblem.conflict('catalog_persistence_conflict'),
      CatalogPersistenceUnavailable: () => retireProductActionProblem.unavailable('catalog_persistence_unavailable'),
      ProductCorrectionRequired: () => retireProductActionProblem.ineligible('product_correction_required'),
      ProductLifecycleConflict: () => retireProductActionProblem.conflict('product_lifecycle_conflict'),
      ProductNotCatalogReady: () => retireProductActionProblem.ineligible('product_not_catalog_ready'),
      ProductNotFound: () => retireProductActionProblem.notFound('product_not_found'),
      ProductPersistenceConflict: () => retireProductActionProblem.conflict('product_persistence_conflict'),
      ProductRevisionConflict: () => retireProductActionProblem.conflict('product_revision_conflict'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RetireProductActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RetireProductActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover the original retire result.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_PRODUCT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: retireProductActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RetireProductActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_PRODUCT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: retireProductActionProblem.internal,
      ActionIdempotencyKeyRequired: retireProductActionProblem.precondition,
      ActionInvocationNotFound: (failure) => retireProductActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => retireProductActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => retireProductActionProblem.conflict(failure.code),
      ActionPayloadValidationError: retireProductActionProblem.invalid,
      ActionPermissionCheckError: (failure) => retireProductActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => retireProductActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => retireProductActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => retireProductActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => retireProductActionProblem.conflict(failure.code),
      ActionResultValidationError: retireProductActionProblem.internal,
      ActionTransactionError: (failure) => retireProductActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: retireProductActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => retireProductActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => retireProductActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: retireProductActionProblem.authentication,
      OperationContextDenied: (failure) => retireProductActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => retireProductActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => retireProductActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(retireProductAction.descriptor.domainErrorSchema);
export const mapRetireProductActionProblem = (error: ActionCoreError | DomainError): RetireProductActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const retireProductActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RetireProductActionSchemaErrorMiddleware,
  () => Effect.fail(retireProductActionProblem.invalid()),
);
