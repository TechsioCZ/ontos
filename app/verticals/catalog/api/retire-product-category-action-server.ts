// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-product-category
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retireProductCategoryAction } from '../src/actions/retire-product-category.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retireProductCategoryActionProblem,
  retireProductCategoryActionSchemaErrorLive,
  mapRetireProductCategoryActionProblem,
} from './retire-product-category-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retireProductCategoryActionProblem.authentication,
  unavailable: () => retireProductCategoryActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetireProductCategoryActionServer.execute')(function* executeRetireProductCategory(
  payload: typeof retireProductCategoryAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(retireProductCategoryActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(retireProductCategoryAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(retireProductCategoryActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: retireProductCategoryActionProblem.internal,
    invalidCorrelationProblem: retireProductCategoryActionProblem.invalid,
    mapError: mapRetireProductCategoryActionProblem,
    payload: decoded,
    registration: retireProductCategoryAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const retireProductCategoryActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'retireProductCategoryAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(retireProductCategoryActionSchemaErrorLive));
