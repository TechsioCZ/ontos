// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-product-unit
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { retireProductUnitAction } from '../src/actions/retire-product-unit.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  retireProductUnitActionProblem,
  retireProductUnitActionSchemaErrorLive,
  mapRetireProductUnitActionProblem,
} from './retire-product-unit-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: retireProductUnitActionProblem.authentication,
  unavailable: () => retireProductUnitActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RetireProductUnitActionServer.execute')(function* executeRetireProductUnit(
  payload: typeof retireProductUnitAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(retireProductUnitActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(retireProductUnitAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(retireProductUnitActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: retireProductUnitActionProblem.internal,
    invalidCorrelationProblem: retireProductUnitActionProblem.invalid,
    mapError: mapRetireProductUnitActionProblem,
    payload: decoded,
    registration: retireProductUnitAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const retireProductUnitActionApiLive = HttpApiBuilder.group(catalogApi, 'retireProductUnitAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(retireProductUnitActionSchemaErrorLive));
