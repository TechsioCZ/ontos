// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug retire-variant
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RetireVariantActionAlreadyCommittedProblemSchema,
  RetireVariantActionAuthenticationProblemSchema,
  RetireVariantActionCommitIndeterminateProblemSchema,
  RetireVariantActionConflictProblemSchema,
  RetireVariantActionForbiddenProblemSchema,
  RetireVariantActionIneligibleProblemSchema,
  RetireVariantActionInternalProblemSchema,
  RetireVariantActionInvalidProblemSchema,
  RetireVariantActionNotFoundProblemSchema,
  RetireVariantActionPreconditionProblemSchema,
  RetireVariantActionSchemaErrorMiddleware,
  RetireVariantActionUnavailableProblemSchema,
} from '../shared/apis/retire-variant-action.ts';
import type { RetireVariantActionProblem } from '../shared/apis/retire-variant-action.ts';
import { retireVariantAction } from '../src/actions/retire-variant.action.ts';

type DomainError = typeof retireVariantAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RetireVariantActionProblem['_tag']> = Extract<
  RetireVariantActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const retireVariantActionProblem = {
  authentication: (): ProblemOf<'RetireVariantActionAuthenticationProblem'> =>
    RetireVariantActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RetireVariantActionConflictProblem'>['code'],
  ): ProblemOf<'RetireVariantActionConflictProblem'> =>
    RetireVariantActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RetireVariantActionForbiddenProblem'>['code'],
  ): ProblemOf<'RetireVariantActionForbiddenProblem'> =>
    RetireVariantActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RetireVariantActionIneligibleProblem'>['code'],
  ): ProblemOf<'RetireVariantActionIneligibleProblem'> =>
    RetireVariantActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RetireVariantActionInternalProblem'> =>
    RetireVariantActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RetireVariantActionInvalidProblem'> =>
    RetireVariantActionInvalidProblemSchema.make({
      detail: 'The retire-variant Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RetireVariantActionNotFoundProblem'>['code'],
  ): ProblemOf<'RetireVariantActionNotFoundProblem'> =>
    RetireVariantActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RetireVariantActionPreconditionProblem'> =>
    RetireVariantActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RetireVariantActionUnavailableProblem'>['code'],
  ): ProblemOf<'RetireVariantActionUnavailableProblem'> =>
    RetireVariantActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RetireVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CatalogPersistenceUnavailable: () => retireVariantActionProblem.unavailable('catalog_persistence_unavailable'),
      VariantActionConflict: () => retireVariantActionProblem.conflict('variant_action_conflict'),
      VariantActionNotFound: () => retireVariantActionProblem.notFound('variant_action_not_found'),
      VariantCurrentBasisUnavailable: () => retireVariantActionProblem.unavailable('variant_current_basis_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RetireVariantActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RetireVariantActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Recover the original retire result.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_VARIANT',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: retireVariantActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RetireVariantActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RECOVER_RETIRE_VARIANT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: retireVariantActionProblem.internal,
      ActionIdempotencyKeyRequired: retireVariantActionProblem.precondition,
      ActionInvocationNotFound: (failure) => retireVariantActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => retireVariantActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => retireVariantActionProblem.conflict(failure.code),
      ActionPayloadValidationError: retireVariantActionProblem.invalid,
      ActionPermissionCheckError: (failure) => retireVariantActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => retireVariantActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => retireVariantActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => retireVariantActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => retireVariantActionProblem.conflict(failure.code),
      ActionResultValidationError: retireVariantActionProblem.internal,
      ActionTransactionError: (failure) => retireVariantActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: retireVariantActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => retireVariantActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => retireVariantActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: retireVariantActionProblem.authentication,
      OperationContextDenied: (failure) => retireVariantActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => retireVariantActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => retireVariantActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(retireVariantAction.descriptor.domainErrorSchema);
export const mapRetireVariantActionProblem = (error: ActionCoreError | DomainError): RetireVariantActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const retireVariantActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RetireVariantActionSchemaErrorMiddleware,
  () => Effect.fail(retireVariantActionProblem.invalid()),
);
