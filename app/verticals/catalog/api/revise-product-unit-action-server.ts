// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug revise-product-unit
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { reviseProductUnitAction } from '../src/actions/revise-product-unit.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reviseProductUnitActionProblem,
  reviseProductUnitActionSchemaErrorLive,
  mapReviseProductUnitActionProblem,
} from './revise-product-unit-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reviseProductUnitActionProblem.authentication,
  unavailable: () => reviseProductUnitActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReviseProductUnitActionServer.execute')(function* executeReviseProductUnit(
  payload: typeof reviseProductUnitAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(reviseProductUnitActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(reviseProductUnitAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(reviseProductUnitActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: reviseProductUnitActionProblem.internal,
    invalidCorrelationProblem: reviseProductUnitActionProblem.invalid,
    mapError: mapReviseProductUnitActionProblem,
    payload: decoded,
    registration: reviseProductUnitAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const reviseProductUnitActionApiLive = HttpApiBuilder.group(catalogApi, 'reviseProductUnitAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(reviseProductUnitActionSchemaErrorLive));
