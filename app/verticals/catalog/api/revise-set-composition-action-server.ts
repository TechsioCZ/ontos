// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug revise-set-composition
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { reviseSetCompositionAction } from '../src/actions/revise-set-composition.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reviseSetCompositionActionProblem,
  reviseSetCompositionActionSchemaErrorLive,
  mapReviseSetCompositionActionProblem,
} from './revise-set-composition-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reviseSetCompositionActionProblem.authentication,
  unavailable: () => reviseSetCompositionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReviseSetCompositionActionServer.execute')(function* executeReviseSetComposition(
  payload: typeof reviseSetCompositionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(reviseSetCompositionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(reviseSetCompositionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(reviseSetCompositionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: reviseSetCompositionActionProblem.internal,
    invalidCorrelationProblem: reviseSetCompositionActionProblem.invalid,
    mapError: mapReviseSetCompositionActionProblem,
    payload: decoded,
    registration: reviseSetCompositionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const reviseSetCompositionActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'reviseSetCompositionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reviseSetCompositionActionSchemaErrorLive));
