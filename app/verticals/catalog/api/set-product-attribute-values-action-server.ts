// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug set-product-attribute-values
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { setProductAttributeValuesAction } from '../src/actions/set-product-attribute-values.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  setProductAttributeValuesActionProblem,
  setProductAttributeValuesActionSchemaErrorLive,
  mapSetProductAttributeValuesActionProblem,
} from './set-product-attribute-values-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: setProductAttributeValuesActionProblem.authentication,
  unavailable: () => setProductAttributeValuesActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('SetProductAttributeValuesActionServer.execute')(function* executeSetProductAttributeValues(
  payload: typeof setProductAttributeValuesAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(setProductAttributeValuesActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(setProductAttributeValuesAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(setProductAttributeValuesActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: setProductAttributeValuesActionProblem.internal,
    invalidCorrelationProblem: setProductAttributeValuesActionProblem.invalid,
    mapError: mapSetProductAttributeValuesActionProblem,
    payload: decoded,
    registration: setProductAttributeValuesAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const setProductAttributeValuesActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'setProductAttributeValuesAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(setProductAttributeValuesActionSchemaErrorLive));
