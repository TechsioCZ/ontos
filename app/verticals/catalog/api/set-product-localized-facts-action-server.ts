// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug set-product-localized-facts
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { setProductLocalizedFactsAction } from '../src/actions/set-product-localized-facts.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  setProductLocalizedFactsActionProblem,
  setProductLocalizedFactsActionSchemaErrorLive,
  mapSetProductLocalizedFactsActionProblem,
} from './set-product-localized-facts-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: setProductLocalizedFactsActionProblem.authentication,
  unavailable: () => setProductLocalizedFactsActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('SetProductLocalizedFactsActionServer.execute')(function* executeSetProductLocalizedFacts(
  payload: typeof setProductLocalizedFactsAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(setProductLocalizedFactsActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(setProductLocalizedFactsAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(setProductLocalizedFactsActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: setProductLocalizedFactsActionProblem.internal,
    invalidCorrelationProblem: setProductLocalizedFactsActionProblem.invalid,
    mapError: mapSetProductLocalizedFactsActionProblem,
    payload: decoded,
    registration: setProductLocalizedFactsAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const setProductLocalizedFactsActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'setProductLocalizedFactsAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(setProductLocalizedFactsActionSchemaErrorLive));
