// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug set-product-type
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { setProductTypeAction } from '../src/actions/set-product-type.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  setProductTypeActionProblem,
  setProductTypeActionSchemaErrorLive,
  mapSetProductTypeActionProblem,
} from './set-product-type-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: setProductTypeActionProblem.authentication,
  unavailable: () => setProductTypeActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('SetProductTypeActionServer.execute')(function* executeSetProductType(
  payload: typeof setProductTypeAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(setProductTypeActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(setProductTypeAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(setProductTypeActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: setProductTypeActionProblem.internal,
    invalidCorrelationProblem: setProductTypeActionProblem.invalid,
    mapError: mapSetProductTypeActionProblem,
    payload: decoded,
    registration: setProductTypeAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const setProductTypeActionApiLive = HttpApiBuilder.group(catalogApi, 'setProductTypeAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(setProductTypeActionSchemaErrorLive));
