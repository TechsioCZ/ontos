// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug set-product-unit-target-divisibility
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { setProductUnitTargetDivisibilityAction } from '../src/actions/set-product-unit-target-divisibility.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  setProductUnitTargetDivisibilityActionProblem,
  setProductUnitTargetDivisibilityActionSchemaErrorLive,
  mapSetProductUnitTargetDivisibilityActionProblem,
} from './set-product-unit-target-divisibility-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: setProductUnitTargetDivisibilityActionProblem.authentication,
  unavailable: () => setProductUnitTargetDivisibilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('SetProductUnitTargetDivisibilityActionServer.execute')(
  function* executeSetProductUnitTargetDivisibility(
    payload: typeof setProductUnitTargetDivisibilityAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(setProductUnitTargetDivisibilityActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(setProductUnitTargetDivisibilityAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(setProductUnitTargetDivisibilityActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: setProductUnitTargetDivisibilityActionProblem.internal,
      invalidCorrelationProblem: setProductUnitTargetDivisibilityActionProblem.invalid,
      mapError: mapSetProductUnitTargetDivisibilityActionProblem,
      payload: decoded,
      registration: setProductUnitTargetDivisibilityAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const setProductUnitTargetDivisibilityActionApiLive = HttpApiBuilder.group(
  catalogApi,
  'setProductUnitTargetDivisibilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(setProductUnitTargetDivisibilityActionSchemaErrorLive));
