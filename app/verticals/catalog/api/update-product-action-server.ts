// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug update-product
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { catalogApi } from '../shared/api.ts';
import { updateProductAction } from '../src/actions/update-product.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  updateProductActionProblem,
  updateProductActionSchemaErrorLive,
  mapUpdateProductActionProblem,
} from './update-product-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: updateProductActionProblem.authentication,
  unavailable: () => updateProductActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('UpdateProductActionServer.execute')(function* executeUpdateProduct(
  payload: typeof updateProductAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(updateProductActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(updateProductAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(updateProductActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: updateProductActionProblem.internal,
    invalidCorrelationProblem: updateProductActionProblem.invalid,
    mapError: mapUpdateProductActionProblem,
    payload: decoded,
    registration: updateProductAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const updateProductActionApiLive = HttpApiBuilder.group(catalogApi, 'updateProductAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(updateProductActionSchemaErrorLive));
