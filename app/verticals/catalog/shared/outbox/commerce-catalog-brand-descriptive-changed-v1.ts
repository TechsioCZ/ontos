// @generated by OntOS Codesmith Outbox Message Contract v1
// @ontos-outbox-producer commerce.catalog
// @ontos-outbox-topic commerce.catalog.brand-descriptive-changed.v1
import { Schema } from 'effect';
import { BrandRefSchema } from '../resources/brand.ts';

export const OutboxPayloadSchema = Schema.Struct({
  brandRef: BrandRefSchema,
  name: Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(240), Schema.isTrimmed()),
  revision: Schema.Int.check(Schema.isGreaterThan(0)),
  tenantId: BrandRefSchema.fields.tenantId,
}).check(
  Schema.makeFilter(({ brandRef, tenantId }) =>
    brandRef.tenantId === tenantId ? undefined : 'Brand event subject must belong to the event Tenant',
  ),
);
export type OutboxPayload = Schema.Schema.Type<typeof OutboxPayloadSchema>;

export const outboxTopic = 'commerce.catalog.brand-descriptive-changed.v1' as const;
export const outboxProducerModuleKey = 'commerce.catalog' as const;
