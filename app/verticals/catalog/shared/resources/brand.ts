// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug brand
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

/** A rename or lifecycle change retains this Tenant-owned Brand identity. */
const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(Schema.brand('CatalogBrandResourceId'), Schema.decodeTo(checkedUuid));
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogTenantId'), Schema.decodeTo(checkedUuid));

export const BrandRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.brand'),
  tenantId: TenantIdSchema,
});
export type BrandRef = typeof BrandRefSchema.Type;

export const brandResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Tenant-owned Brand identity retained through rename, retirement, and confirmed reactivation.',
  key: 'commerce.catalog.brand',
  label: 'Brand',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
