// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug catalog-root
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

/** The tenant's Catalog root is addressed by the tenant UUID. */
const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(Schema.brand('CatalogRootResourceId'), Schema.decodeTo(checkedUuid));
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogRootTenantId'), Schema.decodeTo(checkedUuid));

export const CatalogRootRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.catalog-root'),
  tenantId: TenantIdSchema,
});
export type CatalogRootRef = typeof CatalogRootRefSchema.Type;

export const catalogRootResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Catalog Root resource.',
  key: 'commerce.catalog.catalog-root',
  label: 'Catalog Root',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
