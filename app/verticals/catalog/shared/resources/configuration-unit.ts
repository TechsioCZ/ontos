// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug configuration-unit
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('CatalogConfigurationUnitResourceId'),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('CatalogConfigurationUnitTenantId'),
);

export const ConfigurationUnitRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.unit'),
  tenantId: TenantIdSchema,
});
export type ConfigurationUnitRef = typeof ConfigurationUnitRefSchema.Type;

export const configurationUnitResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Tenant-owned measurement Unit, separate from purchase Quantity Units.',
  key: 'commerce.catalog.unit',
  label: 'Configuration Unit',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
