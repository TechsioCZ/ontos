// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug package-definition
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

/** One stable identity for the definition and its optional selectable role. */
const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(
  Schema.brand('CatalogPackageDefinitionResourceId'),
  Schema.decodeTo(checkedUuid),
);
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogTenantId'), Schema.decodeTo(checkedUuid));

export const PackageDefinitionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.package-definition'),
  tenantId: TenantIdSchema,
});
export type PackageDefinitionRef = typeof PackageDefinitionRefSchema.Type;

export const packageDefinitionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Package Definition resource.',
  key: 'commerce.catalog.package-definition',
  label: 'Package Definition',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
