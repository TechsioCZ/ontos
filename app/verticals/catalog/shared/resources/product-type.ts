// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug product-type
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

/** Product Type identity is tenant-owned and remains stable across rule revisions. */
const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(Schema.brand('CatalogProductTypeResourceId'), Schema.decodeTo(checkedUuid));
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogTenantId'), Schema.decodeTo(checkedUuid));

export const ProductTypeRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.product-type'),
  tenantId: TenantIdSchema,
});
export type ProductTypeRef = typeof ProductTypeRefSchema.Type;

export const productTypeResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Product Type resource.',
  key: 'commerce.catalog.product-type',
  label: 'Product Type',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
