// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.catalog
// @ontos-resource-slug product
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

/** Product identity is a UUID allocated once and retained for the life of the tenant resource. */
const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
const ResourceIdSchema = checkedUuid.pipe(Schema.brand('CatalogProductResourceId'), Schema.decodeTo(checkedUuid));
const TenantIdSchema = checkedUuid.pipe(Schema.brand('CatalogTenantId'), Schema.decodeTo(checkedUuid));

export const ProductRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.catalog.product'),
  tenantId: TenantIdSchema,
});
export type ProductRef = typeof ProductRefSchema.Type;

export const productResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Product resource.',
  key: 'commerce.catalog.product',
  label: 'Product',
  owningModuleId: 'commerce.catalog',
} as const satisfies OntosResourceType;
