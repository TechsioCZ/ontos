// @generated by OntOS Codesmith Action v1
// @ontos-action-owner commerce.catalog
// @ontos-action-slug release-local-override
import type { ActionHandlerContext } from '@app/core-runtime';
import { ActionTransactionError, defineAction, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { DateTime, Effect, Schema } from 'effect';

import {
  CatalogLocalOverrideResultSchema as ReleaseLocalOverrideResultSchema,
  ReleaseLocalOverridePayloadSchema,
} from '../../shared/actions/catalog-source-resolution.ts';
import type {
  CatalogLocalOverrideResult,
  ReleaseLocalOverridePayload,
} from '../../shared/actions/catalog-source-resolution.ts';
import { OutboxPayloadSchema as SelectionSourceChangedEventSchema } from '../../shared/outbox/commerce-catalog-selection-source-changed-v1.ts';
import { captureCatalogActionResult } from '../persistence/catalog-action-result-snapshot.ts';
import { CatalogSourceActionPersistenceFactory } from '../persistence/catalog-source-action-capability.ts';
import type { CatalogLocalOverrideActionPersistence } from '../persistence/catalog-source-action-capability.ts';
import { createReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxMessage } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
import {
  CatalogSourceActionErrorSchema,
  CatalogSourceAuditEvidenceSchema,
  CatalogSourceRequestInvalid,
  catalogResolvedCurrentEventPorts,
} from './catalog-source-action-support.ts';

export { ReleaseLocalOverridePayloadSchema } from '../../shared/actions/catalog-source-resolution.ts';
export { CatalogLocalOverrideResultSchema as ReleaseLocalOverrideResultSchema } from '../../shared/actions/catalog-source-resolution.ts';
export type { ReleaseLocalOverridePayload } from '../../shared/actions/catalog-source-resolution.ts';

const domainEvents = { 'commerce.catalog.selection-source-changed.v1': SelectionSourceChangedEventSchema } as const;
const ACTION_KEY = 'commerce.catalog.release-local-override' as const;
type Services = CatalogLocalOverrideActionPersistence & {
  readonly captureResult: (
    actionInvocationId: string,
    result: CatalogLocalOverrideResult,
  ) => Effect.Effect<void, ActionTransactionError>;
};

const handleReleaseLocalOverride = Effect.fn('ReleaseLocalOverrideAction.handle')(function* handleReleaseLocalOverride(
  payload: ReleaseLocalOverridePayload,
  context: ActionHandlerContext<typeof domainEvents, Services>,
) {
  if (payload.scope.tenantId !== context.scope.tenantId) {
    return yield* new CatalogSourceRequestInvalid({
      code: 'catalog_source_request_invalid',
      reason: 'The Local Override scope must belong to the trusted Tenant',
    });
  }
  const at = yield* DateTime.nowAsDate;
  const result = yield* context.services
    .localOverrideAt({
      actionInvocationId: context.actionInvocationId,
      at,
      events: catalogResolvedCurrentEventPorts(
        context,
        createReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxMessage,
      ),
      principalId: context.scope.principalId,
    })
    .release({ ...payload, at, principalId: context.scope.principalId });
  yield* context.recordAuditEvidence({ evidenceRefs: [payload.evidenceRef], reason: payload.reason });
  return result.status === 'APPLIED'
    ? {
        lifecycle: result.override.lifecycle,
        resolved:
          result.resolved.status === 'CURRENT'
            ? { source: result.resolved.source, status: result.resolved.status }
            : result.resolved,
        resolvedCurrentChanged: result.resolvedCurrentChanged,
        revision: result.override.revision,
        status: result.status,
      }
    : result;
});

export const releaseLocalOverrideAction = defineAction(
  {
    accessEvidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.release-local-override.access.v1',
    },
    actionKey: ACTION_KEY,
    auditEvidenceSchema: CatalogSourceAuditEvidenceSchema,
    auditProfile: 'standard',
    domainErrorSchema: CatalogSourceActionErrorSchema,
    domainEvents,
    entrypoint: defineTenantModuleEntrypoint({
      access: 'write',
      authorization: { kind: 'action_execution', provisioning: 'explicit' },
      entrypointKey: ACTION_KEY,
      moduleKey: 'commerce.catalog',
      role: 'action',
    }),
    idempotency: 'required',
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    payloadSchema: ReleaseLocalOverridePayloadSchema,
    policies: [],
    resultSchema: ReleaseLocalOverrideResultSchema,
    schemaVersion: '1',
  },
  handleReleaseLocalOverride,
  Effect.fn('ReleaseLocalOverrideAction.makeServices')(function* makeReleaseLocalOverrideServices(transaction, scope) {
    const persistenceFactory = yield* CatalogSourceActionPersistenceFactory;
    return {
      ...persistenceFactory.makeLocalOverride(transaction, { allowedOverrideOperation: 'RELEASE', scope }),
      captureResult: (actionInvocationId: string, result: CatalogLocalOverrideResult) =>
        captureCatalogActionResult(
          transaction,
          scope,
          { actionInvocationId, actionKey: ACTION_KEY, schemaVersion: 1 },
          {
            decode: Schema.decodeUnknownEffect(ReleaseLocalOverrideResultSchema),
            encode: Schema.encodeEffect(ReleaseLocalOverrideResultSchema),
          },
          result,
        ).pipe(
          Effect.mapError((cause) =>
            Object.assign(
              new ActionTransactionError({
                code: 'action_transaction_failed',
                reason: 'Catalog result capture failed',
              }),
              { cause },
            ),
          ),
        ),
    } satisfies Services;
  }),
  ({ actionInvocationId, result, services }) => services.captureResult(actionInvocationId, result),
);

// <generated-outbox-message-exports>
export { createReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxMessage } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
export { ReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxPayloadSchema } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
export { ReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxProducerModuleKey } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
export { ReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxTopic } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
export type { ReleaseLocalOverrideCommerceCatalogSelectionSourceChangedV1OutboxPayload } from './release-local-override-commerce-catalog-selection-source-changed-v1.outbox-message.ts';
// </generated-outbox-message-exports>
