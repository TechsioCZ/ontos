// @generated by OntOS Codesmith Action v1
// @ontos-action-owner commerce.catalog
// @ontos-action-slug retire-configuration-unit
import type { ActionHandlerContext, ActionTransactionError } from '@app/core-runtime';
import { defineAction, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect, Schema } from 'effect';

import {
  RetireConfigurationUnitPayloadSchema,
  RetireConfigurationUnitResultSchema,
} from '../../shared/actions/retire-configuration-unit.ts';
import type { RetireConfigurationUnitPayload } from '../../shared/actions/retire-configuration-unit.ts';
import {
  ConfigurationUnitActionError,
  ConfigurationUnitAuditEvidenceSchema,
} from '../../shared/actions/configuration-unit-contract.ts';
import type { ConfigurationUnitPersistence } from '../persistence/configuration-unit-persistence.ts';
import { captureCatalogActionResult } from '../persistence/catalog-action-result-snapshot.ts';
import {
  configurationUnitPersistenceServiceFactory,
  mapConfigurationUnitCaptureError,
  mapConfigurationUnitPersistenceError,
  resolveConfigurationUnitMutation,
} from './configuration-unit-action-support.ts';

const ACTION_KEY = 'commerce.catalog.retire-configuration-unit';

type RetireConfigurationUnitServices = ConfigurationUnitPersistence & {
  readonly captureResult: (
    actionInvocationId: string,
    result: typeof RetireConfigurationUnitResultSchema.Type,
  ) => Effect.Effect<void, ActionTransactionError>;
};

const handleRetireConfigurationUnit = Effect.fn('RetireConfigurationUnitAction.handle')(function* handle(
  payload: RetireConfigurationUnitPayload,
  context: ActionHandlerContext<Readonly<Record<string, never>>, ConfigurationUnitPersistence>,
) {
  if (payload.unitRef.tenantId !== context.scope.tenantId) {
    return yield* new ConfigurationUnitActionError({
      code: 'configuration_unit_invalid',
      reason: 'Unit must belong to the trusted Tenant',
    });
  }
  const outcome = yield* context.services
    .retire({ actionInvocationId: context.actionInvocationId, payload, principalId: context.scope.principalId })
    .pipe(Effect.mapError(mapConfigurationUnitPersistenceError));
  const result = yield* resolveConfigurationUnitMutation(outcome);
  yield* context.recordAuditEvidence({ evidenceRefs: payload.evidenceRefs, reason: payload.reason });
  return result;
});

export const retireConfigurationUnitAction = defineAction(
  {
    accessEvidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.retire-configuration-unit.access.v1',
    },
    actionKey: ACTION_KEY,
    auditEvidenceSchema: ConfigurationUnitAuditEvidenceSchema,
    auditProfile: 'standard',
    domainErrorSchema: ConfigurationUnitActionError,
    domainEvents: {},
    entrypoint: defineTenantModuleEntrypoint({
      access: 'write',
      authorization: { kind: 'action_execution', provisioning: 'explicit' },
      entrypointKey: ACTION_KEY,
      moduleKey: 'commerce.catalog',
      role: 'action',
    }),
    idempotency: 'required',
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    payloadSchema: RetireConfigurationUnitPayloadSchema,
    policies: [],
    resultSchema: RetireConfigurationUnitResultSchema,
    schemaVersion: '1',
  },
  handleRetireConfigurationUnit,
  (transaction, scope) =>
    configurationUnitPersistenceServiceFactory(transaction, scope).pipe(
      Effect.map((services): RetireConfigurationUnitServices => ({
        ...services,
        captureResult: (actionInvocationId: string, result: typeof RetireConfigurationUnitResultSchema.Type) =>
          captureCatalogActionResult(
            transaction,
            scope,
            { actionInvocationId, actionKey: ACTION_KEY, schemaVersion: 1 },
            {
              decode: Schema.decodeUnknownEffect(RetireConfigurationUnitResultSchema),
              encode: Schema.encodeEffect(RetireConfigurationUnitResultSchema),
            },
            result,
          ).pipe(Effect.mapError(mapConfigurationUnitCaptureError)),
      })),
    ),
  ({ actionInvocationId, result, services }) => services.captureResult(actionInvocationId, result),
);

// <generated-outbox-message-exports>
// </generated-outbox-message-exports>
