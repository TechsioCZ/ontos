// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug add-product-category-assignment
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  AddProductCategoryAssignmentActionApi,
  AddProductCategoryAssignmentPayloadSchema,
} from '../../shared/apis/add-product-category-assignment-action.ts';
import type { AddProductCategoryAssignmentPayload } from '../../shared/actions/add-product-category-assignment.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface AddProductCategoryAssignmentActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: AddProductCategoryAssignmentActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: AddProductCategoryAssignmentActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: AddProductCategoryAssignmentActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: AddProductCategoryAssignmentActionApi, credential, defaultApiPrefix: '/catalog-api', requestCorrelation },
    options,
  );

export const executeAddProductCategoryAssignmentWithAuthorization = (
  payload: AddProductCategoryAssignmentPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(AddProductCategoryAssignmentPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.addProductCategoryAssignmentAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeAddProductCategoryAssignment = (
  payload: AddProductCategoryAssignmentPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeAddProductCategoryAssignmentWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
