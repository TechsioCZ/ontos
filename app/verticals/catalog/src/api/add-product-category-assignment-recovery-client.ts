// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AddProductCategoryAssignmentRecoveryApi } from '../../shared/apis/add-product-category-assignment-recovery.ts';
import type { AddProductCategoryAssignmentRecoveryRequest } from '../../shared/apis/add-product-category-assignment-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface AddProductCategoryAssignmentRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type AddProductCategoryAssignmentRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AddProductCategoryAssignmentRecoveryClientOptions,
];

type AddProductCategoryAssignmentRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AddProductCategoryAssignmentRecoveryClientOptions,
];

const addProductCategoryAssignmentRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AddProductCategoryAssignmentRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AddProductCategoryAssignmentRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeAddProductCategoryAssignmentRecoveryWithAuthorization = (
  payload: AddProductCategoryAssignmentRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: AddProductCategoryAssignmentRecoveryAuthorizedInvocation
) =>
  addProductCategoryAssignmentRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.addProductCategoryAssignmentRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeAddProductCategoryAssignmentRecovery = (
  payload: AddProductCategoryAssignmentRecoveryRequest,
  ...[requestCorrelation, options = {}]: AddProductCategoryAssignmentRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAddProductCategoryAssignmentRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
