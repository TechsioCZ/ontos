// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  AddProductCategoryAssignmentRecoveryRequestSchema,
  AddProductCategoryAssignmentRecoveryResponseSchema,
} from '../../shared/apis/add-product-category-assignment-recovery.ts';
import type { AddProductCategoryAssignmentRecoveryRequest } from '../../shared/apis/add-product-category-assignment-recovery.ts';
import { AddProductCategoryAssignmentResultSchema } from '../../shared/actions/add-product-category-assignment.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed add product category assignment result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original add product category assignment outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    {
      actionInvocationId: invocationId,
      actionKey: 'commerce.catalog.add-product-category-assignment',
      schemaVersion: 1,
    },
    {
      decode: Schema.decodeUnknownEffect(AddProductCategoryAssignmentResultSchema),
      encode: Schema.encodeEffect(AddProductCategoryAssignmentResultSchema),
    },
  );

const recoverAddProductCategoryAssignment = Effect.fn('AddProductCategoryAssignmentRecoveryRead.recover')(
  function* recover(input: AddProductCategoryAssignmentRecoveryRequest, context: ReadHandlerContext<RecoveryService>) {
    const recovery = yield* context.services.recover(input.invocationId);
    return yield* Match.value(recovery).pipe(
      Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
      Match.discriminator('status')('absent', () => Effect.fail(notFound())),
      Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
      Match.discriminator('status')('open', () => Effect.fail(unavailable())),
      Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
      Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
      Match.exhaustive,
    );
  },
);

const addProductCategoryAssignmentRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'commerce.catalog.read.add-product-category-assignment-recovery',
  },
  entrypointKey: 'commerce.catalog.api.add-product-category-assignment-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const addProductCategoryAssignmentRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: addProductCategoryAssignmentRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.add-product-category-assignment-recovery.evidence.v1',
    },
    inputSchema: AddProductCategoryAssignmentRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.add-product-category-assignment-recovery',
    resultSchema: AddProductCategoryAssignmentRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverAddProductCategoryAssignment(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
