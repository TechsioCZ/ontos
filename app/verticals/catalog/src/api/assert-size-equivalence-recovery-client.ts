// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AssertSizeEquivalenceRecoveryApi } from '../../shared/apis/assert-size-equivalence-recovery.ts';
import type { AssertSizeEquivalenceRecoveryRequest } from '../../shared/apis/assert-size-equivalence-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface AssertSizeEquivalenceRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type AssertSizeEquivalenceRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AssertSizeEquivalenceRecoveryClientOptions,
];

type AssertSizeEquivalenceRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AssertSizeEquivalenceRecoveryClientOptions,
];

const assertSizeEquivalenceRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AssertSizeEquivalenceRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AssertSizeEquivalenceRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeAssertSizeEquivalenceRecoveryWithAuthorization = (
  payload: AssertSizeEquivalenceRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: AssertSizeEquivalenceRecoveryAuthorizedInvocation
) =>
  assertSizeEquivalenceRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.assertSizeEquivalenceRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeAssertSizeEquivalenceRecovery = (
  payload: AssertSizeEquivalenceRecoveryRequest,
  ...[requestCorrelation, options = {}]: AssertSizeEquivalenceRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAssertSizeEquivalenceRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
