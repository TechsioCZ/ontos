// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AssignCatalogMediaRecoveryApi } from '../../shared/apis/assign-catalog-media-recovery.ts';
import type { AssignCatalogMediaRecoveryRequest } from '../../shared/apis/assign-catalog-media-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface AssignCatalogMediaRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type AssignCatalogMediaRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AssignCatalogMediaRecoveryClientOptions,
];

type AssignCatalogMediaRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AssignCatalogMediaRecoveryClientOptions,
];

const assignCatalogMediaRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AssignCatalogMediaRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AssignCatalogMediaRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeAssignCatalogMediaRecoveryWithAuthorization = (
  payload: AssignCatalogMediaRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: AssignCatalogMediaRecoveryAuthorizedInvocation
) =>
  assignCatalogMediaRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.assignCatalogMediaRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeAssignCatalogMediaRecovery = (
  payload: AssignCatalogMediaRecoveryRequest,
  ...[requestCorrelation, options = {}]: AssignCatalogMediaRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAssignCatalogMediaRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
