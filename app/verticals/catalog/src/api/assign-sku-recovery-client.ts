// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { AssignSkuRecoveryApi } from '../../shared/apis/assign-sku-recovery.ts';
import type { AssignSkuRecoveryRequest } from '../../shared/apis/assign-sku-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface AssignSkuRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type AssignSkuRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AssignSkuRecoveryClientOptions,
];

type AssignSkuRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: AssignSkuRecoveryClientOptions,
];

const assignSkuRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: AssignSkuRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: AssignSkuRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeAssignSkuRecoveryWithAuthorization = (
  payload: AssignSkuRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: AssignSkuRecoveryAuthorizedInvocation
) =>
  assignSkuRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.assignSkuRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeAssignSkuRecovery = (
  payload: AssignSkuRecoveryRequest,
  ...[requestCorrelation, options = {}]: AssignSkuRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAssignSkuRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
