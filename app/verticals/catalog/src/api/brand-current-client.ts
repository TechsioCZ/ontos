// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { BrandCurrentApi } from '../../shared/apis/brand-current.ts';
import type { BrandCurrentRequest } from '../../shared/apis/brand-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface BrandCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type BrandCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: BrandCurrentClientOptions,
];

type BrandCurrentOperationInvocation = readonly [requestCorrelation: string, options?: BrandCurrentClientOptions];

const brandCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: BrandCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: BrandCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeBrandCurrentWithAuthorization = (
  payload: BrandCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: BrandCurrentAuthorizedInvocation
) =>
  brandCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.brandCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeBrandCurrent = (
  payload: BrandCurrentRequest,
  ...[requestCorrelation, options = {}]: BrandCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeBrandCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
