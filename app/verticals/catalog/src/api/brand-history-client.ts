// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { BrandHistoryApi } from '../../shared/apis/brand-history.ts';
import type { BrandHistoryRequest } from '../../shared/apis/brand-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface BrandHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type BrandHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: BrandHistoryClientOptions,
];

type BrandHistoryOperationInvocation = readonly [requestCorrelation: string, options?: BrandHistoryClientOptions];

const brandHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: BrandHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: BrandHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeBrandHistoryWithAuthorization = (
  payload: BrandHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: BrandHistoryAuthorizedInvocation
) =>
  brandHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.brandHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeBrandHistory = (
  payload: BrandHistoryRequest,
  ...[requestCorrelation, options = {}]: BrandHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeBrandHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
