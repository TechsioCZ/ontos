// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CatalogDocumentCurrentApi } from '../../shared/apis/catalog-document-current.ts';
import type { CatalogDocumentCurrentRequest } from '../../shared/apis/catalog-document-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface CatalogDocumentCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type CatalogDocumentCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CatalogDocumentCurrentClientOptions,
];

type CatalogDocumentCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CatalogDocumentCurrentClientOptions,
];

const catalogDocumentCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CatalogDocumentCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CatalogDocumentCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCatalogDocumentCurrentWithAuthorization = (
  payload: CatalogDocumentCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: CatalogDocumentCurrentAuthorizedInvocation
) =>
  catalogDocumentCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.catalogDocumentCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCatalogDocumentCurrent = (
  payload: CatalogDocumentCurrentRequest,
  ...[requestCorrelation, options = {}]: CatalogDocumentCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCatalogDocumentCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
