// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CatalogMediaCurrentApi } from '../../shared/apis/catalog-media-current.ts';
import type { CatalogMediaCurrentRequest } from '../../shared/apis/catalog-media-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface CatalogMediaCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type CatalogMediaCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CatalogMediaCurrentClientOptions,
];

type CatalogMediaCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CatalogMediaCurrentClientOptions,
];

const catalogMediaCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CatalogMediaCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CatalogMediaCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCatalogMediaCurrentWithAuthorization = (
  payload: CatalogMediaCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: CatalogMediaCurrentAuthorizedInvocation
) =>
  catalogMediaCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.catalogMediaCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCatalogMediaCurrent = (
  payload: CatalogMediaCurrentRequest,
  ...[requestCorrelation, options = {}]: CatalogMediaCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCatalogMediaCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
