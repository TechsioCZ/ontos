// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CatalogSourceResolutionApi } from '../../shared/apis/catalog-source-resolution.ts';
import type { CatalogSourceResolutionRequest } from '../../shared/apis/catalog-source-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface CatalogSourceResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type CatalogSourceResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CatalogSourceResolutionClientOptions,
];

type CatalogSourceResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CatalogSourceResolutionClientOptions,
];

const catalogSourceResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CatalogSourceResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CatalogSourceResolutionApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCatalogSourceResolutionWithAuthorization = (
  payload: CatalogSourceResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: CatalogSourceResolutionAuthorizedInvocation
) =>
  catalogSourceResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.catalogSourceResolution.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCatalogSourceResolution = (
  payload: CatalogSourceResolutionRequest,
  ...[requestCorrelation, options = {}]: CatalogSourceResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCatalogSourceResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
