// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ChangeVariantRecoveryApi } from '../../shared/apis/change-variant-recovery.ts';
import type { ChangeVariantRecoveryRequest } from '../../shared/apis/change-variant-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ChangeVariantRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ChangeVariantRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ChangeVariantRecoveryClientOptions,
];

type ChangeVariantRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ChangeVariantRecoveryClientOptions,
];

const changeVariantRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ChangeVariantRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ChangeVariantRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeChangeVariantRecoveryWithAuthorization = (
  payload: ChangeVariantRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ChangeVariantRecoveryAuthorizedInvocation
) =>
  changeVariantRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.changeVariantRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeChangeVariantRecovery = (
  payload: ChangeVariantRecoveryRequest,
  ...[requestCorrelation, options = {}]: ChangeVariantRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeChangeVariantRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
