// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ColorCurrentApi } from '../../shared/apis/color-current.ts';
import type { ColorCurrentRequest } from '../../shared/apis/color-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface ColorCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type ColorCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ColorCurrentClientOptions,
];

type ColorCurrentOperationInvocation = readonly [requestCorrelation: string, options?: ColorCurrentClientOptions];

const colorCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ColorCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ColorCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeColorCurrentWithAuthorization = (
  payload: ColorCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: ColorCurrentAuthorizedInvocation
) =>
  colorCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.colorCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeColorCurrent = (
  payload: ColorCurrentRequest,
  ...[requestCorrelation, options = {}]: ColorCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeColorCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
