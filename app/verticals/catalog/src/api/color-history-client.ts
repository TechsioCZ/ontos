// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ColorHistoryApi } from '../../shared/apis/color-history.ts';
import type { ColorHistoryRequest } from '../../shared/apis/color-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface ColorHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type ColorHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ColorHistoryClientOptions,
];

type ColorHistoryOperationInvocation = readonly [requestCorrelation: string, options?: ColorHistoryClientOptions];

const colorHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ColorHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ColorHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeColorHistoryWithAuthorization = (
  payload: ColorHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: ColorHistoryAuthorizedInvocation
) =>
  colorHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.colorHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeColorHistory = (
  payload: ColorHistoryRequest,
  ...[requestCorrelation, options = {}]: ColorHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeColorHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
