// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ConfirmGtinRecoveryApi } from '../../shared/apis/confirm-gtin-recovery.ts';
import type { ConfirmGtinRecoveryRequest } from '../../shared/apis/confirm-gtin-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ConfirmGtinRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ConfirmGtinRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ConfirmGtinRecoveryClientOptions,
];

type ConfirmGtinRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ConfirmGtinRecoveryClientOptions,
];

const confirmGtinRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ConfirmGtinRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ConfirmGtinRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeConfirmGtinRecoveryWithAuthorization = (
  payload: ConfirmGtinRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ConfirmGtinRecoveryAuthorizedInvocation
) =>
  confirmGtinRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.confirmGtinRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeConfirmGtinRecovery = (
  payload: ConfirmGtinRecoveryRequest,
  ...[requestCorrelation, options = {}]: ConfirmGtinRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeConfirmGtinRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
