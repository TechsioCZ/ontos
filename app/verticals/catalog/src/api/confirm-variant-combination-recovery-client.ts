// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ConfirmVariantCombinationRecoveryApi } from '../../shared/apis/confirm-variant-combination-recovery.ts';
import type { ConfirmVariantCombinationRecoveryRequest } from '../../shared/apis/confirm-variant-combination-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ConfirmVariantCombinationRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ConfirmVariantCombinationRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ConfirmVariantCombinationRecoveryClientOptions,
];

type ConfirmVariantCombinationRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ConfirmVariantCombinationRecoveryClientOptions,
];

const confirmVariantCombinationRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ConfirmVariantCombinationRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ConfirmVariantCombinationRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeConfirmVariantCombinationRecoveryWithAuthorization = (
  payload: ConfirmVariantCombinationRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ConfirmVariantCombinationRecoveryAuthorizedInvocation
) =>
  confirmVariantCombinationRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.confirmVariantCombinationRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeConfirmVariantCombinationRecovery = (
  payload: ConfirmVariantCombinationRecoveryRequest,
  ...[requestCorrelation, options = {}]: ConfirmVariantCombinationRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeConfirmVariantCombinationRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
