// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CorrectProductRecoveryApi } from '../../shared/apis/correct-product-recovery.ts';
import type { CorrectProductRecoveryRequest } from '../../shared/apis/correct-product-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface CorrectProductRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type CorrectProductRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CorrectProductRecoveryClientOptions,
];

type CorrectProductRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CorrectProductRecoveryClientOptions,
];

const correctProductRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CorrectProductRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CorrectProductRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCorrectProductRecoveryWithAuthorization = (
  payload: CorrectProductRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: CorrectProductRecoveryAuthorizedInvocation
) =>
  correctProductRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.correctProductRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCorrectProductRecovery = (
  payload: CorrectProductRecoveryRequest,
  ...[requestCorrelation, options = {}]: CorrectProductRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCorrectProductRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
