// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CorrectSkuRecoveryApi } from '../../shared/apis/correct-sku-recovery.ts';
import type { CorrectSkuRecoveryRequest } from '../../shared/apis/correct-sku-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface CorrectSkuRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type CorrectSkuRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CorrectSkuRecoveryClientOptions,
];

type CorrectSkuRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CorrectSkuRecoveryClientOptions,
];

const correctSkuRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CorrectSkuRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CorrectSkuRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCorrectSkuRecoveryWithAuthorization = (
  payload: CorrectSkuRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: CorrectSkuRecoveryAuthorizedInvocation
) =>
  correctSkuRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.correctSkuRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCorrectSkuRecovery = (
  payload: CorrectSkuRecoveryRequest,
  ...[requestCorrelation, options = {}]: CorrectSkuRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCorrectSkuRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
