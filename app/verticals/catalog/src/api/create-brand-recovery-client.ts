// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CreateBrandRecoveryApi } from '../../shared/apis/create-brand-recovery.ts';
import type { CreateBrandRecoveryRequest } from '../../shared/apis/create-brand-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateBrandRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type CreateBrandRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CreateBrandRecoveryClientOptions,
];

type CreateBrandRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CreateBrandRecoveryClientOptions,
];

const brandRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CreateBrandRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CreateBrandRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCreateBrandRecoveryWithAuthorization = (
  payload: CreateBrandRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: CreateBrandRecoveryAuthorizedInvocation
) =>
  brandRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.createBrandRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCreateBrandRecovery = (
  payload: CreateBrandRecoveryRequest,
  ...[requestCorrelation, options = {}]: CreateBrandRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCreateBrandRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
