// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug create-product-relationship
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  CreateProductRelationshipActionApi,
  CreateProductRelationshipPayloadSchema,
} from '../../shared/apis/create-product-relationship-action.ts';
import type { CreateProductRelationshipPayload } from '../../shared/actions/create-product-relationship.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface CreateProductRelationshipActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: CreateProductRelationshipActionClientOptions,
];
type OperationInvocation = readonly [requestCorrelation: string, options: CreateProductRelationshipActionClientOptions];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: CreateProductRelationshipActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: CreateProductRelationshipActionApi, credential, defaultApiPrefix: '/catalog-api', requestCorrelation },
    options,
  );

export const executeCreateProductRelationshipWithAuthorization = (
  payload: CreateProductRelationshipPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(CreateProductRelationshipPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.createProductRelationshipAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeCreateProductRelationship = (
  payload: CreateProductRelationshipPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) => executeCreateProductRelationshipWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
