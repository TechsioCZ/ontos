// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CreateVariantRecoveryApi } from '../../shared/apis/create-variant-recovery.ts';
import type { CreateVariantRecoveryRequest } from '../../shared/apis/create-variant-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateVariantRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type CreateVariantRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CreateVariantRecoveryClientOptions,
];

type CreateVariantRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CreateVariantRecoveryClientOptions,
];

const variantRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CreateVariantRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CreateVariantRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCreateVariantRecoveryWithAuthorization = (
  payload: CreateVariantRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: CreateVariantRecoveryAuthorizedInvocation
) =>
  variantRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.createVariantRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCreateVariantRecovery = (
  payload: CreateVariantRecoveryRequest,
  ...[requestCorrelation, options = {}]: CreateVariantRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCreateVariantRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
