// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug decide-product-type-unnecessary
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  DecideProductTypeUnnecessaryActionApi,
  DecideProductTypeUnnecessaryPayloadSchema,
} from '../../shared/apis/decide-product-type-unnecessary-action.ts';
import type { DecideProductTypeUnnecessaryPayload } from '../../shared/actions/decide-product-type-unnecessary.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface DecideProductTypeUnnecessaryActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: DecideProductTypeUnnecessaryActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: DecideProductTypeUnnecessaryActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: DecideProductTypeUnnecessaryActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: DecideProductTypeUnnecessaryActionApi, credential, defaultApiPrefix: '/catalog-api', requestCorrelation },
    options,
  );

export const executeDecideProductTypeUnnecessaryWithAuthorization = (
  payload: DecideProductTypeUnnecessaryPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(DecideProductTypeUnnecessaryPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.decideProductTypeUnnecessaryAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeDecideProductTypeUnnecessary = (
  payload: DecideProductTypeUnnecessaryPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeDecideProductTypeUnnecessaryWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
