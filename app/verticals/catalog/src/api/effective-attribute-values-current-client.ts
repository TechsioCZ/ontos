// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { EffectiveAttributeValuesCurrentApi } from '../../shared/apis/effective-attribute-values-current.ts';
import type { EffectiveAttributeValuesCurrentRequest } from '../../shared/apis/effective-attribute-values-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface EffectiveAttributeValuesCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type EffectiveAttributeValuesCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: EffectiveAttributeValuesCurrentClientOptions,
];

type EffectiveAttributeValuesCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: EffectiveAttributeValuesCurrentClientOptions,
];

const effectiveAttributeValuesCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: EffectiveAttributeValuesCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: EffectiveAttributeValuesCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeEffectiveAttributeValuesCurrentWithAuthorization = (
  payload: EffectiveAttributeValuesCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: EffectiveAttributeValuesCurrentAuthorizedInvocation
) =>
  effectiveAttributeValuesCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.effectiveAttributeValuesCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeEffectiveAttributeValuesCurrent = (
  payload: EffectiveAttributeValuesCurrentRequest,
  ...[requestCorrelation, options = {}]: EffectiveAttributeValuesCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeEffectiveAttributeValuesCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
