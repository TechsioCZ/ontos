// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ExternalTargetResolutionApi } from '../../shared/apis/external-target-resolution.ts';
import type { ExternalTargetResolutionRequest } from '../../shared/apis/external-target-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface ExternalTargetResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type ExternalTargetResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ExternalTargetResolutionClientOptions,
];

type ExternalTargetResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ExternalTargetResolutionClientOptions,
];

const externalTargetResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ExternalTargetResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ExternalTargetResolutionApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeExternalTargetResolutionWithAuthorization = (
  payload: ExternalTargetResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: ExternalTargetResolutionAuthorizedInvocation
) =>
  externalTargetResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.externalTargetResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeExternalTargetResolution = (
  payload: ExternalTargetResolutionRequest,
  ...[requestCorrelation, options = {}]: ExternalTargetResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeExternalTargetResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
