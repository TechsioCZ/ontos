// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug govern-product-attribute-applicability
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  GovernProductAttributeApplicabilityActionApi,
  GovernProductAttributeApplicabilityPayloadSchema,
} from '../../shared/apis/govern-product-attribute-applicability-action.ts';
import type { GovernProductAttributeApplicabilityPayload } from '../../shared/actions/govern-product-attribute-applicability.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface GovernProductAttributeApplicabilityActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: GovernProductAttributeApplicabilityActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: GovernProductAttributeApplicabilityActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: GovernProductAttributeApplicabilityActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: GovernProductAttributeApplicabilityActionApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeGovernProductAttributeApplicabilityWithAuthorization = (
  payload: GovernProductAttributeApplicabilityPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(GovernProductAttributeApplicabilityPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.governProductAttributeApplicabilityAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeGovernProductAttributeApplicability = (
  payload: GovernProductAttributeApplicabilityPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeGovernProductAttributeApplicabilityWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
