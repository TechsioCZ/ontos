// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { GovernProductAttributeApplicabilityRecoveryApi } from '../../shared/apis/govern-product-attribute-applicability-recovery.ts';
import type { GovernProductAttributeApplicabilityRecoveryRequest } from '../../shared/apis/govern-product-attribute-applicability-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface GovernProductAttributeApplicabilityRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type GovernProductAttributeApplicabilityRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GovernProductAttributeApplicabilityRecoveryClientOptions,
];

type GovernProductAttributeApplicabilityRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: GovernProductAttributeApplicabilityRecoveryClientOptions,
];

const governProductAttributeApplicabilityRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: GovernProductAttributeApplicabilityRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GovernProductAttributeApplicabilityRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeGovernProductAttributeApplicabilityRecoveryWithAuthorization = (
  payload: GovernProductAttributeApplicabilityRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: GovernProductAttributeApplicabilityRecoveryAuthorizedInvocation
) =>
  governProductAttributeApplicabilityRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.governProductAttributeApplicabilityRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeGovernProductAttributeApplicabilityRecovery = (
  payload: GovernProductAttributeApplicabilityRecoveryRequest,
  ...[requestCorrelation, options = {}]: GovernProductAttributeApplicabilityRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGovernProductAttributeApplicabilityRecoveryWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
