// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { GovernVariantAxesRecoveryApi } from '../../shared/apis/govern-variant-axes-recovery.ts';
import type { GovernVariantAxesRecoveryRequest } from '../../shared/apis/govern-variant-axes-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface GovernVariantAxesRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type GovernVariantAxesRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GovernVariantAxesRecoveryClientOptions,
];

type GovernVariantAxesRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: GovernVariantAxesRecoveryClientOptions,
];

const governVariantAxesRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: GovernVariantAxesRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GovernVariantAxesRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeGovernVariantAxesRecoveryWithAuthorization = (
  payload: GovernVariantAxesRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: GovernVariantAxesRecoveryAuthorizedInvocation
) =>
  governVariantAxesRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.governVariantAxesRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeGovernVariantAxesRecovery = (
  payload: GovernVariantAxesRecoveryRequest,
  ...[requestCorrelation, options = {}]: GovernVariantAxesRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGovernVariantAxesRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
