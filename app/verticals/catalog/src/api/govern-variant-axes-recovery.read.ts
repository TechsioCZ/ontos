// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  GovernVariantAxesRecoveryRequestSchema,
  GovernVariantAxesRecoveryResponseSchema,
} from '../../shared/apis/govern-variant-axes-recovery.ts';
import type { GovernVariantAxesRecoveryRequest } from '../../shared/apis/govern-variant-axes-recovery.ts';
import { GovernVariantAxesResultSchema } from '../../shared/actions/govern-variant-axes.ts';
import { recoverCatalogActionResult, recoverCatalogActionResultVersions } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed govern variant axes result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original govern variant axes outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) => {
  const codec = {
    decode: Schema.decodeUnknownEffect(GovernVariantAxesResultSchema),
    encode: Schema.encodeEffect(GovernVariantAxesResultSchema),
  };
  return recoverCatalogActionResultVersions([1], () =>
    recoverCatalogActionResult(
      transaction,
      scope,
      {
        actionInvocationId: invocationId,
        actionKey: 'commerce.catalog.govern-variant-axes',
        schemaVersion: 1,
      },
      codec,
    ),
  );
};

const recoverGovernVariantAxes = Effect.fn('GovernVariantAxesRecoveryRead.recover')(function* recover(
  input: GovernVariantAxesRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const governVariantAxesRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'commerce.catalog.read.govern-variant-axes-recovery' },
  entrypointKey: 'commerce.catalog.api.govern-variant-axes-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const governVariantAxesRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: governVariantAxesRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.govern-variant-axes-recovery.evidence.v1',
    },
    inputSchema: GovernVariantAxesRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.govern-variant-axes-recovery',
    resultSchema: GovernVariantAxesRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverGovernVariantAxes(input, context).pipe(Effect.map((result) => ({ evidence: { resultCount: 1 }, result }))),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
