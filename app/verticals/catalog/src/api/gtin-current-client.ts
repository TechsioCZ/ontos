// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { GtinCurrentApi } from '../../shared/apis/gtin-current.ts';
import type { GtinCurrentRequest } from '../../shared/apis/gtin-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface GtinCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type GtinCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GtinCurrentClientOptions,
];

type GtinCurrentOperationInvocation = readonly [requestCorrelation: string, options?: GtinCurrentClientOptions];

const gtinCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: GtinCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GtinCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeGtinCurrentWithAuthorization = (
  payload: GtinCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: GtinCurrentAuthorizedInvocation
) =>
  gtinCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.gtinCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeGtinCurrent = (
  payload: GtinCurrentRequest,
  ...[requestCorrelation, options = {}]: GtinCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGtinCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
