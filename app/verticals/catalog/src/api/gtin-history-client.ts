// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { GtinHistoryApi } from '../../shared/apis/gtin-history.ts';
import type { GtinHistoryRequest } from '../../shared/apis/gtin-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface GtinHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type GtinHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GtinHistoryClientOptions,
];

type GtinHistoryOperationInvocation = readonly [requestCorrelation: string, options?: GtinHistoryClientOptions];

const gtinHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: GtinHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GtinHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeGtinHistoryWithAuthorization = (
  payload: GtinHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: GtinHistoryAuthorizedInvocation
) =>
  gtinHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.gtinHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeGtinHistory = (
  payload: GtinHistoryRequest,
  ...[requestCorrelation, options = {}]: GtinHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGtinHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
