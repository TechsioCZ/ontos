// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ManufacturerRelationCurrentApi } from '../../shared/apis/manufacturer-relation-current.ts';
import type { ManufacturerRelationCurrentRequest } from '../../shared/apis/manufacturer-relation-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface ManufacturerRelationCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type ManufacturerRelationCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ManufacturerRelationCurrentClientOptions,
];

type ManufacturerRelationCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ManufacturerRelationCurrentClientOptions,
];

const manufacturerRelationCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ManufacturerRelationCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ManufacturerRelationCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeManufacturerRelationCurrentWithAuthorization = (
  payload: ManufacturerRelationCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: ManufacturerRelationCurrentAuthorizedInvocation
) =>
  manufacturerRelationCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.manufacturerRelationCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeManufacturerRelationCurrent = (
  payload: ManufacturerRelationCurrentRequest,
  ...[requestCorrelation, options = {}]: ManufacturerRelationCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeManufacturerRelationCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
