// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { MarkGtinUnresolvedRecoveryApi } from '../../shared/apis/mark-gtin-unresolved-recovery.ts';
import type { MarkGtinUnresolvedRecoveryRequest } from '../../shared/apis/mark-gtin-unresolved-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface MarkGtinUnresolvedRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type MarkGtinUnresolvedRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: MarkGtinUnresolvedRecoveryClientOptions,
];

type MarkGtinUnresolvedRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: MarkGtinUnresolvedRecoveryClientOptions,
];

const markGtinUnresolvedRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: MarkGtinUnresolvedRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: MarkGtinUnresolvedRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeMarkGtinUnresolvedRecoveryWithAuthorization = (
  payload: MarkGtinUnresolvedRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: MarkGtinUnresolvedRecoveryAuthorizedInvocation
) =>
  markGtinUnresolvedRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.markGtinUnresolvedRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeMarkGtinUnresolvedRecovery = (
  payload: MarkGtinUnresolvedRecoveryRequest,
  ...[requestCorrelation, options = {}]: MarkGtinUnresolvedRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeMarkGtinUnresolvedRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
