// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { MoveProductCategoryRecoveryApi } from '../../shared/apis/move-product-category-recovery.ts';
import type { MoveProductCategoryRecoveryRequest } from '../../shared/apis/move-product-category-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface MoveProductCategoryRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type MoveProductCategoryRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: MoveProductCategoryRecoveryClientOptions,
];

type MoveProductCategoryRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: MoveProductCategoryRecoveryClientOptions,
];

const moveProductCategoryRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: MoveProductCategoryRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: MoveProductCategoryRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeMoveProductCategoryRecoveryWithAuthorization = (
  payload: MoveProductCategoryRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: MoveProductCategoryRecoveryAuthorizedInvocation
) =>
  moveProductCategoryRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.moveProductCategoryRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeMoveProductCategoryRecovery = (
  payload: MoveProductCategoryRecoveryRequest,
  ...[requestCorrelation, options = {}]: MoveProductCategoryRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeMoveProductCategoryRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
