// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PackageDefinitionHistoryApi } from '../../shared/apis/package-definition-history.ts';
import type { PackageDefinitionHistoryRequest } from '../../shared/apis/package-definition-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface PackageDefinitionHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type PackageDefinitionHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PackageDefinitionHistoryClientOptions,
];

type PackageDefinitionHistoryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PackageDefinitionHistoryClientOptions,
];

const packageDefinitionHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PackageDefinitionHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PackageDefinitionHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executePackageDefinitionHistoryWithAuthorization = (
  payload: PackageDefinitionHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: PackageDefinitionHistoryAuthorizedInvocation
) =>
  packageDefinitionHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.packageDefinitionHistory.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePackageDefinitionHistory = (
  payload: PackageDefinitionHistoryRequest,
  ...[requestCorrelation, options = {}]: PackageDefinitionHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePackageDefinitionHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
