// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PackageOptionHistoryApi } from '../../shared/apis/package-option-history.ts';
import type { PackageOptionHistoryRequest } from '../../shared/apis/package-option-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface PackageOptionHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type PackageOptionHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PackageOptionHistoryClientOptions,
];

type PackageOptionHistoryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PackageOptionHistoryClientOptions,
];

const packageOptionHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PackageOptionHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PackageOptionHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executePackageOptionHistoryWithAuthorization = (
  payload: PackageOptionHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: PackageOptionHistoryAuthorizedInvocation
) =>
  packageOptionHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.packageOptionHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executePackageOptionHistory = (
  payload: PackageOptionHistoryRequest,
  ...[requestCorrelation, options = {}]: PackageOptionHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePackageOptionHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
