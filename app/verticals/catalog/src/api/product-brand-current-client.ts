// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductBrandCurrentApi } from '../../shared/apis/product-brand-current.ts';
import type { ProductBrandCurrentRequest } from '../../shared/apis/product-brand-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductBrandCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductBrandCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductBrandCurrentClientOptions,
];

type ProductBrandCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductBrandCurrentClientOptions,
];

const productBrandCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductBrandCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductBrandCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductBrandCurrentWithAuthorization = (
  payload: ProductBrandCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: ProductBrandCurrentAuthorizedInvocation
) =>
  productBrandCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productBrandCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductBrandCurrent = (
  payload: ProductBrandCurrentRequest,
  ...[requestCorrelation, options = {}]: ProductBrandCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductBrandCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
