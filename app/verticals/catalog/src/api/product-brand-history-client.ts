// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductBrandHistoryApi } from '../../shared/apis/product-brand-history.ts';
import type { ProductBrandHistoryRequest } from '../../shared/apis/product-brand-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductBrandHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductBrandHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductBrandHistoryClientOptions,
];

type ProductBrandHistoryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductBrandHistoryClientOptions,
];

const productBrandHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductBrandHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductBrandHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductBrandHistoryWithAuthorization = (
  payload: ProductBrandHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: ProductBrandHistoryAuthorizedInvocation
) =>
  productBrandHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productBrandHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductBrandHistory = (
  payload: ProductBrandHistoryRequest,
  ...[requestCorrelation, options = {}]: ProductBrandHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductBrandHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
