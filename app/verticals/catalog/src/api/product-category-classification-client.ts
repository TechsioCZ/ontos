// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductCategoryClassificationApi } from '../../shared/apis/product-category-classification.ts';
import type { ProductCategoryClassificationRequest } from '../../shared/apis/product-category-classification.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductCategoryClassificationClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductCategoryClassificationAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductCategoryClassificationClientOptions,
];

type ProductCategoryClassificationOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductCategoryClassificationClientOptions,
];

const productCategoryClassificationClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductCategoryClassificationClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductCategoryClassificationApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductCategoryClassificationWithAuthorization = (
  payload: ProductCategoryClassificationRequest,
  ...[credential, requestCorrelation, options = {}]: ProductCategoryClassificationAuthorizedInvocation
) =>
  productCategoryClassificationClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.productCategoryClassification.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeProductCategoryClassification = (
  payload: ProductCategoryClassificationRequest,
  ...[requestCorrelation, options = {}]: ProductCategoryClassificationOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductCategoryClassificationWithAuthorization(payload, credential, requestCorrelation, options),
  );
