// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductCategoryHistoryApi } from '../../shared/apis/product-category-history.ts';
import type { ProductCategoryHistoryRequest } from '../../shared/apis/product-category-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductCategoryHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductCategoryHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductCategoryHistoryClientOptions,
];

type ProductCategoryHistoryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductCategoryHistoryClientOptions,
];

const productCategoryHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductCategoryHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductCategoryHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductCategoryHistoryWithAuthorization = (
  payload: ProductCategoryHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: ProductCategoryHistoryAuthorizedInvocation
) =>
  productCategoryHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productCategoryHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductCategoryHistory = (
  payload: ProductCategoryHistoryRequest,
  ...[requestCorrelation, options = {}]: ProductCategoryHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductCategoryHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
