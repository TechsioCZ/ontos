// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductDetailApi } from '../../shared/apis/product-detail.ts';
import type { ProductDetailRequest } from '../../shared/apis/product-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductDetailClientOptions,
];

type ProductDetailOperationInvocation = readonly [requestCorrelation: string, options?: ProductDetailClientOptions];

const productDetailClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductDetailClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductDetailApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductDetailWithAuthorization = (
  payload: ProductDetailRequest,
  ...[credential, requestCorrelation, options = {}]: ProductDetailAuthorizedInvocation
) =>
  productDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productDetail.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductDetail = (
  payload: ProductDetailRequest,
  ...[requestCorrelation, options = {}]: ProductDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductDetailWithAuthorization(payload, credential, requestCorrelation, options),
  );
