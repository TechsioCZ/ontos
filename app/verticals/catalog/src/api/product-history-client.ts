// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductHistoryApi } from '../../shared/apis/product-history.ts';
import type { ProductHistoryRequest } from '../../shared/apis/product-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductHistoryClientOptions,
];

type ProductHistoryOperationInvocation = readonly [requestCorrelation: string, options?: ProductHistoryClientOptions];

const productHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductHistoryWithAuthorization = (
  payload: ProductHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: ProductHistoryAuthorizedInvocation
) =>
  productHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductHistory = (
  payload: ProductHistoryRequest,
  ...[requestCorrelation, options = {}]: ProductHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
