// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductRelationshipCurrentApi } from '../../shared/apis/product-relationship-current.ts';
import type { ProductRelationshipCurrentRequest } from '../../shared/apis/product-relationship-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductRelationshipCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductRelationshipCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductRelationshipCurrentClientOptions,
];

type ProductRelationshipCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductRelationshipCurrentClientOptions,
];

const productRelationshipCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductRelationshipCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductRelationshipCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductRelationshipCurrentWithAuthorization = (
  payload: ProductRelationshipCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: ProductRelationshipCurrentAuthorizedInvocation
) =>
  productRelationshipCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.productRelationshipCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeProductRelationshipCurrent = (
  payload: ProductRelationshipCurrentRequest,
  ...[requestCorrelation, options = {}]: ProductRelationshipCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductRelationshipCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
