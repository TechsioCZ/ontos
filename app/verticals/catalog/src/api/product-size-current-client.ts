// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ProductSizeCurrentApi } from '../../shared/apis/product-size-current.ts';
import type { ProductSizeCurrentRequest } from '../../shared/apis/product-size-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface ProductSizeCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type ProductSizeCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ProductSizeCurrentClientOptions,
];

type ProductSizeCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ProductSizeCurrentClientOptions,
];

const productSizeCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ProductSizeCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ProductSizeCurrentApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeProductSizeCurrentWithAuthorization = (
  payload: ProductSizeCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: ProductSizeCurrentAuthorizedInvocation
) =>
  productSizeCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.productSizeCurrent.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeProductSizeCurrent = (
  payload: ProductSizeCurrentRequest,
  ...[requestCorrelation, options = {}]: ProductSizeCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeProductSizeCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
