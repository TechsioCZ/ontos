// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { QuantityPreparationApi } from '../../shared/apis/quantity-preparation.ts';
import type { QuantityPreparationRequest } from '../../shared/apis/quantity-preparation.ts';
import { operationGateway } from './action-gateway.ts';

export interface QuantityPreparationClientOptions {
  readonly baseUrl?: string | URL;
}

type QuantityPreparationAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: QuantityPreparationClientOptions,
];

type QuantityPreparationOperationInvocation = readonly [
  requestCorrelation: string,
  options?: QuantityPreparationClientOptions,
];

const quantityPreparationClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: QuantityPreparationClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: QuantityPreparationApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeQuantityPreparationWithAuthorization = (
  payload: QuantityPreparationRequest,
  ...[credential, requestCorrelation, options = {}]: QuantityPreparationAuthorizedInvocation
) =>
  quantityPreparationClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.quantityPreparation.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeQuantityPreparation = (
  payload: QuantityPreparationRequest,
  ...[requestCorrelation, options = {}]: QuantityPreparationOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeQuantityPreparationWithAuthorization(payload, credential, requestCorrelation, options),
  );
