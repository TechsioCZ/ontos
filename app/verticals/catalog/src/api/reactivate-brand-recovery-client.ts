// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ReactivateBrandRecoveryApi } from '../../shared/apis/reactivate-brand-recovery.ts';
import type { ReactivateBrandRecoveryRequest } from '../../shared/apis/reactivate-brand-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ReactivateBrandRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ReactivateBrandRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ReactivateBrandRecoveryClientOptions,
];

type ReactivateBrandRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ReactivateBrandRecoveryClientOptions,
];

const reactivateBrandRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ReactivateBrandRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ReactivateBrandRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeReactivateBrandRecoveryWithAuthorization = (
  payload: ReactivateBrandRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ReactivateBrandRecoveryAuthorizedInvocation
) =>
  reactivateBrandRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.reactivateBrandRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeReactivateBrandRecovery = (
  payload: ReactivateBrandRecoveryRequest,
  ...[requestCorrelation, options = {}]: ReactivateBrandRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeReactivateBrandRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
