// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug reactivate-controlled-attribute-value
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  ReactivateControlledAttributeValueActionApi,
  ReactivateControlledAttributeValuePayloadSchema,
} from '../../shared/apis/reactivate-controlled-attribute-value-action.ts';
import type { ReactivateControlledAttributeValuePayload } from '../../shared/actions/reactivate-controlled-attribute-value.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ReactivateControlledAttributeValueActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ReactivateControlledAttributeValueActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: ReactivateControlledAttributeValueActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ReactivateControlledAttributeValueActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ReactivateControlledAttributeValueActionApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeReactivateControlledAttributeValueWithAuthorization = (
  payload: ReactivateControlledAttributeValuePayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ReactivateControlledAttributeValuePayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.reactivateControlledAttributeValueAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeReactivateControlledAttributeValue = (
  payload: ReactivateControlledAttributeValuePayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeReactivateControlledAttributeValueWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
