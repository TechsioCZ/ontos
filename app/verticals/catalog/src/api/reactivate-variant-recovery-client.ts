// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ReactivateVariantRecoveryApi } from '../../shared/apis/reactivate-variant-recovery.ts';
import type { ReactivateVariantRecoveryRequest } from '../../shared/apis/reactivate-variant-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ReactivateVariantRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ReactivateVariantRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ReactivateVariantRecoveryClientOptions,
];

type ReactivateVariantRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ReactivateVariantRecoveryClientOptions,
];

const reactivateVariantRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ReactivateVariantRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ReactivateVariantRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeReactivateVariantRecoveryWithAuthorization = (
  payload: ReactivateVariantRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ReactivateVariantRecoveryAuthorizedInvocation
) =>
  reactivateVariantRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.reactivateVariantRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeReactivateVariantRecovery = (
  payload: ReactivateVariantRecoveryRequest,
  ...[requestCorrelation, options = {}]: ReactivateVariantRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeReactivateVariantRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
