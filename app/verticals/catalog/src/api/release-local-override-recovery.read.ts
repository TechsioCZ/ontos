// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';

import { ReleaseLocalOverrideResultSchema } from '../../shared/actions/release-local-override.ts';
import {
  ReleaseLocalOverrideRecoveryRequestSchema,
  ReleaseLocalOverrideRecoveryResponseSchema,
} from '../../shared/apis/release-local-override-recovery.ts';
import type { ReleaseLocalOverrideRecoveryRequest } from '../../shared/apis/release-local-override-recovery.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed release local override result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original release local override outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    { actionInvocationId: invocationId, actionKey: 'commerce.catalog.release-local-override', schemaVersion: 1 },
    {
      decode: Schema.decodeUnknownEffect(ReleaseLocalOverrideResultSchema),
      encode: Schema.encodeEffect(ReleaseLocalOverrideResultSchema),
    },
  );

const recoverReleaseLocalOverride = Effect.fn('ReleaseLocalOverrideRecoveryRead.recover')(function* recover(
  input: ReleaseLocalOverrideRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const releaseLocalOverrideRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'commerce.catalog.read.release-local-override-recovery' },
  entrypointKey: 'commerce.catalog.api.release-local-override-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const releaseLocalOverrideRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: releaseLocalOverrideRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.release-local-override-recovery.evidence.v1',
    },
    inputSchema: ReleaseLocalOverrideRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.release-local-override-recovery',
    resultSchema: ReleaseLocalOverrideRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverReleaseLocalOverride(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
