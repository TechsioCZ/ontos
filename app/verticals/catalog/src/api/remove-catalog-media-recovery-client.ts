// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RemoveCatalogMediaRecoveryApi } from '../../shared/apis/remove-catalog-media-recovery.ts';
import type { RemoveCatalogMediaRecoveryRequest } from '../../shared/apis/remove-catalog-media-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RemoveCatalogMediaRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RemoveCatalogMediaRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RemoveCatalogMediaRecoveryClientOptions,
];

type RemoveCatalogMediaRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RemoveCatalogMediaRecoveryClientOptions,
];

const removeCatalogMediaRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RemoveCatalogMediaRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RemoveCatalogMediaRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRemoveCatalogMediaRecoveryWithAuthorization = (
  payload: RemoveCatalogMediaRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RemoveCatalogMediaRecoveryAuthorizedInvocation
) =>
  removeCatalogMediaRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.removeCatalogMediaRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRemoveCatalogMediaRecovery = (
  payload: RemoveCatalogMediaRecoveryRequest,
  ...[requestCorrelation, options = {}]: RemoveCatalogMediaRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRemoveCatalogMediaRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
