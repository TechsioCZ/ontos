// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RemoveProductAttributeValuesRecoveryApi } from '../../shared/apis/remove-product-attribute-values-recovery.ts';
import type { RemoveProductAttributeValuesRecoveryRequest } from '../../shared/apis/remove-product-attribute-values-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RemoveProductAttributeValuesRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RemoveProductAttributeValuesRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RemoveProductAttributeValuesRecoveryClientOptions,
];

type RemoveProductAttributeValuesRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RemoveProductAttributeValuesRecoveryClientOptions,
];

const removeProductAttributeValuesRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RemoveProductAttributeValuesRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RemoveProductAttributeValuesRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRemoveProductAttributeValuesRecoveryWithAuthorization = (
  payload: RemoveProductAttributeValuesRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RemoveProductAttributeValuesRecoveryAuthorizedInvocation
) =>
  removeProductAttributeValuesRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.removeProductAttributeValuesRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRemoveProductAttributeValuesRecovery = (
  payload: RemoveProductAttributeValuesRecoveryRequest,
  ...[requestCorrelation, options = {}]: RemoveProductAttributeValuesRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRemoveProductAttributeValuesRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
