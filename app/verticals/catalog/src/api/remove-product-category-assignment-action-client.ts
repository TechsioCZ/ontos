// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug remove-product-category-assignment
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  RemoveProductCategoryAssignmentActionApi,
  RemoveProductCategoryAssignmentPayloadSchema,
} from '../../shared/apis/remove-product-category-assignment-action.ts';
import type { RemoveProductCategoryAssignmentPayload } from '../../shared/actions/remove-product-category-assignment.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface RemoveProductCategoryAssignmentActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: RemoveProductCategoryAssignmentActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: RemoveProductCategoryAssignmentActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: RemoveProductCategoryAssignmentActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: RemoveProductCategoryAssignmentActionApi, credential, defaultApiPrefix: '/catalog-api', requestCorrelation },
    options,
  );

export const executeRemoveProductCategoryAssignmentWithAuthorization = (
  payload: RemoveProductCategoryAssignmentPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(RemoveProductCategoryAssignmentPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.removeProductCategoryAssignmentAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeRemoveProductCategoryAssignment = (
  payload: RemoveProductCategoryAssignmentPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeRemoveProductCategoryAssignmentWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
