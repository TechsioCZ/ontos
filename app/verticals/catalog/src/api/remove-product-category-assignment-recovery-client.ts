// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RemoveProductCategoryAssignmentRecoveryApi } from '../../shared/apis/remove-product-category-assignment-recovery.ts';
import type { RemoveProductCategoryAssignmentRecoveryRequest } from '../../shared/apis/remove-product-category-assignment-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RemoveProductCategoryAssignmentRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RemoveProductCategoryAssignmentRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RemoveProductCategoryAssignmentRecoveryClientOptions,
];

type RemoveProductCategoryAssignmentRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RemoveProductCategoryAssignmentRecoveryClientOptions,
];

const removeProductCategoryAssignmentRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RemoveProductCategoryAssignmentRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RemoveProductCategoryAssignmentRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRemoveProductCategoryAssignmentRecoveryWithAuthorization = (
  payload: RemoveProductCategoryAssignmentRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RemoveProductCategoryAssignmentRecoveryAuthorizedInvocation
) =>
  removeProductCategoryAssignmentRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.removeProductCategoryAssignmentRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRemoveProductCategoryAssignmentRecovery = (
  payload: RemoveProductCategoryAssignmentRecoveryRequest,
  ...[requestCorrelation, options = {}]: RemoveProductCategoryAssignmentRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRemoveProductCategoryAssignmentRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
