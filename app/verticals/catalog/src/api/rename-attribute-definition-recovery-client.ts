// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RenameAttributeDefinitionRecoveryApi } from '../../shared/apis/rename-attribute-definition-recovery.ts';
import type { RenameAttributeDefinitionRecoveryRequest } from '../../shared/apis/rename-attribute-definition-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RenameAttributeDefinitionRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RenameAttributeDefinitionRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RenameAttributeDefinitionRecoveryClientOptions,
];

type RenameAttributeDefinitionRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RenameAttributeDefinitionRecoveryClientOptions,
];

const renameAttributeDefinitionRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RenameAttributeDefinitionRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RenameAttributeDefinitionRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRenameAttributeDefinitionRecoveryWithAuthorization = (
  payload: RenameAttributeDefinitionRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RenameAttributeDefinitionRecoveryAuthorizedInvocation
) =>
  renameAttributeDefinitionRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.renameAttributeDefinitionRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRenameAttributeDefinitionRecovery = (
  payload: RenameAttributeDefinitionRecoveryRequest,
  ...[requestCorrelation, options = {}]: RenameAttributeDefinitionRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRenameAttributeDefinitionRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
