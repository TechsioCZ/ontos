// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  RenameAttributeDefinitionRecoveryRequestSchema,
  RenameAttributeDefinitionRecoveryResponseSchema,
} from '../../shared/apis/rename-attribute-definition-recovery.ts';
import type { RenameAttributeDefinitionRecoveryRequest } from '../../shared/apis/rename-attribute-definition-recovery.ts';
import { RenameAttributeDefinitionResultSchema } from '../../shared/actions/rename-attribute-definition.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed original Action result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original Action outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    { actionInvocationId: invocationId, actionKey: 'commerce.catalog.rename-attribute-definition', schemaVersion: 1 },
    {
      decode: Schema.decodeUnknownEffect(RenameAttributeDefinitionResultSchema),
      encode: Schema.encodeEffect(RenameAttributeDefinitionResultSchema),
    },
  );

const recoverRenameAttributeDefinition = Effect.fn('RenameAttributeDefinitionRecoveryRead.recover')(function* recover(
  input: RenameAttributeDefinitionRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const renameAttributeDefinitionRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'commerce.catalog.read.rename-attribute-definition-recovery',
  },
  entrypointKey: 'commerce.catalog.api.rename-attribute-definition-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const renameAttributeDefinitionRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: renameAttributeDefinitionRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.rename-attribute-definition-recovery.evidence.v1',
    },
    inputSchema: RenameAttributeDefinitionRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.rename-attribute-definition-recovery',
    resultSchema: RenameAttributeDefinitionRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverRenameAttributeDefinition(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
