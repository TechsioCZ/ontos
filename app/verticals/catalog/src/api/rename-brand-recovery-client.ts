// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RenameBrandRecoveryApi } from '../../shared/apis/rename-brand-recovery.ts';
import type { RenameBrandRecoveryRequest } from '../../shared/apis/rename-brand-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RenameBrandRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RenameBrandRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RenameBrandRecoveryClientOptions,
];

type RenameBrandRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RenameBrandRecoveryClientOptions,
];

const renameBrandRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RenameBrandRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RenameBrandRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRenameBrandRecoveryWithAuthorization = (
  payload: RenameBrandRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RenameBrandRecoveryAuthorizedInvocation
) =>
  renameBrandRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.renameBrandRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRenameBrandRecovery = (
  payload: RenameBrandRecoveryRequest,
  ...[requestCorrelation, options = {}]: RenameBrandRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRenameBrandRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
