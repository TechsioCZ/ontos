// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.catalog
// @ontos-action-http-slug rename-controlled-attribute-value
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  RenameControlledAttributeValueActionApi,
  RenameControlledAttributeValuePayloadSchema,
} from '../../shared/apis/rename-controlled-attribute-value-action.ts';
import type { RenameControlledAttributeValuePayload } from '../../shared/actions/rename-controlled-attribute-value.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface RenameControlledAttributeValueActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: RenameControlledAttributeValueActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: RenameControlledAttributeValueActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: RenameControlledAttributeValueActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: RenameControlledAttributeValueActionApi, credential, defaultApiPrefix: '/catalog-api', requestCorrelation },
    options,
  );

export const executeRenameControlledAttributeValueWithAuthorization = (
  payload: RenameControlledAttributeValuePayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(RenameControlledAttributeValuePayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.renameControlledAttributeValueAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeRenameControlledAttributeValue = (
  payload: RenameControlledAttributeValuePayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeRenameControlledAttributeValueWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
