// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RenameProductCategoryRecoveryApi } from '../../shared/apis/rename-product-category-recovery.ts';
import type { RenameProductCategoryRecoveryRequest } from '../../shared/apis/rename-product-category-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RenameProductCategoryRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RenameProductCategoryRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RenameProductCategoryRecoveryClientOptions,
];

type RenameProductCategoryRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RenameProductCategoryRecoveryClientOptions,
];

const renameProductCategoryRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RenameProductCategoryRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RenameProductCategoryRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRenameProductCategoryRecoveryWithAuthorization = (
  payload: RenameProductCategoryRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RenameProductCategoryRecoveryAuthorizedInvocation
) =>
  renameProductCategoryRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.renameProductCategoryRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRenameProductCategoryRecovery = (
  payload: RenameProductCategoryRecoveryRequest,
  ...[requestCorrelation, options = {}]: RenameProductCategoryRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRenameProductCategoryRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
