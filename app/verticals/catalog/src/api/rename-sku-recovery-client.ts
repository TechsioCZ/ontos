// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RenameSkuRecoveryApi } from '../../shared/apis/rename-sku-recovery.ts';
import type { RenameSkuRecoveryRequest } from '../../shared/apis/rename-sku-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RenameSkuRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RenameSkuRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RenameSkuRecoveryClientOptions,
];

type RenameSkuRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RenameSkuRecoveryClientOptions,
];

const renameSkuRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RenameSkuRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RenameSkuRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRenameSkuRecoveryWithAuthorization = (
  payload: RenameSkuRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RenameSkuRecoveryAuthorizedInvocation
) =>
  renameSkuRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.renameSkuRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRenameSkuRecovery = (
  payload: RenameSkuRecoveryRequest,
  ...[requestCorrelation, options = {}]: RenameSkuRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRenameSkuRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
