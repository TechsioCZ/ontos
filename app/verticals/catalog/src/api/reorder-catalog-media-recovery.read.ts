// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  ReorderCatalogMediaRecoveryRequestSchema,
  ReorderCatalogMediaRecoveryResponseSchema,
} from '../../shared/apis/reorder-catalog-media-recovery.ts';
import type { ReorderCatalogMediaRecoveryRequest } from '../../shared/apis/reorder-catalog-media-recovery.ts';
import { ReorderCatalogMediaResultSchema } from '../../shared/actions/reorder-catalog-media.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed original Action result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original Action outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    { actionInvocationId: invocationId, actionKey: 'commerce.catalog.reorder-catalog-media', schemaVersion: 1 },
    {
      decode: Schema.decodeUnknownEffect(ReorderCatalogMediaResultSchema),
      encode: Schema.encodeEffect(ReorderCatalogMediaResultSchema),
    },
  );

const recoverReorderCatalogMedia = Effect.fn('ReorderCatalogMediaRecoveryRead.recover')(function* recover(
  input: ReorderCatalogMediaRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const reorderCatalogMediaRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'commerce.catalog.read.reorder-catalog-media-recovery' },
  entrypointKey: 'commerce.catalog.api.reorder-catalog-media-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const reorderCatalogMediaRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: reorderCatalogMediaRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.reorder-catalog-media-recovery.evidence.v1',
    },
    inputSchema: ReorderCatalogMediaRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.reorder-catalog-media-recovery',
    resultSchema: ReorderCatalogMediaRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverReorderCatalogMedia(input, context).pipe(Effect.map((result) => ({ evidence: { resultCount: 1 }, result }))),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
