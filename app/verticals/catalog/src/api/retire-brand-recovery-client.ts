// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireBrandRecoveryApi } from '../../shared/apis/retire-brand-recovery.ts';
import type { RetireBrandRecoveryRequest } from '../../shared/apis/retire-brand-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireBrandRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireBrandRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireBrandRecoveryClientOptions,
];

type RetireBrandRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireBrandRecoveryClientOptions,
];

const retireBrandRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireBrandRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireBrandRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireBrandRecoveryWithAuthorization = (
  payload: RetireBrandRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireBrandRecoveryAuthorizedInvocation
) =>
  retireBrandRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.retireBrandRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRetireBrandRecovery = (
  payload: RetireBrandRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireBrandRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireBrandRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
