// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireGtinRecoveryApi } from '../../shared/apis/retire-gtin-recovery.ts';
import type { RetireGtinRecoveryRequest } from '../../shared/apis/retire-gtin-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireGtinRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireGtinRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireGtinRecoveryClientOptions,
];

type RetireGtinRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireGtinRecoveryClientOptions,
];

const retireGtinRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireGtinRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireGtinRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireGtinRecoveryWithAuthorization = (
  payload: RetireGtinRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireGtinRecoveryAuthorizedInvocation
) =>
  retireGtinRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.retireGtinRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRetireGtinRecovery = (
  payload: RetireGtinRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireGtinRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireGtinRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
