// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetirePackageDefinitionRecoveryApi } from '../../shared/apis/retire-package-definition-recovery.ts';
import type { RetirePackageDefinitionRecoveryRequest } from '../../shared/apis/retire-package-definition-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetirePackageDefinitionRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetirePackageDefinitionRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetirePackageDefinitionRecoveryClientOptions,
];

type RetirePackageDefinitionRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetirePackageDefinitionRecoveryClientOptions,
];

const retirePackageDefinitionRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetirePackageDefinitionRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetirePackageDefinitionRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetirePackageDefinitionRecoveryWithAuthorization = (
  payload: RetirePackageDefinitionRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetirePackageDefinitionRecoveryAuthorizedInvocation
) =>
  retirePackageDefinitionRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retirePackageDefinitionRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetirePackageDefinitionRecovery = (
  payload: RetirePackageDefinitionRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetirePackageDefinitionRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetirePackageDefinitionRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
