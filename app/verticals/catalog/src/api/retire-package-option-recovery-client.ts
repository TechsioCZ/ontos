// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetirePackageOptionRecoveryApi } from '../../shared/apis/retire-package-option-recovery.ts';
import type { RetirePackageOptionRecoveryRequest } from '../../shared/apis/retire-package-option-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetirePackageOptionRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetirePackageOptionRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetirePackageOptionRecoveryClientOptions,
];

type RetirePackageOptionRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetirePackageOptionRecoveryClientOptions,
];

const retirePackageOptionRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetirePackageOptionRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetirePackageOptionRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetirePackageOptionRecoveryWithAuthorization = (
  payload: RetirePackageOptionRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetirePackageOptionRecoveryAuthorizedInvocation
) =>
  retirePackageOptionRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retirePackageOptionRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetirePackageOptionRecovery = (
  payload: RetirePackageOptionRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetirePackageOptionRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetirePackageOptionRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
