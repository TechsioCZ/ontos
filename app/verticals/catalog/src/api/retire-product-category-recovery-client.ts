// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireProductCategoryRecoveryApi } from '../../shared/apis/retire-product-category-recovery.ts';
import type { RetireProductCategoryRecoveryRequest } from '../../shared/apis/retire-product-category-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireProductCategoryRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireProductCategoryRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireProductCategoryRecoveryClientOptions,
];

type RetireProductCategoryRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireProductCategoryRecoveryClientOptions,
];

const retireProductCategoryRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireProductCategoryRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireProductCategoryRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireProductCategoryRecoveryWithAuthorization = (
  payload: RetireProductCategoryRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireProductCategoryRecoveryAuthorizedInvocation
) =>
  retireProductCategoryRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retireProductCategoryRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetireProductCategoryRecovery = (
  payload: RetireProductCategoryRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireProductCategoryRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireProductCategoryRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
