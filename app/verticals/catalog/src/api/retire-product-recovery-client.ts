// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireProductRecoveryApi } from '../../shared/apis/retire-product-recovery.ts';
import type { RetireProductRecoveryRequest } from '../../shared/apis/retire-product-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireProductRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireProductRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireProductRecoveryClientOptions,
];

type RetireProductRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireProductRecoveryClientOptions,
];

const retireProductRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireProductRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireProductRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireProductRecoveryWithAuthorization = (
  payload: RetireProductRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireProductRecoveryAuthorizedInvocation
) =>
  retireProductRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.retireProductRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRetireProductRecovery = (
  payload: RetireProductRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireProductRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireProductRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
