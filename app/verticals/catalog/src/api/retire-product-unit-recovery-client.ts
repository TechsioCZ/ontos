// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireProductUnitRecoveryApi } from '../../shared/apis/retire-product-unit-recovery.ts';
import type { RetireProductUnitRecoveryRequest } from '../../shared/apis/retire-product-unit-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireProductUnitRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireProductUnitRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireProductUnitRecoveryClientOptions,
];

type RetireProductUnitRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireProductUnitRecoveryClientOptions,
];

const retireProductUnitRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireProductUnitRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireProductUnitRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireProductUnitRecoveryWithAuthorization = (
  payload: RetireProductUnitRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireProductUnitRecoveryAuthorizedInvocation
) =>
  retireProductUnitRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retireProductUnitRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetireProductUnitRecovery = (
  payload: RetireProductUnitRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireProductUnitRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireProductUnitRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
