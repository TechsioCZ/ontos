// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetireVariantRecoveryApi } from '../../shared/apis/retire-variant-recovery.ts';
import type { RetireVariantRecoveryRequest } from '../../shared/apis/retire-variant-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetireVariantRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RetireVariantRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetireVariantRecoveryClientOptions,
];

type RetireVariantRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetireVariantRecoveryClientOptions,
];

const retireVariantRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RetireVariantRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetireVariantRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetireVariantRecoveryWithAuthorization = (
  payload: RetireVariantRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RetireVariantRecoveryAuthorizedInvocation
) =>
  retireVariantRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.retireVariantRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeRetireVariantRecovery = (
  payload: RetireVariantRecoveryRequest,
  ...[requestCorrelation, options = {}]: RetireVariantRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetireVariantRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
