// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  ReviseConfigurationUnitRecoveryRequestSchema,
  ReviseConfigurationUnitRecoveryResponseSchema,
} from '../../shared/apis/revise-configuration-unit-recovery.ts';
import type { ReviseConfigurationUnitRecoveryRequest } from '../../shared/apis/revise-configuration-unit-recovery.ts';
import { ReviseConfigurationUnitResultSchema } from '../../shared/actions/revise-configuration-unit.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed revise configuration unit result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original revise configuration unit outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    { actionInvocationId: invocationId, actionKey: 'commerce.catalog.revise-configuration-unit', schemaVersion: 1 },
    {
      decode: Schema.decodeUnknownEffect(ReviseConfigurationUnitResultSchema),
      encode: Schema.encodeEffect(ReviseConfigurationUnitResultSchema),
    },
  );

const recoverReviseConfigurationUnit = Effect.fn('ReviseConfigurationUnitRecoveryRead.recover')(function* recover(
  input: ReviseConfigurationUnitRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const reviseConfigurationUnitRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'commerce.catalog.read.revise-configuration-unit-recovery' },
  entrypointKey: 'commerce.catalog.api.revise-configuration-unit-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const reviseConfigurationUnitRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: reviseConfigurationUnitRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.revise-configuration-unit-recovery.evidence.v1',
    },
    inputSchema: ReviseConfigurationUnitRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.revise-configuration-unit-recovery',
    resultSchema: ReviseConfigurationUnitRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverReviseConfigurationUnit(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
