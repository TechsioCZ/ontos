// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RevisePackageDefinitionRecoveryApi } from '../../shared/apis/revise-package-definition-recovery.ts';
import type { RevisePackageDefinitionRecoveryRequest } from '../../shared/apis/revise-package-definition-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface RevisePackageDefinitionRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type RevisePackageDefinitionRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RevisePackageDefinitionRecoveryClientOptions,
];

type RevisePackageDefinitionRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RevisePackageDefinitionRecoveryClientOptions,
];

const revisePackageDefinitionRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: RevisePackageDefinitionRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RevisePackageDefinitionRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeRevisePackageDefinitionRecoveryWithAuthorization = (
  payload: RevisePackageDefinitionRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: RevisePackageDefinitionRecoveryAuthorizedInvocation
) =>
  revisePackageDefinitionRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.revisePackageDefinitionRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRevisePackageDefinitionRecovery = (
  payload: RevisePackageDefinitionRecoveryRequest,
  ...[requestCorrelation, options = {}]: RevisePackageDefinitionRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRevisePackageDefinitionRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
