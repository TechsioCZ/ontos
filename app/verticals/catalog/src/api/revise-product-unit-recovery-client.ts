// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ReviseProductUnitRecoveryApi } from '../../shared/apis/revise-product-unit-recovery.ts';
import type { ReviseProductUnitRecoveryRequest } from '../../shared/apis/revise-product-unit-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface ReviseProductUnitRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type ReviseProductUnitRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ReviseProductUnitRecoveryClientOptions,
];

type ReviseProductUnitRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ReviseProductUnitRecoveryClientOptions,
];

const reviseProductUnitRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ReviseProductUnitRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ReviseProductUnitRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeReviseProductUnitRecoveryWithAuthorization = (
  payload: ReviseProductUnitRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: ReviseProductUnitRecoveryAuthorizedInvocation
) =>
  reviseProductUnitRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.reviseProductUnitRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeReviseProductUnitRecovery = (
  payload: ReviseProductUnitRecoveryRequest,
  ...[requestCorrelation, options = {}]: ReviseProductUnitRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeReviseProductUnitRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
