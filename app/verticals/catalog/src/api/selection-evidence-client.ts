// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SelectionEvidenceApi } from '../../shared/apis/selection-evidence.ts';
import type { SelectionEvidenceRequest } from '../../shared/apis/selection-evidence.ts';
import { operationGateway } from './action-gateway.ts';

export interface SelectionEvidenceClientOptions {
  readonly baseUrl?: string | URL;
}

type SelectionEvidenceAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SelectionEvidenceClientOptions,
];

type SelectionEvidenceOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SelectionEvidenceClientOptions,
];

const selectionEvidenceClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: SelectionEvidenceClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SelectionEvidenceApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeSelectionEvidenceWithAuthorization = (
  payload: SelectionEvidenceRequest,
  ...[credential, requestCorrelation, options = {}]: SelectionEvidenceAuthorizedInvocation
) =>
  selectionEvidenceClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.selectionEvidence.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeSelectionEvidence = (
  payload: SelectionEvidenceRequest,
  ...[requestCorrelation, options = {}]: SelectionEvidenceOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSelectionEvidenceWithAuthorization(payload, credential, requestCorrelation, options),
  );
