// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SetProductAttributeValuesRecoveryApi } from '../../shared/apis/set-product-attribute-values-recovery.ts';
import type { SetProductAttributeValuesRecoveryRequest } from '../../shared/apis/set-product-attribute-values-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface SetProductAttributeValuesRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type SetProductAttributeValuesRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SetProductAttributeValuesRecoveryClientOptions,
];

type SetProductAttributeValuesRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SetProductAttributeValuesRecoveryClientOptions,
];

const setProductAttributeValuesRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: SetProductAttributeValuesRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SetProductAttributeValuesRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeSetProductAttributeValuesRecoveryWithAuthorization = (
  payload: SetProductAttributeValuesRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: SetProductAttributeValuesRecoveryAuthorizedInvocation
) =>
  setProductAttributeValuesRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.setProductAttributeValuesRecovery.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeSetProductAttributeValuesRecovery = (
  payload: SetProductAttributeValuesRecoveryRequest,
  ...[requestCorrelation, options = {}]: SetProductAttributeValuesRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSetProductAttributeValuesRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
