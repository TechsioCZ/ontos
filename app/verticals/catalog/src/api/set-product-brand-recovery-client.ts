// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SetProductBrandRecoveryApi } from '../../shared/apis/set-product-brand-recovery.ts';
import type { SetProductBrandRecoveryRequest } from '../../shared/apis/set-product-brand-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface SetProductBrandRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type SetProductBrandRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SetProductBrandRecoveryClientOptions,
];

type SetProductBrandRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SetProductBrandRecoveryClientOptions,
];

const setProductBrandRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: SetProductBrandRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SetProductBrandRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeSetProductBrandRecoveryWithAuthorization = (
  payload: SetProductBrandRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: SetProductBrandRecoveryAuthorizedInvocation
) =>
  setProductBrandRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.setProductBrandRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeSetProductBrandRecovery = (
  payload: SetProductBrandRecoveryRequest,
  ...[requestCorrelation, options = {}]: SetProductBrandRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSetProductBrandRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
