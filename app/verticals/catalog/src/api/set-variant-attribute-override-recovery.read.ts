// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  SetVariantAttributeOverrideRecoveryRequestSchema,
  SetVariantAttributeOverrideRecoveryResponseSchema,
} from '../../shared/apis/set-variant-attribute-override-recovery.ts';
import type { SetVariantAttributeOverrideRecoveryRequest } from '../../shared/apis/set-variant-attribute-override-recovery.ts';
import { SetVariantAttributeOverrideResultSchema } from '../../shared/actions/set-variant-attribute-override.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed set variant attribute override result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original set variant attribute override outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    {
      actionInvocationId: invocationId,
      actionKey: 'commerce.catalog.set-variant-attribute-override',
      schemaVersion: 1,
    },
    {
      decode: Schema.decodeUnknownEffect(SetVariantAttributeOverrideResultSchema),
      encode: Schema.encodeEffect(SetVariantAttributeOverrideResultSchema),
    },
  );

const recoverSetVariantAttributeOverride = Effect.fn('SetVariantAttributeOverrideRecoveryRead.recover')(
  function* recover(input: SetVariantAttributeOverrideRecoveryRequest, context: ReadHandlerContext<RecoveryService>) {
    const recovery = yield* context.services.recover(input.invocationId);
    return yield* Match.value(recovery).pipe(
      Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
      Match.discriminator('status')('absent', () => Effect.fail(notFound())),
      Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
      Match.discriminator('status')('open', () => Effect.fail(unavailable())),
      Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
      Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
      Match.exhaustive,
    );
  },
);

const setVariantAttributeOverrideRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'commerce.catalog.read.set-variant-attribute-override-recovery',
  },
  entrypointKey: 'commerce.catalog.api.set-variant-attribute-override-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const setVariantAttributeOverrideRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: setVariantAttributeOverrideRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.set-variant-attribute-override-recovery.evidence.v1',
    },
    inputSchema: SetVariantAttributeOverrideRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.set-variant-attribute-override-recovery',
    resultSchema: SetVariantAttributeOverrideRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverSetVariantAttributeOverride(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
