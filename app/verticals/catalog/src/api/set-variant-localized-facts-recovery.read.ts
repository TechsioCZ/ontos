// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match, Schema } from 'effect';
import {
  SetVariantLocalizedFactsRecoveryRequestSchema,
  SetVariantLocalizedFactsRecoveryResponseSchema,
} from '../../shared/apis/set-variant-localized-facts-recovery.ts';
import type { SetVariantLocalizedFactsRecoveryRequest } from '../../shared/apis/set-variant-localized-facts-recovery.ts';
import { SetVariantLocalizedFactsResultSchema } from '../../shared/actions/set-variant-localized-facts.ts';
import { recoverCatalogActionResult } from './catalog-action-result-recovery.ts';

interface RecoveryService {
  readonly recover: (invocationId: string) => ReturnType<typeof recoverResult>;
}

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed set variant localized facts result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original set variant localized facts outcome cannot be verified yet',
  });

const recoverResult = (
  transaction: Parameters<typeof recoverCatalogActionResult>[0],
  scope: Parameters<typeof recoverCatalogActionResult>[1],
  invocationId: string,
) =>
  recoverCatalogActionResult(
    transaction,
    scope,
    { actionInvocationId: invocationId, actionKey: 'commerce.catalog.set-variant-localized-facts', schemaVersion: 1 },
    {
      decode: Schema.decodeUnknownEffect(SetVariantLocalizedFactsResultSchema),
      encode: Schema.encodeEffect(SetVariantLocalizedFactsResultSchema),
    },
  );

const recoverSetVariantLocalizedFacts = Effect.fn('SetVariantLocalizedFactsRecoveryRead.recover')(function* recover(
  input: SetVariantLocalizedFactsRecoveryRequest,
  context: ReadHandlerContext<RecoveryService>,
) {
  const recovery = yield* context.services.recover(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const setVariantLocalizedFactsRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'commerce.catalog.read.set-variant-localized-facts-recovery',
  },
  entrypointKey: 'commerce.catalog.api.set-variant-localized-facts-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const setVariantLocalizedFactsRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: setVariantLocalizedFactsRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.set-variant-localized-facts-recovery.evidence.v1',
    },
    inputSchema: SetVariantLocalizedFactsRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.set-variant-localized-facts-recovery',
    resultSchema: SetVariantLocalizedFactsRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<RecoveryService>) =>
    recoverSetVariantLocalizedFacts(input, context).pipe(
      Effect.map((result) => ({ evidence: { resultCount: 1 }, result })),
    ),
  (transaction, scope) =>
    Effect.succeed({ recover: (invocationId: string) => recoverResult(transaction, scope, invocationId) }),
  () => ({ kind: 'tenant', permission: 'access' }),
);
