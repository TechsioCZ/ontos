// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SkuLookupApi } from '../../shared/apis/sku-lookup.ts';
import type { SkuLookupRequest } from '../../shared/apis/sku-lookup.ts';
import { operationGateway } from './action-gateway.ts';

export interface SkuLookupClientOptions {
  readonly baseUrl?: string | URL;
}

type SkuLookupAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SkuLookupClientOptions,
];

type SkuLookupOperationInvocation = readonly [requestCorrelation: string, options?: SkuLookupClientOptions];

const skuLookupClient = (credential: Redacted.Redacted, requestCorrelation: string, options: SkuLookupClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: SkuLookupApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeSkuLookupWithAuthorization = (
  payload: SkuLookupRequest,
  ...[credential, requestCorrelation, options = {}]: SkuLookupAuthorizedInvocation
) =>
  skuLookupClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.skuLookup.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeSkuLookup = (
  payload: SkuLookupRequest,
  ...[requestCorrelation, options = {}]: SkuLookupOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSkuLookupWithAuthorization(payload, credential, requestCorrelation, options),
  );
