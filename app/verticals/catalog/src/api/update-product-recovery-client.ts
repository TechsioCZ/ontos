// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { UpdateProductRecoveryApi } from '../../shared/apis/update-product-recovery.ts';
import type { UpdateProductRecoveryRequest } from '../../shared/apis/update-product-recovery.ts';
import { operationGateway } from './action-gateway.ts';

export interface UpdateProductRecoveryClientOptions {
  readonly baseUrl?: string | URL;
}

type UpdateProductRecoveryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: UpdateProductRecoveryClientOptions,
];

type UpdateProductRecoveryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: UpdateProductRecoveryClientOptions,
];

const updateProductRecoveryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: UpdateProductRecoveryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: UpdateProductRecoveryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeUpdateProductRecoveryWithAuthorization = (
  payload: UpdateProductRecoveryRequest,
  ...[credential, requestCorrelation, options = {}]: UpdateProductRecoveryAuthorizedInvocation
) =>
  updateProductRecoveryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.updateProductRecovery.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeUpdateProductRecovery = (
  payload: UpdateProductRecoveryRequest,
  ...[requestCorrelation, options = {}]: UpdateProductRecoveryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeUpdateProductRecoveryWithAuthorization(payload, credential, requestCorrelation, options),
  );
