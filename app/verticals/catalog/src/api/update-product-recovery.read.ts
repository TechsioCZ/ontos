// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import {
  ReadHandlerNotFound,
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect, Match } from 'effect';
import {
  UpdateProductRecoveryRequestSchema,
  UpdateProductRecoveryResponseSchema,
} from '../../shared/apis/update-product-recovery.ts';
import type { UpdateProductRecoveryRequest } from '../../shared/apis/update-product-recovery.ts';
import type { CatalogPersistence } from '../persistence/catalog-persistence.ts';
import { catalogPersistenceWithCurrentSelectionEvidenceForScope } from '../persistence/catalog-persistence.ts';

const notFound = () =>
  new ReadHandlerNotFound({
    code: 'read_handler_not_found',
    reason: 'No committed update Product result exists in this principal scope',
  });
const unavailable = () =>
  new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'The original update Product outcome cannot be verified yet',
  });

export const recoverUpdateProduct = Effect.fn('UpdateProductRecoveryRead.recover')(function* recoverUpdateProduct(
  input: UpdateProductRecoveryRequest,
  context: ReadHandlerContext<CatalogPersistence>,
) {
  const recovery = yield* context.services.recoverUpdateProduct(input.invocationId);
  return yield* Match.value(recovery).pipe(
    Match.discriminator('status')('committed', ({ result }) => Effect.succeed(result)),
    Match.discriminator('status')('absent', () => Effect.fail(notFound())),
    Match.discriminator('status')('rejected', () => Effect.fail(unavailable())),
    Match.discriminator('status')('open', () => Effect.fail(unavailable())),
    Match.discriminator('status')('indeterminate', () => Effect.fail(unavailable())),
    Match.discriminator('status')('unavailable', () => Effect.fail(unavailable())),
    Match.exhaustive,
  );
});

const updateProductRecoveryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'commerce.catalog.read.update-product-recovery' },
  entrypointKey: 'commerce.catalog.api.update-product-recovery',
  moduleKey: 'commerce.catalog',
  role: 'api',
});

export const updateProductRecoveryRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: updateProductRecoveryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.catalog.api.update-product-recovery.evidence.v1',
    },
    inputSchema: UpdateProductRecoveryRequestSchema,
    legalEntityScope: 'forbidden',
    owningModuleKey: 'commerce.catalog',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'commerce.catalog.api.update-product-recovery',
    resultSchema: UpdateProductRecoveryResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<CatalogPersistence>) =>
    recoverUpdateProduct(input, context).pipe(Effect.map((result) => ({ evidence: { resultCount: 1 }, result }))),
  (transaction, scope) => catalogPersistenceWithCurrentSelectionEvidenceForScope(transaction, scope),
  () => ({ kind: 'tenant', permission: 'access' }),
);
