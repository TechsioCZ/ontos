// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { VariantHistoryApi } from '../../shared/apis/variant-history.ts';
import type { VariantHistoryRequest } from '../../shared/apis/variant-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface VariantHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type VariantHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: VariantHistoryClientOptions,
];

type VariantHistoryOperationInvocation = readonly [requestCorrelation: string, options?: VariantHistoryClientOptions];

const variantHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: VariantHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: VariantHistoryApi,
      credential,
      defaultApiPrefix: '/catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeVariantHistoryWithAuthorization = (
  payload: VariantHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: VariantHistoryAuthorizedInvocation
) =>
  variantHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.variantHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeVariantHistory = (
  payload: VariantHistoryRequest,
  ...[requestCorrelation, options = {}]: VariantHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeVariantHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
