// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug archive-customer-profile
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { archiveCustomerProfileAction } from '../src/actions/archive-customer-profile.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  archiveCustomerProfileActionProblem,
  archiveCustomerProfileActionSchemaErrorLive,
  mapArchiveCustomerProfileActionProblem,
} from './archive-customer-profile-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: archiveCustomerProfileActionProblem.authentication,
  unavailable: () => archiveCustomerProfileActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ArchiveCustomerProfileActionServer.execute')(function* executeArchiveCustomerProfile(
  payload: typeof archiveCustomerProfileAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(archiveCustomerProfileActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(archiveCustomerProfileAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(archiveCustomerProfileActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: archiveCustomerProfileActionProblem.internal,
    invalidCorrelationProblem: archiveCustomerProfileActionProblem.invalid,
    mapError: mapArchiveCustomerProfileActionProblem,
    payload: decoded,
    registration: archiveCustomerProfileAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const archiveCustomerProfileActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'archiveCustomerProfileAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(archiveCustomerProfileActionSchemaErrorLive));
