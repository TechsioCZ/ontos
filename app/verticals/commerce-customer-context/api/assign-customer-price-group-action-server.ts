// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug assign-customer-price-group
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { assignCustomerPriceGroupAction } from '../src/actions/assign-customer-price-group.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  assignCustomerPriceGroupActionProblem,
  assignCustomerPriceGroupActionSchemaErrorLive,
  mapAssignCustomerPriceGroupActionProblem,
} from './assign-customer-price-group-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: assignCustomerPriceGroupActionProblem.authentication,
  unavailable: () =>
    assignCustomerPriceGroupActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('AssignCustomerPriceGroupActionServer.execute')(
  function* executeAssignCustomerPriceGroup(
    payload: typeof assignCustomerPriceGroupAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(assignCustomerPriceGroupActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      assignCustomerPriceGroupAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(assignCustomerPriceGroupActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: assignCustomerPriceGroupActionProblem.internal,
      invalidCorrelationProblem: assignCustomerPriceGroupActionProblem.invalid,
      mapError: mapAssignCustomerPriceGroupActionProblem,
      payload: decoded,
      registration: assignCustomerPriceGroupAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const assignCustomerPriceGroupActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'assignCustomerPriceGroupAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(assignCustomerPriceGroupActionSchemaErrorLive));
