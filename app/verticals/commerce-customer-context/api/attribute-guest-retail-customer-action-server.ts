// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug attribute-guest-retail-customer
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { attributeGuestRetailCustomerAction } from '../src/actions/attribute-guest-retail-customer.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  attributeGuestRetailCustomerActionProblem,
  attributeGuestRetailCustomerActionSchemaErrorLive,
  mapAttributeGuestRetailCustomerActionProblem,
} from './attribute-guest-retail-customer-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: attributeGuestRetailCustomerActionProblem.authentication,
  unavailable: () => attributeGuestRetailCustomerActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('AttributeGuestRetailCustomerActionServer.execute')(
  function* executeAttributeGuestRetailCustomer(
    payload: typeof attributeGuestRetailCustomerAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(attributeGuestRetailCustomerActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(attributeGuestRetailCustomerAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(attributeGuestRetailCustomerActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: attributeGuestRetailCustomerActionProblem.internal,
      invalidCorrelationProblem: attributeGuestRetailCustomerActionProblem.invalid,
      mapError: mapAttributeGuestRetailCustomerActionProblem,
      payload: decoded,
      registration: attributeGuestRetailCustomerAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const attributeGuestRetailCustomerActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'attributeGuestRetailCustomerAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(attributeGuestRetailCustomerActionSchemaErrorLive));
