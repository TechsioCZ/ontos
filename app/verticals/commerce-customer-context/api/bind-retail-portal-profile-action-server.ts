// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug bind-retail-portal-profile
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { bindRetailPortalProfileAction } from '../src/actions/bind-retail-portal-profile.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  bindRetailPortalProfileActionProblem,
  bindRetailPortalProfileActionSchemaErrorLive,
  mapBindRetailPortalProfileActionProblem,
} from './bind-retail-portal-profile-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: bindRetailPortalProfileActionProblem.authentication,
  unavailable: () =>
    bindRetailPortalProfileActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('BindRetailPortalProfileActionServer.execute')(
  function* executeBindRetailPortalProfile(
    payload: typeof bindRetailPortalProfileAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(bindRetailPortalProfileActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      bindRetailPortalProfileAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(bindRetailPortalProfileActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: bindRetailPortalProfileActionProblem.internal,
      invalidCorrelationProblem: bindRetailPortalProfileActionProblem.invalid,
      mapError: mapBindRetailPortalProfileActionProblem,
      payload: decoded,
      registration: bindRetailPortalProfileAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const bindRetailPortalProfileActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'bindRetailPortalProfileAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(bindRetailPortalProfileActionSchemaErrorLive));
