// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug bootstrap-counterparty-access-administrator
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { bootstrapCounterpartyAccessAdministratorAction } from '../src/actions/bootstrap-counterparty-access-administrator.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  bootstrapCounterpartyAccessAdministratorActionProblem,
  bootstrapCounterpartyAccessAdministratorActionSchemaErrorLive,
  mapBootstrapCounterpartyAccessAdministratorActionProblem,
} from './bootstrap-counterparty-access-administrator-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: bootstrapCounterpartyAccessAdministratorActionProblem.authentication,
  unavailable: () => bootstrapCounterpartyAccessAdministratorActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('BootstrapCounterpartyAccessAdministratorActionServer.execute')(
  function* executeBootstrapCounterpartyAccessAdministrator(
    payload: typeof bootstrapCounterpartyAccessAdministratorAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(bootstrapCounterpartyAccessAdministratorActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(bootstrapCounterpartyAccessAdministratorAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(bootstrapCounterpartyAccessAdministratorActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: bootstrapCounterpartyAccessAdministratorActionProblem.internal,
      invalidCorrelationProblem: bootstrapCounterpartyAccessAdministratorActionProblem.invalid,
      mapError: mapBootstrapCounterpartyAccessAdministratorActionProblem,
      payload: decoded,
      registration: bootstrapCounterpartyAccessAdministratorAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const bootstrapCounterpartyAccessAdministratorActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'bootstrapCounterpartyAccessAdministratorAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(bootstrapCounterpartyAccessAdministratorActionSchemaErrorLive));
