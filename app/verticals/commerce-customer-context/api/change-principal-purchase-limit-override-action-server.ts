// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug change-principal-purchase-limit-override
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { changePrincipalPurchaseLimitOverrideAction } from '../src/actions/change-principal-purchase-limit-override.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  changePrincipalPurchaseLimitOverrideActionProblem,
  changePrincipalPurchaseLimitOverrideActionSchemaErrorLive,
  mapChangePrincipalPurchaseLimitOverrideActionProblem,
} from './change-principal-purchase-limit-override-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: changePrincipalPurchaseLimitOverrideActionProblem.authentication,
  unavailable: () =>
    changePrincipalPurchaseLimitOverrideActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ChangePrincipalPurchaseLimitOverrideActionServer.execute')(
  function* executeChangePrincipalPurchaseLimitOverride(
    payload: typeof changePrincipalPurchaseLimitOverrideAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(changePrincipalPurchaseLimitOverrideActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      changePrincipalPurchaseLimitOverrideAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(changePrincipalPurchaseLimitOverrideActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: changePrincipalPurchaseLimitOverrideActionProblem.internal,
      invalidCorrelationProblem: changePrincipalPurchaseLimitOverrideActionProblem.invalid,
      mapError: mapChangePrincipalPurchaseLimitOverrideActionProblem,
      payload: decoded,
      registration: changePrincipalPurchaseLimitOverrideAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const changePrincipalPurchaseLimitOverrideActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'changePrincipalPurchaseLimitOverrideAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(changePrincipalPurchaseLimitOverrideActionSchemaErrorLive));
