// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug claim-counterparty-access-invitation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { claimCounterpartyAccessInvitationAction } from '../src/actions/claim-counterparty-access-invitation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  claimCounterpartyAccessInvitationActionProblem,
  claimCounterpartyAccessInvitationActionSchemaErrorLive,
  mapClaimCounterpartyAccessInvitationActionProblem,
} from './claim-counterparty-access-invitation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: claimCounterpartyAccessInvitationActionProblem.authentication,
  unavailable: () =>
    claimCounterpartyAccessInvitationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ClaimCounterpartyAccessInvitationActionServer.execute')(
  function* executeClaimCounterpartyAccessInvitation(
    payload: typeof claimCounterpartyAccessInvitationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(claimCounterpartyAccessInvitationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      claimCounterpartyAccessInvitationAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(claimCounterpartyAccessInvitationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: claimCounterpartyAccessInvitationActionProblem.internal,
      invalidCorrelationProblem: claimCounterpartyAccessInvitationActionProblem.invalid,
      mapError: mapClaimCounterpartyAccessInvitationActionProblem,
      payload: decoded,
      registration: claimCounterpartyAccessInvitationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const claimCounterpartyAccessInvitationActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'claimCounterpartyAccessInvitationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(claimCounterpartyAccessInvitationActionSchemaErrorLive));
