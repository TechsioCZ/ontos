// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug claim-portal-enrollment-transition
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { claimPortalEnrollmentTransitionAction } from '../src/actions/claim-portal-enrollment-transition.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  claimPortalEnrollmentTransitionActionProblem,
  claimPortalEnrollmentTransitionActionSchemaErrorLive,
  mapClaimPortalEnrollmentTransitionActionProblem,
} from './claim-portal-enrollment-transition-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: claimPortalEnrollmentTransitionActionProblem.authentication,
  unavailable: () => claimPortalEnrollmentTransitionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ClaimPortalEnrollmentTransitionActionServer.execute')(
  function* executeClaimPortalEnrollmentTransition(
    payload: typeof claimPortalEnrollmentTransitionAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(claimPortalEnrollmentTransitionActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(claimPortalEnrollmentTransitionAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(claimPortalEnrollmentTransitionActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: claimPortalEnrollmentTransitionActionProblem.internal,
      invalidCorrelationProblem: claimPortalEnrollmentTransitionActionProblem.invalid,
      mapError: mapClaimPortalEnrollmentTransitionActionProblem,
      payload: decoded,
      registration: claimPortalEnrollmentTransitionAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const claimPortalEnrollmentTransitionActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'claimPortalEnrollmentTransitionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(claimPortalEnrollmentTransitionActionSchemaErrorLive));
