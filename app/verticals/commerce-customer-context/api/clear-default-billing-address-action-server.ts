// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug clear-default-billing-address
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { clearDefaultBillingAddressAction } from '../src/actions/clear-default-billing-address.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  clearDefaultBillingAddressActionProblem,
  clearDefaultBillingAddressActionSchemaErrorLive,
  mapClearDefaultBillingAddressActionProblem,
} from './clear-default-billing-address-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: clearDefaultBillingAddressActionProblem.authentication,
  unavailable: () =>
    clearDefaultBillingAddressActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ClearDefaultBillingAddressActionServer.execute')(
  function* executeClearDefaultBillingAddress(
    payload: typeof clearDefaultBillingAddressAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(clearDefaultBillingAddressActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      clearDefaultBillingAddressAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(clearDefaultBillingAddressActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: clearDefaultBillingAddressActionProblem.internal,
      invalidCorrelationProblem: clearDefaultBillingAddressActionProblem.invalid,
      mapError: mapClearDefaultBillingAddressActionProblem,
      payload: decoded,
      registration: clearDefaultBillingAddressAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const clearDefaultBillingAddressActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'clearDefaultBillingAddressAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(clearDefaultBillingAddressActionSchemaErrorLive));
