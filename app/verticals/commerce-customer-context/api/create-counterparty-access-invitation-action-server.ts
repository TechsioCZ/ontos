// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug create-counterparty-access-invitation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { createCounterpartyAccessInvitationAction } from '../src/actions/create-counterparty-access-invitation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createCounterpartyAccessInvitationActionProblem,
  createCounterpartyAccessInvitationActionSchemaErrorLive,
  mapCreateCounterpartyAccessInvitationActionProblem,
} from './create-counterparty-access-invitation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createCounterpartyAccessInvitationActionProblem.authentication,
  unavailable: () =>
    createCounterpartyAccessInvitationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateCounterpartyAccessInvitationActionServer.execute')(
  function* executeCreateCounterpartyAccessInvitation(
    payload: typeof createCounterpartyAccessInvitationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(createCounterpartyAccessInvitationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      createCounterpartyAccessInvitationAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(createCounterpartyAccessInvitationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: createCounterpartyAccessInvitationActionProblem.internal,
      invalidCorrelationProblem: createCounterpartyAccessInvitationActionProblem.invalid,
      mapError: mapCreateCounterpartyAccessInvitationActionProblem,
      payload: decoded,
      registration: createCounterpartyAccessInvitationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const createCounterpartyAccessInvitationActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'createCounterpartyAccessInvitationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createCounterpartyAccessInvitationActionSchemaErrorLive));
