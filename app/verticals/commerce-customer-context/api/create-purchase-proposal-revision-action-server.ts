// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug create-purchase-proposal-revision
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { createPurchaseProposalRevisionAction } from '../src/actions/create-purchase-proposal-revision.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createPurchaseProposalRevisionActionProblem,
  createPurchaseProposalRevisionActionSchemaErrorLive,
  mapCreatePurchaseProposalRevisionActionProblem,
} from './create-purchase-proposal-revision-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createPurchaseProposalRevisionActionProblem.authentication,
  unavailable: () => createPurchaseProposalRevisionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreatePurchaseProposalRevisionActionServer.execute')(
  function* executeCreatePurchaseProposalRevision(
    payload: typeof createPurchaseProposalRevisionAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(createPurchaseProposalRevisionActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(createPurchaseProposalRevisionAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(createPurchaseProposalRevisionActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: createPurchaseProposalRevisionActionProblem.internal,
      invalidCorrelationProblem: createPurchaseProposalRevisionActionProblem.invalid,
      mapError: mapCreatePurchaseProposalRevisionActionProblem,
      payload: decoded,
      registration: createPurchaseProposalRevisionAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const createPurchaseProposalRevisionActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'createPurchaseProposalRevisionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createPurchaseProposalRevisionActionSchemaErrorLive));
