// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug decide-purchase-approval-request
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { decidePurchaseApprovalRequestAction } from '../src/actions/decide-purchase-approval-request.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  decidePurchaseApprovalRequestActionProblem,
  decidePurchaseApprovalRequestActionSchemaErrorLive,
  mapDecidePurchaseApprovalRequestActionProblem,
} from './decide-purchase-approval-request-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: decidePurchaseApprovalRequestActionProblem.authentication,
  unavailable: () =>
    decidePurchaseApprovalRequestActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('DecidePurchaseApprovalRequestActionServer.execute')(
  function* executeDecidePurchaseApprovalRequest(
    payload: typeof decidePurchaseApprovalRequestAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(decidePurchaseApprovalRequestActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      decidePurchaseApprovalRequestAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(decidePurchaseApprovalRequestActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: decidePurchaseApprovalRequestActionProblem.internal,
      invalidCorrelationProblem: decidePurchaseApprovalRequestActionProblem.invalid,
      mapError: mapDecidePurchaseApprovalRequestActionProblem,
      payload: decoded,
      registration: decidePurchaseApprovalRequestAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const decidePurchaseApprovalRequestActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'decidePurchaseApprovalRequestAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(decidePurchaseApprovalRequestActionSchemaErrorLive));
