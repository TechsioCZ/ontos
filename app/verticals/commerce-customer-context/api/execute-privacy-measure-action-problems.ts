// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug execute-privacy-measure
// fallow-ignore-file code-duplication -- Owner-local protocol code intentionally mirrors its peer owner while preserving separate deployment and persistence authority.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  ExecutePrivacyMeasureActionAlreadyCommittedProblemSchema,
  ExecutePrivacyMeasureActionAuthenticationProblemSchema,
  ExecutePrivacyMeasureActionCommitIndeterminateProblemSchema,
  ExecutePrivacyMeasureActionConflictProblemSchema,
  ExecutePrivacyMeasureActionForbiddenProblemSchema,
  ExecutePrivacyMeasureActionIneligibleProblemSchema,
  ExecutePrivacyMeasureActionInternalProblemSchema,
  ExecutePrivacyMeasureActionInvalidProblemSchema,
  ExecutePrivacyMeasureActionNotFoundProblemSchema,
  ExecutePrivacyMeasureActionPreconditionProblemSchema,
  ExecutePrivacyMeasureActionSchemaErrorMiddleware,
  ExecutePrivacyMeasureActionUnavailableProblemSchema,
} from '../shared/apis/execute-privacy-measure-action.ts';
import type { ExecutePrivacyMeasureActionProblem } from '../shared/apis/execute-privacy-measure-action.ts';
import { executePrivacyMeasureAction } from '../src/actions/execute-privacy-measure.action.ts';

type DomainError = typeof executePrivacyMeasureAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends ExecutePrivacyMeasureActionProblem['_tag']> = Extract<
  ExecutePrivacyMeasureActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'PRIVACY_MEASURE_IDEMPOTENCY_CONFLICT'; readonly kind: 'conflict' }
  | { readonly code: 'PRIVACY_MEASURE_SCOPE_MISMATCH'; readonly kind: 'forbidden' }
  | { readonly code: 'PRIVACY_MEASURE_PERSISTENCE_UNAVAILABLE'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const executePrivacyMeasureRejectedProblemByCode = {
  PRIVACY_MEASURE_IDEMPOTENCY_CONFLICT: { code: 'PRIVACY_MEASURE_IDEMPOTENCY_CONFLICT', kind: 'conflict' },
  PRIVACY_MEASURE_PERSISTENCE_UNAVAILABLE: { code: 'PRIVACY_MEASURE_PERSISTENCE_UNAVAILABLE', kind: 'unavailable' },
  PRIVACY_MEASURE_SCOPE_MISMATCH: { code: 'PRIVACY_MEASURE_SCOPE_MISMATCH', kind: 'forbidden' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'ExecutePrivacyMeasureRejected' }>['code'],
  DomainProblemIdentity
>;

export const executePrivacyMeasureActionProblem = {
  authentication: (): ProblemOf<'ExecutePrivacyMeasureActionAuthenticationProblem'> =>
    ExecutePrivacyMeasureActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'ExecutePrivacyMeasureActionConflictProblem'>['code'],
  ): ProblemOf<'ExecutePrivacyMeasureActionConflictProblem'> =>
    ExecutePrivacyMeasureActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'ExecutePrivacyMeasureActionForbiddenProblem'>['code'],
  ): ProblemOf<'ExecutePrivacyMeasureActionForbiddenProblem'> =>
    ExecutePrivacyMeasureActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'ExecutePrivacyMeasureActionIneligibleProblem'>['code'],
  ): ProblemOf<'ExecutePrivacyMeasureActionIneligibleProblem'> =>
    ExecutePrivacyMeasureActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'ExecutePrivacyMeasureActionInternalProblem'> =>
    ExecutePrivacyMeasureActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'ExecutePrivacyMeasureActionInvalidProblem'> =>
    ExecutePrivacyMeasureActionInvalidProblemSchema.make({
      detail: 'The execute-privacy-measure Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'ExecutePrivacyMeasureActionNotFoundProblem'>['code'],
  ): ProblemOf<'ExecutePrivacyMeasureActionNotFoundProblem'> =>
    ExecutePrivacyMeasureActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'ExecutePrivacyMeasureActionPreconditionProblem'> =>
    ExecutePrivacyMeasureActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'ExecutePrivacyMeasureActionUnavailableProblem'>['code'],
  ): ProblemOf<'ExecutePrivacyMeasureActionUnavailableProblem'> =>
    ExecutePrivacyMeasureActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): ExecutePrivacyMeasureActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => executePrivacyMeasureActionProblem.conflict(matched.code)),
    Match.when({ kind: 'forbidden' as const }, (matched) => executePrivacyMeasureActionProblem.forbidden(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      executePrivacyMeasureActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): ExecutePrivacyMeasureActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ExecutePrivacyMeasureRejected: (failure) =>
        mapDomainIdentity(executePrivacyMeasureRejectedProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): ExecutePrivacyMeasureActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        ExecutePrivacyMeasureActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: executePrivacyMeasureActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        ExecutePrivacyMeasureActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: executePrivacyMeasureActionProblem.internal,
      ActionIdempotencyKeyRequired: executePrivacyMeasureActionProblem.precondition,
      ActionInvocationNotFound: (failure) => executePrivacyMeasureActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => executePrivacyMeasureActionProblem.conflict(failure.code),
      ActionPayloadValidationError: executePrivacyMeasureActionProblem.invalid,
      ActionPermissionCheckError: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => executePrivacyMeasureActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => executePrivacyMeasureActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => executePrivacyMeasureActionProblem.conflict(failure.code),
      ActionResultValidationError: executePrivacyMeasureActionProblem.internal,
      ActionTransactionError: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: executePrivacyMeasureActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => executePrivacyMeasureActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: executePrivacyMeasureActionProblem.authentication,
      OperationContextDenied: (failure) => executePrivacyMeasureActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => executePrivacyMeasureActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => executePrivacyMeasureActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(executePrivacyMeasureAction.descriptor.domainErrorSchema);
export const mapExecutePrivacyMeasureActionProblem = (
  error: ActionCoreError | DomainError,
): ExecutePrivacyMeasureActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const executePrivacyMeasureActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  ExecutePrivacyMeasureActionSchemaErrorMiddleware,
  () => Effect.fail(executePrivacyMeasureActionProblem.invalid()),
);
/* jscpd:ignore-end */
