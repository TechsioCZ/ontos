// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug grant-counterparty-commerce-access
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { grantCounterpartyCommerceAccessAction } from '../src/actions/grant-counterparty-commerce-access.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  grantCounterpartyCommerceAccessActionProblem,
  grantCounterpartyCommerceAccessActionSchemaErrorLive,
  mapGrantCounterpartyCommerceAccessActionProblem,
} from './grant-counterparty-commerce-access-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: grantCounterpartyCommerceAccessActionProblem.authentication,
  unavailable: () =>
    grantCounterpartyCommerceAccessActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('GrantCounterpartyCommerceAccessActionServer.execute')(
  function* executeGrantCounterpartyCommerceAccess(
    payload: typeof grantCounterpartyCommerceAccessAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(grantCounterpartyCommerceAccessActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      grantCounterpartyCommerceAccessAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(grantCounterpartyCommerceAccessActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: grantCounterpartyCommerceAccessActionProblem.internal,
      invalidCorrelationProblem: grantCounterpartyCommerceAccessActionProblem.invalid,
      mapError: mapGrantCounterpartyCommerceAccessActionProblem,
      payload: decoded,
      registration: grantCounterpartyCommerceAccessAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const grantCounterpartyCommerceAccessActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'grantCounterpartyCommerceAccessAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(grantCounterpartyCommerceAccessActionSchemaErrorLive));
