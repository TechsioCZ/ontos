// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug migrate-counterparty-price-group
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { migrateCounterpartyPriceGroupAction } from '../src/actions/migrate-counterparty-price-group.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  migrateCounterpartyPriceGroupActionProblem,
  migrateCounterpartyPriceGroupActionSchemaErrorLive,
  mapMigrateCounterpartyPriceGroupActionProblem,
} from './migrate-counterparty-price-group-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: migrateCounterpartyPriceGroupActionProblem.authentication,
  unavailable: () => migrateCounterpartyPriceGroupActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('MigrateCounterpartyPriceGroupActionServer.execute')(
  function* executeMigrateCounterpartyPriceGroup(
    payload: typeof migrateCounterpartyPriceGroupAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(migrateCounterpartyPriceGroupActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(migrateCounterpartyPriceGroupAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(migrateCounterpartyPriceGroupActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: migrateCounterpartyPriceGroupActionProblem.internal,
      invalidCorrelationProblem: migrateCounterpartyPriceGroupActionProblem.invalid,
      mapError: mapMigrateCounterpartyPriceGroupActionProblem,
      payload: decoded,
      registration: migrateCounterpartyPriceGroupAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const migrateCounterpartyPriceGroupActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'migrateCounterpartyPriceGroupAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(migrateCounterpartyPriceGroupActionSchemaErrorLive));
