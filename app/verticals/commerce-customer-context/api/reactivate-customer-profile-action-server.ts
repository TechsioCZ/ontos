// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug reactivate-customer-profile
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { reactivateCustomerProfileAction } from '../src/actions/reactivate-customer-profile.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reactivateCustomerProfileActionProblem,
  reactivateCustomerProfileActionSchemaErrorLive,
  mapReactivateCustomerProfileActionProblem,
} from './reactivate-customer-profile-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reactivateCustomerProfileActionProblem.authentication,
  unavailable: () =>
    reactivateCustomerProfileActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReactivateCustomerProfileActionServer.execute')(
  function* executeReactivateCustomerProfile(
    payload: typeof reactivateCustomerProfileAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(reactivateCustomerProfileActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      reactivateCustomerProfileAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(reactivateCustomerProfileActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: reactivateCustomerProfileActionProblem.internal,
      invalidCorrelationProblem: reactivateCustomerProfileActionProblem.invalid,
      mapError: mapReactivateCustomerProfileActionProblem,
      payload: decoded,
      registration: reactivateCustomerProfileAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const reactivateCustomerProfileActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'reactivateCustomerProfileAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reactivateCustomerProfileActionSchemaErrorLive));
