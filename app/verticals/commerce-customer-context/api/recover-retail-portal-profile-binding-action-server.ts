// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug recover-retail-portal-profile-binding
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { recoverRetailPortalProfileBindingAction } from '../src/actions/recover-retail-portal-profile-binding.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recoverRetailPortalProfileBindingActionProblem,
  recoverRetailPortalProfileBindingActionSchemaErrorLive,
  mapRecoverRetailPortalProfileBindingActionProblem,
} from './recover-retail-portal-profile-binding-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recoverRetailPortalProfileBindingActionProblem.authentication,
  unavailable: () => recoverRetailPortalProfileBindingActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecoverRetailPortalProfileBindingActionServer.execute')(
  function* executeRecoverRetailPortalProfileBinding(
    payload: typeof recoverRetailPortalProfileBindingAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(recoverRetailPortalProfileBindingActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(recoverRetailPortalProfileBindingAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(recoverRetailPortalProfileBindingActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: recoverRetailPortalProfileBindingActionProblem.internal,
      invalidCorrelationProblem: recoverRetailPortalProfileBindingActionProblem.invalid,
      mapError: mapRecoverRetailPortalProfileBindingActionProblem,
      payload: decoded,
      registration: recoverRetailPortalProfileBindingAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const recoverRetailPortalProfileBindingActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'recoverRetailPortalProfileBindingAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recoverRetailPortalProfileBindingActionSchemaErrorLive));
