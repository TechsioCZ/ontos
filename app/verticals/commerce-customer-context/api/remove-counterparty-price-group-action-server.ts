// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug remove-counterparty-price-group
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { removeCounterpartyPriceGroupAction } from '../src/actions/remove-counterparty-price-group.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeCounterpartyPriceGroupActionProblem,
  removeCounterpartyPriceGroupActionSchemaErrorLive,
  mapRemoveCounterpartyPriceGroupActionProblem,
} from './remove-counterparty-price-group-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeCounterpartyPriceGroupActionProblem.authentication,
  unavailable: () => removeCounterpartyPriceGroupActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveCounterpartyPriceGroupActionServer.execute')(
  function* executeRemoveCounterpartyPriceGroup(
    payload: typeof removeCounterpartyPriceGroupAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(removeCounterpartyPriceGroupActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(removeCounterpartyPriceGroupAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(removeCounterpartyPriceGroupActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: removeCounterpartyPriceGroupActionProblem.internal,
      invalidCorrelationProblem: removeCounterpartyPriceGroupActionProblem.invalid,
      mapError: mapRemoveCounterpartyPriceGroupActionProblem,
      payload: decoded,
      registration: removeCounterpartyPriceGroupAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const removeCounterpartyPriceGroupActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'removeCounterpartyPriceGroupAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeCounterpartyPriceGroupActionSchemaErrorLive));
