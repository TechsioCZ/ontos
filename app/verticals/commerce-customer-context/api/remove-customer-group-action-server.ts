// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug remove-customer-group
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { removeCustomerGroupAction } from '../src/actions/remove-customer-group.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeCustomerGroupActionProblem,
  removeCustomerGroupActionSchemaErrorLive,
  mapRemoveCustomerGroupActionProblem,
} from './remove-customer-group-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeCustomerGroupActionProblem.authentication,
  unavailable: () => removeCustomerGroupActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveCustomerGroupActionServer.execute')(function* executeRemoveCustomerGroup(
  payload: typeof removeCustomerGroupAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(removeCustomerGroupActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(removeCustomerGroupAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(removeCustomerGroupActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: removeCustomerGroupActionProblem.internal,
    invalidCorrelationProblem: removeCustomerGroupActionProblem.invalid,
    mapError: mapRemoveCustomerGroupActionProblem,
    payload: decoded,
    registration: removeCustomerGroupAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const removeCustomerGroupActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'removeCustomerGroupAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeCustomerGroupActionSchemaErrorLive));
