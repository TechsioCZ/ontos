// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug remove-saved-address
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { removeSavedAddressAction } from '../src/actions/remove-saved-address.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  removeSavedAddressActionProblem,
  removeSavedAddressActionSchemaErrorLive,
  mapRemoveSavedAddressActionProblem,
} from './remove-saved-address-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: removeSavedAddressActionProblem.authentication,
  unavailable: () => removeSavedAddressActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RemoveSavedAddressActionServer.execute')(function* executeRemoveSavedAddress(
  payload: typeof removeSavedAddressAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(removeSavedAddressActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(removeSavedAddressAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(removeSavedAddressActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: removeSavedAddressActionProblem.internal,
    invalidCorrelationProblem: removeSavedAddressActionProblem.invalid,
    mapError: mapRemoveSavedAddressActionProblem,
    payload: decoded,
    registration: removeSavedAddressAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const removeSavedAddressActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'removeSavedAddressAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(removeSavedAddressActionSchemaErrorLive));
