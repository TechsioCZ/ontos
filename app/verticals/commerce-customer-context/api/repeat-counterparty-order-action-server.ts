// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug repeat-counterparty-order
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { repeatCounterpartyOrderAction } from '../src/actions/repeat-counterparty-order.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  repeatCounterpartyOrderActionProblem,
  repeatCounterpartyOrderActionSchemaErrorLive,
  mapRepeatCounterpartyOrderActionProblem,
} from './repeat-counterparty-order-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: repeatCounterpartyOrderActionProblem.authentication,
  unavailable: () => repeatCounterpartyOrderActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RepeatCounterpartyOrderActionServer.execute')(function* executeRepeatCounterpartyOrder(
  payload: typeof repeatCounterpartyOrderAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(repeatCounterpartyOrderActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(repeatCounterpartyOrderAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(repeatCounterpartyOrderActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: repeatCounterpartyOrderActionProblem.internal,
    invalidCorrelationProblem: repeatCounterpartyOrderActionProblem.invalid,
    mapError: mapRepeatCounterpartyOrderActionProblem,
    payload: decoded,
    registration: repeatCounterpartyOrderAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const repeatCounterpartyOrderActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'repeatCounterpartyOrderAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(repeatCounterpartyOrderActionSchemaErrorLive));
