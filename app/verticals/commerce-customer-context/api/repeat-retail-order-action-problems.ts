// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug repeat-retail-order
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RepeatRetailOrderActionAlreadyCommittedProblemSchema,
  RepeatRetailOrderActionAuthenticationProblemSchema,
  RepeatRetailOrderActionCommitIndeterminateProblemSchema,
  RepeatRetailOrderActionConflictProblemSchema,
  RepeatRetailOrderActionForbiddenProblemSchema,
  RepeatRetailOrderActionIneligibleProblemSchema,
  RepeatRetailOrderActionInternalProblemSchema,
  RepeatRetailOrderActionInvalidProblemSchema,
  RepeatRetailOrderActionNotFoundProblemSchema,
  RepeatRetailOrderActionPreconditionProblemSchema,
  RepeatRetailOrderActionSchemaErrorMiddleware,
  RepeatRetailOrderActionUnavailableProblemSchema,
} from '../shared/apis/repeat-retail-order-action.ts';
import type { RepeatRetailOrderActionProblem } from '../shared/apis/repeat-retail-order-action.ts';
import { repeatRetailOrderAction } from '../src/actions/repeat-retail-order.action.ts';

type DomainError = typeof repeatRetailOrderAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RepeatRetailOrderActionProblem['_tag']> = Extract<
  RepeatRetailOrderActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const repeatRetailOrderActionProblem = {
  authentication: (): ProblemOf<'RepeatRetailOrderActionAuthenticationProblem'> =>
    RepeatRetailOrderActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RepeatRetailOrderActionConflictProblem'>['code'],
  ): ProblemOf<'RepeatRetailOrderActionConflictProblem'> =>
    RepeatRetailOrderActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RepeatRetailOrderActionForbiddenProblem'>['code'],
  ): ProblemOf<'RepeatRetailOrderActionForbiddenProblem'> =>
    RepeatRetailOrderActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RepeatRetailOrderActionIneligibleProblem'>['code'],
  ): ProblemOf<'RepeatRetailOrderActionIneligibleProblem'> =>
    RepeatRetailOrderActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RepeatRetailOrderActionInternalProblem'> =>
    RepeatRetailOrderActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RepeatRetailOrderActionInvalidProblem'> =>
    RepeatRetailOrderActionInvalidProblemSchema.make({
      detail: 'The repeat-retail-order Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RepeatRetailOrderActionNotFoundProblem'>['code'],
  ): ProblemOf<'RepeatRetailOrderActionNotFoundProblem'> =>
    RepeatRetailOrderActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RepeatRetailOrderActionPreconditionProblem'> =>
    RepeatRetailOrderActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RepeatRetailOrderActionUnavailableProblem'>['code'],
  ): ProblemOf<'RepeatRetailOrderActionUnavailableProblem'> =>
    RepeatRetailOrderActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RepeatRetailOrderActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      HistoryAccessDenied: () => repeatRetailOrderActionProblem.forbidden('history_access_denied'),
      HistoryActionUnavailable: () => repeatRetailOrderActionProblem.unavailable('history_action_unavailable'),
      HistoryRecordNotFound: () => repeatRetailOrderActionProblem.notFound('history_record_not_found'),
      RepeatOrderConflict: () => repeatRetailOrderActionProblem.conflict('repeat_order_conflict'),
      RepeatOrderNoRepeatableLines: () => repeatRetailOrderActionProblem.ineligible('repeat_order_no_repeatable_lines'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RepeatRetailOrderActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RepeatRetailOrderActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: repeatRetailOrderActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RepeatRetailOrderActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: repeatRetailOrderActionProblem.internal,
      ActionIdempotencyKeyRequired: repeatRetailOrderActionProblem.precondition,
      ActionInvocationNotFound: (failure) => repeatRetailOrderActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => repeatRetailOrderActionProblem.conflict(failure.code),
      ActionPayloadValidationError: repeatRetailOrderActionProblem.invalid,
      ActionPermissionCheckError: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => repeatRetailOrderActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => repeatRetailOrderActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => repeatRetailOrderActionProblem.conflict(failure.code),
      ActionResultValidationError: repeatRetailOrderActionProblem.internal,
      ActionTransactionError: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: repeatRetailOrderActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => repeatRetailOrderActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: repeatRetailOrderActionProblem.authentication,
      OperationContextDenied: (failure) => repeatRetailOrderActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => repeatRetailOrderActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => repeatRetailOrderActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(repeatRetailOrderAction.descriptor.domainErrorSchema);
export const mapRepeatRetailOrderActionProblem = (
  error: ActionCoreError | DomainError,
): RepeatRetailOrderActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const repeatRetailOrderActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RepeatRetailOrderActionSchemaErrorMiddleware,
  () => Effect.fail(repeatRetailOrderActionProblem.invalid()),
);
