// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug reroute-purchase-approval-request
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { reroutePurchaseApprovalRequestAction } from '../src/actions/reroute-purchase-approval-request.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reroutePurchaseApprovalRequestActionProblem,
  reroutePurchaseApprovalRequestActionSchemaErrorLive,
  mapReroutePurchaseApprovalRequestActionProblem,
} from './reroute-purchase-approval-request-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reroutePurchaseApprovalRequestActionProblem.authentication,
  unavailable: () =>
    reroutePurchaseApprovalRequestActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReroutePurchaseApprovalRequestActionServer.execute')(
  function* executeReroutePurchaseApprovalRequest(
    payload: typeof reroutePurchaseApprovalRequestAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(reroutePurchaseApprovalRequestActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      reroutePurchaseApprovalRequestAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(reroutePurchaseApprovalRequestActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: reroutePurchaseApprovalRequestActionProblem.internal,
      invalidCorrelationProblem: reroutePurchaseApprovalRequestActionProblem.invalid,
      mapError: mapReroutePurchaseApprovalRequestActionProblem,
      payload: decoded,
      registration: reroutePurchaseApprovalRequestAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const reroutePurchaseApprovalRequestActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'reroutePurchaseApprovalRequestAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reroutePurchaseApprovalRequestActionSchemaErrorLive));
