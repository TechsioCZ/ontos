// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug reserve-market-retirement
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { reserveMarketRetirementAction } from '../src/actions/reserve-market-retirement.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reserveMarketRetirementActionProblem,
  reserveMarketRetirementActionSchemaErrorLive,
  mapReserveMarketRetirementActionProblem,
} from './reserve-market-retirement-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reserveMarketRetirementActionProblem.authentication,
  unavailable: () => reserveMarketRetirementActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReserveMarketRetirementActionServer.execute')(function* executeReserveMarketRetirement(
  payload: typeof reserveMarketRetirementAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(reserveMarketRetirementActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(reserveMarketRetirementAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(reserveMarketRetirementActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: reserveMarketRetirementActionProblem.internal,
    invalidCorrelationProblem: reserveMarketRetirementActionProblem.invalid,
    mapError: mapReserveMarketRetirementActionProblem,
    payload: decoded,
    registration: reserveMarketRetirementAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const reserveMarketRetirementActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'reserveMarketRetirementAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reserveMarketRetirementActionSchemaErrorLive));
