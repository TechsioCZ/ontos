// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug reserve-payment-term-retirement
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { reservePaymentTermRetirementAction } from '../src/actions/reserve-payment-term-retirement.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  reservePaymentTermRetirementActionProblem,
  reservePaymentTermRetirementActionSchemaErrorLive,
  mapReservePaymentTermRetirementActionProblem,
} from './reserve-payment-term-retirement-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: reservePaymentTermRetirementActionProblem.authentication,
  unavailable: () => reservePaymentTermRetirementActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ReservePaymentTermRetirementActionServer.execute')(
  function* executeReservePaymentTermRetirement(
    payload: typeof reservePaymentTermRetirementAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(reservePaymentTermRetirementActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(reservePaymentTermRetirementAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(reservePaymentTermRetirementActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: reservePaymentTermRetirementActionProblem.internal,
      invalidCorrelationProblem: reservePaymentTermRetirementActionProblem.invalid,
      mapError: mapReservePaymentTermRetirementActionProblem,
      payload: decoded,
      registration: reservePaymentTermRetirementAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const reservePaymentTermRetirementActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'reservePaymentTermRetirementAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(reservePaymentTermRetirementActionSchemaErrorLive));
