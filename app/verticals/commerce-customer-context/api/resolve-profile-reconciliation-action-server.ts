// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug resolve-profile-reconciliation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { resolveProfileReconciliationAction } from '../src/actions/resolve-profile-reconciliation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  resolveProfileReconciliationActionProblem,
  resolveProfileReconciliationActionSchemaErrorLive,
  mapResolveProfileReconciliationActionProblem,
} from './resolve-profile-reconciliation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: resolveProfileReconciliationActionProblem.authentication,
  unavailable: () => resolveProfileReconciliationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ResolveProfileReconciliationActionServer.execute')(
  function* executeResolveProfileReconciliation(
    payload: typeof resolveProfileReconciliationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(resolveProfileReconciliationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(resolveProfileReconciliationAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(resolveProfileReconciliationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: resolveProfileReconciliationActionProblem.internal,
      invalidCorrelationProblem: resolveProfileReconciliationActionProblem.invalid,
      mapError: mapResolveProfileReconciliationActionProblem,
      payload: decoded,
      registration: resolveProfileReconciliationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const resolveProfileReconciliationActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'resolveProfileReconciliationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(resolveProfileReconciliationActionSchemaErrorLive));
