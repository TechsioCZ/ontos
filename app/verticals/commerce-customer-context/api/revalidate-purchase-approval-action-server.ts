// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug revalidate-purchase-approval
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { revalidatePurchaseApprovalAction } from '../src/actions/revalidate-purchase-approval.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  revalidatePurchaseApprovalActionProblem,
  revalidatePurchaseApprovalActionSchemaErrorLive,
  mapRevalidatePurchaseApprovalActionProblem,
} from './revalidate-purchase-approval-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: revalidatePurchaseApprovalActionProblem.authentication,
  unavailable: () => revalidatePurchaseApprovalActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RevalidatePurchaseApprovalActionServer.execute')(function* executeRevalidatePurchaseApproval(
  payload: typeof revalidatePurchaseApprovalAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(revalidatePurchaseApprovalActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(revalidatePurchaseApprovalAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(revalidatePurchaseApprovalActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: revalidatePurchaseApprovalActionProblem.internal,
    invalidCorrelationProblem: revalidatePurchaseApprovalActionProblem.invalid,
    mapError: mapRevalidatePurchaseApprovalActionProblem,
    payload: decoded,
    registration: revalidatePurchaseApprovalAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const revalidatePurchaseApprovalActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'revalidatePurchaseApprovalAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(revalidatePurchaseApprovalActionSchemaErrorLive));
