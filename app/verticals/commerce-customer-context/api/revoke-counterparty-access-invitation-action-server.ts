// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug revoke-counterparty-access-invitation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { revokeCounterpartyAccessInvitationAction } from '../src/actions/revoke-counterparty-access-invitation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  revokeCounterpartyAccessInvitationActionProblem,
  revokeCounterpartyAccessInvitationActionSchemaErrorLive,
  mapRevokeCounterpartyAccessInvitationActionProblem,
} from './revoke-counterparty-access-invitation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: revokeCounterpartyAccessInvitationActionProblem.authentication,
  unavailable: () =>
    revokeCounterpartyAccessInvitationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RevokeCounterpartyAccessInvitationActionServer.execute')(
  function* executeRevokeCounterpartyAccessInvitation(
    payload: typeof revokeCounterpartyAccessInvitationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(revokeCounterpartyAccessInvitationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      revokeCounterpartyAccessInvitationAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(revokeCounterpartyAccessInvitationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: revokeCounterpartyAccessInvitationActionProblem.internal,
      invalidCorrelationProblem: revokeCounterpartyAccessInvitationActionProblem.invalid,
      mapError: mapRevokeCounterpartyAccessInvitationActionProblem,
      payload: decoded,
      registration: revokeCounterpartyAccessInvitationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const revokeCounterpartyAccessInvitationActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'revokeCounterpartyAccessInvitationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(revokeCounterpartyAccessInvitationActionSchemaErrorLive));
