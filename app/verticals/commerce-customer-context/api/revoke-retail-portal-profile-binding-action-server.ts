// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug revoke-retail-portal-profile-binding
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { revokeRetailPortalProfileBindingAction } from '../src/actions/revoke-retail-portal-profile-binding.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  revokeRetailPortalProfileBindingActionProblem,
  revokeRetailPortalProfileBindingActionSchemaErrorLive,
  mapRevokeRetailPortalProfileBindingActionProblem,
} from './revoke-retail-portal-profile-binding-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: revokeRetailPortalProfileBindingActionProblem.authentication,
  unavailable: () =>
    revokeRetailPortalProfileBindingActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RevokeRetailPortalProfileBindingActionServer.execute')(
  function* executeRevokeRetailPortalProfileBinding(
    payload: typeof revokeRetailPortalProfileBindingAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(revokeRetailPortalProfileBindingActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      revokeRetailPortalProfileBindingAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(revokeRetailPortalProfileBindingActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: revokeRetailPortalProfileBindingActionProblem.internal,
      invalidCorrelationProblem: revokeRetailPortalProfileBindingActionProblem.invalid,
      mapError: mapRevokeRetailPortalProfileBindingActionProblem,
      payload: decoded,
      registration: revokeRetailPortalProfileBindingAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const revokeRetailPortalProfileBindingActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'revokeRetailPortalProfileBindingAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(revokeRetailPortalProfileBindingActionSchemaErrorLive));
