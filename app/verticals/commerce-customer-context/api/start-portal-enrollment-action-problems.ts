// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug start-portal-enrollment
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  StartPortalEnrollmentActionAlreadyCommittedProblemSchema,
  StartPortalEnrollmentActionAuthenticationProblemSchema,
  StartPortalEnrollmentActionCommitIndeterminateProblemSchema,
  StartPortalEnrollmentActionConflictProblemSchema,
  StartPortalEnrollmentActionForbiddenProblemSchema,
  StartPortalEnrollmentActionIneligibleProblemSchema,
  StartPortalEnrollmentActionInternalProblemSchema,
  StartPortalEnrollmentActionInvalidProblemSchema,
  StartPortalEnrollmentActionNotFoundProblemSchema,
  StartPortalEnrollmentActionPreconditionProblemSchema,
  StartPortalEnrollmentActionSchemaErrorMiddleware,
  StartPortalEnrollmentActionUnavailableProblemSchema,
} from '../shared/apis/start-portal-enrollment-action.ts';
import type { StartPortalEnrollmentActionProblem } from '../shared/apis/start-portal-enrollment-action.ts';
import { startPortalEnrollmentAction } from '../src/actions/start-portal-enrollment.action.ts';

type DomainError = typeof startPortalEnrollmentAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends StartPortalEnrollmentActionProblem['_tag']> = Extract<
  StartPortalEnrollmentActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | {
      readonly code: 'attempt_conflict' | 'attempt_lease_conflict' | 'attempt_revision_conflict';
      readonly kind: 'conflict';
    }
  | { readonly code: 'attempt_invalid' | 'attempt_terminal'; readonly kind: 'ineligible' }
  | { readonly code: 'attempt_not_found'; readonly kind: 'notFound' }
  | { readonly code: 'attempt_indeterminate' | 'attempt_unavailable'; readonly kind: 'unavailable' };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const commerceEnrollmentAttemptConflictProblemByCode = {
  attempt_conflict: { code: 'attempt_conflict', kind: 'conflict' },
  attempt_lease_conflict: { code: 'attempt_lease_conflict', kind: 'conflict' },
  attempt_revision_conflict: { code: 'attempt_revision_conflict', kind: 'conflict' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'CommerceEnrollmentAttemptConflict' }>['code'],
  DomainProblemIdentity
>;

const commerceEnrollmentAttemptRejectedProblemByCode = {
  attempt_conflict: { code: 'attempt_conflict', kind: 'conflict' },
  attempt_indeterminate: { code: 'attempt_indeterminate', kind: 'unavailable' },
  attempt_invalid: { code: 'attempt_invalid', kind: 'ineligible' },
  attempt_lease_conflict: { code: 'attempt_lease_conflict', kind: 'conflict' },
  attempt_not_found: { code: 'attempt_not_found', kind: 'notFound' },
  attempt_revision_conflict: { code: 'attempt_revision_conflict', kind: 'conflict' },
  attempt_terminal: { code: 'attempt_terminal', kind: 'ineligible' },
  attempt_unavailable: { code: 'attempt_unavailable', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'CommerceEnrollmentAttemptRejected' }>['code'],
  DomainProblemIdentity
>;

export const startPortalEnrollmentActionProblem = {
  authentication: (): ProblemOf<'StartPortalEnrollmentActionAuthenticationProblem'> =>
    StartPortalEnrollmentActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'StartPortalEnrollmentActionConflictProblem'>['code'],
  ): ProblemOf<'StartPortalEnrollmentActionConflictProblem'> =>
    StartPortalEnrollmentActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'StartPortalEnrollmentActionForbiddenProblem'>['code'],
  ): ProblemOf<'StartPortalEnrollmentActionForbiddenProblem'> =>
    StartPortalEnrollmentActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'StartPortalEnrollmentActionIneligibleProblem'>['code'],
  ): ProblemOf<'StartPortalEnrollmentActionIneligibleProblem'> =>
    StartPortalEnrollmentActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'StartPortalEnrollmentActionInternalProblem'> =>
    StartPortalEnrollmentActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'StartPortalEnrollmentActionInvalidProblem'> =>
    StartPortalEnrollmentActionInvalidProblemSchema.make({
      detail: 'The start-portal-enrollment Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'StartPortalEnrollmentActionNotFoundProblem'>['code'],
  ): ProblemOf<'StartPortalEnrollmentActionNotFoundProblem'> =>
    StartPortalEnrollmentActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'StartPortalEnrollmentActionPreconditionProblem'> =>
    StartPortalEnrollmentActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'StartPortalEnrollmentActionUnavailableProblem'>['code'],
  ): ProblemOf<'StartPortalEnrollmentActionUnavailableProblem'> =>
    StartPortalEnrollmentActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): StartPortalEnrollmentActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'conflict' as const }, (matched) => startPortalEnrollmentActionProblem.conflict(matched.code)),
    Match.when({ kind: 'ineligible' as const }, (matched) =>
      startPortalEnrollmentActionProblem.ineligible(matched.code),
    ),
    Match.when({ kind: 'notFound' as const }, (matched) => startPortalEnrollmentActionProblem.notFound(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      startPortalEnrollmentActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): StartPortalEnrollmentActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CommerceEnrollmentAttemptConflict: (failure) =>
        mapDomainIdentity(commerceEnrollmentAttemptConflictProblemByCode[failure.code]),
      CommerceEnrollmentAttemptIndeterminate: () =>
        startPortalEnrollmentActionProblem.unavailable('attempt_indeterminate'),
      CommerceEnrollmentAttemptNotFound: () => startPortalEnrollmentActionProblem.notFound('attempt_not_found'),
      CommerceEnrollmentAttemptRejected: (failure) =>
        mapDomainIdentity(commerceEnrollmentAttemptRejectedProblemByCode[failure.code]),
      CommerceEnrollmentAttemptUnavailable: () => startPortalEnrollmentActionProblem.unavailable('attempt_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): StartPortalEnrollmentActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        StartPortalEnrollmentActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: startPortalEnrollmentActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        StartPortalEnrollmentActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: startPortalEnrollmentActionProblem.internal,
      ActionIdempotencyKeyRequired: startPortalEnrollmentActionProblem.precondition,
      ActionInvocationNotFound: (failure) => startPortalEnrollmentActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => startPortalEnrollmentActionProblem.conflict(failure.code),
      ActionPayloadValidationError: startPortalEnrollmentActionProblem.invalid,
      ActionPermissionCheckError: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => startPortalEnrollmentActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => startPortalEnrollmentActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => startPortalEnrollmentActionProblem.conflict(failure.code),
      ActionResultValidationError: startPortalEnrollmentActionProblem.internal,
      ActionTransactionError: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: startPortalEnrollmentActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => startPortalEnrollmentActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: startPortalEnrollmentActionProblem.authentication,
      OperationContextDenied: (failure) => startPortalEnrollmentActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => startPortalEnrollmentActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => startPortalEnrollmentActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(startPortalEnrollmentAction.descriptor.domainErrorSchema);
export const mapStartPortalEnrollmentActionProblem = (
  error: ActionCoreError | DomainError,
): StartPortalEnrollmentActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const startPortalEnrollmentActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  StartPortalEnrollmentActionSchemaErrorMiddleware,
  () => Effect.fail(startPortalEnrollmentActionProblem.invalid()),
);
