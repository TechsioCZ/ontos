// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug suspend-customer-profile
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { suspendCustomerProfileAction } from '../src/actions/suspend-customer-profile.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  suspendCustomerProfileActionProblem,
  suspendCustomerProfileActionSchemaErrorLive,
  mapSuspendCustomerProfileActionProblem,
} from './suspend-customer-profile-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: suspendCustomerProfileActionProblem.authentication,
  unavailable: () =>
    suspendCustomerProfileActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('SuspendCustomerProfileActionServer.execute')(
  function* executeSuspendCustomerProfile(
    payload: typeof suspendCustomerProfileAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(suspendCustomerProfileActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      suspendCustomerProfileAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(suspendCustomerProfileActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: suspendCustomerProfileActionProblem.internal,
      invalidCorrelationProblem: suspendCustomerProfileActionProblem.invalid,
      mapError: mapSuspendCustomerProfileActionProblem,
      payload: decoded,
      registration: suspendCustomerProfileAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const suspendCustomerProfileActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'suspendCustomerProfileAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(suspendCustomerProfileActionSchemaErrorLive));
