// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug trigger-purchase-approval
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { commerceCustomerContextApi } from '../shared/api.ts';
import { triggerPurchaseApprovalAction } from '../src/actions/trigger-purchase-approval.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  triggerPurchaseApprovalActionProblem,
  triggerPurchaseApprovalActionSchemaErrorLive,
  mapTriggerPurchaseApprovalActionProblem,
} from './trigger-purchase-approval-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: triggerPurchaseApprovalActionProblem.authentication,
  unavailable: () =>
    triggerPurchaseApprovalActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('TriggerPurchaseApprovalActionServer.execute')(
  function* executeTriggerPurchaseApproval(
    payload: typeof triggerPurchaseApprovalAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(triggerPurchaseApprovalActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      triggerPurchaseApprovalAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(triggerPurchaseApprovalActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: triggerPurchaseApprovalActionProblem.internal,
      invalidCorrelationProblem: triggerPurchaseApprovalActionProblem.invalid,
      mapError: mapTriggerPurchaseApprovalActionProblem,
      payload: decoded,
      registration: triggerPurchaseApprovalAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const triggerPurchaseApprovalActionApiLive = HttpApiBuilder.group(
  commerceCustomerContextApi,
  'triggerPurchaseApprovalAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(triggerPurchaseApprovalActionSchemaErrorLive));
