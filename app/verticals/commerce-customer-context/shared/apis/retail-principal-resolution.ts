// @generated by OntOS Codesmith module-api v1
import {
  makeProblemDetailsSchema,
  makeRetryableProblemDetailsSchema,
} from '@app/shared-contracts/problem-details';
import { Schema } from 'effect';
import { HttpApi, HttpApiEndpoint, HttpApiGroup } from 'effect/unstable/httpapi';
import { ProfileReadProvenanceSchema } from '../domain/profile-contracts.ts';
import { ProfileReconciliationCaseRefSchema } from '../resources/profile-reconciliation-case.ts';
import { RetailCustomerProfileRefSchema } from '../resources/retail-customer-profile.ts';
import { RetailPortalProfileBindingRefSchema } from '../resources/retail-portal-profile-binding.ts';

export const RetailPrincipalResolutionRequestSchema = Schema.Struct({
  profileRef: RetailCustomerProfileRefSchema,
});
export type RetailPrincipalResolutionRequest = typeof RetailPrincipalResolutionRequestSchema.Type;
export const RetailPrincipalResolutionResponseSchema = Schema.Union([
  Schema.TaggedStruct('RETAIL_PRINCIPAL_BOUND', {
    bindingRef: RetailPortalProfileBindingRefSchema,
    profileRef: RetailCustomerProfileRefSchema,
    provenance: Schema.Array(ProfileReadProvenanceSchema),
  }),
  Schema.TaggedStruct('RETAIL_PRINCIPAL_NOT_BOUND', {
    profileRef: RetailCustomerProfileRefSchema,
    provenance: Schema.Array(ProfileReadProvenanceSchema),
  }),
  Schema.TaggedStruct('RETAIL_PRINCIPAL_BINDING_REVOKED', {
    bindingRef: RetailPortalProfileBindingRefSchema,
    profileRef: RetailCustomerProfileRefSchema,
    provenance: Schema.Array(ProfileReadProvenanceSchema),
  }),
  Schema.TaggedStruct('RETAIL_PRINCIPAL_RESOLUTION_AMBIGUOUS', {
    profileRef: RetailCustomerProfileRefSchema,
    provenance: Schema.Array(ProfileReadProvenanceSchema),
    reconciliationCaseRef: Schema.OptionFromNullOr(ProfileReconciliationCaseRefSchema),
  }),
]);
export type RetailPrincipalResolutionResponse = typeof RetailPrincipalResolutionResponseSchema.Type;

export const RetailPrincipalResolutionAuthenticationProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionAuthenticationProblem',
  401,
);
export const RetailPrincipalResolutionInvalidProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionInvalidProblem',
  400,
);
export const RetailPrincipalResolutionUnavailableProblemSchema = makeRetryableProblemDetailsSchema(
  'RetailPrincipalResolutionUnavailableProblem',
  503,
);
export const RetailPrincipalResolutionForbiddenProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionForbiddenProblem',
  403,
);
export const RetailPrincipalResolutionNotFoundProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionNotFoundProblem',
  404,
);
export const RetailPrincipalResolutionPolicyProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionPolicyProblem',
  422,
);
export const RetailPrincipalResolutionPolicyConflictProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionPolicyConflictProblem',
  409,
);
export const RetailPrincipalResolutionInternalProblemSchema = makeProblemDetailsSchema(
  'RetailPrincipalResolutionInternalProblem',
  500,
);

export const RetailPrincipalResolutionApi = HttpApi.make('RetailPrincipalResolutionApi').add(
  HttpApiGroup.make('retailPrincipalResolution').add(
    HttpApiEndpoint.post('execute', '/reads/retail-principal-resolution', {
      error: [
        RetailPrincipalResolutionInvalidProblemSchema,
        RetailPrincipalResolutionAuthenticationProblemSchema,
        RetailPrincipalResolutionForbiddenProblemSchema,
        RetailPrincipalResolutionNotFoundProblemSchema,
        RetailPrincipalResolutionPolicyConflictProblemSchema,
        RetailPrincipalResolutionPolicyProblemSchema,
        RetailPrincipalResolutionUnavailableProblemSchema,
        RetailPrincipalResolutionInternalProblemSchema,
      ],
      headers: {},
      params: {},
      payload: RetailPrincipalResolutionRequestSchema,
      query: {},
      success: RetailPrincipalResolutionResponseSchema,
    }),
  ),
);
