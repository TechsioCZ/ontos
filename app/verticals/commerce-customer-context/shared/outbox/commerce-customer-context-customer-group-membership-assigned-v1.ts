// @generated by OntOS Codesmith Outbox Message Contract v1
// @ontos-outbox-producer commerce.customer-context
// @ontos-outbox-topic commerce.customer-context.customer-group-membership-assigned.v1
import { Schema } from 'effect';
import { CommerceCustomerGroupMembershipSchema } from '../domain/group-contract.ts';

export const OutboxPayloadSchema = Schema.Struct({
  membership: CommerceCustomerGroupMembershipSchema,
}).check(
  Schema.makeFilter(({ membership }) =>
    membership.state === 'VALID' && membership.removal === null
      ? undefined
      : 'an assignment event requires one valid, never-removed membership',
  ),
);
export type OutboxPayload = Schema.Schema.Type<typeof OutboxPayloadSchema>;

export const outboxTopic =
  'commerce.customer-context.customer-group-membership-assigned.v1' as const;
export const outboxProducerModuleKey = 'commerce.customer-context' as const;
