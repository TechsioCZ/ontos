// @generated by OntOS Codesmith Outbox Message Contract v1
// @ontos-outbox-producer commerce.customer-context
// @ontos-outbox-topic commerce.customer-context.market-bootstrap-policy.changed
/* oxlint-disable effect-native/no-unbranded-identifier-schema -- Generated wire payload keeps UUID IDs encoded as strings; tracked in #333 until Codesmith accepts encoded branded array elements. */
import { Schema } from 'effect';

const PolicyRevisionIdSchema = Schema.String.check(Schema.isUUID());

export const OutboxPayloadSchema = Schema.Struct({
  changed: Schema.Literal(true),
  field: Schema.Literal('MARKET_BOOTSTRAP'),
  generation: Schema.Int.check(Schema.isGreaterThan(0)),
  revisionIds: Schema.Array(PolicyRevisionIdSchema).check(Schema.isMinLength(1)),
});
export type OutboxPayload = Schema.Schema.Type<typeof OutboxPayloadSchema>;

export const outboxTopic = 'commerce.customer-context.market-bootstrap-policy.changed' as const;
export const outboxProducerModuleKey = 'commerce.customer-context' as const;
