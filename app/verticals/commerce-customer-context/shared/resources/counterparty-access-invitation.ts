// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug counterparty-access-invitation
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('CounterpartyAccessInvitationResourceId'),
  Schema.decodeTo(Schema.String),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('CounterpartyAccessInvitationTenantId'),
  Schema.decodeTo(Schema.String),
);

export const CounterpartyAccessInvitationRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.counterparty-access-invitation'),
  tenantId: TenantIdSchema,
});
export type CounterpartyAccessInvitationRef = typeof CounterpartyAccessInvitationRefSchema.Type;

export const counterpartyAccessInvitationResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Secret-safe, expiring and auditable onboarding intent that grants no authorization itself.',
  key: 'commerce.customer-context.counterparty-access-invitation',
  label: 'Counterparty Access Invitation',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
