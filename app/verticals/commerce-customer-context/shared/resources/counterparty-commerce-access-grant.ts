// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug counterparty-commerce-access-grant
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('CounterpartyCommerceAccessGrantResourceId'),
  Schema.decodeTo(Schema.String),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('CounterpartyCommerceAccessGrantTenantId'),
  Schema.decodeTo(Schema.String),
);

export const CounterpartyCommerceAccessGrantRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.counterparty-commerce-access-grant'),
  tenantId: TenantIdSchema,
});
export type CounterpartyCommerceAccessGrantRef = typeof CounterpartyCommerceAccessGrantRefSchema.Type;

export const counterpartyCommerceAccessGrantResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Auditable current and historical atomic Principal-to-Counterparty Commerce Permission grant.',
  key: 'commerce.customer-context.counterparty-commerce-access-grant',
  label: 'Counterparty Commerce Access Grant',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
