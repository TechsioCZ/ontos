// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug customer-payment-term-entitlement
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('CustomerPaymentTermEntitlementResourceId'),
  Schema.decodeTo(Schema.String),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('CustomerPaymentTermEntitlementTenantId'),
  Schema.decodeTo(Schema.String),
);

export const CustomerPaymentTermEntitlementRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.customer-payment-term-entitlement'),
  tenantId: TenantIdSchema,
});
export type CustomerPaymentTermEntitlementRef = typeof CustomerPaymentTermEntitlementRefSchema.Type;

export const customerPaymentTermEntitlementResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description:
    'Auditable, effective-dated entitlement of one customer profile to one Payment Term.',
  key: 'commerce.customer-context.customer-payment-term-entitlement',
  label: 'Customer Payment Term Entitlement',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
