// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug customer-price-group-assignment
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('CustomerPriceGroupAssignmentResourceId'),
  Schema.decodeTo(Schema.String),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('CustomerPriceGroupAssignmentTenantId'),
  Schema.decodeTo(Schema.String),
);

export const CustomerPriceGroupAssignmentRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.customer-price-group-assignment'),
  tenantId: TenantIdSchema,
});
export type CustomerPriceGroupAssignmentRef = typeof CustomerPriceGroupAssignmentRefSchema.Type;

export const customerPriceGroupAssignmentResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description:
    'Effective-dated assignment of one Commerce customer profile to one Pricing-owned PriceGroup.',
  key: 'commerce.customer-context.customer-price-group-assignment',
  label: 'Customer Price Group Assignment',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
