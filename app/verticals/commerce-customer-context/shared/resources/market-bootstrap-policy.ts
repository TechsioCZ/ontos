// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug market-bootstrap-policy
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('MarketBootstrapPolicyResourceId'));
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(Schema.brand('TenantId'));

export const MarketBootstrapPolicyRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.market-bootstrap-policy'),
  tenantId: TenantIdSchema,
});
export type MarketBootstrapPolicyRef = typeof MarketBootstrapPolicyRefSchema.Type;

export const marketBootstrapPolicyResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Market Bootstrap Policy resource.',
  key: 'commerce.customer-context.market-bootstrap-policy',
  label: 'Market Bootstrap Policy',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
