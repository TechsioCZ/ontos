// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug purchase-limit-policy
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('PurchaseLimitPolicyResourceId'),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('PurchaseLimitPolicyTenantId'),
);

export const PurchaseLimitPolicyRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.purchase-limit-policy'),
  tenantId: TenantIdSchema,
});
export type PurchaseLimitPolicyRef = typeof PurchaseLimitPolicyRefSchema.Type;

export const purchaseLimitPolicyResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Versioned Counterparty default or Principal-specific purchasing authority policy.',
  key: 'commerce.customer-context.purchase-limit-policy',
  label: 'Purchase Limit Policy',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
