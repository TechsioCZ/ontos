// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.customer-context
// @ontos-resource-slug purchase-proposal-revision
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('ResourceId'),
  Schema.decodeTo(Schema.String),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('TenantId'),
  Schema.decodeTo(Schema.String),
);

export const PurchaseProposalRevisionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.customer-context'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.customer-context.purchase-proposal-revision'),
  tenantId: TenantIdSchema,
});
export type PurchaseProposalRevisionRef = typeof PurchaseProposalRevisionRefSchema.Type;

export const purchaseProposalRevisionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Purchase Proposal Revision resource.',
  key: 'commerce.customer-context.purchase-proposal-revision',
  label: 'Purchase Proposal Revision',
  owningModuleId: 'commerce.customer-context',
} as const satisfies OntosResourceType;
