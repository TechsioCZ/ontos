// @generated by OntOS Codesmith Action v1
// @ontos-action-owner commerce.customer-context
// @ontos-action-slug administer-payment-term-policy
/* oxlint-disable effect-native/no-manual-tag-comparison, effect-native/no-unbranded-identifier-schema, eslint/prefer-destructuring -- Generated Action adapter mirrors domain-owned tagged payloads and identifiers; tracked in: #333; remove-when: Codesmith emits shared audit-subject projections. */
import type { ActionHandlerContext } from '@app/core-runtime';
import { defineAction, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { DateTime, Effect, Schema } from 'effect';
import {
  CustomerCommercePolicyAdministrationRejected,
  CustomerCommercePolicyMutationSummarySchema,
  PaymentTermPolicyAdministrationPayloadSchema,
  toTrustedPaymentTermPolicyAdministrationCommand,
} from '../../shared/domain/customer-commerce-policy-administration.ts';
import type { PaymentTermPolicyAdministrationPayload } from '../../shared/domain/customer-commerce-policy-administration.ts';
import {
  OutboxPayloadSchema as PaymentTermPolicyChangedEventSchema,
  outboxProducerModuleKey as MODULE_KEY,
  outboxTopic as EVENT_TYPE,
} from '../../shared/outbox/commerce-customer-context-payment-term-policy-changed.ts';
import { customerCommercePolicyAdministrationServiceFactory } from '../services/customer-commerce-policy-administration.service.ts';
import type { CustomerCommercePolicyAdministrationService } from '../services/customer-commerce-policy-administration.service.ts';
import { createAdministerPaymentTermPolicyCommerceCustomerContextPaymentTermPolicyChangedOutboxMessage as createOutboxMessage } from './administer-payment-term-policy-commerce-customer-context-payment-term-policy-changed.outbox-message.ts';

const stableReference = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300), Schema.isTrimmed());
const reason = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(1000), Schema.isTrimmed());
const AdministerPaymentTermPolicyPayloadSchema = PaymentTermPolicyAdministrationPayloadSchema;
export type AdministerPaymentTermPolicyPayload = PaymentTermPolicyAdministrationPayload;

const AdministerPaymentTermPolicyResultSchema = CustomerCommercePolicyMutationSummarySchema;

const auditEvidenceSchema = Schema.Struct({
  changed: Schema.Boolean,
  field: Schema.Literal('PAYMENT_TERM'),
  generation: Schema.Int,
  operation: Schema.Literals(['ACTIVATE_REVISION', 'CREATE_REVISION', 'REPLACE_REVISION', 'RETIRE_REVISION']),
  reason,
  revisionId: stableReference,
});
const domainEvents = { [EVENT_TYPE]: PaymentTermPolicyChangedEventSchema } as const;

const failScope = (reasonText: string) =>
  new CustomerCommercePolicyAdministrationRejected({ code: 'SCOPE_MISMATCH', reason: reasonText, retryable: false });
const observedAt = DateTime.now.pipe(Effect.map((instant) => DateTime.formatIso(instant)));

const handleAdministerPaymentTermPolicy = Effect.fn('AdministerPaymentTermPolicyAction.handle')(function* handle(
  payload: AdministerPaymentTermPolicyPayload,
  context: ActionHandlerContext<typeof domainEvents, CustomerCommercePolicyAdministrationService>,
) {
  const legalEntityId = context.scope.legalEntityId;
  if (legalEntityId === undefined) {
    return yield* failScope('Payment Term policy administration requires trusted Legal Entity scope');
  }
  const recordedAt = yield* observedAt;
  const trusted = {
    actionInvocationId: context.actionInvocationId,
    actorPrincipalId: context.scope.principalId,
    sellingLegalEntityId: legalEntityId,
    tenantId: context.scope.tenantId,
  } as const;
  let reasonText: string;
  let revisionId: string;
  if (payload._tag === 'CREATE_REVISION' || payload._tag === 'REPLACE_REVISION') {
    const candidate = payload._tag === 'CREATE_REVISION' ? payload.revision : payload.replacement;
    if (candidate.scope.sellingLegalEntityId !== legalEntityId) {
      return yield* failScope('Payment Term policy scope must match the trusted Legal Entity');
    }
    if ('paymentTermRef' in candidate.value && candidate.value.paymentTermRef.tenantId !== context.scope.tenantId) {
      return yield* failScope('Payment Term reference must match the trusted Tenant');
    }
    reasonText = candidate.reason;
    revisionId = candidate.revisionId;
  } else {
    reasonText = payload.reason;
    revisionId = payload.revisionId;
  }
  const command = toTrustedPaymentTermPolicyAdministrationCommand(payload, trusted, recordedAt);
  const result = yield* context.services.administerPaymentTermPolicy(command);
  yield* context.recordAuditEvidence({
    changed: result.changed,
    field: 'PAYMENT_TERM',
    generation: result.generation,
    operation: payload._tag,
    reason: reasonText,
    revisionId,
  });
  yield* context.recordDataAccess({
    accessKind: 'read',
    queryHash: `customer-commerce-policy:payment-term:${revisionId}:${payload.expectedGeneration}`,
    resultCount: result.revisionIds.length,
    servingModuleKey: MODULE_KEY,
    targetModuleKey: MODULE_KEY,
    targetResourceId: revisionId,
    targetResourceType: `${MODULE_KEY}.payment-term-policy`,
  });
  if (result.changed) {
    const eventPayload = {
      changed: result.changed,
      field: 'PAYMENT_TERM' as const,
      generation: result.generation,
      revisionIds: result.revisionIds,
    };
    const event = yield* context.addDomainEvent({
      eventType: EVENT_TYPE,
      payloadJson: eventPayload,
      producerModuleKey: MODULE_KEY,
      subjectModuleKey: MODULE_KEY,
      subjectResourceId: revisionId,
      subjectResourceType: `${MODULE_KEY}.payment-term-policy`,
    });
    yield* context.addOutboxMessage(event, createOutboxMessage(eventPayload));
  }
  return result;
});

export const administerPaymentTermPolicyAction = defineAction(
  {
    accessEvidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.administer-payment-term-policy.access.v1',
    },
    actionKey: 'commerce.customer-context.administer-payment-term-policy',
    auditEvidenceSchema,
    auditProfile: 'standard',
    domainErrorSchema: CustomerCommercePolicyAdministrationRejected,
    domainEvents,
    entrypoint: defineTenantModuleEntrypoint({
      access: 'write',
      authorization: { kind: 'action_execution', provisioning: 'explicit' },
      entrypointKey: 'commerce.customer-context.administer-payment-term-policy',
      moduleKey: 'commerce.customer-context',
      role: 'action',
    }),
    idempotency: 'required',
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    payloadSchema: AdministerPaymentTermPolicyPayloadSchema,
    policies: [],
    resultSchema: AdministerPaymentTermPolicyResultSchema,
    schemaVersion: '1',
  },
  handleAdministerPaymentTermPolicy,
  customerCommercePolicyAdministrationServiceFactory,
);

// <generated-outbox-message-exports>
// </generated-outbox-message-exports>
