// @generated by OntOS Codesmith Action v1
// @ontos-action-owner commerce.customer-context
// @ontos-action-slug repeat-retail-order
import type { ActionBusinessPermissionTarget, OperationalScope } from '@app/core-runtime';
import {
  defineAction,
  defineActionBusinessPermission,
  defineActionResourcePermission,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Schema } from 'effect';
import {
  RepeatRetailOrderPayloadSchema,
  RepeatRetailOrderResultSchema,
} from '../../shared/actions/repeat-retail-order.ts';
import type { RepeatRetailOrderPayload } from '../../shared/actions/repeat-retail-order.ts';
import { HistoryActionAuditEvidenceSchema } from '../../shared/domain/history-action-contracts.ts';
import {
  HistoryActionUnavailable,
  RepeatOrderConflict,
  RepeatOrderNoRepeatableLines,
} from '../../shared/domain/history-action-errors.ts';
import { HistoryAccessDenied, HistoryRecordNotFound } from '../../shared/domain/history-errors.ts';
import { handleRepeatRetailOrder, loadHistoryActionServices } from './history-action-support.ts';

const RepeatRetailOrderErrorSchema = Schema.Union([
  HistoryAccessDenied,
  HistoryActionUnavailable,
  HistoryRecordNotFound,
  RepeatOrderConflict,
  RepeatOrderNoRepeatableLines,
]);

const repeatRetailOrderPermissionTarget = (
  payload: RepeatRetailOrderPayload,
  scope: OperationalScope,
): ActionBusinessPermissionTarget => ({
  permission: 'retail.repeat_order',
  target: {
    kind: 'retail_profile',
    legalEntityId: scope.legalEntityId ?? '',
    profileId: payload.profileRef.resourceId,
    tenantId:
      payload.profileRef.tenantId === scope.tenantId && payload.sourceOrderRef.tenantId === scope.tenantId
        ? scope.tenantId
        : '',
  },
});

export const repeatRetailOrderAction = defineAction(
  {
    accessEvidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.repeat-retail-order.access.v1',
    },
    actionKey: 'commerce.customer-context.repeat-retail-order',
    auditEvidenceSchema: HistoryActionAuditEvidenceSchema,
    auditProfile: 'sensitive',
    businessPermission: defineActionBusinessPermission(repeatRetailOrderPermissionTarget),
    domainErrorSchema: RepeatRetailOrderErrorSchema,
    domainEvents: {},
    entrypoint: defineTenantModuleEntrypoint({
      access: 'write',
      authorization: { kind: 'action_execution', provisioning: 'explicit' },
      entrypointKey: 'commerce.customer-context.repeat-retail-order',
      moduleKey: 'commerce.customer-context',
      role: 'action',
    }),
    idempotency: 'required',
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    payloadSchema: RepeatRetailOrderPayloadSchema,
    policies: [],
    resourcePermission: defineActionResourcePermission<RepeatRetailOrderPayload>(({ sourceOrderRef }) => ({
      permission: 'read',
      resource: sourceOrderRef,
    })),
    resultSchema: RepeatRetailOrderResultSchema,
    schemaVersion: '1',
  },
  handleRepeatRetailOrder,
  (transaction, scope) => loadHistoryActionServices(transaction, scope),
);

export type { RepeatRetailOrderPayload } from '../../shared/actions/repeat-retail-order.ts';

// <generated-outbox-message-exports>
// </generated-outbox-message-exports>
