// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug bootstrap-counterparty-access-administrator
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  BootstrapCounterpartyAccessAdministratorActionApi,
  BootstrapCounterpartyAccessAdministratorPayloadSchema,
} from '../../shared/apis/bootstrap-counterparty-access-administrator-action.ts';
import type { BootstrapCounterpartyAccessAdministratorPayload } from '../../shared/actions/bootstrap-counterparty-access-administrator.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface BootstrapCounterpartyAccessAdministratorActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: BootstrapCounterpartyAccessAdministratorActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: BootstrapCounterpartyAccessAdministratorActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: BootstrapCounterpartyAccessAdministratorActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: BootstrapCounterpartyAccessAdministratorActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeBootstrapCounterpartyAccessAdministratorWithAuthorization = (
  payload: BootstrapCounterpartyAccessAdministratorPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(BootstrapCounterpartyAccessAdministratorPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.bootstrapCounterpartyAccessAdministratorAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeBootstrapCounterpartyAccessAdministrator = (
  payload: BootstrapCounterpartyAccessAdministratorPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeBootstrapCounterpartyAccessAdministratorWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
