// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug change-principal-purchase-limit-override
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  ChangePrincipalPurchaseLimitOverrideActionApi,
  ChangePrincipalPurchaseLimitOverridePayloadSchema,
} from '../../shared/apis/change-principal-purchase-limit-override-action.ts';
import type { ChangePrincipalPurchaseLimitOverridePayload } from '../../shared/actions/change-principal-purchase-limit-override.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ChangePrincipalPurchaseLimitOverrideActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ChangePrincipalPurchaseLimitOverrideActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: ChangePrincipalPurchaseLimitOverrideActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ChangePrincipalPurchaseLimitOverrideActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ChangePrincipalPurchaseLimitOverrideActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeChangePrincipalPurchaseLimitOverrideWithAuthorization = (
  payload: ChangePrincipalPurchaseLimitOverridePayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ChangePrincipalPurchaseLimitOverridePayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.changePrincipalPurchaseLimitOverrideAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeChangePrincipalPurchaseLimitOverride = (
  payload: ChangePrincipalPurchaseLimitOverridePayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeChangePrincipalPurchaseLimitOverrideWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
