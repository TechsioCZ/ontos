// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug claim-counterparty-access-invitation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  ClaimCounterpartyAccessInvitationActionApi,
  ClaimCounterpartyAccessInvitationPayloadSchema,
} from '../../shared/apis/claim-counterparty-access-invitation-action.ts';
import type { ClaimCounterpartyAccessInvitationPayload } from '../../shared/actions/claim-counterparty-access-invitation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ClaimCounterpartyAccessInvitationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ClaimCounterpartyAccessInvitationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: ClaimCounterpartyAccessInvitationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ClaimCounterpartyAccessInvitationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ClaimCounterpartyAccessInvitationActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeClaimCounterpartyAccessInvitationWithAuthorization = (
  payload: ClaimCounterpartyAccessInvitationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ClaimCounterpartyAccessInvitationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.claimCounterpartyAccessInvitationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeClaimCounterpartyAccessInvitation = (
  payload: ClaimCounterpartyAccessInvitationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeClaimCounterpartyAccessInvitationWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
