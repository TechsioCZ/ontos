// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CommerceQuantityPolicyCurrentApi } from '../../shared/apis/commerce-quantity-policy-current.ts';
import type { CommerceQuantityPolicyCurrentRequest } from '../../shared/apis/commerce-quantity-policy-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface CommerceQuantityPolicyCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type CommerceQuantityPolicyCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CommerceQuantityPolicyCurrentClientOptions,
];

type CommerceQuantityPolicyCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CommerceQuantityPolicyCurrentClientOptions,
];

const commerceQuantityPolicyCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CommerceQuantityPolicyCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CommerceQuantityPolicyCurrentApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCommerceQuantityPolicyCurrentWithAuthorization = (
  payload: CommerceQuantityPolicyCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: CommerceQuantityPolicyCurrentAuthorizedInvocation
) =>
  commerceQuantityPolicyCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.commerceQuantityPolicyCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCommerceQuantityPolicyCurrent = (
  payload: CommerceQuantityPolicyCurrentRequest,
  ...[requestCorrelation, options = {}]: CommerceQuantityPolicyCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCommerceQuantityPolicyCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
