// @generated by OntOS Codesmith module-api v1
/* oxlint-disable sonarjs/no-duplicate-string -- Generated boundary checker requires literal module identity; tracked in: #333; remove-when: checker accepts aliases for permission-target module IDs. */
import type { ReadHandlerContext } from '@app/core-runtime';
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  CommerceQuantityPolicyCurrentRequestSchema,
  CommerceQuantityPolicyCurrentResponseSchema,
} from '../../shared/apis/commerce-quantity-policy-current.ts';
import type { CommerceQuantityPolicyCurrentRequest } from '../../shared/apis/commerce-quantity-policy-current.ts';
import { customerCommercePolicyAdministrationServiceFactory } from '../services/customer-commerce-policy-administration.service.ts';
import type { CustomerCommercePolicyAdministrationService } from '../services/customer-commerce-policy-administration.service.ts';

const commerceQuantityPolicyCurrentEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'customer_commerce_policy.quantity.read',
  },
  entrypointKey: 'commerce.customer-context.api.commerce-quantity-policy-current',
  moduleKey: 'commerce.customer-context',
  role: 'api',
});

export const commerceQuantityPolicyCurrentRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: commerceQuantityPolicyCurrentEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.api.commerce-quantity-policy-current.evidence.v1',
    },
    inputSchema: CommerceQuantityPolicyCurrentRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    permissionTarget: 'module',
    policies: [],
    readKey: 'commerce.customer-context.api.commerce-quantity-policy-current',
    resultSchema: CommerceQuantityPolicyCurrentResponseSchema,
    schemaVersion: '1',
  },
  Effect.fn('CommerceQuantityPolicyCurrentRead.handle')(function* handle(
    input: CommerceQuantityPolicyCurrentRequest,
    context: ReadHandlerContext<CustomerCommercePolicyAdministrationService>,
  ) {
    const result = yield* context.services.readCurrentCommerceQuantityPolicy(input.at).pipe(
      Effect.mapError((cause) => {
        const failure = new ReadHandlerUnavailable({
          code: 'read_handler_unavailable',
          reason: 'Current Commerce Quantity policy is temporarily unavailable',
        });
        return Object.defineProperty(failure, 'cause', { configurable: true, value: cause });
      }),
    );
    return {
      evidence: { resultCount: result.ruleSet.candidates.length + result.assignmentSet.assignments.length },
      result,
    };
  }),
  (transaction, scope) => customerCommercePolicyAdministrationServiceFactory(transaction, scope),
  () => ({ kind: 'module', moduleId: 'commerce.customer-context' }),
);
