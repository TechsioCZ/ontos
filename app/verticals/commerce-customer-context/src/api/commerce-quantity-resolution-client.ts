// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CommerceQuantityResolutionApi } from '../../shared/apis/commerce-quantity-resolution.ts';
import type { CommerceQuantityResolutionRequest } from '../../shared/apis/commerce-quantity-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface CommerceQuantityResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type CommerceQuantityResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CommerceQuantityResolutionClientOptions,
];

type CommerceQuantityResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CommerceQuantityResolutionClientOptions,
];

const commerceQuantityResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CommerceQuantityResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CommerceQuantityResolutionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCommerceQuantityResolutionWithAuthorization = (
  payload: CommerceQuantityResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: CommerceQuantityResolutionAuthorizedInvocation
) =>
  commerceQuantityResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.commerceQuantityResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCommerceQuantityResolution = (
  payload: CommerceQuantityResolutionRequest,
  ...[requestCorrelation, options = {}]: CommerceQuantityResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCommerceQuantityResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
