// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CounterpartyAccessInvitationReadApi } from '../../shared/apis/counterparty-access-invitation-read.ts';
import type { CounterpartyAccessInvitationReadRequest } from '../../shared/apis/counterparty-access-invitation-read.ts';
import { operationGateway } from './action-gateway.ts';

export interface CounterpartyAccessInvitationReadClientOptions {
  readonly baseUrl?: string | URL;
}

type CounterpartyAccessInvitationReadAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CounterpartyAccessInvitationReadClientOptions,
];

type CounterpartyAccessInvitationReadOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CounterpartyAccessInvitationReadClientOptions,
];

const counterpartyAccessInvitationReadClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: CounterpartyAccessInvitationReadClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CounterpartyAccessInvitationReadApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCounterpartyAccessInvitationReadWithAuthorization = (
  payload: CounterpartyAccessInvitationReadRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: CounterpartyAccessInvitationReadAuthorizedInvocation
) =>
  counterpartyAccessInvitationReadClient(
    Redacted.make(credential),
    requestCorrelation,
    options,
  ).pipe(
    Effect.flatMap((client) =>
      client.counterpartyAccessInvitationRead.execute({
        headers: {},
        params: {},
        payload,
        query: {},
      }),
    ),
  );

export const executeCounterpartyAccessInvitationRead = (
  payload: CounterpartyAccessInvitationReadRequest,
  ...[requestCorrelation, options = {}]: CounterpartyAccessInvitationReadOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCounterpartyAccessInvitationReadWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
