// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug create-purchase-proposal-revision
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  CreatePurchaseProposalRevisionActionApi,
  CreatePurchaseProposalRevisionPayloadSchema,
} from '../../shared/apis/create-purchase-proposal-revision-action.ts';
import type { CreatePurchaseProposalRevisionPayload } from '../../shared/actions/create-purchase-proposal-revision.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface CreatePurchaseProposalRevisionActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: CreatePurchaseProposalRevisionActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: CreatePurchaseProposalRevisionActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: CreatePurchaseProposalRevisionActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: CreatePurchaseProposalRevisionActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCreatePurchaseProposalRevisionWithAuthorization = (
  payload: CreatePurchaseProposalRevisionPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(CreatePurchaseProposalRevisionPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.createPurchaseProposalRevisionAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeCreatePurchaseProposalRevision = (
  payload: CreatePurchaseProposalRevisionPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeCreatePurchaseProposalRevisionWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
