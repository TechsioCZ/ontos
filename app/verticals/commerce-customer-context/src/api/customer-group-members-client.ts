// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CustomerGroupMembersApi } from '../../shared/apis/customer-group-members.ts';
import type { CustomerGroupMembersRequest } from '../../shared/apis/customer-group-members.ts';
import { operationGateway } from './action-gateway.ts';

export interface CustomerGroupMembersClientOptions {
  readonly baseUrl?: string | URL;
}

type CustomerGroupMembersAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CustomerGroupMembersClientOptions,
];

type CustomerGroupMembersOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CustomerGroupMembersClientOptions,
];

const customerGroupMembersClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CustomerGroupMembersClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CustomerGroupMembersApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCustomerGroupMembersWithAuthorization = (
  payload: CustomerGroupMembersRequest,
  ...[credential, requestCorrelation, options = {}]: CustomerGroupMembersAuthorizedInvocation
) =>
  customerGroupMembersClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.customerGroupMembers.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCustomerGroupMembers = (
  payload: CustomerGroupMembersRequest,
  ...[requestCorrelation, options = {}]: CustomerGroupMembersOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCustomerGroupMembersWithAuthorization(payload, credential, requestCorrelation, options),
  );
