// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CustomerPriceGroupAssignmentReadApi } from '../../shared/apis/customer-price-group-assignment-read.ts';
import type { CustomerPriceGroupAssignmentReadRequest } from '../../shared/apis/customer-price-group-assignment-read.ts';
import { operationGateway } from './action-gateway.ts';

export interface CustomerPriceGroupAssignmentReadClientOptions {
  readonly baseUrl?: string | URL;
}

type CustomerPriceGroupAssignmentReadAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CustomerPriceGroupAssignmentReadClientOptions,
];

type CustomerPriceGroupAssignmentReadOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CustomerPriceGroupAssignmentReadClientOptions,
];

const customerPriceGroupAssignmentReadClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CustomerPriceGroupAssignmentReadClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CustomerPriceGroupAssignmentReadApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCustomerPriceGroupAssignmentReadWithAuthorization = (
  payload: CustomerPriceGroupAssignmentReadRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: CustomerPriceGroupAssignmentReadAuthorizedInvocation
) =>
  customerPriceGroupAssignmentReadClient(
    Redacted.make(credential),
    requestCorrelation,
    options,
  ).pipe(
    Effect.flatMap((client) =>
      client.customerPriceGroupAssignmentRead.execute({
        headers: {},
        params: {},
        payload,
        query: {},
      }),
    ),
  );

export const executeCustomerPriceGroupAssignmentRead = (
  payload: CustomerPriceGroupAssignmentReadRequest,
  ...[requestCorrelation, options = {}]: CustomerPriceGroupAssignmentReadOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCustomerPriceGroupAssignmentReadWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
