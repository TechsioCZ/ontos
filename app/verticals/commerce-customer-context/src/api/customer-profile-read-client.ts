// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CustomerProfileReadApi } from '../../shared/apis/customer-profile-read.ts';
import type { CustomerProfileReadRequest } from '../../shared/apis/customer-profile-read.ts';
import { operationGateway } from './action-gateway.ts';

export interface CustomerProfileReadClientOptions {
  readonly baseUrl?: string | URL;
}

type CustomerProfileReadAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CustomerProfileReadClientOptions,
];

type CustomerProfileReadOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CustomerProfileReadClientOptions,
];

const customerProfileReadClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: CustomerProfileReadClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CustomerProfileReadApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeCustomerProfileReadWithAuthorization = (
  payload: CustomerProfileReadRequest,
  ...[credential, requestCorrelation, options = {}]: CustomerProfileReadAuthorizedInvocation
) =>
  customerProfileReadClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.customerProfileRead.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCustomerProfileRead = (
  payload: CustomerProfileReadRequest,
  ...[requestCorrelation, options = {}]: CustomerProfileReadOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCustomerProfileReadWithAuthorization(payload, credential, requestCorrelation, options),
  );
