// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { GuestOrderClaimApi } from '../../shared/apis/guest-order-claim.ts';
import type { GuestOrderClaimRequest } from '../../shared/apis/guest-order-claim.ts';
import { operationGateway } from './action-gateway.ts';

export interface GuestOrderClaimClientOptions {
  readonly baseUrl?: string | URL;
}

type GuestOrderClaimAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GuestOrderClaimClientOptions,
];

type GuestOrderClaimOperationInvocation = readonly [
  requestCorrelation: string,
  options?: GuestOrderClaimClientOptions,
];

const guestOrderClaimClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: GuestOrderClaimClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GuestOrderClaimApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeGuestOrderClaimWithAuthorization = (
  payload: GuestOrderClaimRequest,
  ...[credential, requestCorrelation, options = {}]: GuestOrderClaimAuthorizedInvocation
) =>
  guestOrderClaimClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.guestOrderClaim.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeGuestOrderClaim = (
  payload: GuestOrderClaimRequest,
  ...[requestCorrelation, options = {}]: GuestOrderClaimOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGuestOrderClaimWithAuthorization(payload, credential, requestCorrelation, options),
  );
