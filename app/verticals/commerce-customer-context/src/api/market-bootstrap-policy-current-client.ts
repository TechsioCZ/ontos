// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { MarketBootstrapPolicyCurrentApi } from '../../shared/apis/market-bootstrap-policy-current.ts';
import type { MarketBootstrapPolicyCurrentRequest } from '../../shared/apis/market-bootstrap-policy-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface MarketBootstrapPolicyCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type MarketBootstrapPolicyCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: MarketBootstrapPolicyCurrentClientOptions,
];

type MarketBootstrapPolicyCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: MarketBootstrapPolicyCurrentClientOptions,
];

const marketBootstrapPolicyCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: MarketBootstrapPolicyCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: MarketBootstrapPolicyCurrentApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeMarketBootstrapPolicyCurrentWithAuthorization = (
  payload: MarketBootstrapPolicyCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: MarketBootstrapPolicyCurrentAuthorizedInvocation
) =>
  marketBootstrapPolicyCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.marketBootstrapPolicyCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeMarketBootstrapPolicyCurrent = (
  payload: MarketBootstrapPolicyCurrentRequest,
  ...[requestCorrelation, options = {}]: MarketBootstrapPolicyCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeMarketBootstrapPolicyCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
