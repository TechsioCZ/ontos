// @generated by OntOS Codesmith module-api v1
/* oxlint-disable sonarjs/no-duplicate-string -- Generated boundary checker requires literal module identity; tracked in: #333; remove-when: checker accepts aliases for permission-target module IDs. */
import type { ReadHandlerContext } from '@app/core-runtime';
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  MarketBootstrapPolicyCurrentRequestSchema,
  MarketBootstrapPolicyCurrentResponseSchema,
} from '../../shared/apis/market-bootstrap-policy-current.ts';
import type { MarketBootstrapPolicyCurrentRequest } from '../../shared/apis/market-bootstrap-policy-current.ts';
import { customerCommercePolicyAdministrationServiceFactory } from '../services/customer-commerce-policy-administration.service.ts';
import type { CustomerCommercePolicyAdministrationService } from '../services/customer-commerce-policy-administration.service.ts';

const marketBootstrapPolicyCurrentEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'customer_commerce_policy.market_bootstrap.read',
  },
  entrypointKey: 'commerce.customer-context.api.market-bootstrap-policy-current',
  moduleKey: 'commerce.customer-context',
  role: 'api',
});

export const marketBootstrapPolicyCurrentRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: marketBootstrapPolicyCurrentEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.api.market-bootstrap-policy-current.evidence.v1',
    },
    inputSchema: MarketBootstrapPolicyCurrentRequestSchema,
    legalEntityScope: 'optional',
    owningModuleKey: 'commerce.customer-context',
    permissionTarget: 'module',
    policies: [],
    readKey: 'commerce.customer-context.api.market-bootstrap-policy-current',
    resultSchema: MarketBootstrapPolicyCurrentResponseSchema,
    schemaVersion: '1',
  },
  Effect.fn('MarketBootstrapPolicyCurrentRead.handle')(function* handle(
    input: MarketBootstrapPolicyCurrentRequest,
    context: ReadHandlerContext<CustomerCommercePolicyAdministrationService>,
  ) {
    const result = yield* context.services.readCurrentMarketBootstrapPolicyCandidates(input).pipe(
      Effect.mapError((cause) => {
        const failure = new ReadHandlerUnavailable({
          code: 'read_handler_unavailable',
          reason: 'Current Market Bootstrap policy is temporarily unavailable',
        });
        return Object.defineProperty(failure, 'cause', { configurable: true, value: cause });
      }),
    );
    return {
      evidence: { resultCount: result.sellers.reduce((count, seller) => count + seller.candidates.length, 0) },
      result,
    };
  }),
  (transaction, scope) => customerCommercePolicyAdministrationServiceFactory(transaction, scope),
  () => ({ kind: 'module', moduleId: 'commerce.customer-context' }),
);
