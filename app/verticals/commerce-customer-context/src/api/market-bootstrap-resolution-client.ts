// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { MarketBootstrapResolutionApi } from '../../shared/apis/market-bootstrap-resolution.ts';
import type { MarketBootstrapResolutionRequest } from '../../shared/apis/market-bootstrap-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface MarketBootstrapResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type MarketBootstrapResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: MarketBootstrapResolutionClientOptions,
];

type MarketBootstrapResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: MarketBootstrapResolutionClientOptions,
];

const marketBootstrapResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: MarketBootstrapResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: MarketBootstrapResolutionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeMarketBootstrapResolutionWithAuthorization = (
  payload: MarketBootstrapResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: MarketBootstrapResolutionAuthorizedInvocation
) =>
  marketBootstrapResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.marketBootstrapResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeMarketBootstrapResolution = (
  payload: MarketBootstrapResolutionRequest,
  ...[requestCorrelation, options = {}]: MarketBootstrapResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeMarketBootstrapResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
