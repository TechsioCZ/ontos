// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PaymentTermPolicyCurrentApi } from '../../shared/apis/payment-term-policy-current.ts';
import type { PaymentTermPolicyCurrentRequest } from '../../shared/apis/payment-term-policy-current.ts';
import { operationGateway } from './action-gateway.ts';

export interface PaymentTermPolicyCurrentClientOptions {
  readonly baseUrl?: string | URL;
}

type PaymentTermPolicyCurrentAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PaymentTermPolicyCurrentClientOptions,
];

type PaymentTermPolicyCurrentOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PaymentTermPolicyCurrentClientOptions,
];

const paymentTermPolicyCurrentClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PaymentTermPolicyCurrentClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PaymentTermPolicyCurrentApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executePaymentTermPolicyCurrentWithAuthorization = (
  payload: PaymentTermPolicyCurrentRequest,
  ...[credential, requestCorrelation, options = {}]: PaymentTermPolicyCurrentAuthorizedInvocation
) =>
  paymentTermPolicyCurrentClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.paymentTermPolicyCurrent.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePaymentTermPolicyCurrent = (
  payload: PaymentTermPolicyCurrentRequest,
  ...[requestCorrelation, options = {}]: PaymentTermPolicyCurrentOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePaymentTermPolicyCurrentWithAuthorization(payload, credential, requestCorrelation, options),
  );
