// @generated by OntOS Codesmith module-api v1
/* oxlint-disable sonarjs/no-duplicate-string -- Generated boundary checker requires literal module identity; tracked in: #333; remove-when: checker accepts aliases for permission-target module IDs. */
import type { ReadHandlerContext } from '@app/core-runtime';
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  PaymentTermPolicyCurrentRequestSchema,
  PaymentTermPolicyCurrentResponseSchema,
} from '../../shared/apis/payment-term-policy-current.ts';
import type { PaymentTermPolicyCurrentRequest } from '../../shared/apis/payment-term-policy-current.ts';
import { customerCommercePolicyAdministrationServiceFactory } from '../services/customer-commerce-policy-administration.service.ts';
import type { CustomerCommercePolicyAdministrationService } from '../services/customer-commerce-policy-administration.service.ts';

const paymentTermPolicyCurrentEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'customer_commerce_policy.payment_term.read',
  },
  entrypointKey: 'commerce.customer-context.api.payment-term-policy-current',
  moduleKey: 'commerce.customer-context',
  role: 'api',
});

export const paymentTermPolicyCurrentRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: paymentTermPolicyCurrentEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.api.payment-term-policy-current.evidence.v1',
    },
    inputSchema: PaymentTermPolicyCurrentRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    permissionTarget: 'module',
    policies: [],
    readKey: 'commerce.customer-context.api.payment-term-policy-current',
    resultSchema: PaymentTermPolicyCurrentResponseSchema,
    schemaVersion: '1',
  },
  Effect.fn('PaymentTermPolicyCurrentRead.handle')(function* handle(
    input: PaymentTermPolicyCurrentRequest,
    context: ReadHandlerContext<CustomerCommercePolicyAdministrationService>,
  ) {
    const result = yield* context.services.readCurrentPaymentTermPolicy(input.at).pipe(
      Effect.mapError((cause) => {
        const failure = new ReadHandlerUnavailable({
          code: 'read_handler_unavailable',
          reason: 'Current Payment Term policy is temporarily unavailable',
        });
        return Object.defineProperty(failure, 'cause', { configurable: true, value: cause });
      }),
    );
    return { evidence: { resultCount: result.candidates.length }, result };
  }),
  (transaction, scope) => customerCommercePolicyAdministrationServiceFactory(transaction, scope),
  () => ({ kind: 'module', moduleId: 'commerce.customer-context' }),
);
