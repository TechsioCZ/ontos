// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PaymentTermsResolutionApi } from '../../shared/apis/payment-terms-resolution.ts';
import type { PaymentTermsResolutionRequest } from '../../shared/apis/payment-terms-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface PaymentTermsResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type PaymentTermsResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PaymentTermsResolutionClientOptions,
];

type PaymentTermsResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PaymentTermsResolutionClientOptions,
];

const paymentTermsResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PaymentTermsResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PaymentTermsResolutionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executePaymentTermsResolutionWithAuthorization = (
  payload: PaymentTermsResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: PaymentTermsResolutionAuthorizedInvocation
) =>
  paymentTermsResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.paymentTermsResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePaymentTermsResolution = (
  payload: PaymentTermsResolutionRequest,
  ...[requestCorrelation, options = {}]: PaymentTermsResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePaymentTermsResolutionWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
