// @generated by OntOS Codesmith module-api v1
// fallow-ignore-file code-duplication -- Owner-local protocol code intentionally mirrors its peer owner while preserving separate deployment and persistence authority.
/* jscpd:ignore-start -- Owner-local protocol code intentionally mirrors its peer owner while preserving separate deployment and persistence authority. */
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PrivacyMeasureExecutionApi } from '../../shared/apis/privacy-measure-execution.ts';
import type { PrivacyMeasureExecutionRequest } from '../../shared/apis/privacy-measure-execution.ts';
import { operationGateway } from './action-gateway.ts';

export interface PrivacyMeasureExecutionClientOptions {
  readonly baseUrl?: string | URL;
}

type PrivacyMeasureExecutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PrivacyMeasureExecutionClientOptions,
];

type PrivacyMeasureExecutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PrivacyMeasureExecutionClientOptions,
];

const privacyMeasureExecutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: PrivacyMeasureExecutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PrivacyMeasureExecutionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executePrivacyMeasureExecutionWithAuthorization = (
  payload: PrivacyMeasureExecutionRequest,
  ...[credential, requestCorrelation, options = {}]: PrivacyMeasureExecutionAuthorizedInvocation
) =>
  privacyMeasureExecutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.privacyMeasureExecution.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executePrivacyMeasureExecution = (
  payload: PrivacyMeasureExecutionRequest,
  ...[requestCorrelation, options = {}]: PrivacyMeasureExecutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePrivacyMeasureExecutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
/* jscpd:ignore-end */
