// @generated by OntOS Codesmith module-api v1
/* oxlint-disable sonarjs/no-duplicate-string -- Generated boundary checker requires literal module identity; tracked in: #333; remove-when: checker accepts aliases for permission-target module IDs. */
import type { ReadHandlerContext } from '@app/core-runtime';
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  PurchaseCurrencyPolicyCurrentRequestSchema,
  PurchaseCurrencyPolicyCurrentResponseSchema,
} from '../../shared/apis/purchase-currency-policy-current.ts';
import type { PurchaseCurrencyPolicyCurrentRequest } from '../../shared/apis/purchase-currency-policy-current.ts';
import { customerCommercePolicyAdministrationServiceFactory } from '../services/customer-commerce-policy-administration.service.ts';
import type { CustomerCommercePolicyAdministrationService } from '../services/customer-commerce-policy-administration.service.ts';

const purchaseCurrencyPolicyCurrentEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: {
    kind: 'context_permission',
    permission: 'customer_commerce_policy.purchase_currency.read',
  },
  entrypointKey: 'commerce.customer-context.api.purchase-currency-policy-current',
  moduleKey: 'commerce.customer-context',
  role: 'api',
});

export const purchaseCurrencyPolicyCurrentRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: purchaseCurrencyPolicyCurrentEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.api.purchase-currency-policy-current.evidence.v1',
    },
    inputSchema: PurchaseCurrencyPolicyCurrentRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    permissionTarget: 'module',
    policies: [],
    readKey: 'commerce.customer-context.api.purchase-currency-policy-current',
    resultSchema: PurchaseCurrencyPolicyCurrentResponseSchema,
    schemaVersion: '1',
  },
  Effect.fn('PurchaseCurrencyPolicyCurrentRead.handle')(function* handle(
    input: PurchaseCurrencyPolicyCurrentRequest,
    context: ReadHandlerContext<CustomerCommercePolicyAdministrationService>,
  ) {
    const result = yield* context.services.readCurrentPurchaseCurrencyPolicy(input.at).pipe(
      Effect.mapError((cause) => {
        const failure = new ReadHandlerUnavailable({
          code: 'read_handler_unavailable',
          reason: 'Current Purchase Currency policy is temporarily unavailable',
        });
        return Object.defineProperty(failure, 'cause', { configurable: true, value: cause });
      }),
    );
    return { evidence: { resultCount: result.candidates.length }, result };
  }),
  (transaction, scope) => customerCommercePolicyAdministrationServiceFactory(transaction, scope),
  () => ({ kind: 'module', moduleId: 'commerce.customer-context' }),
);
