// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug recover-retail-portal-profile-binding
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  RecoverRetailPortalProfileBindingActionApi,
  RecoverRetailPortalProfileBindingPayloadSchema,
} from '../../shared/apis/recover-retail-portal-profile-binding-action.ts';
import type { RecoverRetailPortalProfileBindingPayload } from '../../shared/actions/recover-retail-portal-profile-binding.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface RecoverRetailPortalProfileBindingActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: RecoverRetailPortalProfileBindingActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: RecoverRetailPortalProfileBindingActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: RecoverRetailPortalProfileBindingActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: RecoverRetailPortalProfileBindingActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRecoverRetailPortalProfileBindingWithAuthorization = (
  payload: RecoverRetailPortalProfileBindingPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(RecoverRetailPortalProfileBindingPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.recoverRetailPortalProfileBindingAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeRecoverRetailPortalProfileBinding = (
  payload: RecoverRetailPortalProfileBindingPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeRecoverRetailPortalProfileBindingWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
