// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RepeatOrderPreparationApi } from '../../shared/apis/repeat-order-preparation.ts';
import type { RepeatOrderPreparationRequest } from '../../shared/apis/repeat-order-preparation.ts';
import { operationGateway } from './action-gateway.ts';

export interface RepeatOrderPreparationClientOptions {
  readonly baseUrl?: string | URL;
}

type RepeatOrderPreparationAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RepeatOrderPreparationClientOptions,
];

type RepeatOrderPreparationOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RepeatOrderPreparationClientOptions,
];

const repeatOrderPreparationClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: RepeatOrderPreparationClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RepeatOrderPreparationApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRepeatOrderPreparationWithAuthorization = (
  payload: RepeatOrderPreparationRequest,
  ...[credential, requestCorrelation, options = {}]: RepeatOrderPreparationAuthorizedInvocation
) =>
  repeatOrderPreparationClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.repeatOrderPreparation.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRepeatOrderPreparation = (
  payload: RepeatOrderPreparationRequest,
  ...[requestCorrelation, options = {}]: RepeatOrderPreparationOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRepeatOrderPreparationWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
