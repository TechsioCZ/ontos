// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug resend-counterparty-access-invitation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  ResendCounterpartyAccessInvitationActionApi,
  ResendCounterpartyAccessInvitationPayloadSchema,
} from '../../shared/apis/resend-counterparty-access-invitation-action.ts';
import type { ResendCounterpartyAccessInvitationPayload } from '../../shared/actions/resend-counterparty-access-invitation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ResendCounterpartyAccessInvitationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ResendCounterpartyAccessInvitationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: ResendCounterpartyAccessInvitationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ResendCounterpartyAccessInvitationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ResendCounterpartyAccessInvitationActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeResendCounterpartyAccessInvitationWithAuthorization = (
  payload: ResendCounterpartyAccessInvitationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ResendCounterpartyAccessInvitationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.resendCounterpartyAccessInvitationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeResendCounterpartyAccessInvitation = (
  payload: ResendCounterpartyAccessInvitationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeResendCounterpartyAccessInvitationWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
