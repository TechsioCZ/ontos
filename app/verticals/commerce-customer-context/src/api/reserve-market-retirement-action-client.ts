// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug reserve-market-retirement
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Match, Redacted, Schema } from 'effect';
import {
  ReserveMarketRetirementActionApi,
  ReserveMarketRetirementPayloadSchema,
} from '../../shared/apis/reserve-market-retirement-action.ts';
import type { ReserveMarketRetirementPayload } from '../../shared/actions/reserve-market-retirement.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ReserveMarketRetirementActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ReserveMarketRetirementActionClientOptions,
];
type OperationInvocation = readonly [requestCorrelation: string, options: ReserveMarketRetirementActionClientOptions];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ReserveMarketRetirementActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ReserveMarketRetirementActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeReserveMarketRetirementWithAuthorization = (
  payload: ReserveMarketRetirementPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ReserveMarketRetirementPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          Match.value(encoded).pipe(
            Match.when({ operation: 'COMMIT' }, (commitPayload) =>
              client.reserveMarketRetirementAction.execute({
                headers: { 'idempotency-key': options.idempotencyKey },
                payload: commitPayload,
              }),
            ),
            Match.when({ operation: 'RELEASE' }, (releasePayload) =>
              client.reserveMarketRetirementAction.execute({
                headers: { 'idempotency-key': options.idempotencyKey },
                payload: releasePayload,
              }),
            ),
            Match.when({ operation: 'RESERVE' }, (reservePayload) =>
              client.reserveMarketRetirementAction.execute({
                headers: { 'idempotency-key': options.idempotencyKey },
                payload: reservePayload,
              }),
            ),
            Match.exhaustive,
          ),
        ),
      ),
    ),
  );

export const executeReserveMarketRetirement = (
  payload: ReserveMarketRetirementPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) => executeReserveMarketRetirementWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
