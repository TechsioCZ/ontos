// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetailAccessDecisionApi } from '../../shared/apis/retail-access-decision.ts';
import type { RetailAccessDecisionRequest } from '../../shared/apis/retail-access-decision.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetailAccessDecisionClientOptions {
  readonly baseUrl?: string | URL;
}

type RetailAccessDecisionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetailAccessDecisionClientOptions,
];

type RetailAccessDecisionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetailAccessDecisionClientOptions,
];

const retailAccessDecisionClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: RetailAccessDecisionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetailAccessDecisionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetailAccessDecisionWithAuthorization = (
  payload: RetailAccessDecisionRequest,
  ...[credential, requestCorrelation, options = {}]: RetailAccessDecisionAuthorizedInvocation
) =>
  retailAccessDecisionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retailAccessDecision.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetailAccessDecision = (
  payload: RetailAccessDecisionRequest,
  ...[requestCorrelation, options = {}]: RetailAccessDecisionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetailAccessDecisionWithAuthorization(payload, credential, requestCorrelation, options),
  );
