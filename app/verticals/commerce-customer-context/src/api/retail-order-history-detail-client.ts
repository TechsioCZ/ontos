// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetailOrderHistoryDetailApi } from '../../shared/apis/retail-order-history-detail.ts';
import type { RetailOrderHistoryDetailRequest } from '../../shared/apis/retail-order-history-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetailOrderHistoryDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type RetailOrderHistoryDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetailOrderHistoryDetailClientOptions,
];

type RetailOrderHistoryDetailOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetailOrderHistoryDetailClientOptions,
];

const retailOrderHistoryDetailClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: RetailOrderHistoryDetailClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetailOrderHistoryDetailApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetailOrderHistoryDetailWithAuthorization = (
  payload: RetailOrderHistoryDetailRequest,
  ...[credential, requestCorrelation, options = {}]: RetailOrderHistoryDetailAuthorizedInvocation
) =>
  retailOrderHistoryDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retailOrderHistoryDetail.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetailOrderHistoryDetail = (
  payload: RetailOrderHistoryDetailRequest,
  ...[requestCorrelation, options = {}]: RetailOrderHistoryDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetailOrderHistoryDetailWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
