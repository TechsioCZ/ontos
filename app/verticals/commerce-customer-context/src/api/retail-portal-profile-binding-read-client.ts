// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetailPortalProfileBindingReadApi } from '../../shared/apis/retail-portal-profile-binding-read.ts';
import type { RetailPortalProfileBindingReadRequest } from '../../shared/apis/retail-portal-profile-binding-read.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetailPortalProfileBindingReadClientOptions {
  readonly baseUrl?: string | URL;
}

type RetailPortalProfileBindingReadAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetailPortalProfileBindingReadClientOptions,
];

type RetailPortalProfileBindingReadOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetailPortalProfileBindingReadClientOptions,
];

const retailPortalProfileBindingReadClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: RetailPortalProfileBindingReadClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetailPortalProfileBindingReadApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetailPortalProfileBindingReadWithAuthorization = (
  payload: RetailPortalProfileBindingReadRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: RetailPortalProfileBindingReadAuthorizedInvocation
) =>
  retailPortalProfileBindingReadClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retailPortalProfileBindingRead.execute({
        headers: {},
        params: {},
        payload,
        query: {},
      }),
    ),
  );

export const executeRetailPortalProfileBindingRead = (
  payload: RetailPortalProfileBindingReadRequest,
  ...[requestCorrelation, options = {}]: RetailPortalProfileBindingReadOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetailPortalProfileBindingReadWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
