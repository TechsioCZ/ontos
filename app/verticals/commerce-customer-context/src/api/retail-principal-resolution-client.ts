// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { RetailPrincipalResolutionApi } from '../../shared/apis/retail-principal-resolution.ts';
import type { RetailPrincipalResolutionRequest } from '../../shared/apis/retail-principal-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface RetailPrincipalResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type RetailPrincipalResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: RetailPrincipalResolutionClientOptions,
];

type RetailPrincipalResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: RetailPrincipalResolutionClientOptions,
];

const retailPrincipalResolutionClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: RetailPrincipalResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: RetailPrincipalResolutionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRetailPrincipalResolutionWithAuthorization = (
  payload: RetailPrincipalResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: RetailPrincipalResolutionAuthorizedInvocation
) =>
  retailPrincipalResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.retailPrincipalResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeRetailPrincipalResolution = (
  payload: RetailPrincipalResolutionRequest,
  ...[requestCorrelation, options = {}]: RetailPrincipalResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeRetailPrincipalResolutionWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
