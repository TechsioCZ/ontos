// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.customer-context
// @ontos-action-http-slug revoke-counterparty-access-invitation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  RevokeCounterpartyAccessInvitationActionApi,
  RevokeCounterpartyAccessInvitationPayloadSchema,
} from '../../shared/apis/revoke-counterparty-access-invitation-action.ts';
import type { RevokeCounterpartyAccessInvitationPayload } from '../../shared/actions/revoke-counterparty-access-invitation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface RevokeCounterpartyAccessInvitationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: RevokeCounterpartyAccessInvitationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: RevokeCounterpartyAccessInvitationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: RevokeCounterpartyAccessInvitationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: RevokeCounterpartyAccessInvitationActionApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeRevokeCounterpartyAccessInvitationWithAuthorization = (
  payload: RevokeCounterpartyAccessInvitationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(RevokeCounterpartyAccessInvitationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.revokeCounterpartyAccessInvitationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeRevokeCounterpartyAccessInvitation = (
  payload: RevokeCounterpartyAccessInvitationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeRevokeCounterpartyAccessInvitationWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
