// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SavedAddressDefaultsApi } from '../../shared/apis/saved-address-defaults.ts';
import type { SavedAddressDefaultsRequest } from '../../shared/apis/saved-address-defaults.ts';
import { operationGateway } from './action-gateway.ts';

export interface SavedAddressDefaultsClientOptions {
  readonly baseUrl?: string | URL;
}

type SavedAddressDefaultsAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SavedAddressDefaultsClientOptions,
];

type SavedAddressDefaultsOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SavedAddressDefaultsClientOptions,
];

const savedAddressDefaultsClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: SavedAddressDefaultsClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SavedAddressDefaultsApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeSavedAddressDefaultsWithAuthorization = (
  payload: SavedAddressDefaultsRequest,
  ...[credential, requestCorrelation, options = {}]: SavedAddressDefaultsAuthorizedInvocation
) =>
  savedAddressDefaultsClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.savedAddressDefaults.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeSavedAddressDefaults = (
  payload: SavedAddressDefaultsRequest,
  ...[requestCorrelation, options = {}]: SavedAddressDefaultsOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSavedAddressDefaultsWithAuthorization(payload, credential, requestCorrelation, options),
  );
