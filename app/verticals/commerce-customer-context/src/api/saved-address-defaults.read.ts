// @generated by OntOS Codesmith module-api v1
import { defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import type { ReadHandlerContext, ReadHandlerUnavailable } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  SavedAddressDefaultsRequestSchema,
  SavedAddressDefaultsResponseSchema,
} from '../../shared/apis/saved-address-defaults.ts';
import type {
  SavedAddressDefaultsRequest,
  SavedAddressDefaultsResponse,
} from '../../shared/apis/saved-address-defaults.ts';
import { profileTarget } from './address-read-support.ts';
import { addressReadServicesForTransaction } from '../persistence/address-persistence.ts';

export interface SavedAddressDefaultsServices {
  readonly defaults: (
    input: SavedAddressDefaultsRequest,
    tenantId: string,
  ) => Effect.Effect<SavedAddressDefaultsResponse, ReadHandlerUnavailable>;
}
export const savedAddressDefaultsEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'authenticated_principal' },
  entrypointKey: 'commerce.customer-context.api.saved-address-defaults',
  moduleKey: 'commerce.customer-context',
  role: 'api',
});
export const savedAddressDefaultsRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: savedAddressDefaultsEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.customer-context.api.saved-address-defaults.evidence.v1',
    },
    inputSchema: SavedAddressDefaultsRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'commerce.customer-context',
    permissionTarget: 'business_permission',
    policies: [],
    readKey: 'commerce.customer-context.api.saved-address-defaults',
    resultSchema: SavedAddressDefaultsResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<SavedAddressDefaultsServices>) =>
    context.services
      .defaults(input, context.scope.tenantId)
      .pipe(Effect.map((result) => ({ evidence: { resultCount: 1 }, result }))),
  (transaction, scope) =>
    Effect.succeed<SavedAddressDefaultsServices>(
      addressReadServicesForTransaction(transaction, scope),
    ),
  profileTarget,
);
