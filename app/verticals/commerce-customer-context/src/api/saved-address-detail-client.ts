// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SavedAddressDetailApi } from '../../shared/apis/saved-address-detail.ts';
import type { SavedAddressDetailRequest } from '../../shared/apis/saved-address-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface SavedAddressDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type SavedAddressDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SavedAddressDetailClientOptions,
];

type SavedAddressDetailOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SavedAddressDetailClientOptions,
];

const savedAddressDetailClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: SavedAddressDetailClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SavedAddressDetailApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeSavedAddressDetailWithAuthorization = (
  payload: SavedAddressDetailRequest,
  ...[credential, requestCorrelation, options = {}]: SavedAddressDetailAuthorizedInvocation
) =>
  savedAddressDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.savedAddressDetail.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeSavedAddressDetail = (
  payload: SavedAddressDetailRequest,
  ...[requestCorrelation, options = {}]: SavedAddressDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSavedAddressDetailWithAuthorization(payload, credential, requestCorrelation, options),
  );
