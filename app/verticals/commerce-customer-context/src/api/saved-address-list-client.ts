// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { SavedAddressListApi } from '../../shared/apis/saved-address-list.ts';
import type { SavedAddressListRequest } from '../../shared/apis/saved-address-list.ts';
import { operationGateway } from './action-gateway.ts';

export interface SavedAddressListClientOptions {
  readonly baseUrl?: string | URL;
}

type SavedAddressListAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: SavedAddressListClientOptions,
];

type SavedAddressListOperationInvocation = readonly [
  requestCorrelation: string,
  options?: SavedAddressListClientOptions,
];

const savedAddressListClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: SavedAddressListClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: SavedAddressListApi,
      credential,
      defaultApiPrefix: '/commerce-customer-context-api',
      requestCorrelation,
    },
    options,
  );

export const executeSavedAddressListWithAuthorization = (
  payload: SavedAddressListRequest,
  ...[credential, requestCorrelation, options = {}]: SavedAddressListAuthorizedInvocation
) =>
  savedAddressListClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.savedAddressList.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeSavedAddressList = (
  payload: SavedAddressListRequest,
  ...[requestCorrelation, options = {}]: SavedAddressListOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeSavedAddressListWithAuthorization(payload, credential, requestCorrelation, options),
  );
