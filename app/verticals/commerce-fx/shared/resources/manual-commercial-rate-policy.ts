// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.fx
// @ontos-resource-slug manual-commercial-rate-policy
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const checkedResourceId = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
const ResourceIdSchema = checkedResourceId.pipe(
  Schema.brand('ManualCommercialRatePolicyResourceId'),
  Schema.decodeTo(checkedResourceId),
);
const checkedTenantId = Schema.String.check(Schema.isUUID());
const TenantIdSchema = checkedTenantId.pipe(
  Schema.brand('ManualCommercialRatePolicyTenantId'),
  Schema.decodeTo(checkedTenantId),
);

export const ManualCommercialRatePolicyRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.fx'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.fx.manual-commercial-rate-policy'),
  tenantId: TenantIdSchema,
});
export type ManualCommercialRatePolicyRef = typeof ManualCommercialRatePolicyRefSchema.Type;

export const manualCommercialRatePolicyResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Manual Commercial Rate Policy resource.',
  key: 'commerce.fx.manual-commercial-rate-policy',
  label: 'Manual Commercial Rate Policy',
  owningModuleId: 'commerce.fx',
} as const satisfies OntosResourceType;
