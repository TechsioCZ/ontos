// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.fx
// @ontos-action-http-slug change-manual-commercial-rate-policy
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  ChangeManualCommercialRatePolicyActionApi,
  ChangeManualCommercialRatePolicyPayloadSchema,
} from '../../shared/apis/change-manual-commercial-rate-policy-action.ts';
import type { ChangeManualCommercialRatePolicyPayload } from '../../shared/actions/change-manual-commercial-rate-policy.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface ChangeManualCommercialRatePolicyActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: ChangeManualCommercialRatePolicyActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: ChangeManualCommercialRatePolicyActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: ChangeManualCommercialRatePolicyActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: ChangeManualCommercialRatePolicyActionApi,
      credential,
      defaultApiPrefix: '/commerce-fx-api',
      requestCorrelation,
    },
    options,
  );

export const executeChangeManualCommercialRatePolicyWithAuthorization = (
  payload: ChangeManualCommercialRatePolicyPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(ChangeManualCommercialRatePolicyPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.changeManualCommercialRatePolicyAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeChangeManualCommercialRatePolicy = (
  payload: ChangeManualCommercialRatePolicyPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeChangeManualCommercialRatePolicyWithAuthorization(
        payload,
        credential,
        requestCorrelation,
        options,
      ),
    options.gateway,
  );
