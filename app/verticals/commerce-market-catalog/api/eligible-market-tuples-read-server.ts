// @generated by OntOS Codesmith module-api v1
import { makeGovernedReadHttpHandler } from '@app/core-runtime/http/governed-read';
import { makeGovernedReadProblems } from '@app/shared-contracts/server/effect-bff-runtime';
import { HttpApiBuilder } from '@modern-js/bff-effect/effect-edge';
import { commerceMarketCatalogApi } from '../shared/api.ts';
import {
  EligibleMarketTuplesAuthenticationProblemSchema,
  EligibleMarketTuplesForbiddenProblemSchema,
  EligibleMarketTuplesInternalProblemSchema,
  EligibleMarketTuplesInvalidProblemSchema,
  EligibleMarketTuplesNotFoundProblemSchema,
  EligibleMarketTuplesPolicyConflictProblemSchema,
  EligibleMarketTuplesPolicyProblemSchema,
  EligibleMarketTuplesUnavailableProblemSchema,
} from '../shared/apis/eligible-market-tuples.ts';
import { eligibleMarketTuplesRead } from '../src/api/eligible-market-tuples.read.ts';
import { authenticateOperationPrincipal } from './auth/action-principal.ts';

const problems = makeGovernedReadProblems({
  authentication: EligibleMarketTuplesAuthenticationProblemSchema,
  forbidden: EligibleMarketTuplesForbiddenProblemSchema,
  internal: EligibleMarketTuplesInternalProblemSchema,
  invalid: EligibleMarketTuplesInvalidProblemSchema,
  notFound: EligibleMarketTuplesNotFoundProblemSchema,
  policyConflict: EligibleMarketTuplesPolicyConflictProblemSchema,
  policyIneligible: EligibleMarketTuplesPolicyProblemSchema,
  unavailable: EligibleMarketTuplesUnavailableProblemSchema,
});

export const eligibleMarketTuplesReadApiLive = HttpApiBuilder.group(
  commerceMarketCatalogApi,
  'eligibleMarketTuples',
  (handlers) =>
    handlers.handle(
      'execute',
      makeGovernedReadHttpHandler({
        authenticatePrincipal: authenticateOperationPrincipal,
        problems,
        registration: eligibleMarketTuplesRead,
      }),
    ),
);
