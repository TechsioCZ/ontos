// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.market-catalog
// @ontos-resource-slug market-catalog-root
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300)).pipe(
  Schema.brand('CommerceMarketCatalogRootResourceId'),
  Schema.decodeTo(Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300))),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID()).pipe(
  Schema.brand('CommerceMarketCatalogRootTenantId'),
  Schema.decodeTo(Schema.String.check(Schema.isUUID())),
);

export const MarketCatalogRootRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.market-catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('commerce.market-catalog.market-catalog-root'),
  tenantId: TenantIdSchema,
});
export type MarketCatalogRootRef = typeof MarketCatalogRootRefSchema.Type;

export const marketCatalogRootResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Market Catalog Root resource.',
  key: 'commerce.market-catalog.market-catalog-root',
  label: 'Market Catalog Root',
  owningModuleId: 'commerce.market-catalog',
} as const satisfies OntosResourceType;
