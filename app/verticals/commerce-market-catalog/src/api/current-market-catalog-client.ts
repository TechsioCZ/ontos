// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CurrentMarketCatalogApi } from '../../shared/apis/current-market-catalog.ts';
import type { CurrentMarketCatalogRequest } from '../../shared/apis/current-market-catalog.ts';
import { operationGateway } from './action-gateway.ts';

export interface CurrentMarketCatalogClientOptions {
  readonly baseUrl?: string | URL;
}

type CurrentMarketCatalogAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CurrentMarketCatalogClientOptions,
];

type CurrentMarketCatalogOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CurrentMarketCatalogClientOptions,
];

const currentMarketCatalogClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CurrentMarketCatalogClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CurrentMarketCatalogApi,
      credential,
      defaultApiPrefix: '/commerce-market-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeCurrentMarketCatalogWithAuthorization = (
  payload: CurrentMarketCatalogRequest,
  ...[credential, requestCorrelation, options = {}]: CurrentMarketCatalogAuthorizedInvocation
) =>
  currentMarketCatalogClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.currentMarketCatalog.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeCurrentMarketCatalog = (
  payload: CurrentMarketCatalogRequest,
  ...[requestCorrelation, options = {}]: CurrentMarketCatalogOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCurrentMarketCatalogWithAuthorization(payload, credential, requestCorrelation, options),
  );
