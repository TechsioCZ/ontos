// @generated by OntOS Codesmith module-api v1
import type { ReadHandlerContext } from '@app/core-runtime';
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  CurrentMarketCatalogRequestSchema,
  CurrentMarketCatalogResponseSchema,
} from '../../shared/apis/current-market-catalog.ts';
import type { CurrentMarketCatalogRequest } from '../../shared/apis/current-market-catalog.ts';
import type { MarketCatalogReadPersistence } from '../persistence/market-catalog-read-persistence.ts';
import { marketCatalogReadPersistenceForScope } from '../persistence/market-catalog-read-persistence.ts';

const commerceMarketCatalogModuleKey = 'commerce.market-catalog';

const currentMarketCatalogEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'module.access' },
  entrypointKey: 'commerce.market-catalog.api.current-market-catalog',
  moduleKey: commerceMarketCatalogModuleKey,
  role: 'api',
});

const unavailable = (cause: unknown) => {
  const failure = new ReadHandlerUnavailable({
    code: 'read_handler_unavailable',
    reason: 'Current Commerce Market Catalog is temporarily unavailable',
  });
  return Object.defineProperty(failure, 'cause', { configurable: true, value: cause });
};

export const handleCurrentMarketCatalog = Effect.fn('CurrentMarketCatalogRead.handle')(
  function* handleCurrentMarketCatalogEffect(
    input: CurrentMarketCatalogRequest,
    context: ReadHandlerContext<MarketCatalogReadPersistence>,
  ) {
    const result = yield* context.services.current(input).pipe(Effect.mapError(unavailable));
    return { evidence: { resultCount: result.markets.length + result.associations.length }, result };
  },
);

export const currentMarketCatalogRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: currentMarketCatalogEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'commerce.market-catalog.api.current-market-catalog.evidence.v1',
    },
    inputSchema: CurrentMarketCatalogRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: commerceMarketCatalogModuleKey,
    permissionTarget: 'module',
    policies: [],
    readKey: 'commerce.market-catalog.api.current-market-catalog',
    resultSchema: CurrentMarketCatalogResponseSchema,
    schemaVersion: '1',
  },
  handleCurrentMarketCatalog,
  (transaction, scope) => marketCatalogReadPersistenceForScope(transaction, scope),
  () => ({ kind: 'module', moduleId: commerceMarketCatalogModuleKey }),
);
