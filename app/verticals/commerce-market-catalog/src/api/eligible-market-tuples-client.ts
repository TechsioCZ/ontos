// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { EligibleMarketTuplesApi } from '../../shared/apis/eligible-market-tuples.ts';
import type { EligibleMarketTuplesRequest } from '../../shared/apis/eligible-market-tuples.ts';
import { operationGateway } from './action-gateway.ts';

export interface EligibleMarketTuplesClientOptions {
  readonly baseUrl?: string | URL;
}

type EligibleMarketTuplesAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: EligibleMarketTuplesClientOptions,
];

type EligibleMarketTuplesOperationInvocation = readonly [
  requestCorrelation: string,
  options?: EligibleMarketTuplesClientOptions,
];

const eligibleMarketTuplesClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: EligibleMarketTuplesClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: EligibleMarketTuplesApi,
      credential,
      defaultApiPrefix: '/commerce-market-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeEligibleMarketTuplesWithAuthorization = (
  payload: EligibleMarketTuplesRequest,
  ...[credential, requestCorrelation, options = {}]: EligibleMarketTuplesAuthorizedInvocation
) =>
  eligibleMarketTuplesClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.eligibleMarketTuples.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeEligibleMarketTuples = (
  payload: EligibleMarketTuplesRequest,
  ...[requestCorrelation, options = {}]: EligibleMarketTuplesOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeEligibleMarketTuplesWithAuthorization(payload, credential, requestCorrelation, options),
  );
