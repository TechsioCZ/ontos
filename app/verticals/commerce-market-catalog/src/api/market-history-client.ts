// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { MarketHistoryApi } from '../../shared/apis/market-history.ts';
import type { MarketHistoryRequest } from '../../shared/apis/market-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface MarketHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type MarketHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: MarketHistoryClientOptions,
];

type MarketHistoryOperationInvocation = readonly [requestCorrelation: string, options?: MarketHistoryClientOptions];

const marketHistoryClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: MarketHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: MarketHistoryApi,
      credential,
      defaultApiPrefix: '/commerce-market-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeMarketHistoryWithAuthorization = (
  payload: MarketHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: MarketHistoryAuthorizedInvocation
) =>
  marketHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.marketHistory.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeMarketHistory = (
  payload: MarketHistoryRequest,
  ...[requestCorrelation, options = {}]: MarketHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeMarketHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
