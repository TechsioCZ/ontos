// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ResolveCommerceMarketApi } from '../../shared/apis/resolve-commerce-market.ts';
import type { ResolveCommerceMarketRequest } from '../../shared/apis/resolve-commerce-market.ts';
import { operationGateway } from './action-gateway.ts';

export interface ResolveCommerceMarketClientOptions {
  readonly baseUrl?: string | URL;
}

type ResolveCommerceMarketAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ResolveCommerceMarketClientOptions,
];

type ResolveCommerceMarketOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ResolveCommerceMarketClientOptions,
];

const resolveCommerceMarketClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ResolveCommerceMarketClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ResolveCommerceMarketApi,
      credential,
      defaultApiPrefix: '/commerce-market-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executeResolveCommerceMarketWithAuthorization = (
  payload: ResolveCommerceMarketRequest,
  ...[credential, requestCorrelation, options = {}]: ResolveCommerceMarketAuthorizedInvocation
) =>
  resolveCommerceMarketClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.resolveCommerceMarket.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeResolveCommerceMarket = (
  payload: ResolveCommerceMarketRequest,
  ...[requestCorrelation, options = {}]: ResolveCommerceMarketOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeResolveCommerceMarketWithAuthorization(payload, credential, requestCorrelation, options),
  );
