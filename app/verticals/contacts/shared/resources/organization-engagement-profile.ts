// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner contacts.core
// @ontos-resource-slug organization-engagement-profile
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
const TenantIdSchema = Schema.String.check(Schema.isUUID());

export const OrganizationEngagementProfileRefSchema = Schema.Struct({
  moduleId: Schema.Literal('contacts.core'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('contacts.core.organization-engagement-profile'),
  tenantId: TenantIdSchema,
});
export type OrganizationEngagementProfileRef = typeof OrganizationEngagementProfileRefSchema.Type;

export const organizationEngagementProfileResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Organization Engagement Profile resource.',
  key: 'contacts.core.organization-engagement-profile',
  label: 'Organization Engagement Profile',
  owningModuleId: 'contacts.core',
} as const satisfies OntosResourceType;
