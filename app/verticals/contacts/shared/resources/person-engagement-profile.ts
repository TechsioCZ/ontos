// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner contacts.core
// @ontos-resource-slug person-engagement-profile
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
const TenantIdSchema = Schema.String.check(Schema.isUUID());

export const PersonEngagementProfileRefSchema = Schema.Struct({
  moduleId: Schema.Literal('contacts.core'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('contacts.core.person-engagement-profile'),
  tenantId: TenantIdSchema,
});
export type PersonEngagementProfileRef = typeof PersonEngagementProfileRefSchema.Type;

export const personEngagementProfileResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Person Engagement Profile resource.',
  key: 'contacts.core.person-engagement-profile',
  label: 'Person Engagement Profile',
  owningModuleId: 'contacts.core',
} as const satisfies OntosResourceType;
