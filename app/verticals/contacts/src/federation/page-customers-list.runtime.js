// Keep the generated Contacts route in the runtime graph while the adjacent declaration
// preserves the generated src-only Module Federation DTS boundary.
export { default } from '../routes/[lang]/contacts/customers/page.tsx';
