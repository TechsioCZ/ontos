// @generated by OntOS Codesmith Resource Provider v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { ContactDetailApi } from '../../shared/apis/contact-detail.ts';
import type { ContactDetailRequest } from '../../shared/apis/contact-detail.ts';
import { operationGateway } from './action-gateway.ts';

export const executeContactDetailWithAuthorization = (
  payload: ContactDetailRequest,
  authorization: string,
  correlationId: string,
) =>
  makeEffectHttpApiClient(ContactDetailApi, {
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(Effect.flatMap((client) => client.contactDetail.execute({ payload })));

export const executeContactDetail = (payload: ContactDetailRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeContactDetailWithAuthorization(payload, authorization, correlationId),
  );
