// @generated by OntOS Codesmith Action v1
// @ontos-action-slug create-contact
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { CreateContactActionApi } from '../../shared/apis/create-contact-action.ts';
import type { CreateContactPayload } from '../../shared/apis/create-contact-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateContactActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeCreateContactActionWithAuthorization = (
  payload: CreateContactPayload,
  authorization: string,
  options: CreateContactActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    CreateContactActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.createContactActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteCreateContactAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: CreateContactPayload, options: CreateContactActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeCreateContactActionWithAuthorization(payload, authorization, options),
    );

export const executeCreateContactAction = makeExecuteCreateContactAction();
