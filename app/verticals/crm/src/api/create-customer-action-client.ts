// @generated by OntOS Codesmith Action v1
// @ontos-action-slug create-customer
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { CreateCustomerActionApi } from '../../shared/apis/create-customer-action.ts';
import type { CreateCustomerPayload } from '../../shared/apis/create-customer-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateCustomerActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeCreateCustomerActionWithAuthorization = (
  payload: CreateCustomerPayload,
  authorization: string,
  options: CreateCustomerActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    CreateCustomerActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.createCustomerActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteCreateCustomerAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: CreateCustomerPayload, options: CreateCustomerActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeCreateCustomerActionWithAuthorization(payload, authorization, options),
    );

export const executeCreateCustomerAction = makeExecuteCreateCustomerAction();
