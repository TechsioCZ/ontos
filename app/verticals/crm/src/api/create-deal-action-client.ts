// @generated by OntOS Codesmith Action v1
// @ontos-action-slug create-deal
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { CreateDealActionApi } from '../../shared/apis/create-deal-action.ts';
import type { CreateDealPayload } from '../../shared/apis/create-deal-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface CreateDealActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeCreateDealActionWithAuthorization = (
  payload: CreateDealPayload,
  authorization: string,
  options: CreateDealActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    CreateDealActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.createDealActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteCreateDealAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: CreateDealPayload, options: CreateDealActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeCreateDealActionWithAuthorization(payload, authorization, options),
    );

export const executeCreateDealAction = makeExecuteCreateDealAction();
