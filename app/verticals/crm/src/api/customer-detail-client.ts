// @generated by OntOS Codesmith Resource Provider v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { CustomerDetailApi } from '../../shared/apis/customer-detail.ts';
import type { CustomerDetailRequest } from '../../shared/apis/customer-detail.ts';
import { operationGateway } from './action-gateway.ts';

export const executeCustomerDetailWithAuthorization = (
  payload: CustomerDetailRequest,
  authorization: string,
  correlationId: string,
) =>
  makeEffectHttpApiClient(CustomerDetailApi, {
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(Effect.flatMap((client) => client.customerDetail.execute({ payload })));

export const executeCustomerDetail = (payload: CustomerDetailRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeCustomerDetailWithAuthorization(payload, authorization, correlationId),
  );
