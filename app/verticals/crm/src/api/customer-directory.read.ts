// @generated by OntOS Codesmith module-api v1
import { defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import type { ReadHandlerContext } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  CustomerDirectoryDetailRequestSchema,
  CustomerDirectoryDetailResponseSchema,
  CustomerDirectoryListRequestSchema,
  CustomerDirectoryListResponseSchema,
} from '../../shared/apis/customer-directory.ts';
import { makeCustomerService } from '../customers/customer-service.ts';
import type { CustomerService } from '../customers/customer-service.ts';

export const customerDirectoryEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  entrypointKey: 'crm.core.api.customer-directory',
  moduleKey: 'crm.core',
  role: 'api',
});

const customerServiceFactory = (
  transaction: Parameters<typeof makeCustomerService>[0],
  scope: { readonly tenantId: string },
) => Effect.succeed(makeCustomerService(transaction, scope.tenantId));

export const customerDirectoryListRead = defineRead(
  {
    accessKind: 'list',
    entrypoint: customerDirectoryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'crm.core.api.customer-directory.evidence.v1',
    },
    inputSchema: CustomerDirectoryListRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'crm.core',
    permissionTarget: 'module',
    policies: [],
    readKey: 'crm.core.api.customer-directory.list',
    resultSchema: CustomerDirectoryListResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<CustomerService>) =>
    context.services
      .listCustomers(input.limit, input.cursor)
      .pipe(Effect.map((result) => ({ evidence: { resultCount: result.items.length }, result }))),
  customerServiceFactory,
  () => ({ kind: 'module', moduleId: 'crm.core' }),
);

export const customerDirectoryDetailRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: customerDirectoryEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'crm.core.api.customer-directory.detail.evidence.v1',
    },
    inputSchema: CustomerDirectoryDetailRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'crm.core',
    permissionTarget: 'resource',
    policies: [],
    readKey: 'crm.core.api.customer-directory.detail',
    resultSchema: CustomerDirectoryDetailResponseSchema,
    schemaVersion: '1',
  },
  (input, context: ReadHandlerContext<CustomerService>) =>
    context.services.getCustomer(input.customerId).pipe(
      Effect.map((customer) => ({
        evidence: { resultCount: 1 },
        result: { customer, operation: 'detail' as const },
      })),
    ),
  customerServiceFactory,
  ({ customerId }) => ({
    kind: 'resource',
    resource: { moduleId: 'crm.core', resourceId: customerId, resourceType: 'crm.core.customer' },
  }),
);
