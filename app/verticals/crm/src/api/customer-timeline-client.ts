// @generated by OntOS Codesmith Resource Provider v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { CustomerTimelineApi } from '../../shared/apis/customer-timeline.ts';
import type { CustomerTimelineRequest } from '../../shared/apis/customer-timeline.ts';
import { operationGateway } from './action-gateway.ts';

export const executeCustomerTimelineWithAuthorization = (
  payload: CustomerTimelineRequest,
  authorization: string,
  correlationId: string,
) =>
  makeEffectHttpApiClient(CustomerTimelineApi, {
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(Effect.flatMap((client) => client.customerTimeline.execute({ payload })));

export const executeCustomerTimeline = (payload: CustomerTimelineRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeCustomerTimelineWithAuthorization(payload, authorization, correlationId),
  );
