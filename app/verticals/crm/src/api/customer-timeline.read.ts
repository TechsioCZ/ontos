// @generated by OntOS Codesmith Resource Provider v1
import {
  ReadHandlerUnavailable,
  defineRead,
  defineTenantModuleEntrypoint,
} from '@app/core-runtime';
import { Effect } from 'effect';
import {
  CustomerTimelineRequestSchema,
  CustomerTimelineResponseSchema,
} from '../../shared/apis/customer-timeline.ts';

export const customerTimelineEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  entrypointKey: 'crm.core.api.customer-timeline',
  moduleKey: 'crm.core',
  role: 'api',
});

export const customerTimelineRead = defineRead(
  {
    accessKind: 'list',
    entrypoint: customerTimelineEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'crm.core.api.customer-timeline.evidence.v1',
    },
    inputSchema: CustomerTimelineRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'crm.core',
    permissionTarget: 'resource',
    policies: [],
    readKey: 'crm.core.api.customer-timeline',
    resultSchema: CustomerTimelineResponseSchema,
    schemaVersion: '1',
  },
  () =>
    Effect.fail(
      new ReadHandlerUnavailable({
        code: 'read_handler_unavailable',
        reason: 'The generated Customer timeline provider has no business implementation',
      }),
    ),
  () => Effect.succeed({}),
  ({ moduleId, resourceId, resourceType }) => ({
    kind: 'resource',
    resource: { moduleId, resourceId, resourceType },
  }),
);
