// @generated by OntOS Codesmith Resource Provider v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { DealDetailApi } from '../../shared/apis/deal-detail.ts';
import type { DealDetailRequest } from '../../shared/apis/deal-detail.ts';
import { operationGateway } from './action-gateway.ts';

export const executeDealDetailWithAuthorization = (
  payload: DealDetailRequest,
  authorization: string,
  correlationId: string,
) =>
  makeEffectHttpApiClient(DealDetailApi, {
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(Effect.flatMap((client) => client.dealDetail.execute({ payload })));

export const executeDealDetail = (payload: DealDetailRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeDealDetailWithAuthorization(payload, authorization, correlationId),
  );
