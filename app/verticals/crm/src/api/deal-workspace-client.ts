// @generated by OntOS Codesmith module-api v1
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { DealWorkspaceApi } from '../../shared/apis/deal-workspace.ts';
import type { DealWorkspaceRequest } from '../../shared/apis/deal-workspace.ts';
import { operationGateway } from './action-gateway.ts';

export const executeDealWorkspaceWithAuthorization = (
  payload: DealWorkspaceRequest,
  authorization: string,
  correlationId: string,
  baseUrl?: string,
) =>
  makeEffectHttpApiClient(DealWorkspaceApi, baseUrl === undefined ? {} : { baseUrl }).pipe(
    Effect.flatMap((client) => {
      const headers = { authorization, 'x-correlation-id': correlationId };
      // The generated client exposes one overload per discriminated request variant.
      return payload.operation === 'list'
        ? client.dealWorkspaceReads.execute({ headers, payload })
        : client.dealWorkspaceReads.execute({ headers, payload });
    }),
  );

export const executeDealWorkspace = (payload: DealWorkspaceRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeDealWorkspaceWithAuthorization(payload, authorization, correlationId),
  );
