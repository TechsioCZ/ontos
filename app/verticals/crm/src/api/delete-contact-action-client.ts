// @generated by OntOS Codesmith Action v1
// @ontos-action-slug delete-contact
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { DeleteContactActionApi } from '../../shared/apis/delete-contact-action.ts';
import type { DeleteContactPayload } from '../../shared/apis/delete-contact-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface DeleteContactActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeDeleteContactActionWithAuthorization = (
  payload: DeleteContactPayload,
  authorization: string,
  options: DeleteContactActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    DeleteContactActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.deleteContactActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteDeleteContactAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: DeleteContactPayload, options: DeleteContactActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeDeleteContactActionWithAuthorization(payload, authorization, options),
    );

export const executeDeleteContactAction = makeExecuteDeleteContactAction();
