// @generated by OntOS Codesmith Action v1
// @ontos-action-slug delete-customer
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { DeleteCustomerActionApi } from '../../shared/apis/delete-customer-action.ts';
import type { DeleteCustomerPayload } from '../../shared/apis/delete-customer-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface DeleteCustomerActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeDeleteCustomerActionWithAuthorization = (
  payload: DeleteCustomerPayload,
  authorization: string,
  options: DeleteCustomerActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    DeleteCustomerActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.deleteCustomerActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteDeleteCustomerAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: DeleteCustomerPayload, options: DeleteCustomerActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeDeleteCustomerActionWithAuthorization(payload, authorization, options),
    );

export const executeDeleteCustomerAction = makeExecuteDeleteCustomerAction();
