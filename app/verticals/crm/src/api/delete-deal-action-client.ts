// @generated by OntOS Codesmith Action v1
// @ontos-action-slug delete-deal
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { DeleteDealActionApi } from '../../shared/apis/delete-deal-action.ts';
import type { DeleteDealPayload } from '../../shared/apis/delete-deal-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface DeleteDealActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeDeleteDealActionWithAuthorization = (
  payload: DeleteDealPayload,
  authorization: string,
  options: DeleteDealActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    DeleteDealActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.deleteDealActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteDeleteDealAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: DeleteDealPayload, options: DeleteDealActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeDeleteDealActionWithAuthorization(payload, authorization, options),
    );

export const executeDeleteDealAction = makeExecuteDeleteDealAction();
