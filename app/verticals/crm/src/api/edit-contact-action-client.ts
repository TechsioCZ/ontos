// @generated by OntOS Codesmith Action v1
// @ontos-action-slug edit-contact
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { EditContactActionApi } from '../../shared/apis/edit-contact-action.ts';
import type { EditContactPayload } from '../../shared/apis/edit-contact-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface EditContactActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeEditContactActionWithAuthorization = (
  payload: EditContactPayload,
  authorization: string,
  options: EditContactActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    EditContactActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.editContactActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteEditContactAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: EditContactPayload, options: EditContactActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeEditContactActionWithAuthorization(payload, authorization, options),
    );

export const executeEditContactAction = makeExecuteEditContactAction();
