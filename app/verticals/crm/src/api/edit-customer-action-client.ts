// @generated by OntOS Codesmith Action v1
// @ontos-action-slug edit-customer
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { EditCustomerActionApi } from '../../shared/apis/edit-customer-action.ts';
import type { EditCustomerPayload } from '../../shared/apis/edit-customer-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface EditCustomerActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeEditCustomerActionWithAuthorization = (
  payload: EditCustomerPayload,
  authorization: string,
  options: EditCustomerActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    EditCustomerActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.editCustomerActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteEditCustomerAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: EditCustomerPayload, options: EditCustomerActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeEditCustomerActionWithAuthorization(payload, authorization, options),
    );

export const executeEditCustomerAction = makeExecuteEditCustomerAction();
