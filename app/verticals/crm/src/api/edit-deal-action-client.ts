// @generated by OntOS Codesmith Action v1
// @ontos-action-slug edit-deal
import { Effect } from 'effect';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { EditDealActionApi } from '../../shared/apis/edit-deal-action.ts';
import type { EditDealPayload } from '../../shared/apis/edit-deal-action.ts';
import { operationGateway } from './action-gateway.ts';

export interface EditDealActionRequestOptions {
  readonly baseUrl?: string;
  readonly correlationId: string;
  readonly idempotencyKey: string;
}

export const executeEditDealActionWithAuthorization = (
  payload: EditDealPayload,
  authorization: string,
  options: EditDealActionRequestOptions,
) =>
  makeEffectHttpApiClient(
    EditDealActionApi,
    options.baseUrl === undefined ? {} : { baseUrl: options.baseUrl },
  ).pipe(
    Effect.flatMap((client) =>
      client.editDealActions.execute({
        headers: {
          authorization,
          'x-correlation-id': options.correlationId,
          'x-idempotency-key': options.idempotencyKey,
        },
        payload,
      }),
    ),
  );

export const makeExecuteEditDealAction =
  (gateway: Pick<typeof operationGateway, 'invoke'> = operationGateway) =>
  (payload: EditDealPayload, options: EditDealActionRequestOptions) =>
    gateway.invoke((authorization) =>
      executeEditDealActionWithAuthorization(payload, authorization, options),
    );

export const executeEditDealAction = makeExecuteEditDealAction();
