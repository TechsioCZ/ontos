// Keep the generated CRM route in the runtime graph while the adjacent declaration
// preserves the generated src-only Module Federation DTS boundary.
export { default } from '../routes/[lang]/crm/customers/page.tsx';
