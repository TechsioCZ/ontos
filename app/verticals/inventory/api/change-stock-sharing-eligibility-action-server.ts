// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug change-stock-sharing-eligibility
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { changeStockSharingEligibilityAction } from '../src/actions/change-stock-sharing-eligibility.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  changeStockSharingEligibilityActionProblem,
  changeStockSharingEligibilityActionSchemaErrorLive,
  mapChangeStockSharingEligibilityActionProblem,
} from './change-stock-sharing-eligibility-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: changeStockSharingEligibilityActionProblem.authentication,
  unavailable: () => changeStockSharingEligibilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ChangeStockSharingEligibilityActionServer.execute')(
  function* executeChangeStockSharingEligibility(
    payload: typeof changeStockSharingEligibilityAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(changeStockSharingEligibilityActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(changeStockSharingEligibilityAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(changeStockSharingEligibilityActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: changeStockSharingEligibilityActionProblem.internal,
      invalidCorrelationProblem: changeStockSharingEligibilityActionProblem.invalid,
      mapError: mapChangeStockSharingEligibilityActionProblem,
      payload: decoded,
      registration: changeStockSharingEligibilityAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const changeStockSharingEligibilityActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'changeStockSharingEligibilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(changeStockSharingEligibilityActionSchemaErrorLive));
