// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug compensate-inventory-pre-commit
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CompensateInventoryPreCommitActionAlreadyCommittedProblemSchema,
  CompensateInventoryPreCommitActionAuthenticationProblemSchema,
  CompensateInventoryPreCommitActionCommitIndeterminateProblemSchema,
  CompensateInventoryPreCommitActionConflictProblemSchema,
  CompensateInventoryPreCommitActionForbiddenProblemSchema,
  CompensateInventoryPreCommitActionIneligibleProblemSchema,
  CompensateInventoryPreCommitActionInternalProblemSchema,
  CompensateInventoryPreCommitActionInvalidProblemSchema,
  CompensateInventoryPreCommitActionNotFoundProblemSchema,
  CompensateInventoryPreCommitActionPreconditionProblemSchema,
  CompensateInventoryPreCommitActionSchemaErrorMiddleware,
  CompensateInventoryPreCommitActionUnavailableProblemSchema,
} from '../shared/apis/compensate-inventory-pre-commit-action.ts';
import type { CompensateInventoryPreCommitActionProblem } from '../shared/apis/compensate-inventory-pre-commit-action.ts';
import { compensateInventoryPreCommitAction } from '../src/actions/compensate-inventory-pre-commit.action.ts';

type DomainError = typeof compensateInventoryPreCommitAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CompensateInventoryPreCommitActionProblem['_tag']> = Extract<
  CompensateInventoryPreCommitActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const compensateInventoryPreCommitActionProblem = {
  authentication: (): ProblemOf<'CompensateInventoryPreCommitActionAuthenticationProblem'> =>
    CompensateInventoryPreCommitActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CompensateInventoryPreCommitActionConflictProblem'>['code'],
  ): ProblemOf<'CompensateInventoryPreCommitActionConflictProblem'> =>
    CompensateInventoryPreCommitActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CompensateInventoryPreCommitActionForbiddenProblem'>['code'],
  ): ProblemOf<'CompensateInventoryPreCommitActionForbiddenProblem'> =>
    CompensateInventoryPreCommitActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CompensateInventoryPreCommitActionIneligibleProblem'>['code'],
  ): ProblemOf<'CompensateInventoryPreCommitActionIneligibleProblem'> =>
    CompensateInventoryPreCommitActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CompensateInventoryPreCommitActionInternalProblem'> =>
    CompensateInventoryPreCommitActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CompensateInventoryPreCommitActionInvalidProblem'> =>
    CompensateInventoryPreCommitActionInvalidProblemSchema.make({
      detail: 'The compensate-inventory-pre-commit Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CompensateInventoryPreCommitActionNotFoundProblem'>['code'],
  ): ProblemOf<'CompensateInventoryPreCommitActionNotFoundProblem'> =>
    CompensateInventoryPreCommitActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CompensateInventoryPreCommitActionPreconditionProblem'> =>
    CompensateInventoryPreCommitActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CompensateInventoryPreCommitActionUnavailableProblem'>['code'],
  ): ProblemOf<'CompensateInventoryPreCommitActionUnavailableProblem'> =>
    CompensateInventoryPreCommitActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CompensateInventoryPreCommitActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      InventoryPreCommitCompensationRejected: () =>
        compensateInventoryPreCommitActionProblem.ineligible('inventory_pre_commit_compensation_rejected'),
      InventoryPreCommitCompensationUnavailable: () =>
        compensateInventoryPreCommitActionProblem.unavailable('inventory_pre_commit_compensation_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CompensateInventoryPreCommitActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CompensateInventoryPreCommitActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: compensateInventoryPreCommitActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CompensateInventoryPreCommitActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: compensateInventoryPreCommitActionProblem.internal,
      ActionIdempotencyKeyRequired: compensateInventoryPreCommitActionProblem.precondition,
      ActionInvocationNotFound: (failure) => compensateInventoryPreCommitActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) =>
        compensateInventoryPreCommitActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => compensateInventoryPreCommitActionProblem.conflict(failure.code),
      ActionPayloadValidationError: compensateInventoryPreCommitActionProblem.invalid,
      ActionPermissionCheckError: (failure) => compensateInventoryPreCommitActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => compensateInventoryPreCommitActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => compensateInventoryPreCommitActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => compensateInventoryPreCommitActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => compensateInventoryPreCommitActionProblem.conflict(failure.code),
      ActionResultValidationError: compensateInventoryPreCommitActionProblem.internal,
      ActionTransactionError: (failure) => compensateInventoryPreCommitActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: compensateInventoryPreCommitActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) =>
        compensateInventoryPreCommitActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => compensateInventoryPreCommitActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: compensateInventoryPreCommitActionProblem.authentication,
      OperationContextDenied: (failure) => compensateInventoryPreCommitActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => compensateInventoryPreCommitActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => compensateInventoryPreCommitActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(compensateInventoryPreCommitAction.descriptor.domainErrorSchema);
export const mapCompensateInventoryPreCommitActionProblem = (
  error: ActionCoreError | DomainError,
): CompensateInventoryPreCommitActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const compensateInventoryPreCommitActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CompensateInventoryPreCommitActionSchemaErrorMiddleware,
  () => Effect.fail(compensateInventoryPreCommitActionProblem.invalid()),
);
