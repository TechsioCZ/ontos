// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug correct-catalog-to-stock-binding
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { correctCatalogToStockBindingAction } from '../src/actions/correct-catalog-to-stock-binding.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  correctCatalogToStockBindingActionProblem,
  correctCatalogToStockBindingActionSchemaErrorLive,
  mapCorrectCatalogToStockBindingActionProblem,
} from './correct-catalog-to-stock-binding-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: correctCatalogToStockBindingActionProblem.authentication,
  unavailable: () => correctCatalogToStockBindingActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CorrectCatalogToStockBindingActionServer.execute')(
  function* executeCorrectCatalogToStockBinding(
    payload: typeof correctCatalogToStockBindingAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(correctCatalogToStockBindingActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(correctCatalogToStockBindingAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(correctCatalogToStockBindingActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: correctCatalogToStockBindingActionProblem.internal,
      invalidCorrelationProblem: correctCatalogToStockBindingActionProblem.invalid,
      mapError: mapCorrectCatalogToStockBindingActionProblem,
      payload: decoded,
      registration: correctCatalogToStockBindingAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const correctCatalogToStockBindingActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'correctCatalogToStockBindingAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(correctCatalogToStockBindingActionSchemaErrorLive));
