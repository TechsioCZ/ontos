// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug correct-external-stock-correlation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { correctExternalStockCorrelationAction } from '../src/actions/correct-external-stock-correlation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  correctExternalStockCorrelationActionProblem,
  correctExternalStockCorrelationActionSchemaErrorLive,
  mapCorrectExternalStockCorrelationActionProblem,
} from './correct-external-stock-correlation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: correctExternalStockCorrelationActionProblem.authentication,
  unavailable: () => correctExternalStockCorrelationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CorrectExternalStockCorrelationActionServer.execute')(
  function* executeCorrectExternalStockCorrelation(
    payload: typeof correctExternalStockCorrelationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(correctExternalStockCorrelationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(correctExternalStockCorrelationAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(correctExternalStockCorrelationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: correctExternalStockCorrelationActionProblem.internal,
      invalidCorrelationProblem: correctExternalStockCorrelationActionProblem.invalid,
      mapError: mapCorrectExternalStockCorrelationActionProblem,
      payload: decoded,
      registration: correctExternalStockCorrelationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const correctExternalStockCorrelationActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'correctExternalStockCorrelationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(correctExternalStockCorrelationActionSchemaErrorLive));
