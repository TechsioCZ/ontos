// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug correct-stock-position
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { correctStockPositionAction } from '../src/actions/correct-stock-position.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  correctStockPositionActionProblem,
  correctStockPositionActionSchemaErrorLive,
  mapCorrectStockPositionActionProblem,
} from './correct-stock-position-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: correctStockPositionActionProblem.authentication,
  unavailable: () => correctStockPositionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CorrectStockPositionActionServer.execute')(function* executeCorrectStockPosition(
  payload: typeof correctStockPositionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(correctStockPositionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(correctStockPositionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(correctStockPositionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: correctStockPositionActionProblem.internal,
    invalidCorrelationProblem: correctStockPositionActionProblem.invalid,
    mapError: mapCorrectStockPositionActionProblem,
    payload: decoded,
    registration: correctStockPositionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const correctStockPositionActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'correctStockPositionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(correctStockPositionActionSchemaErrorLive));
