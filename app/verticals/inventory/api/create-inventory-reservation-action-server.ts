// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug create-inventory-reservation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { createInventoryReservationAction } from '../src/actions/create-inventory-reservation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createInventoryReservationActionProblem,
  createInventoryReservationActionSchemaErrorLive,
  mapCreateInventoryReservationActionProblem,
} from './create-inventory-reservation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createInventoryReservationActionProblem.authentication,
  unavailable: () => createInventoryReservationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateInventoryReservationActionServer.execute')(function* executeCreateInventoryReservation(
  payload: typeof createInventoryReservationAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createInventoryReservationActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createInventoryReservationAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createInventoryReservationActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createInventoryReservationActionProblem.internal,
    invalidCorrelationProblem: createInventoryReservationActionProblem.invalid,
    mapError: mapCreateInventoryReservationActionProblem,
    payload: decoded,
    registration: createInventoryReservationAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createInventoryReservationActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'createInventoryReservationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createInventoryReservationActionSchemaErrorLive));
