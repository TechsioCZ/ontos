// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug end-catalog-to-stock-binding
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { endCatalogToStockBindingAction } from '../src/actions/end-catalog-to-stock-binding.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  endCatalogToStockBindingActionProblem,
  endCatalogToStockBindingActionSchemaErrorLive,
  mapEndCatalogToStockBindingActionProblem,
} from './end-catalog-to-stock-binding-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: endCatalogToStockBindingActionProblem.authentication,
  unavailable: () => endCatalogToStockBindingActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('EndCatalogToStockBindingActionServer.execute')(function* executeEndCatalogToStockBinding(
  payload: typeof endCatalogToStockBindingAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(endCatalogToStockBindingActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(endCatalogToStockBindingAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(endCatalogToStockBindingActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: endCatalogToStockBindingActionProblem.internal,
    invalidCorrelationProblem: endCatalogToStockBindingActionProblem.invalid,
    mapError: mapEndCatalogToStockBindingActionProblem,
    payload: decoded,
    registration: endCatalogToStockBindingAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const endCatalogToStockBindingActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'endCatalogToStockBindingAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(endCatalogToStockBindingActionSchemaErrorLive));
