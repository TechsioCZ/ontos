// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug end-stock-sharing-eligibility
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  EndStockSharingEligibilityActionAlreadyCommittedProblemSchema,
  EndStockSharingEligibilityActionAuthenticationProblemSchema,
  EndStockSharingEligibilityActionCommitIndeterminateProblemSchema,
  EndStockSharingEligibilityActionConflictProblemSchema,
  EndStockSharingEligibilityActionForbiddenProblemSchema,
  EndStockSharingEligibilityActionIneligibleProblemSchema,
  EndStockSharingEligibilityActionInternalProblemSchema,
  EndStockSharingEligibilityActionInvalidProblemSchema,
  EndStockSharingEligibilityActionNotFoundProblemSchema,
  EndStockSharingEligibilityActionPreconditionProblemSchema,
  EndStockSharingEligibilityActionSchemaErrorMiddleware,
  EndStockSharingEligibilityActionUnavailableProblemSchema,
} from '../shared/apis/end-stock-sharing-eligibility-action.ts';
import type { EndStockSharingEligibilityActionProblem } from '../shared/apis/end-stock-sharing-eligibility-action.ts';
import { endStockSharingEligibilityAction } from '../src/actions/end-stock-sharing-eligibility.action.ts';

type DomainError = typeof endStockSharingEligibilityAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends EndStockSharingEligibilityActionProblem['_tag']> = Extract<
  EndStockSharingEligibilityActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const endStockSharingEligibilityActionProblem = {
  authentication: (): ProblemOf<'EndStockSharingEligibilityActionAuthenticationProblem'> =>
    EndStockSharingEligibilityActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'EndStockSharingEligibilityActionConflictProblem'>['code'],
  ): ProblemOf<'EndStockSharingEligibilityActionConflictProblem'> =>
    EndStockSharingEligibilityActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'EndStockSharingEligibilityActionForbiddenProblem'>['code'],
  ): ProblemOf<'EndStockSharingEligibilityActionForbiddenProblem'> =>
    EndStockSharingEligibilityActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'EndStockSharingEligibilityActionIneligibleProblem'>['code'],
  ): ProblemOf<'EndStockSharingEligibilityActionIneligibleProblem'> =>
    EndStockSharingEligibilityActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'EndStockSharingEligibilityActionInternalProblem'> =>
    EndStockSharingEligibilityActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'EndStockSharingEligibilityActionInvalidProblem'> =>
    EndStockSharingEligibilityActionInvalidProblemSchema.make({
      detail: 'The end-stock-sharing-eligibility Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'EndStockSharingEligibilityActionNotFoundProblem'>['code'],
  ): ProblemOf<'EndStockSharingEligibilityActionNotFoundProblem'> =>
    EndStockSharingEligibilityActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'EndStockSharingEligibilityActionPreconditionProblem'> =>
    EndStockSharingEligibilityActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'EndStockSharingEligibilityActionUnavailableProblem'>['code'],
  ): ProblemOf<'EndStockSharingEligibilityActionUnavailableProblem'> =>
    EndStockSharingEligibilityActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): EndStockSharingEligibilityActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      StockSharingEligibilityLifecycleRejected: () =>
        endStockSharingEligibilityActionProblem.conflict('stock_sharing_eligibility_lifecycle_rejected'),
      StockSharingEligibilityRejected: () =>
        endStockSharingEligibilityActionProblem.ineligible('stock_sharing_eligibility_rejected'),
      StockSharingEligibilityUnavailable: () =>
        endStockSharingEligibilityActionProblem.unavailable('stock_sharing_eligibility_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): EndStockSharingEligibilityActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        EndStockSharingEligibilityActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: endStockSharingEligibilityActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        EndStockSharingEligibilityActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: endStockSharingEligibilityActionProblem.internal,
      ActionIdempotencyKeyRequired: endStockSharingEligibilityActionProblem.precondition,
      ActionInvocationNotFound: (failure) => endStockSharingEligibilityActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => endStockSharingEligibilityActionProblem.conflict(failure.code),
      ActionPayloadValidationError: endStockSharingEligibilityActionProblem.invalid,
      ActionPermissionCheckError: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => endStockSharingEligibilityActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => endStockSharingEligibilityActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => endStockSharingEligibilityActionProblem.conflict(failure.code),
      ActionResultValidationError: endStockSharingEligibilityActionProblem.internal,
      ActionTransactionError: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: endStockSharingEligibilityActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => endStockSharingEligibilityActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: endStockSharingEligibilityActionProblem.authentication,
      OperationContextDenied: (failure) => endStockSharingEligibilityActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => endStockSharingEligibilityActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => endStockSharingEligibilityActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(endStockSharingEligibilityAction.descriptor.domainErrorSchema);
export const mapEndStockSharingEligibilityActionProblem = (
  error: ActionCoreError | DomainError,
): EndStockSharingEligibilityActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const endStockSharingEligibilityActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  EndStockSharingEligibilityActionSchemaErrorMiddleware,
  () => Effect.fail(endStockSharingEligibilityActionProblem.invalid()),
);
