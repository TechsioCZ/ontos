// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug end-stock-sharing-eligibility
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { endStockSharingEligibilityAction } from '../src/actions/end-stock-sharing-eligibility.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  endStockSharingEligibilityActionProblem,
  endStockSharingEligibilityActionSchemaErrorLive,
  mapEndStockSharingEligibilityActionProblem,
} from './end-stock-sharing-eligibility-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: endStockSharingEligibilityActionProblem.authentication,
  unavailable: () => endStockSharingEligibilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('EndStockSharingEligibilityActionServer.execute')(function* executeEndStockSharingEligibility(
  payload: typeof endStockSharingEligibilityAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(endStockSharingEligibilityActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(endStockSharingEligibilityAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(endStockSharingEligibilityActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: endStockSharingEligibilityActionProblem.internal,
    invalidCorrelationProblem: endStockSharingEligibilityActionProblem.invalid,
    mapError: mapEndStockSharingEligibilityActionProblem,
    payload: decoded,
    registration: endStockSharingEligibilityAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const endStockSharingEligibilityActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'endStockSharingEligibilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(endStockSharingEligibilityActionSchemaErrorLive));
