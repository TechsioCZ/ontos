// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug establish-commitment-protection
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { establishCommitmentProtectionAction } from '../src/actions/establish-commitment-protection.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  establishCommitmentProtectionActionProblem,
  establishCommitmentProtectionActionSchemaErrorLive,
  mapEstablishCommitmentProtectionActionProblem,
} from './establish-commitment-protection-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: establishCommitmentProtectionActionProblem.authentication,
  unavailable: () => establishCommitmentProtectionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('EstablishCommitmentProtectionActionServer.execute')(
  function* executeEstablishCommitmentProtection(
    payload: typeof establishCommitmentProtectionAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(establishCommitmentProtectionActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(establishCommitmentProtectionAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(establishCommitmentProtectionActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: establishCommitmentProtectionActionProblem.internal,
      invalidCorrelationProblem: establishCommitmentProtectionActionProblem.invalid,
      mapError: mapEstablishCommitmentProtectionActionProblem,
      payload: decoded,
      registration: establishCommitmentProtectionAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const establishCommitmentProtectionActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'establishCommitmentProtectionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(establishCommitmentProtectionActionSchemaErrorLive));
