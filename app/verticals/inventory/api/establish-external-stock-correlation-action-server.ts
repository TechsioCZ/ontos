// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug establish-external-stock-correlation
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { establishExternalStockCorrelationAction } from '../src/actions/establish-external-stock-correlation.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  establishExternalStockCorrelationActionProblem,
  establishExternalStockCorrelationActionSchemaErrorLive,
  mapEstablishExternalStockCorrelationActionProblem,
} from './establish-external-stock-correlation-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: establishExternalStockCorrelationActionProblem.authentication,
  unavailable: () => establishExternalStockCorrelationActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('EstablishExternalStockCorrelationActionServer.execute')(
  function* executeEstablishExternalStockCorrelation(
    payload: typeof establishExternalStockCorrelationAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(establishExternalStockCorrelationActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(establishExternalStockCorrelationAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(establishExternalStockCorrelationActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: establishExternalStockCorrelationActionProblem.internal,
      invalidCorrelationProblem: establishExternalStockCorrelationActionProblem.invalid,
      mapError: mapEstablishExternalStockCorrelationActionProblem,
      payload: decoded,
      registration: establishExternalStockCorrelationAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const establishExternalStockCorrelationActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'establishExternalStockCorrelationAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(establishExternalStockCorrelationActionSchemaErrorLive));
