// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug recover-inventory-effect
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecoverInventoryEffectActionAlreadyCommittedProblemSchema,
  RecoverInventoryEffectActionAuthenticationProblemSchema,
  RecoverInventoryEffectActionCommitIndeterminateProblemSchema,
  RecoverInventoryEffectActionConflictProblemSchema,
  RecoverInventoryEffectActionForbiddenProblemSchema,
  RecoverInventoryEffectActionIneligibleProblemSchema,
  RecoverInventoryEffectActionInternalProblemSchema,
  RecoverInventoryEffectActionInvalidProblemSchema,
  RecoverInventoryEffectActionNotFoundProblemSchema,
  RecoverInventoryEffectActionPreconditionProblemSchema,
  RecoverInventoryEffectActionSchemaErrorMiddleware,
  RecoverInventoryEffectActionUnavailableProblemSchema,
} from '../shared/apis/recover-inventory-effect-action.ts';
import type { RecoverInventoryEffectActionProblem } from '../shared/apis/recover-inventory-effect-action.ts';
import { recoverInventoryEffectAction } from '../src/actions/recover-inventory-effect.action.ts';

type DomainError = typeof recoverInventoryEffectAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecoverInventoryEffectActionProblem['_tag']> = Extract<
  RecoverInventoryEffectActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const recoverInventoryEffectActionProblem = {
  authentication: (): ProblemOf<'RecoverInventoryEffectActionAuthenticationProblem'> =>
    RecoverInventoryEffectActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecoverInventoryEffectActionConflictProblem'>['code'],
  ): ProblemOf<'RecoverInventoryEffectActionConflictProblem'> =>
    RecoverInventoryEffectActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecoverInventoryEffectActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecoverInventoryEffectActionForbiddenProblem'> =>
    RecoverInventoryEffectActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecoverInventoryEffectActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecoverInventoryEffectActionIneligibleProblem'> =>
    RecoverInventoryEffectActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecoverInventoryEffectActionInternalProblem'> =>
    RecoverInventoryEffectActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecoverInventoryEffectActionInvalidProblem'> =>
    RecoverInventoryEffectActionInvalidProblemSchema.make({
      detail: 'The recover-inventory-effect Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecoverInventoryEffectActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecoverInventoryEffectActionNotFoundProblem'> =>
    RecoverInventoryEffectActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecoverInventoryEffectActionPreconditionProblem'> =>
    RecoverInventoryEffectActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecoverInventoryEffectActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecoverInventoryEffectActionUnavailableProblem'> =>
    RecoverInventoryEffectActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RecoverInventoryEffectActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      InventoryEffectLedgerConflict: () =>
        recoverInventoryEffectActionProblem.conflict('inventory_effect_ledger_conflict'),
      InventoryEffectLedgerRejected: () =>
        recoverInventoryEffectActionProblem.ineligible('inventory_effect_ledger_rejected'),
      InventoryEffectLedgerUnavailable: () =>
        recoverInventoryEffectActionProblem.unavailable('inventory_effect_ledger_unavailable'),
      InventoryEffectRecoveryRejected: () =>
        recoverInventoryEffectActionProblem.ineligible('inventory_effect_recovery_rejected'),
      RecoverInventoryEffectActionRejected: () =>
        recoverInventoryEffectActionProblem.ineligible('recover_inventory_effect_action_rejected'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecoverInventoryEffectActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecoverInventoryEffectActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recoverInventoryEffectActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecoverInventoryEffectActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recoverInventoryEffectActionProblem.internal,
      ActionIdempotencyKeyRequired: recoverInventoryEffectActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recoverInventoryEffectActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recoverInventoryEffectActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recoverInventoryEffectActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recoverInventoryEffectActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recoverInventoryEffectActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recoverInventoryEffectActionProblem.conflict(failure.code),
      ActionResultValidationError: recoverInventoryEffectActionProblem.internal,
      ActionTransactionError: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recoverInventoryEffectActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recoverInventoryEffectActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recoverInventoryEffectActionProblem.authentication,
      OperationContextDenied: (failure) => recoverInventoryEffectActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recoverInventoryEffectActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recoverInventoryEffectActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recoverInventoryEffectAction.descriptor.domainErrorSchema);
export const mapRecoverInventoryEffectActionProblem = (
  error: ActionCoreError | DomainError,
): RecoverInventoryEffectActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const recoverInventoryEffectActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecoverInventoryEffectActionSchemaErrorMiddleware,
  () => Effect.fail(recoverInventoryEffectActionProblem.invalid()),
);
