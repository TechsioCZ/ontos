// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug resolve-inventory-source-conflict
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { inventoryApi } from '../shared/api.ts';
import { resolveInventorySourceConflictAction } from '../src/actions/resolve-inventory-source-conflict.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  resolveInventorySourceConflictActionProblem,
  resolveInventorySourceConflictActionSchemaErrorLive,
  mapResolveInventorySourceConflictActionProblem,
} from './resolve-inventory-source-conflict-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: resolveInventorySourceConflictActionProblem.authentication,
  unavailable: () => resolveInventorySourceConflictActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ResolveInventorySourceConflictActionServer.execute')(
  function* executeResolveInventorySourceConflict(
    payload: typeof resolveInventorySourceConflictAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(resolveInventorySourceConflictActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(resolveInventorySourceConflictAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(resolveInventorySourceConflictActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: resolveInventorySourceConflictActionProblem.internal,
      invalidCorrelationProblem: resolveInventorySourceConflictActionProblem.invalid,
      mapError: mapResolveInventorySourceConflictActionProblem,
      payload: decoded,
      registration: resolveInventorySourceConflictAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const resolveInventorySourceConflictActionApiLive = HttpApiBuilder.group(
  inventoryApi,
  'resolveInventorySourceConflictAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(resolveInventorySourceConflictActionSchemaErrorLive));
