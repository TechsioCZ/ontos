// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug stock-issue
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  StockIssueActionAlreadyCommittedProblemSchema,
  StockIssueActionAuthenticationProblemSchema,
  StockIssueActionCommitIndeterminateProblemSchema,
  StockIssueActionConflictProblemSchema,
  StockIssueActionForbiddenProblemSchema,
  StockIssueActionIneligibleProblemSchema,
  StockIssueActionInternalProblemSchema,
  StockIssueActionInvalidProblemSchema,
  StockIssueActionNotFoundProblemSchema,
  StockIssueActionPreconditionProblemSchema,
  StockIssueActionSchemaErrorMiddleware,
  StockIssueActionUnavailableProblemSchema,
} from '../shared/apis/stock-issue-action.ts';
import type { StockIssueActionProblem } from '../shared/apis/stock-issue-action.ts';
import { stockIssueAction } from '../src/actions/stock-issue.action.ts';

type DomainError = typeof stockIssueAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends StockIssueActionProblem['_tag']> = Extract<StockIssueActionProblem, { readonly _tag: Tag }>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const stockIssueActionProblem = {
  authentication: (): ProblemOf<'StockIssueActionAuthenticationProblem'> =>
    StockIssueActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'StockIssueActionConflictProblem'>['code'],
  ): ProblemOf<'StockIssueActionConflictProblem'> =>
    StockIssueActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'StockIssueActionForbiddenProblem'>['code'],
  ): ProblemOf<'StockIssueActionForbiddenProblem'> =>
    StockIssueActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'StockIssueActionIneligibleProblem'>['code'],
  ): ProblemOf<'StockIssueActionIneligibleProblem'> =>
    StockIssueActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'StockIssueActionInternalProblem'> =>
    StockIssueActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'StockIssueActionInvalidProblem'> =>
    StockIssueActionInvalidProblemSchema.make({
      detail: 'The stock-issue Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'StockIssueActionNotFoundProblem'>['code'],
  ): ProblemOf<'StockIssueActionNotFoundProblem'> =>
    StockIssueActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'StockIssueActionPreconditionProblem'> =>
    StockIssueActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'StockIssueActionUnavailableProblem'>['code'],
  ): ProblemOf<'StockIssueActionUnavailableProblem'> =>
    StockIssueActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): StockIssueActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PhysicalStockEffectConflict: () => stockIssueActionProblem.conflict('physical_stock_effect_conflict'),
      PhysicalStockEffectIndeterminate: () =>
        stockIssueActionProblem.unavailable('physical_stock_effect_indeterminate'),
      PhysicalStockEffectRejected: () => stockIssueActionProblem.ineligible('physical_stock_effect_rejected'),
      PhysicalStockEffectUnavailable: () => stockIssueActionProblem.unavailable('physical_stock_effect_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): StockIssueActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        StockIssueActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: stockIssueActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        StockIssueActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: stockIssueActionProblem.internal,
      ActionIdempotencyKeyRequired: stockIssueActionProblem.precondition,
      ActionInvocationNotFound: (failure) => stockIssueActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => stockIssueActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => stockIssueActionProblem.conflict(failure.code),
      ActionPayloadValidationError: stockIssueActionProblem.invalid,
      ActionPermissionCheckError: (failure) => stockIssueActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => stockIssueActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => stockIssueActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => stockIssueActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => stockIssueActionProblem.conflict(failure.code),
      ActionResultValidationError: stockIssueActionProblem.internal,
      ActionTransactionError: (failure) => stockIssueActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: stockIssueActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => stockIssueActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => stockIssueActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: stockIssueActionProblem.authentication,
      OperationContextDenied: (failure) => stockIssueActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => stockIssueActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => stockIssueActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(stockIssueAction.descriptor.domainErrorSchema);
export const mapStockIssueActionProblem = (error: ActionCoreError | DomainError): StockIssueActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const stockIssueActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  StockIssueActionSchemaErrorMiddleware,
  () => Effect.fail(stockIssueActionProblem.invalid()),
);
