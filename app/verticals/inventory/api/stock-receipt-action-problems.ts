// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug stock-receipt
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  StockReceiptActionAlreadyCommittedProblemSchema,
  StockReceiptActionAuthenticationProblemSchema,
  StockReceiptActionCommitIndeterminateProblemSchema,
  StockReceiptActionConflictProblemSchema,
  StockReceiptActionForbiddenProblemSchema,
  StockReceiptActionIneligibleProblemSchema,
  StockReceiptActionInternalProblemSchema,
  StockReceiptActionInvalidProblemSchema,
  StockReceiptActionNotFoundProblemSchema,
  StockReceiptActionPreconditionProblemSchema,
  StockReceiptActionSchemaErrorMiddleware,
  StockReceiptActionUnavailableProblemSchema,
} from '../shared/apis/stock-receipt-action.ts';
import type { StockReceiptActionProblem } from '../shared/apis/stock-receipt-action.ts';
import { stockReceiptAction } from '../src/actions/stock-receipt.action.ts';

type DomainError = typeof stockReceiptAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends StockReceiptActionProblem['_tag']> = Extract<
  StockReceiptActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const stockReceiptActionProblem = {
  authentication: (): ProblemOf<'StockReceiptActionAuthenticationProblem'> =>
    StockReceiptActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'StockReceiptActionConflictProblem'>['code'],
  ): ProblemOf<'StockReceiptActionConflictProblem'> =>
    StockReceiptActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'StockReceiptActionForbiddenProblem'>['code'],
  ): ProblemOf<'StockReceiptActionForbiddenProblem'> =>
    StockReceiptActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'StockReceiptActionIneligibleProblem'>['code'],
  ): ProblemOf<'StockReceiptActionIneligibleProblem'> =>
    StockReceiptActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'StockReceiptActionInternalProblem'> =>
    StockReceiptActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'StockReceiptActionInvalidProblem'> =>
    StockReceiptActionInvalidProblemSchema.make({
      detail: 'The stock-receipt Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'StockReceiptActionNotFoundProblem'>['code'],
  ): ProblemOf<'StockReceiptActionNotFoundProblem'> =>
    StockReceiptActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'StockReceiptActionPreconditionProblem'> =>
    StockReceiptActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'StockReceiptActionUnavailableProblem'>['code'],
  ): ProblemOf<'StockReceiptActionUnavailableProblem'> =>
    StockReceiptActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): StockReceiptActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PhysicalStockEffectConflict: () => stockReceiptActionProblem.conflict('physical_stock_effect_conflict'),
      PhysicalStockEffectIndeterminate: () =>
        stockReceiptActionProblem.unavailable('physical_stock_effect_indeterminate'),
      PhysicalStockEffectRejected: () => stockReceiptActionProblem.ineligible('physical_stock_effect_rejected'),
      PhysicalStockEffectUnavailable: () => stockReceiptActionProblem.unavailable('physical_stock_effect_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): StockReceiptActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        StockReceiptActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: stockReceiptActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        StockReceiptActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: stockReceiptActionProblem.internal,
      ActionIdempotencyKeyRequired: stockReceiptActionProblem.precondition,
      ActionInvocationNotFound: (failure) => stockReceiptActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => stockReceiptActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => stockReceiptActionProblem.conflict(failure.code),
      ActionPayloadValidationError: stockReceiptActionProblem.invalid,
      ActionPermissionCheckError: (failure) => stockReceiptActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => stockReceiptActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => stockReceiptActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => stockReceiptActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => stockReceiptActionProblem.conflict(failure.code),
      ActionResultValidationError: stockReceiptActionProblem.internal,
      ActionTransactionError: (failure) => stockReceiptActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: stockReceiptActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => stockReceiptActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => stockReceiptActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: stockReceiptActionProblem.authentication,
      OperationContextDenied: (failure) => stockReceiptActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => stockReceiptActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => stockReceiptActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(stockReceiptAction.descriptor.domainErrorSchema);
export const mapStockReceiptActionProblem = (error: ActionCoreError | DomainError): StockReceiptActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const stockReceiptActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  StockReceiptActionSchemaErrorMiddleware,
  () => Effect.fail(stockReceiptActionProblem.invalid()),
);
