// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.inventory
// @ontos-resource-slug commitment-protection
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

export const CommitmentProtectionIdSchema = Schema.String.check(Schema.isTrimmed(), Schema.isUUID()).pipe(
  Schema.brand('CommitmentProtectionId'),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(Schema.brand('InventoryTenantId'));

export const CommitmentProtectionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.inventory'),
  resourceId: CommitmentProtectionIdSchema,
  resourceType: Schema.Literal('commerce.inventory.commitment-protection'),
  tenantId: TenantIdSchema,
});
export type CommitmentProtectionRef = typeof CommitmentProtectionRefSchema.Type;

export const commitmentProtectionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Attempt-bound owner guarantee fencing one exact Inventory Reservation through Order commitment.',
  key: 'commerce.inventory.commitment-protection',
  label: 'Commitment Protection',
  owningModuleId: 'commerce.inventory',
} as const satisfies OntosResourceType;
