// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.inventory
// @ontos-resource-slug inventory-reservation
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

export const InventoryReservationIdSchema = Schema.String.check(Schema.isTrimmed(), Schema.isUUID()).pipe(
  Schema.brand('InventoryReservationId'),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(Schema.brand('InventoryTenantId'));

export const InventoryReservationRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.inventory'),
  resourceId: InventoryReservationIdSchema,
  resourceType: Schema.Literal('commerce.inventory.inventory-reservation'),
  tenantId: TenantIdSchema,
});
export type InventoryReservationRef = typeof InventoryReservationRefSchema.Type;

export const inventoryReservationResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'One durable provisional stock obligation for one exact Order Commitment Attempt.',
  key: 'commerce.inventory.inventory-reservation',
  label: 'Inventory Reservation',
  owningModuleId: 'commerce.inventory',
} as const satisfies OntosResourceType;
