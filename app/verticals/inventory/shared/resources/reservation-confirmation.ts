// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.inventory
// @ontos-resource-slug reservation-confirmation
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

export const ReservationConfirmationIdSchema = Schema.String.check(Schema.isTrimmed(), Schema.isUUID()).pipe(
  Schema.brand('ReservationConfirmationId'),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(Schema.brand('InventoryTenantId'));

export const ReservationConfirmationRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.inventory'),
  resourceId: ReservationConfirmationIdSchema,
  resourceType: Schema.Literal('commerce.inventory.reservation-confirmation'),
  tenantId: TenantIdSchema,
});
export type ReservationConfirmationRef = typeof ReservationConfirmationRefSchema.Type;

export const reservationConfirmationResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Single Attempt-bound proof issued by the selected Reservation Authority.',
  key: 'commerce.inventory.reservation-confirmation',
  label: 'Reservation Confirmation',
  owningModuleId: 'commerce.inventory',
} as const satisfies OntosResourceType;
