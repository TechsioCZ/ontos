// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.inventory
// @ontos-resource-slug stock-item
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

export const StockItemIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('InventoryStockItemId'),
);
export const InventoryTenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('InventoryTenantId'),
);

export const StockItemRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.inventory'),
  resourceId: StockItemIdSchema,
  resourceType: Schema.Literal('commerce.inventory.stock-item'),
  tenantId: InventoryTenantIdSchema,
});
export type StockItemRef = typeof StockItemRefSchema.Type;

export const stockItemResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Durable Inventory identity for one immutable exact Catalog Selection meaning and Unit.',
  key: 'commerce.inventory.stock-item',
  label: 'Stock Item',
  owningModuleId: 'commerce.inventory',
} as const satisfies OntosResourceType;
