// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner commerce.inventory
// @ontos-resource-slug stock-position
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const checkedUuid = Schema.String.check(Schema.isUUID(), Schema.isTrimmed());
export const StockPositionIdSchema = checkedUuid.pipe(
  Schema.brand('InventoryStockPositionId'),
  Schema.decodeTo(checkedUuid),
);
export const StockPositionTenantIdSchema = checkedUuid.pipe(
  Schema.brand('InventoryStockPositionTenantId'),
  Schema.decodeTo(checkedUuid),
);

export const StockPositionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('commerce.inventory'),
  resourceId: StockPositionIdSchema,
  resourceType: Schema.Literal('commerce.inventory.stock-position'),
  tenantId: StockPositionTenantIdSchema,
});
export type StockPositionRef = typeof StockPositionRefSchema.Type;

export const stockPositionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Durable constrained quantity scope for one exact Stock Item and Stock Location.',
  key: 'commerce.inventory.stock-position',
  label: 'Stock Position',
  owningModuleId: 'commerce.inventory',
} as const satisfies OntosResourceType;
