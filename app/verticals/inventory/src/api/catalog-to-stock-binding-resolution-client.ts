// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CatalogToStockBindingResolutionApi } from '../../shared/apis/catalog-to-stock-binding-resolution.ts';
import type { CatalogToStockBindingResolutionRequest } from '../../shared/apis/catalog-to-stock-binding-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface CatalogToStockBindingResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type CatalogToStockBindingResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CatalogToStockBindingResolutionClientOptions,
];

type CatalogToStockBindingResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CatalogToStockBindingResolutionClientOptions,
];

const catalogToStockBindingResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CatalogToStockBindingResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CatalogToStockBindingResolutionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeCatalogToStockBindingResolutionWithAuthorization = (
  payload: CatalogToStockBindingResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: CatalogToStockBindingResolutionAuthorizedInvocation
) =>
  catalogToStockBindingResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.catalogToStockBindingResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCatalogToStockBindingResolution = (
  payload: CatalogToStockBindingResolutionRequest,
  ...[requestCorrelation, options = {}]: CatalogToStockBindingResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCatalogToStockBindingResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
