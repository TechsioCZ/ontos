// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { CommitmentProtectionVerificationApi } from '../../shared/apis/commitment-protection-verification.ts';
import type { CommitmentProtectionVerificationRequest } from '../../shared/apis/commitment-protection-verification.ts';
import { operationGateway } from './action-gateway.ts';

export interface CommitmentProtectionVerificationClientOptions {
  readonly baseUrl?: string | URL;
}

type CommitmentProtectionVerificationAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: CommitmentProtectionVerificationClientOptions,
];

type CommitmentProtectionVerificationOperationInvocation = readonly [
  requestCorrelation: string,
  options?: CommitmentProtectionVerificationClientOptions,
];

const commitmentProtectionVerificationClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: CommitmentProtectionVerificationClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: CommitmentProtectionVerificationApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeCommitmentProtectionVerificationWithAuthorization = (
  payload: CommitmentProtectionVerificationRequest,
  ...[credential, requestCorrelation, options = {}]: CommitmentProtectionVerificationAuthorizedInvocation
) =>
  commitmentProtectionVerificationClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.commitmentProtectionVerification.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeCommitmentProtectionVerification = (
  payload: CommitmentProtectionVerificationRequest,
  ...[requestCorrelation, options = {}]: CommitmentProtectionVerificationOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeCommitmentProtectionVerificationWithAuthorization(payload, credential, requestCorrelation, options),
  );
