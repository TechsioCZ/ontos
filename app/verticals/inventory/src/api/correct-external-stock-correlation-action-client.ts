// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug correct-external-stock-correlation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  CorrectExternalStockCorrelationActionApi,
  CorrectExternalStockCorrelationPayloadSchema,
} from '../../shared/apis/correct-external-stock-correlation-action.ts';
import type { CorrectExternalStockCorrelationPayload } from '../../shared/actions/correct-external-stock-correlation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface CorrectExternalStockCorrelationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: CorrectExternalStockCorrelationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: CorrectExternalStockCorrelationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: CorrectExternalStockCorrelationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: CorrectExternalStockCorrelationActionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeCorrectExternalStockCorrelationWithAuthorization = (
  payload: CorrectExternalStockCorrelationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(CorrectExternalStockCorrelationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.correctExternalStockCorrelationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeCorrectExternalStockCorrelation = (
  payload: CorrectExternalStockCorrelationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeCorrectExternalStockCorrelationWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
