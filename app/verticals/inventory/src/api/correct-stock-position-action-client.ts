// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug correct-stock-position
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  CorrectStockPositionActionApi,
  CorrectStockPositionPayloadSchema,
} from '../../shared/apis/correct-stock-position-action.ts';
import type { CorrectStockPositionPayload } from '../../shared/actions/correct-stock-position.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface CorrectStockPositionActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: CorrectStockPositionActionClientOptions,
];
type OperationInvocation = readonly [requestCorrelation: string, options: CorrectStockPositionActionClientOptions];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: CorrectStockPositionActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: CorrectStockPositionActionApi, credential, defaultApiPrefix: '/inventory-api', requestCorrelation },
    options,
  );

export const executeCorrectStockPositionWithAuthorization = (
  payload: CorrectStockPositionPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(CorrectStockPositionPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.correctStockPositionAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeCorrectStockPosition = (
  payload: CorrectStockPositionPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) => executeCorrectStockPositionWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
