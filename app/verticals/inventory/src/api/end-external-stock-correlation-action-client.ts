// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug end-external-stock-correlation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  EndExternalStockCorrelationActionApi,
  EndExternalStockCorrelationPayloadSchema,
} from '../../shared/apis/end-external-stock-correlation-action.ts';
import type { EndExternalStockCorrelationPayload } from '../../shared/actions/end-external-stock-correlation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface EndExternalStockCorrelationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: EndExternalStockCorrelationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: EndExternalStockCorrelationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: EndExternalStockCorrelationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    { api: EndExternalStockCorrelationActionApi, credential, defaultApiPrefix: '/inventory-api', requestCorrelation },
    options,
  );

export const executeEndExternalStockCorrelationWithAuthorization = (
  payload: EndExternalStockCorrelationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(EndExternalStockCorrelationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.endExternalStockCorrelationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeEndExternalStockCorrelation = (
  payload: EndExternalStockCorrelationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeEndExternalStockCorrelationWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
