// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug establish-catalog-to-stock-binding
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  EstablishCatalogToStockBindingActionApi,
  EstablishCatalogToStockBindingPayloadSchema,
} from '../../shared/apis/establish-catalog-to-stock-binding-action.ts';
import type { EstablishCatalogToStockBindingPayload } from '../../shared/actions/establish-catalog-to-stock-binding.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface EstablishCatalogToStockBindingActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: EstablishCatalogToStockBindingActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: EstablishCatalogToStockBindingActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: EstablishCatalogToStockBindingActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: EstablishCatalogToStockBindingActionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeEstablishCatalogToStockBindingWithAuthorization = (
  payload: EstablishCatalogToStockBindingPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(EstablishCatalogToStockBindingPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.establishCatalogToStockBindingAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeEstablishCatalogToStockBinding = (
  payload: EstablishCatalogToStockBindingPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeEstablishCatalogToStockBindingWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
