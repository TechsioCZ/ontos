// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug establish-external-stock-correlation
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  EstablishExternalStockCorrelationActionApi,
  EstablishExternalStockCorrelationPayloadSchema,
} from '../../shared/apis/establish-external-stock-correlation-action.ts';
import type { EstablishExternalStockCorrelationPayload } from '../../shared/actions/establish-external-stock-correlation.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface EstablishExternalStockCorrelationActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: EstablishExternalStockCorrelationActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: EstablishExternalStockCorrelationActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: EstablishExternalStockCorrelationActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: EstablishExternalStockCorrelationActionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeEstablishExternalStockCorrelationWithAuthorization = (
  payload: EstablishExternalStockCorrelationPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(EstablishExternalStockCorrelationPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.establishExternalStockCorrelationAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeEstablishExternalStockCorrelation = (
  payload: EstablishExternalStockCorrelationPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeEstablishExternalStockCorrelationWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
