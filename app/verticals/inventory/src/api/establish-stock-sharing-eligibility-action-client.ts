// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner commerce.inventory
// @ontos-action-http-slug establish-stock-sharing-eligibility
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  EstablishStockSharingEligibilityActionApi,
  EstablishStockSharingEligibilityPayloadSchema,
} from '../../shared/apis/establish-stock-sharing-eligibility-action.ts';
import type { EstablishStockSharingEligibilityPayload } from '../../shared/actions/establish-stock-sharing-eligibility.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface EstablishStockSharingEligibilityActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: EstablishStockSharingEligibilityActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: EstablishStockSharingEligibilityActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: EstablishStockSharingEligibilityActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: EstablishStockSharingEligibilityActionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeEstablishStockSharingEligibilityWithAuthorization = (
  payload: EstablishStockSharingEligibilityPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(EstablishStockSharingEligibilityPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.establishStockSharingEligibilityAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeEstablishStockSharingEligibility = (
  payload: EstablishStockSharingEligibilityPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeEstablishStockSharingEligibilityWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
