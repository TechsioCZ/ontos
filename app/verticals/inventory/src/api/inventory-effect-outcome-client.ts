// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InventoryEffectOutcomeApi } from '../../shared/apis/inventory-effect-outcome.ts';
import type { InventoryEffectOutcomeRequest } from '../../shared/apis/inventory-effect-outcome.ts';
import { operationGateway } from './action-gateway.ts';

export interface InventoryEffectOutcomeClientOptions {
  readonly baseUrl?: string | URL;
}

type InventoryEffectOutcomeAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InventoryEffectOutcomeClientOptions,
];

type InventoryEffectOutcomeOperationInvocation = readonly [
  requestCorrelation: string,
  options?: InventoryEffectOutcomeClientOptions,
];

const inventoryEffectOutcomeClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InventoryEffectOutcomeClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InventoryEffectOutcomeApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeInventoryEffectOutcomeWithAuthorization = (
  payload: InventoryEffectOutcomeRequest,
  ...[credential, requestCorrelation, options = {}]: InventoryEffectOutcomeAuthorizedInvocation
) =>
  inventoryEffectOutcomeClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.inventoryEffectOutcome.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeInventoryEffectOutcome = (
  payload: InventoryEffectOutcomeRequest,
  ...[requestCorrelation, options = {}]: InventoryEffectOutcomeOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInventoryEffectOutcomeWithAuthorization(payload, credential, requestCorrelation, options),
  );
