// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InventoryReconciliationEvidenceApi } from '../../shared/apis/inventory-reconciliation-evidence.ts';
import type { InventoryReconciliationEvidenceRequest } from '../../shared/apis/inventory-reconciliation-evidence.ts';
import { operationGateway } from './action-gateway.ts';

export interface InventoryReconciliationEvidenceClientOptions {
  readonly baseUrl?: string | URL;
}

type InventoryReconciliationEvidenceAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InventoryReconciliationEvidenceClientOptions,
];

type InventoryReconciliationEvidenceOperationInvocation = readonly [
  requestCorrelation: string,
  options?: InventoryReconciliationEvidenceClientOptions,
];

const inventoryReconciliationEvidenceClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InventoryReconciliationEvidenceClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InventoryReconciliationEvidenceApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeInventoryReconciliationEvidenceWithAuthorization = (
  payload: InventoryReconciliationEvidenceRequest,
  ...[credential, requestCorrelation, options = {}]: InventoryReconciliationEvidenceAuthorizedInvocation
) =>
  inventoryReconciliationEvidenceClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.inventoryReconciliationEvidence.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeInventoryReconciliationEvidence = (
  payload: InventoryReconciliationEvidenceRequest,
  ...[requestCorrelation, options = {}]: InventoryReconciliationEvidenceOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInventoryReconciliationEvidenceWithAuthorization(payload, credential, requestCorrelation, options),
  );
