// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { InventoryReservationDetailApi } from '../../shared/apis/inventory-reservation-detail.ts';
import type { InventoryReservationDetailRequest } from '../../shared/apis/inventory-reservation-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface InventoryReservationDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type InventoryReservationDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: InventoryReservationDetailClientOptions,
];

type InventoryReservationDetailOperationInvocation = readonly [
  requestCorrelation: string,
  options?: InventoryReservationDetailClientOptions,
];

const inventoryReservationDetailClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: InventoryReservationDetailClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: InventoryReservationDetailApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeInventoryReservationDetailWithAuthorization = (
  payload: InventoryReservationDetailRequest,
  ...[credential, requestCorrelation, options = {}]: InventoryReservationDetailAuthorizedInvocation
) =>
  inventoryReservationDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.inventoryReservationDetail.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeInventoryReservationDetail = (
  payload: InventoryReservationDetailRequest,
  ...[requestCorrelation, options = {}]: InventoryReservationDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeInventoryReservationDetailWithAuthorization(payload, credential, requestCorrelation, options),
  );
