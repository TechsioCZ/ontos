// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { ReservationConfirmationVerificationApi } from '../../shared/apis/reservation-confirmation-verification.ts';
import type { ReservationConfirmationVerificationRequest } from '../../shared/apis/reservation-confirmation-verification.ts';
import { operationGateway } from './action-gateway.ts';

export interface ReservationConfirmationVerificationClientOptions {
  readonly baseUrl?: string | URL;
}

type ReservationConfirmationVerificationAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: ReservationConfirmationVerificationClientOptions,
];

type ReservationConfirmationVerificationOperationInvocation = readonly [
  requestCorrelation: string,
  options?: ReservationConfirmationVerificationClientOptions,
];

const reservationConfirmationVerificationClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: ReservationConfirmationVerificationClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: ReservationConfirmationVerificationApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeReservationConfirmationVerificationWithAuthorization = (
  payload: ReservationConfirmationVerificationRequest,
  ...[credential, requestCorrelation, options = {}]: ReservationConfirmationVerificationAuthorizedInvocation
) =>
  reservationConfirmationVerificationClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.reservationConfirmationVerification.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeReservationConfirmationVerification = (
  payload: ReservationConfirmationVerificationRequest,
  ...[requestCorrelation, options = {}]: ReservationConfirmationVerificationOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeReservationConfirmationVerificationWithAuthorization(payload, credential, requestCorrelation, options),
  );
