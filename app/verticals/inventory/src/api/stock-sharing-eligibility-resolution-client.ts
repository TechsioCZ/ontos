// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { StockSharingEligibilityResolutionApi } from '../../shared/apis/stock-sharing-eligibility-resolution.ts';
import type { StockSharingEligibilityResolutionRequest } from '../../shared/apis/stock-sharing-eligibility-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface StockSharingEligibilityResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type StockSharingEligibilityResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: StockSharingEligibilityResolutionClientOptions,
];

type StockSharingEligibilityResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: StockSharingEligibilityResolutionClientOptions,
];

const stockSharingEligibilityResolutionClient = (
  credential: Redacted.Redacted,
  requestCorrelation: string,
  options: StockSharingEligibilityResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: StockSharingEligibilityResolutionApi,
      credential,
      defaultApiPrefix: '/inventory-api',
      requestCorrelation,
    },
    options,
  );

export const executeStockSharingEligibilityResolutionWithAuthorization = (
  payload: StockSharingEligibilityResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: StockSharingEligibilityResolutionAuthorizedInvocation
) =>
  stockSharingEligibilityResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.stockSharingEligibilityResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeStockSharingEligibilityResolution = (
  payload: StockSharingEligibilityResolutionRequest,
  ...[requestCorrelation, options = {}]: StockSharingEligibilityResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeStockSharingEligibilityResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
