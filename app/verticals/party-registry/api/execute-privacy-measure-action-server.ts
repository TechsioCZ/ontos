// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner party.registry
// @ontos-action-http-slug execute-privacy-measure
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { partyRegistryApi } from '../shared/api.ts';
import { executePrivacyMeasureAction } from '../src/actions/execute-privacy-measure.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  executePrivacyMeasureActionProblem,
  executePrivacyMeasureActionSchemaErrorLive,
  mapExecutePrivacyMeasureActionProblem,
} from './execute-privacy-measure-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: executePrivacyMeasureActionProblem.authentication,
  unavailable: () => executePrivacyMeasureActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('ExecutePrivacyMeasureActionServer.execute')(function* executeExecutePrivacyMeasure(
  payload: typeof executePrivacyMeasureAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(executePrivacyMeasureActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(executePrivacyMeasureAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(executePrivacyMeasureActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: executePrivacyMeasureActionProblem.internal,
    invalidCorrelationProblem: executePrivacyMeasureActionProblem.invalid,
    mapError: mapExecutePrivacyMeasureActionProblem,
    payload: decoded,
    registration: executePrivacyMeasureAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const executePrivacyMeasureActionApiLive = HttpApiBuilder.group(
  partyRegistryApi,
  'executePrivacyMeasureAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(executePrivacyMeasureActionSchemaErrorLive));
