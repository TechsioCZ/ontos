// @generated by OntOS Codesmith module-api v1
import { Effect, Schema } from 'effect';
import { HttpApi, HttpApiEndpoint, HttpApiGroup, HttpApiSchema } from 'effect/unstable/httpapi';
import {
  PartyMatchPreviewResponseSchema,
  PartyMatchRequestSchema as RequestSchema,
} from '../domain/matching-contracts.ts';

export const PartyMatchRequestSchema = RequestSchema;
export type PartyMatchRequest = typeof PartyMatchRequestSchema.Type;
export const PartyMatchResponseSchema = PartyMatchPreviewResponseSchema;
export type PartyMatchResponse = typeof PartyMatchResponseSchema.Type;

const problemString = (value: string) =>
  Schema.String.pipe(Schema.withConstructorDefault(Effect.succeed(value)));
const problemStatus = <const Status extends number>(status: Status) =>
  Schema.Literal(status).pipe(Schema.withConstructorDefault(Effect.succeed(status)));

export const PartyMatchAuthenticationProblemSchema = Schema.TaggedStruct(
  'PartyMatchAuthenticationProblem',
  {
    detail: problemString('A valid audience-scoped Bearer assertion is required.'),
    status: problemStatus(401),
    title: problemString('Authentication required'),
    type: problemString('https://ontos.dev/problems/operation-authentication-required'),
  },
).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(401),
);
export const PartyMatchInvalidProblemSchema = Schema.TaggedStruct('PartyMatchInvalidProblem', {
  detail: problemString('The governed read request is invalid.'),
  status: problemStatus(400),
  title: problemString('Invalid read request'),
  type: problemString('https://ontos.dev/problems/read-invalid'),
}).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(400),
);
export const PartyMatchUnavailableProblemSchema = Schema.TaggedStruct(
  'PartyMatchUnavailableProblem',
  {
    detail: problemString('The governed read is temporarily unavailable.'),
    retryable: Schema.Literal(true).pipe(Schema.withConstructorDefault(Effect.succeed(true))),
    status: problemStatus(503),
    title: problemString('Read unavailable'),
    type: problemString('https://ontos.dev/problems/read-unavailable'),
  },
).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(503),
);
export const PartyMatchForbiddenProblemSchema = Schema.TaggedStruct('PartyMatchForbiddenProblem', {
  detail: problemString('The principal is not permitted to perform this read.'),
  status: problemStatus(403),
  title: problemString('Read forbidden'),
  type: problemString('https://ontos.dev/problems/read-forbidden'),
}).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(403),
);
export const PartyMatchNotFoundProblemSchema = Schema.TaggedStruct('PartyMatchNotFoundProblem', {
  detail: problemString('The requested resource was not found.'),
  status: problemStatus(404),
  title: problemString('Resource not found'),
  type: problemString('https://ontos.dev/problems/read-not-found'),
}).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(404),
);
export const PartyMatchPolicyProblemSchema = Schema.TaggedStruct('PartyMatchPolicyProblem', {
  detail: problemString('The read is not eligible under the current business policy.'),
  status: problemStatus(422),
  title: problemString('Read ineligible'),
  type: problemString('https://ontos.dev/problems/read-policy-denied'),
}).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(422),
);
export const PartyMatchPolicyConflictProblemSchema = Schema.TaggedStruct(
  'PartyMatchPolicyConflictProblem',
  {
    detail: problemString('The read conflicts with the current business state.'),
    status: problemStatus(409),
    title: problemString('Read conflict'),
    type: problemString('https://ontos.dev/problems/read-policy-conflict'),
  },
).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(409),
);
export const PartyMatchInternalProblemSchema = Schema.TaggedStruct('PartyMatchInternalProblem', {
  detail: problemString('The governed read could not be completed.'),
  status: problemStatus(500),
  title: problemString('Read failed'),
  type: problemString('https://ontos.dev/problems/read-failed'),
}).pipe(
  HttpApiSchema.asJson({ contentType: 'application/problem+json' }),
  HttpApiSchema.status(500),
);

export const PartyMatchApi = HttpApi.make('PartyMatchApi').add(
  HttpApiGroup.make('partyMatch').add(
    HttpApiEndpoint.post('execute', '/reads/party-match', {
      error: [
        PartyMatchInvalidProblemSchema,
        PartyMatchAuthenticationProblemSchema,
        PartyMatchForbiddenProblemSchema,
        PartyMatchNotFoundProblemSchema,
        PartyMatchPolicyConflictProblemSchema,
        PartyMatchPolicyProblemSchema,
        PartyMatchUnavailableProblemSchema,
        PartyMatchInternalProblemSchema,
      ],
      headers: {},
      params: {},
      payload: PartyMatchRequestSchema,
      query: {},
      success: PartyMatchResponseSchema,
    }),
  ),
);
