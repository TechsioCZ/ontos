// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner party.registry
// @ontos-resource-slug party-correction
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
const TenantIdSchema = Schema.String.check(Schema.isUUID());

export const PartyCorrectionRefSchema = Schema.Struct({
  moduleId: Schema.Literal('party.registry'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('party.registry.party-correction'),
  tenantId: TenantIdSchema,
});
export type PartyCorrectionRef = typeof PartyCorrectionRefSchema.Type;
export const makePartyCorrectionRef = (
  tenantId: string,
  resourceId: string,
): PartyCorrectionRef => ({
  moduleId: 'party.registry',
  resourceId,
  resourceType: 'party.registry.party-correction',
  tenantId,
});

export const partyCorrectionResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Party Correction resource.',
  key: 'party.registry.party-correction',
  label: 'Party Correction',
  owningModuleId: 'party.registry',
} as const satisfies OntosResourceType;
