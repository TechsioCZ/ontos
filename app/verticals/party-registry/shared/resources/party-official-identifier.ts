// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner party.registry
// @ontos-resource-slug party-official-identifier
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

// eslint-disable-next-line effect-native/no-unbranded-identifier-schema -- Generated ResourceRef wire shape; the resourceType literal supplies identity.
const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
// eslint-disable-next-line effect-native/no-unbranded-identifier-schema -- Generated ResourceRef wire shape preserves the canonical Tenant UUID encoding.
const TenantIdSchema = Schema.String.check(Schema.isUUID());

export const PartyOfficialIdentifierRefSchema = Schema.Struct({
  moduleId: Schema.Literal('party.registry'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('party.registry.party-official-identifier'),
  tenantId: TenantIdSchema,
});
export type PartyOfficialIdentifierRef = typeof PartyOfficialIdentifierRefSchema.Type;

export const partyOfficialIdentifierResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: true,
  },
  description: 'Party Official Identifier resource.',
  key: 'party.registry.party-official-identifier',
  label: 'Party Official Identifier',
  owningModuleId: 'party.registry',
} as const satisfies OntosResourceType;
