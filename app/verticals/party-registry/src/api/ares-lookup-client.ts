// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { AresLookupRequest } from '../../shared/apis/ares-lookup.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface AresLookupClientOptions {
  readonly baseUrl?: string | URL;
}

type AuthorizedAresLookupInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: AresLookupClientOptions,
];

type AresLookupInvocation = readonly [
  requestCorrelation: string,
  options?: AresLookupClientOptions,
];

export const executeAresLookupWithAuthorization = (
  payload: AresLookupRequest,
  ...[credential, requestCorrelation, options = {}]: AuthorizedAresLookupInvocation
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.aresLookup.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executeAresLookup = (
  payload: AresLookupRequest,
  ...[requestCorrelation, options = {}]: AresLookupInvocation
) =>
  operationGateway.invoke((credential) =>
    executeAresLookupWithAuthorization(payload, credential, requestCorrelation, options),
  );
