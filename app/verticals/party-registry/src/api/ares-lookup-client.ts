// @generated by OntOS Codesmith module-api v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { AresLookupApi } from '../../shared/apis/ares-lookup.ts';
import type { AresLookupRequest } from '../../shared/apis/ares-lookup.ts';
import { operationGateway } from './action-gateway.ts';

export interface AresLookupClientOptions {
  readonly baseUrl?: string | URL;
}

export const executeAresLookupWithAuthorization = (
  payload: AresLookupRequest,
  authorization: string,
  correlationId: string,
  options: AresLookupClientOptions = {},
) =>
  makeEffectHttpApiClient(AresLookupApi, {
    baseUrl: options.baseUrl ?? '/party-registry-api',
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(
    Effect.flatMap((client) =>
      client.aresLookup.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeAresLookup = (
  payload: AresLookupRequest,
  correlationId: string,
  options: AresLookupClientOptions = {},
) =>
  operationGateway.invoke((authorization) =>
    executeAresLookupWithAuthorization(payload, authorization, correlationId, options),
  );
