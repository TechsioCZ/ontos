// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { CounterpartyReadRequest } from '../../shared/apis/counterparty-read.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface CounterpartyReadClientOptions {
  readonly baseUrl?: string | URL;
}

export const executeCounterpartyReadWithAuthorization = (
  payload: CounterpartyReadRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<CounterpartyReadClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.counterpartyRead.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executeCounterpartyRead = (
  payload: CounterpartyReadRequest,
  ...[
    requestCorrelation,
    options = {},
  ]: PartyRegistryOperationInvocation<CounterpartyReadClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executeCounterpartyReadWithAuthorization(payload, credential, requestCorrelation, options),
  );
