// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';

import { GuestPartyResolutionApi } from '../../shared/apis/guest-party-resolution.ts';
import type { GuestPartyResolutionRequest } from '../../shared/apis/guest-party-resolution.ts';
import { operationGateway } from './action-gateway.ts';

export interface GuestPartyResolutionClientOptions {
  readonly baseUrl?: string | URL;
}

type GuestPartyResolutionAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: GuestPartyResolutionClientOptions,
];

type GuestPartyResolutionOperationInvocation = readonly [
  requestCorrelation: string,
  options?: GuestPartyResolutionClientOptions,
];

const guestPartyResolutionClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: GuestPartyResolutionClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: GuestPartyResolutionApi,
      credential,
      defaultApiPrefix: '/party-registry-api',
      requestCorrelation,
    },
    options,
  );

export const executeGuestPartyResolutionWithAuthorization = (
  payload: GuestPartyResolutionRequest,
  ...[credential, requestCorrelation, options = {}]: GuestPartyResolutionAuthorizedInvocation
) =>
  guestPartyResolutionClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.guestPartyResolution.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeGuestPartyResolution = (
  payload: GuestPartyResolutionRequest,
  ...[requestCorrelation, options = {}]: GuestPartyResolutionOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeGuestPartyResolutionWithAuthorization(payload, credential, requestCorrelation, options),
  );
