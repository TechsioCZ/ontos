// @generated by OntOS Codesmith module-api v1
import { makeEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { OrganizationEngagementProfileApi } from '../../shared/apis/organization-engagement-profile.ts';
import type { OrganizationEngagementProfileRequest } from '../../shared/apis/organization-engagement-profile.ts';
import { operationGateway } from './action-gateway.ts';

export interface OrganizationEngagementProfileClientOptions {
  readonly baseUrl?: string | URL;
}

type OrganizationEngagementProfileAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: OrganizationEngagementProfileClientOptions,
];

type OrganizationEngagementProfileOperationInvocation = readonly [
  requestCorrelation: string,
  options?: OrganizationEngagementProfileClientOptions,
];

const organizationEngagementProfileClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: OrganizationEngagementProfileClientOptions,
) => {
  const clientConfig = {
    api: OrganizationEngagementProfileApi,
    defaultApiPrefix: '/party-registry-api',
    transportHeaders: {
      authorization: Redacted.value(credential),
      'x-correlation-id': requestCorrelation,
    },
  };
  return makeEffectBffClient(
    options.baseUrl === undefined ? clientConfig : { ...clientConfig, baseUrl: options.baseUrl },
  );
};

export const executeOrganizationEngagementProfileWithAuthorization = (
  payload: OrganizationEngagementProfileRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: OrganizationEngagementProfileAuthorizedInvocation
) =>
  organizationEngagementProfileClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.organizationEngagementProfile.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executeOrganizationEngagementProfile = (
  payload: OrganizationEngagementProfileRequest,
  ...[requestCorrelation, options = {}]: OrganizationEngagementProfileOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeOrganizationEngagementProfileWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
