// @generated by OntOS Codesmith Governed Contribution v1
// @ontos-contribution-kind search-provider
import { Redacted } from 'effect';
import type { PartiesProviderRequest } from '../../shared/apis/parties-search.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

export interface PartiesSearchClientOptions {
  readonly baseUrl?: string | URL;
}

export const loadPartiesClientWithAuthorization = (
  payload: PartiesProviderRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartiesSearchClientOptions>
) =>
  invokePartyRegistryHttpClient(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
    ),
    (client) => client.partiesSearch.execute({ payload }),
  );

export const loadPartiesClient = (
  payload: PartiesProviderRequest,
  ...[
    requestCorrelation,
    options = {},
  ]: PartyRegistryOperationInvocation<PartiesSearchClientOptions>
) =>
  operationGateway.invoke((credential) =>
    loadPartiesClientWithAuthorization(payload, credential, requestCorrelation, options),
  );
