// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { PartyContactPointsRequest } from '../../shared/apis/party-contact-points.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface PartyContactPointsClientOptions {
  readonly baseUrl?: string | URL;
}

export const executePartyContactPointsWithAuthorization = (
  payload: PartyContactPointsRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartyContactPointsClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.partyContactPoints.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executePartyContactPoints = (
  payload: PartyContactPointsRequest,
  ...[
    requestCorrelation,
    options = {},
  ]: PartyRegistryOperationInvocation<PartyContactPointsClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executePartyContactPointsWithAuthorization(payload, credential, requestCorrelation, options),
  );
