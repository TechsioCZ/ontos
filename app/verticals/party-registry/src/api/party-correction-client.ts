// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { PartyCorrectionRequest } from '../../shared/apis/party-correction.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface PartyCorrectionClientOptions {
  readonly baseUrl?: string | URL;
}

export const executePartyCorrectionWithAuthorization = (
  payload: PartyCorrectionRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartyCorrectionClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.partyCorrection.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executePartyCorrection = (
  payload: PartyCorrectionRequest,
  ...[
    requestCorrelation,
    options = {},
  ]: PartyRegistryOperationInvocation<PartyCorrectionClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executePartyCorrectionWithAuthorization(payload, credential, requestCorrelation, options),
  );
