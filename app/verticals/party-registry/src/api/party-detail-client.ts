// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { PartyDetailRequest } from '../../shared/apis/party-detail.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface PartyDetailClientOptions {
  readonly baseUrl?: string | URL;
}

export const executePartyDetailWithAuthorization = (
  payload: PartyDetailRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartyDetailClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.partyDetail.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executePartyDetail = (
  payload: PartyDetailRequest,
  ...[requestCorrelation, options = {}]: PartyRegistryOperationInvocation<PartyDetailClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executePartyDetailWithAuthorization(payload, credential, requestCorrelation, options),
  );
