// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { PartyMatchRequest } from '../../shared/apis/party-match.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface PartyMatchClientOptions {
  readonly baseUrl?: string | URL;
}

export const executePartyMatchWithAuthorization = (
  payload: PartyMatchRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartyMatchClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.partyMatch.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executePartyMatch = (
  payload: PartyMatchRequest,
  ...[requestCorrelation, options = {}]: PartyRegistryOperationInvocation<PartyMatchClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executePartyMatchWithAuthorization(payload, credential, requestCorrelation, options),
  );
