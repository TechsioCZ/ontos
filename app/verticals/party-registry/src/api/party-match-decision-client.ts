// @generated by OntOS Codesmith module-api v1
import { Redacted } from 'effect';
import type { PartyMatchDecisionRequest } from '../../shared/apis/party-match-decision.ts';
import { operationGateway } from './action-gateway.ts';
import {
  authenticatePartyRegistryHttpRequest,
  invokePartyRegistryHttpClient,
  partyRegistryHttpRequestContext,
} from './party-registry-http-client.ts';
import type {
  PartyRegistryAuthorizedInvocation,
  PartyRegistryOperationInvocation,
} from './party-registry-http-client.ts';

const makeEffectHttpApiClientBoundary = invokePartyRegistryHttpClient;

export interface PartyMatchDecisionClientOptions {
  readonly baseUrl?: string | URL;
}

export const executePartyMatchDecisionWithAuthorization = (
  payload: PartyMatchDecisionRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyRegistryAuthorizedInvocation<PartyMatchDecisionClientOptions>
) =>
  makeEffectHttpApiClientBoundary(
    authenticatePartyRegistryHttpRequest(
      partyRegistryHttpRequestContext(options),
      Redacted.make(credential),
      requestCorrelation,
      'x-correlation-id',
    ),
    (client) => client.partyMatchDecision.execute({ headers: {}, params: {}, payload, query: {} }),
  );

export const executePartyMatchDecision = (
  payload: PartyMatchDecisionRequest,
  ...[
    requestCorrelation,
    options = {},
  ]: PartyRegistryOperationInvocation<PartyMatchDecisionClientOptions>
) =>
  operationGateway.invoke((credential) =>
    executePartyMatchDecisionWithAuthorization(payload, credential, requestCorrelation, options),
  );
