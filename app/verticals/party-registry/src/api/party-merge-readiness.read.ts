// @generated by OntOS Codesmith module-api v1
import { defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  PartyMergeReadinessRequestSchema,
  PartyMergeReadinessResponseSchema,
} from '../../shared/apis/party-merge-readiness.ts';
import { evaluateDisabledMergeReadiness } from '../merge/merge-readiness.ts';

export const partyMergeReadinessEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  entrypointKey: 'party.registry.api.party-merge-readiness',
  moduleKey: 'party.registry',
  role: 'api',
});

export const partyMergeReadinessRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: partyMergeReadinessEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'party.registry.api.party-merge-readiness.evidence.v1',
    },
    inputSchema: PartyMergeReadinessRequestSchema,
    legalEntityScope: 'optional',
    owningModuleKey: 'party.registry',
    permissionTarget: 'tenant',
    policies: [],
    readKey: 'party.registry.api.party-merge-readiness',
    resultSchema: PartyMergeReadinessResponseSchema,
    schemaVersion: '1',
  },
  (input) =>
    Effect.succeed({
      evidence: {
        metadata: {
          evaluatedPartyCount: input.partyRefs.length,
          mergeExecutionEnabled: false,
        },
        resultCount: 1,
      },
      result: evaluateDisabledMergeReadiness(input.partyRefs),
    }),
  () => Effect.succeed({}),
  () => ({ kind: 'tenant', permission: 'merge_party_identity' }),
);
