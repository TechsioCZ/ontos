// @generated by OntOS Codesmith module-api v1
import { makeEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PartyOfficialIdentifierDetailApi } from '../../shared/apis/party-official-identifier-detail.ts';
import type { PartyOfficialIdentifierDetailRequest } from '../../shared/apis/party-official-identifier-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface PartyOfficialIdentifierDetailClientOptions {
  readonly baseUrl?: string | URL;
}

type PartyOfficialIdentifierDetailAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PartyOfficialIdentifierDetailClientOptions,
];

type PartyOfficialIdentifierDetailOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PartyOfficialIdentifierDetailClientOptions,
];

const partyOfficialIdentifierDetailClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: PartyOfficialIdentifierDetailClientOptions,
) => {
  const clientConfig = {
    api: PartyOfficialIdentifierDetailApi,
    defaultApiPrefix: '/party-registry-api',
    transportHeaders: {
      authorization: Redacted.value(credential),
      'x-correlation-id': requestCorrelation,
    },
  };
  return makeEffectBffClient(
    options.baseUrl === undefined ? clientConfig : { ...clientConfig, baseUrl: options.baseUrl },
  );
};

export const executePartyOfficialIdentifierDetailWithAuthorization = (
  payload: PartyOfficialIdentifierDetailRequest,
  ...[
    credential,
    requestCorrelation,
    options = {},
  ]: PartyOfficialIdentifierDetailAuthorizedInvocation
) =>
  partyOfficialIdentifierDetailClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.partyOfficialIdentifierDetail.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePartyOfficialIdentifierDetail = (
  payload: PartyOfficialIdentifierDetailRequest,
  ...[requestCorrelation, options = {}]: PartyOfficialIdentifierDetailOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePartyOfficialIdentifierDetailWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
