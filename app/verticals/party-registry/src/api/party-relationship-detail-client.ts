// @generated by OntOS Codesmith module-api v1
import { Context, Effect, Redacted } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import type { Headers } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { PartyRelationshipDetailApi } from '../../shared/apis/party-relationship-detail.ts';
import type { PartyRelationshipDetailRequest } from '../../shared/apis/party-relationship-detail.ts';
import { operationGateway } from './action-gateway.ts';

export interface PartyRelationshipDetailClientOptions {
  readonly baseUrl?: string | URL;
  readonly timeoutMs?: number;
}

const DEFAULT_BASE_URL = '/party-registry-api';

/** Whole-operation budget, response decode included. Overridable per call so tests stay bounded. */
const DEFAULT_TIMEOUT_MS = 10_000;

/** What one in-flight call contributes to its request; never held by the client. */
interface PartyRelationshipDetailCallTransport {
  readonly baseUrl: string;
  readonly headers: Headers.Input;
}

/** A `Context.Reference`, so both public Effects keep `R = never`; its default is fail-closed. */
const CurrentPartyRelationshipDetailCall =
  Context.Reference<PartyRelationshipDetailCallTransport | null>(
    'party-registry/CurrentPartyRelationshipDetailCall',
    { defaultValue: () => null },
  );

/** Resolved in the fiber that issues the request, so interleaved calls each read their own transport. */
const applyCallTransport = (client: HttpClient.HttpClient): HttpClient.HttpClient =>
  HttpClient.mapRequestEffect(client, (request) =>
    Effect.flatMap(Effect.service(CurrentPartyRelationshipDetailCall), (transport) =>
      transport === null
        ? Effect.die('The Party relationship detail client was used outside a prepared call')
        : Effect.succeed(
            request.pipe(
              HttpClientRequest.setHeaders(transport.headers),
              HttpClientRequest.prependUrl(transport.baseUrl),
            ),
          ),
    ),
  );

/** Built once at module load, on its own root fiber, so no caller's context is captured or shared. */
const partyRelationshipDetailClient = Effect.runSync(
  makeEffectHttpApiClient(PartyRelationshipDetailApi, { transformClient: applyCallTransport }),
);

export const executePartyRelationshipDetailWithAuthorization = (
  payload: PartyRelationshipDetailRequest,
  authorization: Redacted.Redacted<string>,
  correlationId: string,
  options: PartyRelationshipDetailClientOptions = {},
) =>
  partyRelationshipDetailClient.partyRelationshipDetail
    .execute({ headers: {}, params: {}, payload, query: {} })
    .pipe(
      Effect.provideService(CurrentPartyRelationshipDetailCall, {
        baseUrl: (options.baseUrl ?? DEFAULT_BASE_URL).toString(),
        headers: {
          authorization: Redacted.value(authorization),
          'x-correlation-id': correlationId,
        },
      }),
      Effect.timeout(options.timeoutMs ?? DEFAULT_TIMEOUT_MS),
    );

export const executePartyRelationshipDetail = (
  payload: PartyRelationshipDetailRequest,
  correlationId: string,
  options: PartyRelationshipDetailClientOptions = {},
) =>
  operationGateway.invoke((authorization) =>
    executePartyRelationshipDetailWithAuthorization(payload, authorization, correlationId, options),
  );
