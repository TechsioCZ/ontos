// @generated by OntOS Codesmith module-api v1
import { makeEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PersonEngagementProfileApi } from '../../shared/apis/person-engagement-profile.ts';
import type { PersonEngagementProfileRequest } from '../../shared/apis/person-engagement-profile.ts';
import { operationGateway } from './action-gateway.ts';

export interface PersonEngagementProfileClientOptions {
  readonly baseUrl?: string | URL;
}

type PersonEngagementProfileAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PersonEngagementProfileClientOptions,
];

type PersonEngagementProfileOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PersonEngagementProfileClientOptions,
];

const personEngagementProfileClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: PersonEngagementProfileClientOptions,
) => {
  const clientConfig = {
    api: PersonEngagementProfileApi,
    defaultApiPrefix: '/party-registry-api',
    transportHeaders: {
      authorization: Redacted.value(credential),
      'x-correlation-id': requestCorrelation,
    },
  };
  return makeEffectBffClient(
    options.baseUrl === undefined ? clientConfig : { ...clientConfig, baseUrl: options.baseUrl },
  );
};

export const executePersonEngagementProfileWithAuthorization = (
  payload: PersonEngagementProfileRequest,
  ...[credential, requestCorrelation, options = {}]: PersonEngagementProfileAuthorizedInvocation
) =>
  personEngagementProfileClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.personEngagementProfile.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePersonEngagementProfile = (
  payload: PersonEngagementProfileRequest,
  ...[requestCorrelation, options = {}]: PersonEngagementProfileOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePersonEngagementProfileWithAuthorization(
      payload,
      credential,
      requestCorrelation,
      options,
    ),
  );
