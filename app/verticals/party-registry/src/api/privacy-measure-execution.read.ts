// @generated by OntOS Codesmith module-api v1
import { ReadHandlerUnavailable, defineRead, defineTenantModuleEntrypoint } from '@app/core-runtime';
import type { ReadHandlerContext } from '@app/core-runtime';
import { Effect, Option } from 'effect';
import {
  PrivacyMeasureExecutionRequestSchema,
  PrivacyMeasureExecutionResponseSchema,
} from '../../shared/apis/privacy-measure-execution.ts';
import type { PrivacyMeasureExecutionRequest } from '../../shared/apis/privacy-measure-execution.ts';
import { privacyMeasureExecutionService } from '../services/privacy-measure-execution.service.ts';
import type { PrivacyMeasureExecutionService } from '../services/privacy-measure-execution.service.ts';

const privacyMeasureExecutionEntrypoint = defineTenantModuleEntrypoint({
  access: 'read',
  authorization: { kind: 'context_permission', permission: 'module.access' },
  entrypointKey: 'party.registry.api.privacy-measure-execution',
  moduleKey: 'party.registry',
  role: 'api',
});

export const privacyMeasureExecutionRead = defineRead(
  {
    accessKind: 'detail',
    entrypoint: privacyMeasureExecutionEntrypoint,
    evidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'party.registry.api.privacy-measure-execution.evidence.v1',
    },
    inputSchema: PrivacyMeasureExecutionRequestSchema,
    legalEntityScope: 'required',
    owningModuleKey: 'party.registry',
    permissionTarget: 'module',
    policies: [],
    readKey: 'party.registry.api.privacy-measure-execution',
    resultSchema: PrivacyMeasureExecutionResponseSchema,
    schemaVersion: '1',
  },
  (input: PrivacyMeasureExecutionRequest, context: ReadHandlerContext<PrivacyMeasureExecutionService>) =>
    context.services.load(input.idempotencyKey).pipe(
      Effect.mapError((cause) =>
        Object.assign(
          new ReadHandlerUnavailable({
            code: 'read_handler_unavailable',
            reason: 'Party Registry Privacy Measure receipt reconciliation is temporarily unavailable',
          }),
          { cause },
        ),
      ),
      Effect.map((receipt) => ({
        evidence: { resultCount: Option.isSome(receipt) ? 1 : 0 },
        result: receipt,
      })),
    ),
  (transaction, scope) => privacyMeasureExecutionService(transaction, scope),
  () => ({ kind: 'module', moduleId: 'party.registry' }),
);
