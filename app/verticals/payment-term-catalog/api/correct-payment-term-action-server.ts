// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner payment.term-catalog
// @ontos-action-http-slug correct-payment-term
import { Effect, HttpApiBuilder, Layer } from '@modern-js/plugin-bff/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { paymentTermCatalogApi } from '../shared/api.ts';
import { correctPaymentTermAction } from '../src/actions/correct-payment-term.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  correctPaymentTermActionProblem,
  correctPaymentTermActionSchemaErrorLive,
  mapCorrectPaymentTermActionProblem,
} from './correct-payment-term-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: correctPaymentTermActionProblem.authentication,
  unavailable: () => correctPaymentTermActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CorrectPaymentTermActionServer.execute')(
  function* executeCorrectPaymentTerm(
    payload: typeof correctPaymentTermAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(correctPaymentTermActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeUnknownEffect(
      correctPaymentTermAction.descriptor.payloadSchema,
    )(payload).pipe(Effect.mapError(correctPaymentTermActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: correctPaymentTermActionProblem.internal,
      invalidCorrelationProblem: correctPaymentTermActionProblem.invalid,
      mapError: mapCorrectPaymentTermActionProblem,
      payload: decoded,
      registration: correctPaymentTermAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const correctPaymentTermActionApiLive = HttpApiBuilder.group(
  paymentTermCatalogApi,
  'correctPaymentTermAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(correctPaymentTermActionSchemaErrorLive));
