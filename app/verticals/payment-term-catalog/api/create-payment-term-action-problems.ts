// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner payment.term-catalog
// @ontos-action-http-slug create-payment-term
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CreatePaymentTermActionAlreadyCommittedProblemSchema,
  CreatePaymentTermActionAuthenticationProblemSchema,
  CreatePaymentTermActionCommitIndeterminateProblemSchema,
  CreatePaymentTermActionConflictProblemSchema,
  CreatePaymentTermActionForbiddenProblemSchema,
  CreatePaymentTermActionIneligibleProblemSchema,
  CreatePaymentTermActionInternalProblemSchema,
  CreatePaymentTermActionInvalidProblemSchema,
  CreatePaymentTermActionNotFoundProblemSchema,
  CreatePaymentTermActionPreconditionProblemSchema,
  CreatePaymentTermActionSchemaErrorMiddleware,
  CreatePaymentTermActionUnavailableProblemSchema,
} from '../shared/apis/create-payment-term-action.ts';
import type { CreatePaymentTermActionProblem } from '../shared/apis/create-payment-term-action.ts';
import { createPaymentTermAction } from '../src/actions/create-payment-term.action.ts';

type DomainError = typeof createPaymentTermAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CreatePaymentTermActionProblem['_tag']> = Extract<
  CreatePaymentTermActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const createPaymentTermActionProblem = {
  authentication: (): ProblemOf<'CreatePaymentTermActionAuthenticationProblem'> =>
    CreatePaymentTermActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CreatePaymentTermActionConflictProblem'>['code'],
  ): ProblemOf<'CreatePaymentTermActionConflictProblem'> =>
    CreatePaymentTermActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CreatePaymentTermActionForbiddenProblem'>['code'],
  ): ProblemOf<'CreatePaymentTermActionForbiddenProblem'> =>
    CreatePaymentTermActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CreatePaymentTermActionIneligibleProblem'>['code'],
  ): ProblemOf<'CreatePaymentTermActionIneligibleProblem'> =>
    CreatePaymentTermActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CreatePaymentTermActionInternalProblem'> =>
    CreatePaymentTermActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CreatePaymentTermActionInvalidProblem'> =>
    CreatePaymentTermActionInvalidProblemSchema.make({
      detail: 'The create-payment-term Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CreatePaymentTermActionNotFoundProblem'>['code'],
  ): ProblemOf<'CreatePaymentTermActionNotFoundProblem'> =>
    CreatePaymentTermActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CreatePaymentTermActionPreconditionProblem'> =>
    CreatePaymentTermActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CreatePaymentTermActionUnavailableProblem'>['code'],
  ): ProblemOf<'CreatePaymentTermActionUnavailableProblem'> =>
    CreatePaymentTermActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CreatePaymentTermActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PaymentTermCatalogPersistenceConflict: () =>
        createPaymentTermActionProblem.unavailable('payment_term_catalog_persistence_conflict'),
      PaymentTermCatalogPersistenceUnavailable: () =>
        createPaymentTermActionProblem.unavailable('payment_term_catalog_persistence_unavailable'),
      PaymentTermCodeConflict: () => createPaymentTermActionProblem.conflict('payment_term_code_conflict'),
      PaymentTermDuplicateSemantics: () => createPaymentTermActionProblem.conflict('payment_term_duplicate_semantics'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CreatePaymentTermActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CreatePaymentTermActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: createPaymentTermActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CreatePaymentTermActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: createPaymentTermActionProblem.internal,
      ActionIdempotencyKeyRequired: createPaymentTermActionProblem.precondition,
      ActionInvocationNotFound: (failure) => createPaymentTermActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => createPaymentTermActionProblem.conflict(failure.code),
      ActionPayloadValidationError: createPaymentTermActionProblem.invalid,
      ActionPermissionCheckError: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => createPaymentTermActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => createPaymentTermActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => createPaymentTermActionProblem.conflict(failure.code),
      ActionResultValidationError: createPaymentTermActionProblem.internal,
      ActionTransactionError: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: createPaymentTermActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => createPaymentTermActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: createPaymentTermActionProblem.authentication,
      OperationContextDenied: (failure) => createPaymentTermActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => createPaymentTermActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => createPaymentTermActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(createPaymentTermAction.descriptor.domainErrorSchema);
export const mapCreatePaymentTermActionProblem = (
  error: ActionCoreError | DomainError,
): CreatePaymentTermActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const createPaymentTermActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CreatePaymentTermActionSchemaErrorMiddleware,
  () => Effect.fail(createPaymentTermActionProblem.invalid()),
);
