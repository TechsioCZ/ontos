// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner payment.term-catalog
// @ontos-resource-slug payment-term-catalog-root
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('PaymentTermCatalogRootResourceId'),
  Schema.decodeTo(Schema.String.check(Schema.isUUID(), Schema.isTrimmed())),
);
const TenantIdSchema = Schema.String.check(Schema.isUUID(), Schema.isTrimmed()).pipe(
  Schema.brand('PaymentTermCatalogRootTenantId'),
  Schema.decodeTo(Schema.String.check(Schema.isUUID(), Schema.isTrimmed())),
);

export const PaymentTermCatalogRootRefSchema = Schema.Struct({
  moduleId: Schema.Literal('payment.term-catalog'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('payment.term-catalog.payment-term-catalog-root'),
  tenantId: TenantIdSchema,
});
export type PaymentTermCatalogRootRef = typeof PaymentTermCatalogRootRefSchema.Type;

export const paymentTermCatalogRootResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Payment Term Catalog Root resource.',
  key: 'payment.term-catalog.payment-term-catalog-root',
  label: 'Payment Term Catalog Root',
  owningModuleId: 'payment.term-catalog',
} as const satisfies OntosResourceType;
