// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { PaymentTermHistoryApi } from '../../shared/apis/payment-term-history.ts';
import type { PaymentTermHistoryRequest } from '../../shared/apis/payment-term-history.ts';
import { operationGateway } from './action-gateway.ts';

export interface PaymentTermHistoryClientOptions {
  readonly baseUrl?: string | URL;
}

type PaymentTermHistoryAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: PaymentTermHistoryClientOptions,
];

type PaymentTermHistoryOperationInvocation = readonly [
  requestCorrelation: string,
  options?: PaymentTermHistoryClientOptions,
];

const paymentTermHistoryClient = (
  credential: Redacted.Redacted<string>,
  requestCorrelation: string,
  options: PaymentTermHistoryClientOptions,
) =>
  makeGovernedEffectBffClient(
    {
      api: PaymentTermHistoryApi,
      credential,
      defaultApiPrefix: '/payment-term-catalog-api',
      requestCorrelation,
    },
    options,
  );

export const executePaymentTermHistoryWithAuthorization = (
  payload: PaymentTermHistoryRequest,
  ...[credential, requestCorrelation, options = {}]: PaymentTermHistoryAuthorizedInvocation
) =>
  paymentTermHistoryClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) =>
      client.paymentTermHistory.execute({ headers: {}, params: {}, payload, query: {} }),
    ),
  );

export const executePaymentTermHistory = (
  payload: PaymentTermHistoryRequest,
  ...[requestCorrelation, options = {}]: PaymentTermHistoryOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executePaymentTermHistoryWithAuthorization(payload, credential, requestCorrelation, options),
  );
