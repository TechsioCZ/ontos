// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner pricing.price-group-catalog
// @ontos-action-http-slug retire-price-group
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RetirePriceGroupActionAlreadyCommittedProblemSchema,
  RetirePriceGroupActionAuthenticationProblemSchema,
  RetirePriceGroupActionCommitIndeterminateProblemSchema,
  RetirePriceGroupActionConflictProblemSchema,
  RetirePriceGroupActionForbiddenProblemSchema,
  RetirePriceGroupActionIneligibleProblemSchema,
  RetirePriceGroupActionInternalProblemSchema,
  RetirePriceGroupActionInvalidProblemSchema,
  RetirePriceGroupActionNotFoundProblemSchema,
  RetirePriceGroupActionPreconditionProblemSchema,
  RetirePriceGroupActionSchemaErrorMiddleware,
  RetirePriceGroupActionUnavailableProblemSchema,
} from '../shared/apis/retire-price-group-action.ts';
import type { RetirePriceGroupActionProblem } from '../shared/apis/retire-price-group-action.ts';
import { retirePriceGroupAction } from '../src/actions/retire-price-group.action.ts';

type DomainError = typeof retirePriceGroupAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RetirePriceGroupActionProblem['_tag']> = Extract<
  RetirePriceGroupActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const retirePriceGroupActionProblem = {
  authentication: (): ProblemOf<'RetirePriceGroupActionAuthenticationProblem'> =>
    RetirePriceGroupActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RetirePriceGroupActionConflictProblem'>['code'],
  ): ProblemOf<'RetirePriceGroupActionConflictProblem'> =>
    RetirePriceGroupActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RetirePriceGroupActionForbiddenProblem'>['code'],
  ): ProblemOf<'RetirePriceGroupActionForbiddenProblem'> =>
    RetirePriceGroupActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RetirePriceGroupActionIneligibleProblem'>['code'],
  ): ProblemOf<'RetirePriceGroupActionIneligibleProblem'> =>
    RetirePriceGroupActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RetirePriceGroupActionInternalProblem'> =>
    RetirePriceGroupActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RetirePriceGroupActionInvalidProblem'> =>
    RetirePriceGroupActionInvalidProblemSchema.make({
      detail: 'The retire-price-group Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RetirePriceGroupActionNotFoundProblem'>['code'],
  ): ProblemOf<'RetirePriceGroupActionNotFoundProblem'> =>
    RetirePriceGroupActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RetirePriceGroupActionPreconditionProblem'> =>
    RetirePriceGroupActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RetirePriceGroupActionUnavailableProblem'>['code'],
  ): ProblemOf<'RetirePriceGroupActionUnavailableProblem'> =>
    RetirePriceGroupActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): RetirePriceGroupActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PriceGroupCodeConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupCurrentnessFailure: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      PriceGroupEffectivePeriodConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupExpectedCurrentConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupIdempotencyReuseConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupLifecycleConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupMeaningChangeRequired: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupNotFound: (failure) => retirePriceGroupActionProblem.notFound(failure.code),
      PriceGroupPersistenceUnavailable: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      PriceGroupRetirementEffectiveTimeConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupSemanticIdentityConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      PriceGroupTenantScopeFailure: (failure) => retirePriceGroupActionProblem.forbidden(failure.code),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RetirePriceGroupActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RetirePriceGroupActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: retirePriceGroupActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RetirePriceGroupActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: retirePriceGroupActionProblem.internal,
      ActionIdempotencyKeyRequired: retirePriceGroupActionProblem.precondition,
      ActionInvocationNotFound: (failure) => retirePriceGroupActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      ActionPayloadValidationError: retirePriceGroupActionProblem.invalid,
      ActionPermissionCheckError: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => retirePriceGroupActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => retirePriceGroupActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => retirePriceGroupActionProblem.conflict(failure.code),
      ActionResultValidationError: retirePriceGroupActionProblem.internal,
      ActionTransactionError: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: retirePriceGroupActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => retirePriceGroupActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: retirePriceGroupActionProblem.authentication,
      OperationContextDenied: (failure) => retirePriceGroupActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => retirePriceGroupActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => retirePriceGroupActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(retirePriceGroupAction.descriptor.domainErrorSchema);
export const mapRetirePriceGroupActionProblem = (
  error: ActionCoreError | DomainError,
): RetirePriceGroupActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const retirePriceGroupActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RetirePriceGroupActionSchemaErrorMiddleware,
  () => Effect.fail(retirePriceGroupActionProblem.invalid()),
);
