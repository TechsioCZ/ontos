// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner pricing.price-group-catalog
// @ontos-action-http-slug create-price-group-definition-revision
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  CreatePriceGroupDefinitionRevisionActionApi,
  CreatePriceGroupDefinitionRevisionPayloadSchema,
} from '../../shared/apis/create-price-group-definition-revision-action.ts';
import type { CreatePriceGroupDefinitionRevisionPayload } from '../../shared/actions/create-price-group-definition-revision.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface CreatePriceGroupDefinitionRevisionActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: CreatePriceGroupDefinitionRevisionActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: CreatePriceGroupDefinitionRevisionActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: CreatePriceGroupDefinitionRevisionActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: CreatePriceGroupDefinitionRevisionActionApi,
      credential,
      defaultApiPrefix: '/price-group-catalog-api',
      requestCorrelation,
    },
    options,
  );

const actionHeaders = (options: CreatePriceGroupDefinitionRevisionActionClientOptions) =>
  options.traceId === undefined
    ? { 'idempotency-key': options.idempotencyKey }
    : { 'idempotency-key': options.idempotencyKey, 'x-trace-id': options.traceId };

export const executeCreatePriceGroupDefinitionRevisionWithAuthorization = (
  payload: CreatePriceGroupDefinitionRevisionPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(CreatePriceGroupDefinitionRevisionPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.createPriceGroupDefinitionRevisionAction.execute({
            headers: actionHeaders(options),
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeCreatePriceGroupDefinitionRevision = (
  payload: CreatePriceGroupDefinitionRevisionPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeCreatePriceGroupDefinitionRevisionWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
