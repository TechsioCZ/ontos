// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug assign-legal-basis
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  AssignLegalBasisActionAlreadyCommittedProblemSchema,
  AssignLegalBasisActionAuthenticationProblemSchema,
  AssignLegalBasisActionCommitIndeterminateProblemSchema,
  AssignLegalBasisActionConflictProblemSchema,
  AssignLegalBasisActionForbiddenProblemSchema,
  AssignLegalBasisActionIneligibleProblemSchema,
  AssignLegalBasisActionInternalProblemSchema,
  AssignLegalBasisActionInvalidProblemSchema,
  AssignLegalBasisActionNotFoundProblemSchema,
  AssignLegalBasisActionPreconditionProblemSchema,
  AssignLegalBasisActionSchemaErrorMiddleware,
  AssignLegalBasisActionUnavailableProblemSchema,
} from '../shared/apis/assign-legal-basis-action.ts';
import type { AssignLegalBasisActionProblem } from '../shared/apis/assign-legal-basis-action.ts';
import { assignLegalBasisAction } from '../src/actions/assign-legal-basis.action.ts';

type DomainError = typeof assignLegalBasisAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends AssignLegalBasisActionProblem['_tag']> = Extract<
  AssignLegalBasisActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const assignLegalBasisActionProblem = {
  authentication: (): ProblemOf<'AssignLegalBasisActionAuthenticationProblem'> =>
    AssignLegalBasisActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'AssignLegalBasisActionConflictProblem'>['code'],
  ): ProblemOf<'AssignLegalBasisActionConflictProblem'> =>
    AssignLegalBasisActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'AssignLegalBasisActionForbiddenProblem'>['code'],
  ): ProblemOf<'AssignLegalBasisActionForbiddenProblem'> =>
    AssignLegalBasisActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'AssignLegalBasisActionIneligibleProblem'>['code'],
  ): ProblemOf<'AssignLegalBasisActionIneligibleProblem'> =>
    AssignLegalBasisActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'AssignLegalBasisActionInternalProblem'> =>
    AssignLegalBasisActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'AssignLegalBasisActionInvalidProblem'> =>
    AssignLegalBasisActionInvalidProblemSchema.make({
      detail: 'The assign-legal-basis Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'AssignLegalBasisActionNotFoundProblem'>['code'],
  ): ProblemOf<'AssignLegalBasisActionNotFoundProblem'> =>
    AssignLegalBasisActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'AssignLegalBasisActionPreconditionProblem'> =>
    AssignLegalBasisActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'AssignLegalBasisActionUnavailableProblem'>['code'],
  ): ProblemOf<'AssignLegalBasisActionUnavailableProblem'> =>
    AssignLegalBasisActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): AssignLegalBasisActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) => assignLegalBasisActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => assignLegalBasisActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): AssignLegalBasisActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => assignLegalBasisActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () => assignLegalBasisActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): AssignLegalBasisActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        AssignLegalBasisActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: assignLegalBasisActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        AssignLegalBasisActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: assignLegalBasisActionProblem.internal,
      ActionIdempotencyKeyRequired: assignLegalBasisActionProblem.precondition,
      ActionInvocationNotFound: (failure) => assignLegalBasisActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => assignLegalBasisActionProblem.conflict(failure.code),
      ActionPayloadValidationError: assignLegalBasisActionProblem.invalid,
      ActionPermissionCheckError: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => assignLegalBasisActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => assignLegalBasisActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => assignLegalBasisActionProblem.conflict(failure.code),
      ActionResultValidationError: assignLegalBasisActionProblem.internal,
      ActionTransactionError: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: assignLegalBasisActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => assignLegalBasisActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: assignLegalBasisActionProblem.authentication,
      OperationContextDenied: (failure) => assignLegalBasisActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => assignLegalBasisActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => assignLegalBasisActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(assignLegalBasisAction.descriptor.domainErrorSchema);
export const mapAssignLegalBasisActionProblem = (
  error: ActionCoreError | DomainError,
): AssignLegalBasisActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const assignLegalBasisActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  AssignLegalBasisActionSchemaErrorMiddleware,
  () => Effect.fail(assignLegalBasisActionProblem.invalid()),
);
/* jscpd:ignore-end */
