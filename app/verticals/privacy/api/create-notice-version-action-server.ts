// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug create-notice-version
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { createNoticeVersionAction } from '../src/actions/create-notice-version.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createNoticeVersionActionProblem,
  createNoticeVersionActionSchemaErrorLive,
  mapCreateNoticeVersionActionProblem,
} from './create-notice-version-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createNoticeVersionActionProblem.authentication,
  unavailable: () => createNoticeVersionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateNoticeVersionActionServer.execute')(function* executeCreateNoticeVersion(
  payload: typeof createNoticeVersionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createNoticeVersionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createNoticeVersionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createNoticeVersionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createNoticeVersionActionProblem.internal,
    invalidCorrelationProblem: createNoticeVersionActionProblem.invalid,
    mapError: mapCreateNoticeVersionActionProblem,
    payload: decoded,
    registration: createNoticeVersionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createNoticeVersionActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'createNoticeVersionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createNoticeVersionActionSchemaErrorLive));
