// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug create-privacy-subject
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { createPrivacySubjectAction } from '../src/actions/create-privacy-subject.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createPrivacySubjectActionProblem,
  createPrivacySubjectActionSchemaErrorLive,
  mapCreatePrivacySubjectActionProblem,
} from './create-privacy-subject-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createPrivacySubjectActionProblem.authentication,
  unavailable: () => createPrivacySubjectActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreatePrivacySubjectActionServer.execute')(function* executeCreatePrivacySubject(
  payload: typeof createPrivacySubjectAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createPrivacySubjectActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createPrivacySubjectAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createPrivacySubjectActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createPrivacySubjectActionProblem.internal,
    invalidCorrelationProblem: createPrivacySubjectActionProblem.invalid,
    mapError: mapCreatePrivacySubjectActionProblem,
    payload: decoded,
    registration: createPrivacySubjectAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createPrivacySubjectActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'createPrivacySubjectAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createPrivacySubjectActionSchemaErrorLive));
