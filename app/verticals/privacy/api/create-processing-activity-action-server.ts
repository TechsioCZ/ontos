// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug create-processing-activity
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { createProcessingActivityAction } from '../src/actions/create-processing-activity.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  createProcessingActivityActionProblem,
  createProcessingActivityActionSchemaErrorLive,
  mapCreateProcessingActivityActionProblem,
} from './create-processing-activity-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: createProcessingActivityActionProblem.authentication,
  unavailable: () => createProcessingActivityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('CreateProcessingActivityActionServer.execute')(function* executeCreateProcessingActivity(
  payload: typeof createProcessingActivityAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(createProcessingActivityActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(createProcessingActivityAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(createProcessingActivityActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: createProcessingActivityActionProblem.internal,
    invalidCorrelationProblem: createProcessingActivityActionProblem.invalid,
    mapError: mapCreateProcessingActivityActionProblem,
    payload: decoded,
    registration: createProcessingActivityAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const createProcessingActivityActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'createProcessingActivityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(createProcessingActivityActionSchemaErrorLive));
