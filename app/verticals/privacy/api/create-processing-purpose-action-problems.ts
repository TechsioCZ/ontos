// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug create-processing-purpose
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  CreateProcessingPurposeActionAlreadyCommittedProblemSchema,
  CreateProcessingPurposeActionAuthenticationProblemSchema,
  CreateProcessingPurposeActionCommitIndeterminateProblemSchema,
  CreateProcessingPurposeActionConflictProblemSchema,
  CreateProcessingPurposeActionForbiddenProblemSchema,
  CreateProcessingPurposeActionIneligibleProblemSchema,
  CreateProcessingPurposeActionInternalProblemSchema,
  CreateProcessingPurposeActionInvalidProblemSchema,
  CreateProcessingPurposeActionNotFoundProblemSchema,
  CreateProcessingPurposeActionPreconditionProblemSchema,
  CreateProcessingPurposeActionSchemaErrorMiddleware,
  CreateProcessingPurposeActionUnavailableProblemSchema,
} from '../shared/apis/create-processing-purpose-action.ts';
import type { CreateProcessingPurposeActionProblem } from '../shared/apis/create-processing-purpose-action.ts';
import { createProcessingPurposeAction } from '../src/actions/create-processing-purpose.action.ts';

type DomainError = typeof createProcessingPurposeAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends CreateProcessingPurposeActionProblem['_tag']> = Extract<
  CreateProcessingPurposeActionProblem,
  { readonly _tag: Tag }
>;

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

export const createProcessingPurposeActionProblem = {
  authentication: (): ProblemOf<'CreateProcessingPurposeActionAuthenticationProblem'> =>
    CreateProcessingPurposeActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'CreateProcessingPurposeActionConflictProblem'>['code'],
  ): ProblemOf<'CreateProcessingPurposeActionConflictProblem'> =>
    CreateProcessingPurposeActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'CreateProcessingPurposeActionForbiddenProblem'>['code'],
  ): ProblemOf<'CreateProcessingPurposeActionForbiddenProblem'> =>
    CreateProcessingPurposeActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'CreateProcessingPurposeActionIneligibleProblem'>['code'],
  ): ProblemOf<'CreateProcessingPurposeActionIneligibleProblem'> =>
    CreateProcessingPurposeActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'CreateProcessingPurposeActionInternalProblem'> =>
    CreateProcessingPurposeActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'CreateProcessingPurposeActionInvalidProblem'> =>
    CreateProcessingPurposeActionInvalidProblemSchema.make({
      detail: 'The create-processing-purpose Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'CreateProcessingPurposeActionNotFoundProblem'>['code'],
  ): ProblemOf<'CreateProcessingPurposeActionNotFoundProblem'> =>
    CreateProcessingPurposeActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'CreateProcessingPurposeActionPreconditionProblem'> =>
    CreateProcessingPurposeActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'CreateProcessingPurposeActionUnavailableProblem'>['code'],
  ): ProblemOf<'CreateProcessingPurposeActionUnavailableProblem'> =>
    CreateProcessingPurposeActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainProblem = (error: DomainError): CreateProcessingPurposeActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      CreateProcessingPurposeScopeRequired: () =>
        createProcessingPurposeActionProblem.ineligible('privacy_processing_purpose_scope_required'),
      ProcessingPurposePersistenceUnavailable: () =>
        createProcessingPurposeActionProblem.unavailable('privacy_processing_purpose_persistence_unavailable'),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): CreateProcessingPurposeActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        CreateProcessingPurposeActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: createProcessingPurposeActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        CreateProcessingPurposeActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: createProcessingPurposeActionProblem.internal,
      ActionIdempotencyKeyRequired: createProcessingPurposeActionProblem.precondition,
      ActionInvocationNotFound: (failure) => createProcessingPurposeActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => createProcessingPurposeActionProblem.conflict(failure.code),
      ActionPayloadValidationError: createProcessingPurposeActionProblem.invalid,
      ActionPermissionCheckError: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => createProcessingPurposeActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => createProcessingPurposeActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => createProcessingPurposeActionProblem.conflict(failure.code),
      ActionResultValidationError: createProcessingPurposeActionProblem.internal,
      ActionTransactionError: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: createProcessingPurposeActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => createProcessingPurposeActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: createProcessingPurposeActionProblem.authentication,
      OperationContextDenied: (failure) => createProcessingPurposeActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => createProcessingPurposeActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => createProcessingPurposeActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(createProcessingPurposeAction.descriptor.domainErrorSchema);
export const mapCreateProcessingPurposeActionProblem = (
  error: ActionCoreError | DomainError,
): CreateProcessingPurposeActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const createProcessingPurposeActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  CreateProcessingPurposeActionSchemaErrorMiddleware,
  () => Effect.fail(createProcessingPurposeActionProblem.invalid()),
);
