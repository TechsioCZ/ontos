// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug dispatch-privacy-measure
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { dispatchPrivacyMeasureAction } from '../src/actions/dispatch-privacy-measure.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  dispatchPrivacyMeasureActionProblem,
  dispatchPrivacyMeasureActionSchemaErrorLive,
  mapDispatchPrivacyMeasureActionProblem,
} from './dispatch-privacy-measure-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: dispatchPrivacyMeasureActionProblem.authentication,
  unavailable: () => dispatchPrivacyMeasureActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('DispatchPrivacyMeasureActionServer.execute')(function* executeDispatchPrivacyMeasure(
  payload: typeof dispatchPrivacyMeasureAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(dispatchPrivacyMeasureActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(dispatchPrivacyMeasureAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(dispatchPrivacyMeasureActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: dispatchPrivacyMeasureActionProblem.internal,
    invalidCorrelationProblem: dispatchPrivacyMeasureActionProblem.invalid,
    mapError: mapDispatchPrivacyMeasureActionProblem,
    payload: decoded,
    registration: dispatchPrivacyMeasureAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const dispatchPrivacyMeasureActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'dispatchPrivacyMeasureAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(dispatchPrivacyMeasureActionSchemaErrorLive));
