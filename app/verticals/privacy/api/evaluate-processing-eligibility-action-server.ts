// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug evaluate-processing-eligibility
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { evaluateProcessingEligibilityAction } from '../src/actions/evaluate-processing-eligibility.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  evaluateProcessingEligibilityActionProblem,
  evaluateProcessingEligibilityActionSchemaErrorLive,
  mapEvaluateProcessingEligibilityActionProblem,
} from './evaluate-processing-eligibility-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: evaluateProcessingEligibilityActionProblem.authentication,
  unavailable: () => evaluateProcessingEligibilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('EvaluateProcessingEligibilityActionServer.execute')(
  function* executeEvaluateProcessingEligibility(
    payload: typeof evaluateProcessingEligibilityAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(evaluateProcessingEligibilityActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(evaluateProcessingEligibilityAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(evaluateProcessingEligibilityActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: evaluateProcessingEligibilityActionProblem.internal,
      invalidCorrelationProblem: evaluateProcessingEligibilityActionProblem.invalid,
      mapError: mapEvaluateProcessingEligibilityActionProblem,
      payload: decoded,
      registration: evaluateProcessingEligibilityAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const evaluateProcessingEligibilityActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'evaluateProcessingEligibilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(evaluateProcessingEligibilityActionSchemaErrorLive));
