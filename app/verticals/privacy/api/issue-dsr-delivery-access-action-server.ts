// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug issue-dsr-delivery-access
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { issueDsrDeliveryAccessAction } from '../src/actions/issue-dsr-delivery-access.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  issueDsrDeliveryAccessActionProblem,
  issueDsrDeliveryAccessActionSchemaErrorLive,
  mapIssueDsrDeliveryAccessActionProblem,
} from './issue-dsr-delivery-access-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: issueDsrDeliveryAccessActionProblem.authentication,
  unavailable: () => issueDsrDeliveryAccessActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('IssueDsrDeliveryAccessActionServer.execute')(function* executeIssueDsrDeliveryAccess(
  payload: typeof issueDsrDeliveryAccessAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(issueDsrDeliveryAccessActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(issueDsrDeliveryAccessAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(issueDsrDeliveryAccessActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: issueDsrDeliveryAccessActionProblem.internal,
    invalidCorrelationProblem: issueDsrDeliveryAccessActionProblem.invalid,
    mapError: mapIssueDsrDeliveryAccessActionProblem,
    payload: decoded,
    registration: issueDsrDeliveryAccessAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const issueDsrDeliveryAccessActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'issueDsrDeliveryAccessAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(issueDsrDeliveryAccessActionSchemaErrorLive));
