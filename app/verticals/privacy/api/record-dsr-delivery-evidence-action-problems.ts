// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-dsr-delivery-evidence
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecordDsrDeliveryEvidenceActionAlreadyCommittedProblemSchema,
  RecordDsrDeliveryEvidenceActionAuthenticationProblemSchema,
  RecordDsrDeliveryEvidenceActionCommitIndeterminateProblemSchema,
  RecordDsrDeliveryEvidenceActionConflictProblemSchema,
  RecordDsrDeliveryEvidenceActionForbiddenProblemSchema,
  RecordDsrDeliveryEvidenceActionIneligibleProblemSchema,
  RecordDsrDeliveryEvidenceActionInternalProblemSchema,
  RecordDsrDeliveryEvidenceActionInvalidProblemSchema,
  RecordDsrDeliveryEvidenceActionNotFoundProblemSchema,
  RecordDsrDeliveryEvidenceActionPreconditionProblemSchema,
  RecordDsrDeliveryEvidenceActionSchemaErrorMiddleware,
  RecordDsrDeliveryEvidenceActionUnavailableProblemSchema,
} from '../shared/apis/record-dsr-delivery-evidence-action.ts';
import type { RecordDsrDeliveryEvidenceActionProblem } from '../shared/apis/record-dsr-delivery-evidence-action.ts';
import { recordDsrDeliveryEvidenceAction } from '../src/actions/record-dsr-delivery-evidence.action.ts';

type DomainError = typeof recordDsrDeliveryEvidenceAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecordDsrDeliveryEvidenceActionProblem['_tag']> = Extract<
  RecordDsrDeliveryEvidenceActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const recordDsrDeliveryEvidenceActionProblem = {
  authentication: (): ProblemOf<'RecordDsrDeliveryEvidenceActionAuthenticationProblem'> =>
    RecordDsrDeliveryEvidenceActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecordDsrDeliveryEvidenceActionConflictProblem'>['code'],
  ): ProblemOf<'RecordDsrDeliveryEvidenceActionConflictProblem'> =>
    RecordDsrDeliveryEvidenceActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecordDsrDeliveryEvidenceActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecordDsrDeliveryEvidenceActionForbiddenProblem'> =>
    RecordDsrDeliveryEvidenceActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecordDsrDeliveryEvidenceActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecordDsrDeliveryEvidenceActionIneligibleProblem'> =>
    RecordDsrDeliveryEvidenceActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecordDsrDeliveryEvidenceActionInternalProblem'> =>
    RecordDsrDeliveryEvidenceActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecordDsrDeliveryEvidenceActionInvalidProblem'> =>
    RecordDsrDeliveryEvidenceActionInvalidProblemSchema.make({
      detail: 'The record-dsr-delivery-evidence Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecordDsrDeliveryEvidenceActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecordDsrDeliveryEvidenceActionNotFoundProblem'> =>
    RecordDsrDeliveryEvidenceActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecordDsrDeliveryEvidenceActionPreconditionProblem'> =>
    RecordDsrDeliveryEvidenceActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecordDsrDeliveryEvidenceActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecordDsrDeliveryEvidenceActionUnavailableProblem'> =>
    RecordDsrDeliveryEvidenceActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RecordDsrDeliveryEvidenceActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) =>
      recordDsrDeliveryEvidenceActionProblem.ineligible(matched.code),
    ),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      recordDsrDeliveryEvidenceActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RecordDsrDeliveryEvidenceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => recordDsrDeliveryEvidenceActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () =>
        recordDsrDeliveryEvidenceActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecordDsrDeliveryEvidenceActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecordDsrDeliveryEvidenceActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recordDsrDeliveryEvidenceActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecordDsrDeliveryEvidenceActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recordDsrDeliveryEvidenceActionProblem.internal,
      ActionIdempotencyKeyRequired: recordDsrDeliveryEvidenceActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recordDsrDeliveryEvidenceActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recordDsrDeliveryEvidenceActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recordDsrDeliveryEvidenceActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recordDsrDeliveryEvidenceActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recordDsrDeliveryEvidenceActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recordDsrDeliveryEvidenceActionProblem.conflict(failure.code),
      ActionResultValidationError: recordDsrDeliveryEvidenceActionProblem.internal,
      ActionTransactionError: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recordDsrDeliveryEvidenceActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recordDsrDeliveryEvidenceActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recordDsrDeliveryEvidenceActionProblem.authentication,
      OperationContextDenied: (failure) => recordDsrDeliveryEvidenceActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recordDsrDeliveryEvidenceActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recordDsrDeliveryEvidenceActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recordDsrDeliveryEvidenceAction.descriptor.domainErrorSchema);
export const mapRecordDsrDeliveryEvidenceActionProblem = (
  error: ActionCoreError | DomainError,
): RecordDsrDeliveryEvidenceActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const recordDsrDeliveryEvidenceActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecordDsrDeliveryEvidenceActionSchemaErrorMiddleware,
  () => Effect.fail(recordDsrDeliveryEvidenceActionProblem.invalid()),
);
/* jscpd:ignore-end */
