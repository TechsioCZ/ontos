// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-dsr-response
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecordDsrResponseActionAlreadyCommittedProblemSchema,
  RecordDsrResponseActionAuthenticationProblemSchema,
  RecordDsrResponseActionCommitIndeterminateProblemSchema,
  RecordDsrResponseActionConflictProblemSchema,
  RecordDsrResponseActionForbiddenProblemSchema,
  RecordDsrResponseActionIneligibleProblemSchema,
  RecordDsrResponseActionInternalProblemSchema,
  RecordDsrResponseActionInvalidProblemSchema,
  RecordDsrResponseActionNotFoundProblemSchema,
  RecordDsrResponseActionPreconditionProblemSchema,
  RecordDsrResponseActionSchemaErrorMiddleware,
  RecordDsrResponseActionUnavailableProblemSchema,
} from '../shared/apis/record-dsr-response-action.ts';
import type { RecordDsrResponseActionProblem } from '../shared/apis/record-dsr-response-action.ts';
import { recordDsrResponseAction } from '../src/actions/record-dsr-response.action.ts';

type DomainError = typeof recordDsrResponseAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecordDsrResponseActionProblem['_tag']> = Extract<
  RecordDsrResponseActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const recordDsrResponseActionProblem = {
  authentication: (): ProblemOf<'RecordDsrResponseActionAuthenticationProblem'> =>
    RecordDsrResponseActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecordDsrResponseActionConflictProblem'>['code'],
  ): ProblemOf<'RecordDsrResponseActionConflictProblem'> =>
    RecordDsrResponseActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecordDsrResponseActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecordDsrResponseActionForbiddenProblem'> =>
    RecordDsrResponseActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecordDsrResponseActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecordDsrResponseActionIneligibleProblem'> =>
    RecordDsrResponseActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecordDsrResponseActionInternalProblem'> =>
    RecordDsrResponseActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecordDsrResponseActionInvalidProblem'> =>
    RecordDsrResponseActionInvalidProblemSchema.make({
      detail: 'The record-dsr-response Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecordDsrResponseActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecordDsrResponseActionNotFoundProblem'> =>
    RecordDsrResponseActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecordDsrResponseActionPreconditionProblem'> =>
    RecordDsrResponseActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecordDsrResponseActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecordDsrResponseActionUnavailableProblem'> =>
    RecordDsrResponseActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RecordDsrResponseActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) => recordDsrResponseActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => recordDsrResponseActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RecordDsrResponseActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => recordDsrResponseActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () => recordDsrResponseActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecordDsrResponseActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecordDsrResponseActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recordDsrResponseActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecordDsrResponseActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recordDsrResponseActionProblem.internal,
      ActionIdempotencyKeyRequired: recordDsrResponseActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recordDsrResponseActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recordDsrResponseActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recordDsrResponseActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recordDsrResponseActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recordDsrResponseActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recordDsrResponseActionProblem.conflict(failure.code),
      ActionResultValidationError: recordDsrResponseActionProblem.internal,
      ActionTransactionError: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recordDsrResponseActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recordDsrResponseActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recordDsrResponseActionProblem.authentication,
      OperationContextDenied: (failure) => recordDsrResponseActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recordDsrResponseActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recordDsrResponseActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recordDsrResponseAction.descriptor.domainErrorSchema);
export const mapRecordDsrResponseActionProblem = (
  error: ActionCoreError | DomainError,
): RecordDsrResponseActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const recordDsrResponseActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecordDsrResponseActionSchemaErrorMiddleware,
  () => Effect.fail(recordDsrResponseActionProblem.invalid()),
);
/* jscpd:ignore-end */
