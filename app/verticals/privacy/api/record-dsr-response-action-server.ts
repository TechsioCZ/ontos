// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-dsr-response
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordDsrResponseAction } from '../src/actions/record-dsr-response.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordDsrResponseActionProblem,
  recordDsrResponseActionSchemaErrorLive,
  mapRecordDsrResponseActionProblem,
} from './record-dsr-response-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordDsrResponseActionProblem.authentication,
  unavailable: () => recordDsrResponseActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordDsrResponseActionServer.execute')(function* executeRecordDsrResponse(
  payload: typeof recordDsrResponseAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(recordDsrResponseActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(recordDsrResponseAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(recordDsrResponseActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: recordDsrResponseActionProblem.internal,
    invalidCorrelationProblem: recordDsrResponseActionProblem.invalid,
    mapError: mapRecordDsrResponseActionProblem,
    payload: decoded,
    registration: recordDsrResponseAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const recordDsrResponseActionApiLive = HttpApiBuilder.group(privacyApi, 'recordDsrResponseAction', (handlers) =>
  handlers.handle('execute', ({ headers, payload, request }) => execute(payload, headers['idempotency-key'], request)),
).pipe(Layer.provide(recordDsrResponseActionSchemaErrorLive));
