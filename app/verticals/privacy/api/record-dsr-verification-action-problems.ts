// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-dsr-verification
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecordDsrVerificationActionAlreadyCommittedProblemSchema,
  RecordDsrVerificationActionAuthenticationProblemSchema,
  RecordDsrVerificationActionCommitIndeterminateProblemSchema,
  RecordDsrVerificationActionConflictProblemSchema,
  RecordDsrVerificationActionForbiddenProblemSchema,
  RecordDsrVerificationActionIneligibleProblemSchema,
  RecordDsrVerificationActionInternalProblemSchema,
  RecordDsrVerificationActionInvalidProblemSchema,
  RecordDsrVerificationActionNotFoundProblemSchema,
  RecordDsrVerificationActionPreconditionProblemSchema,
  RecordDsrVerificationActionSchemaErrorMiddleware,
  RecordDsrVerificationActionUnavailableProblemSchema,
} from '../shared/apis/record-dsr-verification-action.ts';
import type { RecordDsrVerificationActionProblem } from '../shared/apis/record-dsr-verification-action.ts';
import { recordDsrVerificationAction } from '../src/actions/record-dsr-verification.action.ts';

type DomainError = typeof recordDsrVerificationAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecordDsrVerificationActionProblem['_tag']> = Extract<
  RecordDsrVerificationActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const recordDsrVerificationActionProblem = {
  authentication: (): ProblemOf<'RecordDsrVerificationActionAuthenticationProblem'> =>
    RecordDsrVerificationActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecordDsrVerificationActionConflictProblem'>['code'],
  ): ProblemOf<'RecordDsrVerificationActionConflictProblem'> =>
    RecordDsrVerificationActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecordDsrVerificationActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecordDsrVerificationActionForbiddenProblem'> =>
    RecordDsrVerificationActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecordDsrVerificationActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecordDsrVerificationActionIneligibleProblem'> =>
    RecordDsrVerificationActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecordDsrVerificationActionInternalProblem'> =>
    RecordDsrVerificationActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecordDsrVerificationActionInvalidProblem'> =>
    RecordDsrVerificationActionInvalidProblemSchema.make({
      detail: 'The record-dsr-verification Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecordDsrVerificationActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecordDsrVerificationActionNotFoundProblem'> =>
    RecordDsrVerificationActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecordDsrVerificationActionPreconditionProblem'> =>
    RecordDsrVerificationActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecordDsrVerificationActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecordDsrVerificationActionUnavailableProblem'> =>
    RecordDsrVerificationActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RecordDsrVerificationActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) =>
      recordDsrVerificationActionProblem.ineligible(matched.code),
    ),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      recordDsrVerificationActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RecordDsrVerificationActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => recordDsrVerificationActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () => recordDsrVerificationActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecordDsrVerificationActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecordDsrVerificationActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recordDsrVerificationActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecordDsrVerificationActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recordDsrVerificationActionProblem.internal,
      ActionIdempotencyKeyRequired: recordDsrVerificationActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recordDsrVerificationActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recordDsrVerificationActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recordDsrVerificationActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recordDsrVerificationActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recordDsrVerificationActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recordDsrVerificationActionProblem.conflict(failure.code),
      ActionResultValidationError: recordDsrVerificationActionProblem.internal,
      ActionTransactionError: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recordDsrVerificationActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recordDsrVerificationActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recordDsrVerificationActionProblem.authentication,
      OperationContextDenied: (failure) => recordDsrVerificationActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recordDsrVerificationActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recordDsrVerificationActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recordDsrVerificationAction.descriptor.domainErrorSchema);
export const mapRecordDsrVerificationActionProblem = (
  error: ActionCoreError | DomainError,
): RecordDsrVerificationActionProblem => (isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error));

export const recordDsrVerificationActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecordDsrVerificationActionSchemaErrorMiddleware,
  () => Effect.fail(recordDsrVerificationActionProblem.invalid()),
);
/* jscpd:ignore-end */
