// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-legal-hold
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecordLegalHoldActionAlreadyCommittedProblemSchema,
  RecordLegalHoldActionAuthenticationProblemSchema,
  RecordLegalHoldActionCommitIndeterminateProblemSchema,
  RecordLegalHoldActionConflictProblemSchema,
  RecordLegalHoldActionForbiddenProblemSchema,
  RecordLegalHoldActionIneligibleProblemSchema,
  RecordLegalHoldActionInternalProblemSchema,
  RecordLegalHoldActionInvalidProblemSchema,
  RecordLegalHoldActionNotFoundProblemSchema,
  RecordLegalHoldActionPreconditionProblemSchema,
  RecordLegalHoldActionSchemaErrorMiddleware,
  RecordLegalHoldActionUnavailableProblemSchema,
} from '../shared/apis/record-legal-hold-action.ts';
import type { RecordLegalHoldActionProblem } from '../shared/apis/record-legal-hold-action.ts';
import { recordLegalHoldAction } from '../src/actions/record-legal-hold.action.ts';

type DomainError = typeof recordLegalHoldAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecordLegalHoldActionProblem['_tag']> = Extract<
  RecordLegalHoldActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const recordLegalHoldActionProblem = {
  authentication: (): ProblemOf<'RecordLegalHoldActionAuthenticationProblem'> =>
    RecordLegalHoldActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecordLegalHoldActionConflictProblem'>['code'],
  ): ProblemOf<'RecordLegalHoldActionConflictProblem'> =>
    RecordLegalHoldActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecordLegalHoldActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecordLegalHoldActionForbiddenProblem'> =>
    RecordLegalHoldActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecordLegalHoldActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecordLegalHoldActionIneligibleProblem'> =>
    RecordLegalHoldActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecordLegalHoldActionInternalProblem'> =>
    RecordLegalHoldActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecordLegalHoldActionInvalidProblem'> =>
    RecordLegalHoldActionInvalidProblemSchema.make({
      detail: 'The record-legal-hold Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecordLegalHoldActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecordLegalHoldActionNotFoundProblem'> =>
    RecordLegalHoldActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecordLegalHoldActionPreconditionProblem'> =>
    RecordLegalHoldActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecordLegalHoldActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecordLegalHoldActionUnavailableProblem'> =>
    RecordLegalHoldActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RecordLegalHoldActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) => recordLegalHoldActionProblem.ineligible(matched.code)),
    Match.when({ kind: 'unavailable' as const }, (matched) => recordLegalHoldActionProblem.unavailable(matched.code)),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RecordLegalHoldActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => recordLegalHoldActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () => recordLegalHoldActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecordLegalHoldActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecordLegalHoldActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recordLegalHoldActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecordLegalHoldActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recordLegalHoldActionProblem.internal,
      ActionIdempotencyKeyRequired: recordLegalHoldActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recordLegalHoldActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recordLegalHoldActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recordLegalHoldActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recordLegalHoldActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recordLegalHoldActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recordLegalHoldActionProblem.conflict(failure.code),
      ActionResultValidationError: recordLegalHoldActionProblem.internal,
      ActionTransactionError: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recordLegalHoldActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recordLegalHoldActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recordLegalHoldActionProblem.authentication,
      OperationContextDenied: (failure) => recordLegalHoldActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recordLegalHoldActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recordLegalHoldActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recordLegalHoldAction.descriptor.domainErrorSchema);
export const mapRecordLegalHoldActionProblem = (error: ActionCoreError | DomainError): RecordLegalHoldActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const recordLegalHoldActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecordLegalHoldActionSchemaErrorMiddleware,
  () => Effect.fail(recordLegalHoldActionProblem.invalid()),
);
/* jscpd:ignore-end */
