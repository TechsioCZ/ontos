// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-notice-provision
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordNoticeProvisionAction } from '../src/actions/record-notice-provision.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordNoticeProvisionActionProblem,
  recordNoticeProvisionActionSchemaErrorLive,
  mapRecordNoticeProvisionActionProblem,
} from './record-notice-provision-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordNoticeProvisionActionProblem.authentication,
  unavailable: () => recordNoticeProvisionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordNoticeProvisionActionServer.execute')(function* executeRecordNoticeProvision(
  payload: typeof recordNoticeProvisionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(recordNoticeProvisionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(recordNoticeProvisionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(recordNoticeProvisionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: recordNoticeProvisionActionProblem.internal,
    invalidCorrelationProblem: recordNoticeProvisionActionProblem.invalid,
    mapError: mapRecordNoticeProvisionActionProblem,
    payload: decoded,
    registration: recordNoticeProvisionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const recordNoticeProvisionActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'recordNoticeProvisionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recordNoticeProvisionActionSchemaErrorLive));
