// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-owner-contribution
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordOwnerContributionAction } from '../src/actions/record-owner-contribution.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordOwnerContributionActionProblem,
  recordOwnerContributionActionSchemaErrorLive,
  mapRecordOwnerContributionActionProblem,
} from './record-owner-contribution-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordOwnerContributionActionProblem.authentication,
  unavailable: () => recordOwnerContributionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordOwnerContributionActionServer.execute')(function* executeRecordOwnerContribution(
  payload: typeof recordOwnerContributionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(recordOwnerContributionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(recordOwnerContributionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(recordOwnerContributionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: recordOwnerContributionActionProblem.internal,
    invalidCorrelationProblem: recordOwnerContributionActionProblem.invalid,
    mapError: mapRecordOwnerContributionActionProblem,
    payload: decoded,
    registration: recordOwnerContributionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const recordOwnerContributionActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'recordOwnerContributionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recordOwnerContributionActionSchemaErrorLive));
