// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-owner-execution-outcome
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordOwnerExecutionOutcomeAction } from '../src/actions/record-owner-execution-outcome.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordOwnerExecutionOutcomeActionProblem,
  recordOwnerExecutionOutcomeActionSchemaErrorLive,
  mapRecordOwnerExecutionOutcomeActionProblem,
} from './record-owner-execution-outcome-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordOwnerExecutionOutcomeActionProblem.authentication,
  unavailable: () => recordOwnerExecutionOutcomeActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordOwnerExecutionOutcomeActionServer.execute')(
  function* executeRecordOwnerExecutionOutcome(
    payload: typeof recordOwnerExecutionOutcomeAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(recordOwnerExecutionOutcomeActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(recordOwnerExecutionOutcomeAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(recordOwnerExecutionOutcomeActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: recordOwnerExecutionOutcomeActionProblem.internal,
      invalidCorrelationProblem: recordOwnerExecutionOutcomeActionProblem.invalid,
      mapError: mapRecordOwnerExecutionOutcomeActionProblem,
      payload: decoded,
      registration: recordOwnerExecutionOutcomeAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const recordOwnerExecutionOutcomeActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'recordOwnerExecutionOutcomeAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recordOwnerExecutionOutcomeActionSchemaErrorLive));
