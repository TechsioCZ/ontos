// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-privacy-applicability
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordPrivacyApplicabilityAction } from '../src/actions/record-privacy-applicability.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordPrivacyApplicabilityActionProblem,
  recordPrivacyApplicabilityActionSchemaErrorLive,
  mapRecordPrivacyApplicabilityActionProblem,
} from './record-privacy-applicability-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordPrivacyApplicabilityActionProblem.authentication,
  unavailable: () => recordPrivacyApplicabilityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordPrivacyApplicabilityActionServer.execute')(function* executeRecordPrivacyApplicability(
  payload: typeof recordPrivacyApplicabilityAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(recordPrivacyApplicabilityActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(recordPrivacyApplicabilityAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(recordPrivacyApplicabilityActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: recordPrivacyApplicabilityActionProblem.internal,
    invalidCorrelationProblem: recordPrivacyApplicabilityActionProblem.invalid,
    mapError: mapRecordPrivacyApplicabilityActionProblem,
    payload: decoded,
    registration: recordPrivacyApplicabilityAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const recordPrivacyApplicabilityActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'recordPrivacyApplicabilityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recordPrivacyApplicabilityActionSchemaErrorLive));
