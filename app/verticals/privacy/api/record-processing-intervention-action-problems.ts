// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-processing-intervention
// fallow-ignore-file code-duplication -- Generated Action problem adapters deliberately preserve the same protocol mapping for each independently owned Action.
/* jscpd:ignore-start -- Generated Action problem adapters intentionally share the Core problem mapping protocol while preserving owner-specific domain errors. */
// oxlint-disable sonarjs/function-name -- Effect Match.tags requires owner-declared tag keys; remove-when: sonarjs accepts discriminant-map properties.
import type { ActionCoreError } from '@app/core-runtime';
import { Effect, HttpApiMiddleware } from '@modern-js/bff-effect/effect-edge';
import { Match, Schema } from 'effect';
import {
  RecordProcessingInterventionActionAlreadyCommittedProblemSchema,
  RecordProcessingInterventionActionAuthenticationProblemSchema,
  RecordProcessingInterventionActionCommitIndeterminateProblemSchema,
  RecordProcessingInterventionActionConflictProblemSchema,
  RecordProcessingInterventionActionForbiddenProblemSchema,
  RecordProcessingInterventionActionIneligibleProblemSchema,
  RecordProcessingInterventionActionInternalProblemSchema,
  RecordProcessingInterventionActionInvalidProblemSchema,
  RecordProcessingInterventionActionNotFoundProblemSchema,
  RecordProcessingInterventionActionPreconditionProblemSchema,
  RecordProcessingInterventionActionSchemaErrorMiddleware,
  RecordProcessingInterventionActionUnavailableProblemSchema,
} from '../shared/apis/record-processing-intervention-action.ts';
import type { RecordProcessingInterventionActionProblem } from '../shared/apis/record-processing-intervention-action.ts';
import { recordProcessingInterventionAction } from '../src/actions/record-processing-intervention.action.ts';

type DomainError = typeof recordProcessingInterventionAction.descriptor.domainErrorSchema.Type;
type ProblemOf<Tag extends RecordProcessingInterventionActionProblem['_tag']> = Extract<
  RecordProcessingInterventionActionProblem,
  { readonly _tag: Tag }
>;
type DomainProblemIdentity =
  | { readonly code: 'privacy_action_rejected' | 'privacy_action_scope_required'; readonly kind: 'ineligible' }
  | {
      readonly code:
        | 'privacy_operation_conflict'
        | 'privacy_operation_not_found'
        | 'privacy_operation_persistence_unavailable'
        | 'privacy_operation_scope_mismatch';
      readonly kind: 'unavailable';
    };

const problemStatus = {
  authentication: 401,
  conflict: 409,
  forbidden: 403,
  ineligible: 422,
  internal: 500,
  invalid: 400,
  notFound: 404,
  precondition: 428,
  rateLimited: 429,
  timeout: 504,
  unavailable: 503,
} as const;

const privacyOperationPersistenceErrorProblemByCode = {
  privacy_operation_conflict: { code: 'privacy_operation_conflict', kind: 'unavailable' },
  privacy_operation_not_found: { code: 'privacy_operation_not_found', kind: 'unavailable' },
  privacy_operation_persistence_unavailable: { code: 'privacy_operation_persistence_unavailable', kind: 'unavailable' },
  privacy_operation_scope_mismatch: { code: 'privacy_operation_scope_mismatch', kind: 'unavailable' },
} as const satisfies Record<
  Extract<DomainError, { readonly _tag: 'PrivacyOperationPersistenceError' }>['code'],
  DomainProblemIdentity
>;

export const recordProcessingInterventionActionProblem = {
  authentication: (): ProblemOf<'RecordProcessingInterventionActionAuthenticationProblem'> =>
    RecordProcessingInterventionActionAuthenticationProblemSchema.make({
      detail: 'A valid audience-scoped Bearer assertion is required.',
      status: problemStatus.authentication,
      title: 'Authentication required',
      type: 'https://ontos.dev/problems/operation-authentication-required',
    }),
  conflict: (
    code: ProblemOf<'RecordProcessingInterventionActionConflictProblem'>['code'],
  ): ProblemOf<'RecordProcessingInterventionActionConflictProblem'> =>
    RecordProcessingInterventionActionConflictProblemSchema.make({
      code,
      detail: 'The Action conflicts with current state.',
      status: problemStatus.conflict,
      title: 'Action conflict',
      type: 'https://ontos.dev/problems/action-conflict',
    }),
  forbidden: (
    code: ProblemOf<'RecordProcessingInterventionActionForbiddenProblem'>['code'],
  ): ProblemOf<'RecordProcessingInterventionActionForbiddenProblem'> =>
    RecordProcessingInterventionActionForbiddenProblemSchema.make({
      code,
      detail: 'The principal is not permitted to perform this Action.',
      status: problemStatus.forbidden,
      title: 'Action forbidden',
      type: 'https://ontos.dev/problems/action-forbidden',
    }),
  ineligible: (
    code: ProblemOf<'RecordProcessingInterventionActionIneligibleProblem'>['code'],
  ): ProblemOf<'RecordProcessingInterventionActionIneligibleProblem'> =>
    RecordProcessingInterventionActionIneligibleProblemSchema.make({
      code,
      detail: 'The request is not eligible for this Action.',
      status: problemStatus.ineligible,
      title: 'Action ineligible',
      type: 'https://ontos.dev/problems/action-ineligible',
    }),
  internal: (): ProblemOf<'RecordProcessingInterventionActionInternalProblem'> =>
    RecordProcessingInterventionActionInternalProblemSchema.make({
      detail: 'The Action could not be completed.',
      status: problemStatus.internal,
      title: 'Action failed',
      type: 'https://ontos.dev/problems/action-failed',
    }),
  invalid: (): ProblemOf<'RecordProcessingInterventionActionInvalidProblem'> =>
    RecordProcessingInterventionActionInvalidProblemSchema.make({
      detail: 'The record-processing-intervention Action request is invalid.',
      status: problemStatus.invalid,
      title: 'Invalid Action request',
      type: 'https://ontos.dev/problems/action-invalid',
    }),
  notFound: (
    code: ProblemOf<'RecordProcessingInterventionActionNotFoundProblem'>['code'],
  ): ProblemOf<'RecordProcessingInterventionActionNotFoundProblem'> =>
    RecordProcessingInterventionActionNotFoundProblemSchema.make({
      code,
      detail: 'The requested resource was not found.',
      status: problemStatus.notFound,
      title: 'Resource not found',
      type: 'https://ontos.dev/problems/action-resource-not-found',
    }),
  precondition: (): ProblemOf<'RecordProcessingInterventionActionPreconditionProblem'> =>
    RecordProcessingInterventionActionPreconditionProblemSchema.make({
      detail: 'An Idempotency-Key header is required.',
      status: problemStatus.precondition,
      title: 'Idempotency key required',
      type: 'https://ontos.dev/problems/idempotency-key-required',
    }),
  unavailable: (
    code: ProblemOf<'RecordProcessingInterventionActionUnavailableProblem'>['code'],
  ): ProblemOf<'RecordProcessingInterventionActionUnavailableProblem'> =>
    RecordProcessingInterventionActionUnavailableProblemSchema.make({
      code,
      detail: 'The Action capability is temporarily unavailable.',
      retryable: true,
      status: problemStatus.unavailable,
      title: 'Action unavailable',
      type: 'https://ontos.dev/problems/action-unavailable',
    }),
} as const;

const mapDomainIdentity = (identity: DomainProblemIdentity): RecordProcessingInterventionActionProblem =>
  Match.value(identity).pipe(
    Match.when({ kind: 'ineligible' as const }, (matched) =>
      recordProcessingInterventionActionProblem.ineligible(matched.code),
    ),
    Match.when({ kind: 'unavailable' as const }, (matched) =>
      recordProcessingInterventionActionProblem.unavailable(matched.code),
    ),
    Match.exhaustive,
  );

const mapDomainProblem = (error: DomainError): RecordProcessingInterventionActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      PrivacyActionRejected: () => recordProcessingInterventionActionProblem.ineligible('privacy_action_rejected'),
      PrivacyActionScopeRequired: () =>
        recordProcessingInterventionActionProblem.ineligible('privacy_action_scope_required'),
      PrivacyOperationPersistenceError: (failure) =>
        mapDomainIdentity(privacyOperationPersistenceErrorProblemByCode[failure.code]),
    }),
    Match.exhaustive,
  );

const mapCoreProblem = (error: ActionCoreError): RecordProcessingInterventionActionProblem =>
  Match.value(error).pipe(
    Match.tags({
      ActionAlreadyCommitted: (failure) =>
        RecordProcessingInterventionActionAlreadyCommittedProblemSchema.make({
          code: failure.code,
          detail: 'This Action is already committed. Refresh governed reads.',
          invocationId: failure.invocationId,
          resolution: 'REFRESH_GOVERNED_READS',
          retryCommand: false,
          status: problemStatus.conflict,
          title: 'Action already committed',
          type: 'https://ontos.dev/problems/action-already-committed',
        }),
      ActionCollectorError: recordProcessingInterventionActionProblem.internal,
      ActionCommitIndeterminate: (failure) =>
        RecordProcessingInterventionActionCommitIndeterminateProblemSchema.make({
          detail: 'The Action commit is uncertain. Resolve this invocation before another attempt.',
          invocationId: failure.invocationId,
          resolution: 'RESOLVE_COMMIT',
          retryCommand: false,
          status: problemStatus.unavailable,
          title: 'Action commit uncertain',
          type: 'https://ontos.dev/problems/action-commit-indeterminate',
        }),
      ActionHandlerExecutionError: recordProcessingInterventionActionProblem.internal,
      ActionIdempotencyKeyRequired: recordProcessingInterventionActionProblem.precondition,
      ActionInvocationNotFound: (failure) => recordProcessingInterventionActionProblem.notFound(failure.code),
      ActionInvocationPersistenceError: (failure) =>
        recordProcessingInterventionActionProblem.unavailable(failure.code),
      ActionInvocationStateError: (failure) => recordProcessingInterventionActionProblem.conflict(failure.code),
      ActionPayloadValidationError: recordProcessingInterventionActionProblem.invalid,
      ActionPermissionCheckError: (failure) => recordProcessingInterventionActionProblem.unavailable(failure.code),
      ActionPermissionDenied: (failure) => recordProcessingInterventionActionProblem.forbidden(failure.code),
      ActionPolicyDenied: (failure) => recordProcessingInterventionActionProblem.ineligible(failure.code),
      ActionPolicyEvaluationError: (failure) => recordProcessingInterventionActionProblem.unavailable(failure.code),
      ActionRequestHashConflict: (failure) => recordProcessingInterventionActionProblem.conflict(failure.code),
      ActionResultValidationError: recordProcessingInterventionActionProblem.internal,
      ActionTransactionError: (failure) => recordProcessingInterventionActionProblem.unavailable(failure.code),
      ActionTrustedContextValidationError: recordProcessingInterventionActionProblem.authentication,
      ModuleStateCheckUnavailableError: (failure) =>
        recordProcessingInterventionActionProblem.unavailable(failure.code),
      ModuleStateDeniedError: (failure) => recordProcessingInterventionActionProblem.forbidden(failure.code),
      OperationAuthenticationRequired: recordProcessingInterventionActionProblem.authentication,
      OperationContextDenied: (failure) => recordProcessingInterventionActionProblem.forbidden(failure.code),
      OperationContextInvalid: (failure) => recordProcessingInterventionActionProblem.forbidden(failure.code),
      OperationContextUnavailable: (failure) => recordProcessingInterventionActionProblem.unavailable(failure.code),
    }),
    Match.exhaustive,
  );

const isDomainError = Schema.is(recordProcessingInterventionAction.descriptor.domainErrorSchema);
export const mapRecordProcessingInterventionActionProblem = (
  error: ActionCoreError | DomainError,
): RecordProcessingInterventionActionProblem =>
  isDomainError(error) ? mapDomainProblem(error) : mapCoreProblem(error);

export const recordProcessingInterventionActionSchemaErrorLive = HttpApiMiddleware.layerSchemaErrorTransform(
  RecordProcessingInterventionActionSchemaErrorMiddleware,
  () => Effect.fail(recordProcessingInterventionActionProblem.invalid()),
);
/* jscpd:ignore-end */
