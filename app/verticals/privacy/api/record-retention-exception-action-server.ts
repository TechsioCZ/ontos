// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-retention-exception
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { recordRetentionExceptionAction } from '../src/actions/record-retention-exception.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  recordRetentionExceptionActionProblem,
  recordRetentionExceptionActionSchemaErrorLive,
  mapRecordRetentionExceptionActionProblem,
} from './record-retention-exception-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: recordRetentionExceptionActionProblem.authentication,
  unavailable: () => recordRetentionExceptionActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('RecordRetentionExceptionActionServer.execute')(function* executeRecordRetentionException(
  payload: typeof recordRetentionExceptionAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(recordRetentionExceptionActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(recordRetentionExceptionAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(recordRetentionExceptionActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: recordRetentionExceptionActionProblem.internal,
    invalidCorrelationProblem: recordRetentionExceptionActionProblem.invalid,
    mapError: mapRecordRetentionExceptionActionProblem,
    payload: decoded,
    registration: recordRetentionExceptionAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const recordRetentionExceptionActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'recordRetentionExceptionAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(recordRetentionExceptionActionSchemaErrorLive));
