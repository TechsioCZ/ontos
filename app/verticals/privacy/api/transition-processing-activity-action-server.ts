// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug transition-processing-activity
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { transitionProcessingActivityAction } from '../src/actions/transition-processing-activity.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  transitionProcessingActivityActionProblem,
  transitionProcessingActivityActionSchemaErrorLive,
  mapTransitionProcessingActivityActionProblem,
} from './transition-processing-activity-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: transitionProcessingActivityActionProblem.authentication,
  unavailable: () => transitionProcessingActivityActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('TransitionProcessingActivityActionServer.execute')(
  function* executeTransitionProcessingActivity(
    payload: typeof transitionProcessingActivityAction.descriptor.payloadSchema.Encoded,
    idempotencyKey: string | undefined,
    request: HttpServerRequest.HttpServerRequest,
  ) {
    const correlationId = request.headers['x-correlation-id'];
    if (correlationId !== undefined && correlationId.length > 200) {
      return yield* Effect.fail(transitionProcessingActivityActionProblem.invalid());
    }
    const decoded = yield* Schema.decodeEffect(transitionProcessingActivityAction.descriptor.payloadSchema)(
      payload,
    ).pipe(Effect.mapError(transitionProcessingActivityActionProblem.invalid));
    return yield* runActionHttp({
      endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
      internalProblem: transitionProcessingActivityActionProblem.internal,
      invalidCorrelationProblem: transitionProcessingActivityActionProblem.invalid,
      mapError: mapTransitionProcessingActivityActionProblem,
      payload: decoded,
      registration: transitionProcessingActivityAction,
      requestHeaders: {
        authorization: Redacted.make(request.headers['authorization']),
        'x-correlation-id': correlationId,
      },
    });
  },
);

export const transitionProcessingActivityActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'transitionProcessingActivityAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(transitionProcessingActivityActionSchemaErrorLive));
