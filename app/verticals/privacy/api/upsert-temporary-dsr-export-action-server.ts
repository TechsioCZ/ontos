// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug upsert-temporary-dsr-export
import { Effect, HttpApiBuilder, Layer } from '@modern-js/bff-effect/effect-edge';
import type { HttpServerRequest } from 'effect/unstable/http';
import { Redacted, Schema } from 'effect';
import { privacyApi } from '../shared/api.ts';
import { upsertTemporaryDsrExportAction } from '../src/actions/upsert-temporary-dsr-export.action.ts';
import { bindActionHttpRunner } from './action-http-runner.ts';
import {
  upsertTemporaryDsrExportActionProblem,
  upsertTemporaryDsrExportActionSchemaErrorLive,
  mapUpsertTemporaryDsrExportActionProblem,
} from './upsert-temporary-dsr-export-action-problems.ts';

const runActionHttp = bindActionHttpRunner({
  authentication: upsertTemporaryDsrExportActionProblem.authentication,
  unavailable: () => upsertTemporaryDsrExportActionProblem.unavailable('operation_context_unavailable'),
});

const execute = Effect.fn('UpsertTemporaryDsrExportActionServer.execute')(function* executeUpsertTemporaryDsrExport(
  payload: typeof upsertTemporaryDsrExportAction.descriptor.payloadSchema.Encoded,
  idempotencyKey: string | undefined,
  request: HttpServerRequest.HttpServerRequest,
) {
  const correlationId = request.headers['x-correlation-id'];
  if (correlationId !== undefined && correlationId.length > 200) {
    return yield* Effect.fail(upsertTemporaryDsrExportActionProblem.invalid());
  }
  const decoded = yield* Schema.decodeEffect(upsertTemporaryDsrExportAction.descriptor.payloadSchema)(payload).pipe(
    Effect.mapError(upsertTemporaryDsrExportActionProblem.invalid),
  );
  return yield* runActionHttp({
    endpointHeaders: { idempotencyKey, traceId: request.headers['x-trace-id'] },
    internalProblem: upsertTemporaryDsrExportActionProblem.internal,
    invalidCorrelationProblem: upsertTemporaryDsrExportActionProblem.invalid,
    mapError: mapUpsertTemporaryDsrExportActionProblem,
    payload: decoded,
    registration: upsertTemporaryDsrExportAction,
    requestHeaders: {
      authorization: Redacted.make(request.headers['authorization']),
      'x-correlation-id': correlationId,
    },
  });
});

export const upsertTemporaryDsrExportActionApiLive = HttpApiBuilder.group(
  privacyApi,
  'upsertTemporaryDsrExportAction',
  (handlers) =>
    handlers.handle('execute', ({ headers, payload, request }) =>
      execute(payload, headers['idempotency-key'], request),
    ),
).pipe(Layer.provide(upsertTemporaryDsrExportActionSchemaErrorLive));
