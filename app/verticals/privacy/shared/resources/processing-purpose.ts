/* eslint-disable effect-native/no-unbranded-identifier-schema -- Privacy cross-owner wire contracts preserve explicit JSON null, canonical UTC string encodings, and owner-issued opaque references; generated API and Resource boundaries validate provenance without a misleading shared brand. expires: 2027-03-31. */
// @generated by OntOS Codesmith Resource v1
// @ontos-resource-owner privacy.core
// @ontos-resource-slug processing-purpose
import type { OntosResourceType } from '@app/core-runtime';
import { Schema } from 'effect';

const ResourceIdSchema = Schema.String.check(Schema.isMinLength(1), Schema.isMaxLength(300));
const TenantIdSchema = Schema.String.check(Schema.isUUID());

export const ProcessingPurposeRefSchema = Schema.Struct({
  moduleId: Schema.Literal('privacy.core'),
  resourceId: ResourceIdSchema,
  resourceType: Schema.Literal('privacy.core.processing-purpose'),
  tenantId: TenantIdSchema,
});
export type ProcessingPurposeRef = typeof ProcessingPurposeRefSchema.Type;

export const processingPurposeResourceDescriptor = {
  capabilities: {
    graphVisible: false,
    linkable: false,
    mediaAttachable: false,
    searchable: false,
    timelineVisible: false,
  },
  description: 'Processing Purpose resource.',
  key: 'privacy.core.processing-purpose',
  label: 'Processing Purpose',
  owningModuleId: 'privacy.core',
} as const satisfies OntosResourceType;
