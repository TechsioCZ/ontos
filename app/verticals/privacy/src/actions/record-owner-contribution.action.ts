// @generated by OntOS Codesmith Action v1
// @ontos-action-owner privacy.core
// @ontos-action-slug record-owner-contribution
import type { ActionHandlerContext } from '@app/core-runtime';
import { defineAction, defineTenantModuleEntrypoint } from '@app/core-runtime';
import { Effect } from 'effect';
import {
  RecordOwnerContributionPayloadSchema,
  RecordOwnerContributionResultSchema,
} from '../../shared/actions/record-owner-contribution.ts';
import type { RecordOwnerContributionPayload } from '../../shared/actions/record-owner-contribution.ts';
import { privacyOperationRepositoryForScope } from '../persistence/privacy-operation-postgres-repository.ts';
import type { PrivacyOperationRepositoryService } from '../persistence/privacy-operation-repository.ts';
import {
  PrivacyActionAuditEvidenceSchema,
  PrivacyActionErrorSchema,
  completePrivacyAction,
  privacyActionDomainEvents,
  requirePrivacyActionScope,
} from './privacy-operation-action-support.ts';

const handleRecordOwnerContribution = Effect.fn('RecordOwnerContributionAction.handle')(function* handle(
  payload: RecordOwnerContributionPayload,
  context: ActionHandlerContext<typeof privacyActionDomainEvents, PrivacyOperationRepositoryService>,
) {
  const scope = yield* requirePrivacyActionScope(context.scope);
  const result = yield* context.services.recordOwnerContribution(
    scope.tenantId,
    scope.legalEntityId,
    context.actionInvocationId,
    payload.contribution,
  );
  return yield* completePrivacyAction(context, 'record-owner-contribution', result.contributionId, result);
});
export const recordOwnerContributionAction = defineAction(
  {
    accessEvidencePolicy: {
      captureMode: 'metadata_only',
      policyKey: 'privacy.core.record-owner-contribution.access.v1',
    },
    actionKey: 'privacy.core.record-owner-contribution',
    auditEvidenceSchema: PrivacyActionAuditEvidenceSchema,
    auditProfile: 'standard',
    domainErrorSchema: PrivacyActionErrorSchema,
    domainEvents: privacyActionDomainEvents,
    entrypoint: defineTenantModuleEntrypoint({
      access: 'write',
      authorization: { kind: 'action_execution', provisioning: 'explicit' },
      entrypointKey: 'privacy.core.record-owner-contribution',
      moduleKey: 'privacy.core',
      role: 'action',
    }),
    idempotency: 'required',
    legalEntityScope: 'required',
    owningModuleKey: 'privacy.core',
    payloadSchema: RecordOwnerContributionPayloadSchema,
    policies: [],
    resultSchema: RecordOwnerContributionResultSchema,
    schemaVersion: '1',
  },
  handleRecordOwnerContribution,
  privacyOperationRepositoryForScope,
);
// <generated-outbox-message-exports>
// </generated-outbox-message-exports>
