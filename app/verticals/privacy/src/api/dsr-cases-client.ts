// @generated by OntOS Codesmith module-api v1
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted } from 'effect';
import { DsrCasesApi } from '../../shared/apis/dsr-cases.ts';
import type { DsrCasesRequest } from '../../shared/apis/dsr-cases.ts';
import { operationGateway } from './action-gateway.ts';

export interface DsrCasesClientOptions {
  readonly baseUrl?: string | URL;
}

type DsrCasesAuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options?: DsrCasesClientOptions,
];

type DsrCasesOperationInvocation = readonly [requestCorrelation: string, options?: DsrCasesClientOptions];

const dsrCasesClient = (credential: Redacted.Redacted, requestCorrelation: string, options: DsrCasesClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: DsrCasesApi,
      credential,
      defaultApiPrefix: '/privacy-api',
      requestCorrelation,
    },
    options,
  );

export const executeDsrCasesWithAuthorization = (
  payload: DsrCasesRequest,
  ...[credential, requestCorrelation, options = {}]: DsrCasesAuthorizedInvocation
) =>
  dsrCasesClient(Redacted.make(credential), requestCorrelation, options).pipe(
    Effect.flatMap((client) => client.dsrCases.execute({ headers: {}, params: {}, payload, query: {} })),
  );

export const executeDsrCases = (
  payload: DsrCasesRequest,
  ...[requestCorrelation, options = {}]: DsrCasesOperationInvocation
) =>
  operationGateway.invoke((credential) =>
    executeDsrCasesWithAuthorization(payload, credential, requestCorrelation, options),
  );
