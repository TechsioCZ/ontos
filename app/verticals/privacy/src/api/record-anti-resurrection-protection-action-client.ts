// @generated by OntOS Codesmith Action HTTP v1
// @ontos-action-http-owner privacy.core
// @ontos-action-http-slug record-anti-resurrection-protection
import { makeGovernedEffectBffClient } from '@app/shared-contracts/client-runtime';
import { Effect, Redacted, Schema } from 'effect';
import {
  RecordAntiResurrectionProtectionActionApi,
  RecordAntiResurrectionProtectionPayloadSchema,
} from '../../shared/apis/record-anti-resurrection-protection-action.ts';
import type { RecordAntiResurrectionProtectionPayload } from '../../shared/actions/record-anti-resurrection-protection.ts';
import { operationGateway } from './action-gateway.ts';

const traceIdOption = 'traceId' as const;

export interface RecordAntiResurrectionProtectionActionClientOptions {
  readonly baseUrl?: string | URL;
  readonly gateway?: Parameters<typeof operationGateway.invoke>[1];
  readonly idempotencyKey: string;
  readonly [traceIdOption]?: string;
}

type AuthorizedInvocation = readonly [
  credential: string,
  requestCorrelation: string,
  options: RecordAntiResurrectionProtectionActionClientOptions,
];
type OperationInvocation = readonly [
  requestCorrelation: string,
  options: RecordAntiResurrectionProtectionActionClientOptions,
];

interface MakeClientOptions {
  readonly credential: Redacted.Redacted;
  readonly options: RecordAntiResurrectionProtectionActionClientOptions;
  readonly requestCorrelation: string;
}

const makeClient = ({ credential, options, requestCorrelation }: MakeClientOptions) =>
  makeGovernedEffectBffClient(
    {
      api: RecordAntiResurrectionProtectionActionApi,
      credential,
      defaultApiPrefix: '/privacy-api',
      requestCorrelation,
    },
    options,
  );

export const executeRecordAntiResurrectionProtectionWithAuthorization = (
  payload: RecordAntiResurrectionProtectionPayload,
  ...[credential, requestCorrelation, options]: AuthorizedInvocation
) =>
  Schema.encodeUnknownEffect(RecordAntiResurrectionProtectionPayloadSchema)(payload).pipe(
    Effect.flatMap((encoded) =>
      makeClient({ credential: Redacted.make(credential), options, requestCorrelation }).pipe(
        Effect.flatMap((client) =>
          client.recordAntiResurrectionProtectionAction.execute({
            headers: { 'idempotency-key': options.idempotencyKey },
            payload: encoded,
          }),
        ),
      ),
    ),
  );

export const executeRecordAntiResurrectionProtection = (
  payload: RecordAntiResurrectionProtectionPayload,
  ...[requestCorrelation, options]: OperationInvocation
) =>
  operationGateway.invoke(
    (credential) =>
      executeRecordAntiResurrectionProtectionWithAuthorization(payload, credential, requestCorrelation, options),
    options.gateway,
  );
