// @generated by OntOS Codesmith module-api v1
import { Effect } from 'effect';
import { HttpClient, HttpClientRequest } from 'effect/unstable/http';
import { makeEffectHttpApiClient } from '@modern-js/plugin-bff/effect-client';
import { ContactListApi } from '../../shared/apis/contact-list.ts';
import type { ContactListRequest } from '../../shared/apis/contact-list.ts';
import { operationGateway } from './action-gateway.ts';

export const executeContactListWithAuthorization = (
  payload: ContactListRequest,
  authorization: string,
  correlationId: string,
) =>
  makeEffectHttpApiClient(ContactListApi, {
    transformClient: HttpClient.mapRequest(
      HttpClientRequest.setHeaders({ authorization, 'x-correlation-id': correlationId }),
    ),
  }).pipe(Effect.flatMap((client) => client.contactList.getContactList({ payload })));

export const executeContactList = (payload: ContactListRequest, correlationId: string) =>
  operationGateway.invoke((authorization) =>
    executeContactListWithAuthorization(payload, authorization, correlationId),
  );
