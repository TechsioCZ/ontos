// Keep the generated Projects route in the runtime graph while the adjacent declaration
// preserves the generated src-only Module Federation DTS boundary.
export { default } from '../routes/[lang]/projects/customers/page.tsx';
